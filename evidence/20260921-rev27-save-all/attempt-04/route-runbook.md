# Rev27 Gate A — Save All dispatch + resulting-surface observation (attempt-04; route/Gate-A lineage continues attempt-02/attempt-03) — runbook, pre-declared BEFORE any live observation or input

## Binding
- App `jp.naver.line.mac`; group `旻謙允禎成長日記`; album `2024/05/13～05/17`; expected count 57.
- Authorization (this round, new owner one-shot): `AUTHORIZATION_TYPE = SAVE_ALL_GATE_A`, `AUTHORIZATION_ID = REV27-SAVE-ALL-GATE-A-AFTER-ATTEMPT19`. Allowed GUI activation: exactly one Save All / 「儲存全部」 menu-item click. Allowed filesystem preflight: `staging_setup_mkdir <= 2` (empty staging parent/leaf only). Everything else 0 (album-card, ellipsis, other menu-item, chooser interaction, Go to Folder, path entry, Return, destination confirm, keyboard, Escape, scroll, AX write, bring-to-front, app acquisition, download-completion, cleanup/delete, retry).
- Predecessor lineage: attempt-02 and attempt-03 both stopped fail-closed at S2 (`MENU_NOT_PRESENT`; zero GUI input; zero staging mkdir). Route attempt-19 (authorization `REV27B-ROUTE-TO-MENU-AFTER-ATTEMPT18`, committed `198d371`) freshly reopened the album-detail at 18:15:47 (+0800) and dispatched exactly one ellipsis click at 18:19:43.889 (+0800), then observed the menu affirmatively (items 選擇項目/修改相簿名稱/儲存全部/刪除相簿/分享相簿) and LEFT THE MENU OPEN (no dismissal input).
- Historical menu presence is NEVER assumed this round: S2 must freshly re-validate the menu on a fresh frame. If the menu is gone ⇒ fail-closed STOP, zero GUI input, zero staging mkdir, blocker `MENU_NOT_PRESENT_REQUIRES_NEW_ELLIPSIS_AUTHORIZATION` (never re-click the ellipsis).
- Frozen tools (SHAs re-verified from disk this round):
  - `tools/detect_menu_popup.py` `6ae9c250bfaec7c639482deafa65b0c66f85927a21584e3dac6c9511c7f740bc`
  - `tools/v5/verify_album_open.py` `ffa82aed789f9e752c26cc3ae1380c69b8cfe8badbf2567c0ac36693a373cf58`
  - `tools/v7/album_detail_lineage_guard.py` `cdff187e0b4f2fab2ba537e99aaacc810279c14f28a995d034fc76c73b6dde52`
  - `tools/v5/vision_reader.py` `22a4e9ef86c419bdb80723484b3745fe61d848c77fc45c34e0b73e9d4b8801b8`
  - `tools/vision/vision_ocr.swift` `4fc9fa2be748f0620344bdfd501f7ef2d3349f6f03fc29290dd91550f3523b32`
  - round-local byte-identical copies (attempt-03/tools -> attempt-04/tools; read-only observation + locator; compiled binary `1c34c62c6183a4db8f809edc9fdfc7a9ade4db63837a9c24090cd7770156d6ae` in /tmp/attempt04): `ax_window_bounds.swift` `5c615a90…`, `validate_live_geometry.py` `ea4230e4…`, `locate_save_all_menu_item.py` `ea09c1ca…` (v7-class locator, re-frozen this round in `locator-freeze-attempt04.json`, re-selftested and re-reviewed this round before any click).
- No historical coordinate is ever used as input (`[304,50]`/`[305,50]`/`[39,923]`/`[39,926]` etc. are permanently barred). No threshold tuning. No retry of any consumed input.

## Pre-declared spaces and mechanisms (fixed BEFORE the run)
1. **CU window space = window-local pt** (1 px = 1 pt). This is the space the dispatch consumes (`lineApp.click([x, y], {mouseButton: "left", clickCount: 1})` via `cua_repl`; proven live in attempts 12–19).
2. **Full-screen frame** = `/usr/sbin/screencapture -x`, device pixels at capture scale 2 (2294x1490 px = 1147x745 pt display). Window rect in frame px = window_pt * 2 (re-read fresh from the AX tool this round).
3. **Dispatch form**: `await lineApp.click([x, y], {mouseButton:"left", clickCount:1})` via `cua_repl` with the live `lineApp` handle obtained read-only (`cua.getApp`). Exactly one call. No other input. No bring-to-front.
4. **Pre-declared order**: P0 (this file + authorization-scope-check.json + authorization-record.json + 00-start-state.json, all before any live observation) -> S1 read-only surface revalidation -> S2 menu determination -> **if menu absent: STOP with zero GUI input and zero staging mkdir** -> S0 staging preflight (conditional; still strictly before any GUI input) -> S2b fresh candidate -> S3 intent -> S4 exactly one click -> S5 observe + STOP. Rationale: a menu-absent STOP performs zero filesystem writes (attempt-02/03 precedent, owner decision tree).

## S1 — fresh read-only surface revalidation (zero input)
- Read-only AX window list via `/tmp/attempt04/ax_window_bounds` (read-only queries only) -> require `ax_frontmost=true`, `frontmost_application=jp.naver.line.mac`, exactly one focused/main standard LINE window, position/size readable; fresh full-screen `screencapture -x` frame; post-frame AX read -> require window list identical (position/size/pid/frontmost).
- Fresh CU app state via `cua_repl` (read-only) -> save window screenshot + AX text.
- **Formal album-open revalidation pair** (frozen `verify_album_open.py`): pre = `attempt-19/a4-precheck-window-cu.jpg` `b9be8bfc60678c648740d567ced176909ea242e5ea12202dd2c8606fc6659ff1` (byte-authenticated album-LIST window frame of the very transition being revalidated), post = THIS round's fresh CU window screenshot. Require `ALBUM_OPEN_VERIFIED` (exit 0) with `count_text` MATCH; supporting (non-gating) screen pair pre = `attempt-19/frame-menu-pre.png` `56959e940b6a0a3f37fbd83b77e9bf7f2b557f63a114a837b088efb7720ce7be`, post = fresh full-screen frame.
- Approved v7 lineage guard (`album_detail_lineage_guard.py`) on THIS round's fresh evidence (AX inventory + CU AX text + CU window shot + fresh frame + `--frozen-album-open-json` whose `post_sha256` == fresh frame SHA) -> require `ALBUM_DETAIL_WINDOW_LINEAGE_VERIFIED` (exit 0, C0–C6 PASS) => composite `ALBUM_OPEN_VERIFIED_COMPOSITE`. Any other verdict -> STOP, Save All 0, no reopen, no ellipsis.
- Geometry binding (pre-declared rule: dims exact AND gray MAD <= 12.0): `live-window-geometry.json` bound to the fresh frame SHA + `validate_live_geometry.py` -> require `GEOMETRY_BINDING_PASS`.
- Visual surface check (read-only): target album title + 57-photo context, album-detail grid, no album list, no permission prompt, no unknown overlay, no occluding window over the menu region.

## S2 — fresh menu determination (zero input; fail-closed)
- **Primary (affirmative rule, pre-declared)**: frozen `detect_menu_popup.py` (defaults: delta 12 / min-w 40 / min-h 20 / min-pixels 200 / min-strings 2 / min-strlen 2) with pre = `attempt-19/frame-menu-pre.png` `56959e94…` (byte-authenticated menu-absent reference of the current detail surface), post = THIS round's fresh frame. Menu is PRESENT only if `MENU_DETECTED` AND the freshly chosen bbox overlaps the fresh LINE window rect (allowed to extend past the right edge as in attempt-19).
- **Independent ROI cross-check (pre-declared, read-only, never a click input)**: gray MAD over the attempt-19 menu ROI px `[1245,163,1494,516]` against both byte-authenticated attempt-19 references (`frame-menu-pre.png` = menu-absent, `frame-menu-post1.png` `e321069d…` = menu-open) AND this round's fresh frame. Pre-declared rule: `MENU_DETECTED` + (fresh-vs-open MAD strictly smaller than fresh-vs-absent MAD, and fresh-vs-open MAD <= 12.0) = affirmative. Detector `MENU_DETECTED` but cross-check contradicts or is indeterminate -> treated as MENU_NOT_PRESENT -> STOP.
- **OCR (read-only)**: crop the fresh detector bbox from the fresh frame -> 3x LANCZOS -> tesseract `-l chi_tra+eng --psm 6`; machine-readable item list must freshly contain 「儲存全部」 among the item strings. Absent/ambiguous -> STOP.
- **If MENU_NOT_PRESENT -> STOP**: final scoped blocker `MENU_NOT_PRESENT_REQUIRES_NEW_ELLIPSIS_AUTHORIZATION`; Save All 0/1; staging mkdir 0/2; never reopen the ellipsis; never use a historical menu coordinate; record all evidence and stop.

## S0 — staging preflight (conditional: only if the menu chain holds; strictly before any GUI input)
- Fresh `run_id = RUN-YYYYMMDD-HHMMSS-01` generated at preflight; frozen into `/Users/hsiaojohnny/Downloads/LINE-Backup-PoC/staging/<run_id>/`. Exactly one `mkdir` of the leaf plus, only if `staging/` is machine-proven absent, exactly one `mkdir` of `staging/` (max 2 total; both empty). No other filesystem writes.
- Machine proofs (all required): canonical path equality / no symlink component; parent under authorized backup root and owner-writable; leaf absent before mkdir; after mkdir 0 entries / 0 files / 0 bytes / not symlink / not mount; staging != baseline and no ancestor/descendant relation; run_id pattern + not reused in any prior ledger; writable; accepted baseline unchanged before/after (tripwire digest `b7debe92…`).

## S2b — fresh candidate (frozen v7-class locator; zero input)
- This round re-proves the locator: determinism selftest (positive fixture `attempt-19/frame-menu-post1.png` twice -> byte-identical ELIGIBLE results; negative fixture `attempt-19/frame-menu-pre.png` -> NOT_FOUND), barred-literal grep, SHA verification; results recorded in `selftest-attempt04/` + `locator-freeze-attempt04.json` + `locator-review-attempt04.md`.
- Fresh run: `locate_save_all_menu_item.py <fresh frame> --detector-json <fresh detector JSON bound to that frame> --geometry <fresh frame-bound geometry>` -> must be `ELIGIBLE` with exactly one candidate and all bindings recorded. Any other verdict -> STOP (zero input).

## S3 — intent record before dispatch
- `save-all-click-intent.json` written and read back (atomic) containing at least: authorization_id, authorization_type, run_id, gate, group, fingerprint `{2024-05-13, 2024-05-17, 57}`, staging path, staging-preflight evidence SHA, fresh album-detail evidence SHA, fresh composite evidence SHA, fresh menu frame SHA, detector/tool SHA, locator SHA, fresh candidate in all three spaces, candidate derivation, click budget = 1, click used = 0, `dispatch_state = NOT_ATTEMPTED`, `save_all_retry_allowed = false`, retry_budget = 0, chooser_budget = 0, formal state write = false, timestamp. Read-back + re-hash before any click; failure -> STOP.

## S4 — exactly one Save All click (at-most-once barrier)
- Final freshness gate (<= 15 s before dispatch, dispatch call as the very next action): re-capture frame F2 + fresh AX read; require LINE still frontmost, geometry binding F2 PASS, menu still detected (affirmative rule), locator on F2 `ELIGIBLE`, and candidate2 within +/-3 frame px (x and y) of the S2b candidate. Any deviation -> `ABORTED_BEFORE_DISPATCH`, zero input, STOP.
- Boundary: `SAVE_ALL_DISPATCH_ATTEMPTED` recorded immediately before the click call (atomic write + read-back). The call is invoked exactly once; a returned call proves only that the call returned. Call throws/ambiguous -> `dispatch_state = UNKNOWN`; the click is never re-invoked.

## S5 — post-dispatch observation only
- Immediately capture post frames (~0.4 s and ~2.2 s) + read-only CU/AX state; classify the resulting surface fail-closed: menu disappeared?, new panel/modal/window?, app/process identity, panel role/title, AX structure, visible buttons/fields/labels, chooser affirmatively observed?, chooser type, default destination readable?, filesystem side effects?. Record `CHOOSER_AFFIRMATIVELY_OBSERVED` only if freshly proven; never upgrade from historical evidence.
- Filesystem tripwire (read-only, at preflight and post-dispatch and end): baseline manifest digest must equal `b7debe92…`; staging inventory read-only. If staging unexpectedly gains files: record `UNEXPECTED_PRE_CONFIRM_FILESYSTEM_WRITE` and STOP. Never delete; never re-dispatch; never confirm the chooser.
- **STOP.** No input of any kind afterwards (no chooser click, no keyboard, no Escape, no scroll, no AX write). Owner hand-off note: keep hands off the keyboard/pointer until the next authorized round.

## Budgets (this round)
Save All menu-item click <= 1; staging mkdir <= 2; ellipsis 0; album-card 0; navigation 0; retry 0; double click 0; other menu-item 0; chooser interaction 0; keyboard 0 (Escape included); scroll 0; AX write 0; app acquisition 0; bring-to-front 0; destination confirm 0; download 0; cleanup/delete 0; formal state/config write 0.
