# Rev27d adversarial review 02 — at-most-once / false-retry

- Reviewer: review-02 (adversarial: at-most-once / false-retry)
- Round reviewed: `Rev27d ZERO_GUI_SAVE_ALL_DISPATCH_RECONCILIATION` (authorization `REV27D-RECONCILE-ATTEMPT07-SAVE-ALL`)
- Subject: the single historical Save All dispatch, attempt-07, 2026-09-23T11:29:18.383+0800, `dispatch_state RETURNED`; Save All budget consumed (1/1).
- Method: **zero GUI input**. Read-only analysis of this round's artifacts, of the cited attempt-07 evidence, and of the archived historical state files; bounded read-only unified-log checks (the 11:28:48–11:29:48 window and this round's own 13:15–13:50 window); independent read-only pixel re-measurement of the cited cursor anchors. Exactly two files were written: this file and `review-02.json`. Nothing else was created or modified; no commit was made.

## Verdict

**ADVERSARIAL_REVIEW_APPROVED** — no false-retry path exists; the classification (`SAVE_ALL_SEMANTIC_EFFECT_INDETERMINATE`) and the reconciliation barrier ("NO RETRY; SAVE_ALL_RECONCILIATION_BARRIER_REMAINS") survived every falsification attempt below.

## What I tried to falsify, and what I confirmed myself

1. **"RETURNED + menu closed = activation" overclaim.** Attacked every artifact for a statement that the returned click call proves activation. Found none: `semantic-classification.json` explicitly defines `RETURNED` as "the CU click API call returned normally", `root-cause-analysis.md/.json` keep the attempt-07 verdict unmodified, `hit-test-reconstruction.json` lists "the menu item's activation handler executed" as **not claimed**, and `system-log-observation.json` labels the click-moment log signal "not activation evidence". Confirmed the guard holds everywhere.
2. **The strongest false-retry loophole: "no chooser ⇒ the click missed ⇒ DISMISSED_WITHOUT_ACTIVATION ⇒ design-eligible retry"** (also reachable via "outside-click dismissal by a wrong coordinate space"). If either had been affirmatively provable, the round's own conditional policy would have opened a design path (never an execution path this round). I attacked it through the hit test (item 4 below) and through the round's guard vocabulary. The attack failed: (a) the round never treats chooser absence as miss proof (`semantic-classification.json` guard "chooser absence != miss proof"; `root-cause-analysis.json` `chooser_absence.interpretation`), and (b) outside-click dismissal is geometrically excluded under the proven mapping, whose alternatives are refuted by two independent physical anchors (overlay center and rendered glyph) plus the semantic ellipsis anchor.
3. **Hit test is the load-bearing exclusion, so I attacked it hardest.** I re-verified every cited hash against attempt-07 and re-measured the cursor anchors myself (see checklist item 4). All cited SHAs match; my independent pixel measurement reproduces the anchor within ~4 px; the reconstruction's own margins (≥5.67 px even inside the v8 y-safe band; alternative mappings off by ≥420 px in x) make `SAVE_ALL_HIT_CONFIRMED` well-justified. Minor internal inconsistencies exist (see caveats), none at mapping scale, none able to flip the hit verdict.
4. **Hidden retry authorization.** Searched repo-wide for `ELIGIBLE_TO_PLAN_NEW_SAVE_ALL_RETRY_REVISION` (exact fixed string): **0 occurrences anywhere**. `retry-policy.json` states `retry_allowed_now=false` and `retry_allowed_ever_without_new_authorization=false`; no artifact claims eligibility or asks the owner for a retry.
5. **Hidden scheduling / ledger mutation.** No artifact performs or schedules any dispatch: the round directory contains data artifacts only (no scripts); no scheduling tokens (`schedul|queue|cron|timer|auto-retry` → 0 hits); a repo-wide `find` shows **no file outside the round directory written after 13:20**; `git status` shows no modified tracked files (the round directory is still untracked at review time). Attempt-07's own record ("second_save_all FORBIDDEN_NOT_PERFORMED") stands unmodified — verified through its manifest (154 entries; spot-checked files hash-match).
6. **Historical-coordinate leakage into any future dispatch.** Historical coordinates ((305,50), (350,83.6), (350,148.4)) appear only in `historical-success-comparison.md/.json` as *historical record*; the file marks coordinate space, menu geometry, row geometry, click API and versions **UNKNOWN** and re-affirms "no historical coordinate may feed any future dispatch". `retry-policy.json` design prerequisites require "current-evidence-derived coordinates only (no historical coordinates)"; the hit test records `historical_coordinate_input=false`. I spot-checked the archived state file itself: the calibration is retained, but the coordinate space/consumer is indeed not documented there (x=350 > the recorded 327-wide screenshot, noted honestly as an anomaly). No leak exists.
7. **"Zero GUI input this round" claim.** Verified the declared counters are all 0 in `00-start-state.json`, `authorization-record.json` and `authorization-scope-check.json`, and corroborated independently via the unified log for the round's active window (13:15–13:50): **no** tccd lines mentioning `com.openai.sky.CUAService` (0), **no** LINE `TCCAccessRequest` event-trust lines, **no** `kTCCServiceScreenCapture` preflights, **no** swift-frontend AX lines. The only `kTCCServiceListenEvent` preflights in the window belong to LM Studio's unrelated `splash` backend (pids 72942/73111, authValue=1 denied) — not the CU harness. I also verified the forensic crops are nearest-neighbor 4x crops of stored attempt-07 frames (exact pixel match, mean abs diff 0.0 for the target-row crop), i.e. no fresh capture. Overclaiming checks: `system-log-observation.json` explicitly labels silence as UNKNOWN and warns against over-reading; `filesystem-reconciliation.json` scopes its verdict to "anywhere checked" with an explicit residual — both sound. The one ambiguous line (openAndSavePanelService 27832.4 event-trust preflight at 11:29:17.925) is labeled UNKNOWN; I agree it is ambiguous (if anything it is a *further open thread*, not evidence either way) and it does not disturb the classification or the retry policy.

## Checklist (7 required items, each independently verified)

1. **RETURNED != semantic activation — PASS.** No artifact equates the returned click API call with activation; the meaning is stated in `semantic-classification.json`, `root-cause-analysis.md` §0 and `root-cause-analysis.json.returned_definition`; activation is explicitly not claimed in `hit-test-reconstruction.json.not_claimed`; the click-moment log signal is labeled "not activation evidence". `affirmative_activation_evidence: []` and `affirmative_non_activation_evidence: []` are both empty and the three-way vocabulary is applied honestly (INDETERMINATE).
2. **Menu disappearance != activation proof — PASS.** Rejected in `root-cause-analysis.md` §4 ("dismissal may be followed by action execution or not"), in `root-cause-analysis.json.menu_dismissal_alternative` (`menu_disappearance_as_activation_proof: REJECTED`, `axdialog_disappearance_as_activation_proof: REJECTED`), and in `semantic-classification.json.why_not_CONFIRMED_NO_CHOOSER`. I confirmed the AX dialog did disappear (4 windows at 11:29:06 incl. AXDialog [494,81,137×167] → 3 windows after) and that this fact is correctly given no activation semantics.
3. **Chooser absence != miss proof — PASS.** Stated as a guard in `semantic-classification.json`, elaborated in `root-cause-analysis.json.chooser_absence.interpretation` ("NOT proof of miss; consistent with both non-activation and failed/absent downstream panel display"), and the no-writes/empty-staging facts in `filesystem-reconciliation.json` are not used to infer non-activation. The historical precedent `RUN-20260908-124357-01` (returned, no chooser, trigger UNKNOWN) is correctly cited as showing the same *observable shape* without resolving its semantics.
4. **Exact click hit-test provenance — PASS (spot-checked against attempt-07).** Verified myself: dispatch args [320,135] (save-all-dispatch-record.json; recorded screen [531,164], frame px [1062,328], window [211,29,327×643]); v8 result `s11e-v8-locate-save-all.json` sha256 `5f5ba237…` with candidate app-local [319.75,135.25] and x_safe [1049,1074] / y_safe [322.333,334.667], verdict ELIGIBLE; fresh frame `s11e-preclick-frame.png` sha256 `d545043d…`; final gate `s11e-final-gate.json` sha256 `fd44018d…`, 14/14 checks pass at 11:29:06 (12.4 s before dispatch); AX read `s11e-preclick-ax.json` sha256 `668bed55…` (AXDialog [494,81,137×167]; detail window focused+main). Physical anchors: `s12-cg-window-list.json` sha256 `24af6134…` shows the harness `Software Cursor` overlay at (468,101,126×126) → center (531,164) pt = the reconstructed point, and the LINE window [211,29,327×643]; the only CG-set change vs the 11:09 list is that overlay (plus a window-title spinner character, same window number). My own re-measurement: |diff| of `post-save-all-frame-1.png` vs `frame-s1.png` around the click yields a bright cluster bbox x[1046,1085] y[306,350], centroid (1066.0, 329.6) px — within ~4 px of (1062,328) — and **no** residual cluster at the old ellipsis point; the ellipsis glyph anchor is present near (1030,158) in `frame-menu-post1.png`. Under the falsified screen-global mapping the anchor would sit at px (640,270), i.e. ~426 px away in x — refuted. Margins: the point is ≥5.67 px inside y_safe, ≥12 px inside x_safe, ≥11.67 px inside the text band; the worst-case anchor interpretation moves it ~3.2 px and it stays inside. `SAVE_ALL_HIT_CONFIRMED` is justified.
5. **No retry unless non-activation is affirmatively proven — PASS.**
   (a) `semantic-classification.json.classification = SAVE_ALL_SEMANTIC_EFFECT_INDETERMINATE`; `why_not_DISMISSED_WITHOUT_ACTIVATION` correctly holds that in-popup dismissal-without-action is only an unproven hypothesis (H2) — my adversarial reading agrees this cannot be affirmatively proven from the retained evidence.
   (b) `retry-policy.json`: `retry_allowed_now=false`, `retry_allowed_ever_without_new_authorization=false`, `applied_consequence = NO RETRY`; `ELIGIBLE_TO_PLAN_NEW_SAVE_ALL_RETRY_REVISION` does not appear anywhere in the repository (verified, 0 hits); no artifact concludes eligibility of any retry.
   (c) No artifact or ledger entry performs or schedules a dispatch/retry: no scripts in the round directory; no scheduling tokens; no file outside the round directory modified after 13:20; no modified tracked files; the only design text (§8) is explicitly "design only, nothing executed"; `forbidden_now` covers any Save All dispatch, GUI input, chooser interaction, staging/destination write.
   (d) The conditional policy matches Section K in effect as stated in the authorization: design of a retry is possible **only** under `SAVE_ALL_CLICK_DISMISSED_MENU_WITHOUT_ACTIVATION` (a classification that by construction requires affirmative non-activation), only in a future revision, **never executed in this round**, and even then requiring a new reviewed plan + a new owner one-shot authorization + a fresh entire live chain. The applied branch is INDETERMINATE → "no retry; permanent reconciliation barrier remains". One wording caveat is recorded below (barrier-lift condition #2); it cannot authorize execution by itself.
6. **No historical coordinate fallback — PASS.** `historical-success-comparison.json` marks the historical coordinate space, menu geometry, row geometry, click API and versions UNKNOWN (9 UNKNOWN tokens; explicitly "INSUFFICIENT for mechanism comparison"), and the .md bottom line forbids reuse. Historical coordinates appear nowhere as inputs/proposals for a future dispatch (design prerequisites say current-evidence-derived only; hit-test and coordinate-chain record `historical_coordinate_input_used=false`). I spot-checked the archived `backup_state.json`/`run_log.md`: the calibration ((305,50), row_step 32.4, save_all (350,148.4)) and the CHOOSER_CONFIRMED success, the 2026-09-08 returned-without-chooser run and the 2026-09-07 miss/SAFE_ABORT precedents are all retained as described; the coordinate space is genuinely undocumented there.
7. **No live GUI input by this round; no overclaiming — PASS.** Counters are 0 in all three declaration artifacts; independent log forensics for 13:15–13:50 show no CUAService event/trust activity, no LINE event-trust lines, no screen-capture or AX activity by round tools (only unrelated LM Studio splash preflights, denied). Crops provably derive from stored attempt-07 frames (exact nearest-neighbor match). Log/fs "silence" is labeled UNKNOWN, not proof, in both `system-log-observation.json` and `filesystem-reconciliation.json` (whose verdict is scoped "anywhere checked" with a residual disclosing unproven coverage outside scanned roots).

## Loopholes found

- **No false-retry path was found.** The strongest candidate (chooser absence → miss → DISMISSED → design-eligible retry, and its variant via outside-click dismissal) is closed twice: the round's explicit guard, and the hit-test geometry (all outside-popup mappings refuted by ≥420 px, with two independent physical anchors plus the semantic ellipsis anchor).
- **Non-blocking caveats/nits (recorded, none verdict-changing):**
  1. Glyph-centroid numbers differ slightly between artifacts: `coordinate-chain.json` gives the Save-All glyph centroid as (1064.5, 329.3) px (+2.6,+1.4 from the point) while `hit-test-reconstruction.json`/`root-cause-analysis.md` give (1064.4, 331.2) px (+2.4,+3.2); likewise the ellipsis differs ((-0.5,+1.5) vs (+2.6,+3.4)). These come from different isolation methods, ~2 px apart; the anchors discriminate a ~420 px mapping question, so the verdict is unaffected — but the two artifacts should not both be cited as if they were the same measurement.
  2. "v8 candidate identical across S7 and S11e" is slightly loose: the actual difference is 0.083 pt (0.167 px). "Stable within 0.2 px" would be exact.
  3. The Rev27d claim that the overlay "did not exist in the pre-click window lists" is true of those two lists, but attempt-07's own run-ledger disclosed the overlay as present "on screen for the whole session". The tension is cosmetic for the anchor (its center matches the last click position and no pointer action occurred after 11:29:18), but the wording should not be read as proof the overlay was created by the Save-All click.
  4. `retry-policy.json` barrier-lift condition #2 ("a future investigation determines a different, evidence-backed product/chooser behavior … with an approved plan") is textually broader than the strict "affirmative non-activation proof" criterion. It cannot produce an illegitimate retry: the same block requires "new reviewed plan + new owner one-shot authorization + fresh full live chain", and `retry_allowed_ever_without_new_authorization=false`. Recorded as a drafting observation only.
  5. Section 8 (dispatch-architecture design) and the "design_prerequisites_for_a_future_retry_revision" list exist although the classification is INDETERMINATE. They are explicitly inert (advisory, nothing executed, only "if a future revision is ever authorized"), so the effect matches Section K; if Section K were read literally as forbidding *any* retry design before affirmative non-activation, this would be the only sentence to relabel ("advisory, non-authorizing").
  6. Residuals already disclosed by the round and confirmed by me: the native translation is empirically pinned (compiled binary, not source-audited); the freshest menu snapshot is 12.4 s before dispatch (no input/state change in between); the bounded log shows no affirmative panel/creation signal but coverage is not exhaustive. None of these upgrades the classification in either direction.
  7. The round directory is untracked in git at review time (no commit yet); I made no commit. My own log-based no-GUI corroboration is absence-based corroboration, not absolute proof (the declared counters remain self-reported, though now log-consistent).

## Evidence handling

Files read (read-only): all 20 files of this round's directory including `forensic-crops/`; attempt-07 files `save-all-dispatch-record.json`, `save-all-click-intent.json`, `s11e-*.json/.png`, `s12-cg-window-list.json`, `v8-locate-save-all.json`, `post-save-all-frame-1/2.png`, `frame-menu-post1.png`, `frame-s1.png`, `s1-window-zorder.json`, `s5-preclick-zorder.json`, `s1-ax-pre.json`, `s12-post-ax.json`, `run-ledger.json`, `post-save-all-observation.json`, `99-scope-status.json`; archived `/Users/hsiaojohnny/Documents/Codex/2026-09-07/line-album-backup-line-backup-state/state/{backup_state.json, run_log.md}`; repository indexes and `git` state. No attempt-07 or historical file was modified. This review wrote exactly `review-02.md` and `review-02.json` (SHA-256 hashes of everything reviewed, including this file, are listed in `review-02.json`).
