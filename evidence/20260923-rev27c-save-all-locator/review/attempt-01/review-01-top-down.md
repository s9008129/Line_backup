# REVIEW 1 (top-down: target identity / geometry / fail-closed)

- Review role: REVIEW 1 of 2 (fresh-context independent review; offline, zero GUI input)
- Round: Rev27c `ZERO_GUI_SAVE_ALL_LOCATOR_REPLAN` / `REV27C-SAVE-ALL-CANDIDATE-LOCATOR-AFTER-ATTEMPT05`
- Reviewed: PLAN-2026-09-23-rev27c-save-all-locator.md, the v8 locator + selftest + results + replay record/out it binds
- Review date (local): 2026-09-23
- GUI/computer-use actions performed: 0. Downloads: 0. Repo files created: this review pair only.
- All experimental commands ran read-only against the repo; every experimental output was written under `/tmp/rev27c-review/` (never into the repo). No repo artifact was modified; no `__pycache__` was created (`python3 -B` + `sys.dont_write_bytecode`).

## 0. Target SHA-256 verification (computed from disk at review time)

| target | expected | observed from disk | match |
|---|---|---|---|
| PLAN-2026-09-23-rev27c-save-all-locator.md | 30ef72400b8e02ca7e83e1dddf5d3dcae8e9f440e599fb3b081a5fa9fee5e6fe | same | YES |
| evidence/20260916-route/tools/v8/locate_save_all_menu_item.py | c5ad46861706a9a2c1c4d477caeda2a809c87c5488844f14a4d3efb34c668a86 | same | YES |
| evidence/20260916-route/tools/v8/selftest/run_selftest.py | c94fc4074f0cd323ee53fe09428b0d38b61af04f5993ceb344acaa02e236f462 | same | YES |
| evidence/20260916-route/tools/v8/selftest/results.json | 943f293995b8ecc3f51ad9fc672c1fe899ba37312dc377d14a809ee118affe6a | same | YES |
| evidence/20260923-rev27c-save-all-locator/replay/replay-record.json | 2afcd7bec8f6e28826fbf0687c7dd2c604eed7b7746bc854ab2913368ac51540 | same | YES |

Replay `out/*.json` (6 files, 3 pairs; each pair `cmp` byte-identical):

| pair | sha256 (both files) | byte-identical |
|---|---|---|
| attempt-05-replay-1/2 | e8f53bd3b7827fad944322dd5e26db004be43359aa4559959eb8513dc17f6868 | YES |
| attempt-13-replay-1/2 | b802113b44387222196253c91b840f31830d02bee50a485021f6a0768635f525 | YES |
| attempt-19-replay-1/2 | 768c5cda80d4deac5694fbb7643926f313dba1b30414ed7ddbca634b1b32a859 | YES |

Collateral freeze facts re-verified from disk: the frozen v7-class locator copies in `evidence/20260921-rev27-save-all/attempt-02..05/tools/` are all still byte-identical `ea09c1ca4b97cf2f3a1c210f43a5ae7bc026d1db12bbc12768ff510d60b4012a`; `replay/build-replay-record.py` = `034f6abbca1fc09b25c326ff69d44c330e77f3e0cded293241ee689763d9a7f0` (matches the freeze record); `git status --porcelain` shows no modified tracked files before or after this review.

## 0-bis. Review-window integrity incident (found at end-of-review re-verification) — drives the final gate

- **At review start (all five targets verified from disk, ≈09:38 local):** run_selftest.py = `c94fc4074f0cd323ee53fe09428b0d38b61af04f5993ceb344acaa02e236f462` and results.json = `943f293995b8ecc3f51ad9fc672c1fe899ba37312dc377d14a809ee118affe6a` — both matched the plan/freeze bindings; the reviewed selftest contained 43 cases (results: 43/43 pass, 9/9 globals).
- **At end-of-review re-verification (≈09:47 local):** plan (`30ef72…`), tool (`c5ad…`) and replay record (`2afc…`) still match; **run_selftest.py and results.json no longer match the bound SHAs**: run_selftest.py is now `0dde65b3ef7687660b46b6eb7f4de869ec0b0dbc28c4928e440427192705a7ee` (mtime 09:43:55, size 32,886 B) and results.json is now `c83e2ed6654164d63e0a0c163d3d69d37fcbfed4dc74a92cbda1c76682da1116` (mtime 09:44:18, size 344,673 B). `fixtures/` and `out/` were also rewritten in the same window (09:43–09:44).
- **Change identified from the new bytes:** one added selftest case, `c19-historical-coordinate-injection`, with a new fixture `fixtures/geo-a05-injected-history.json` (`842cb93330e3183cf5c3cc7cd28e69b5714084499aa994f49cdc93d7fa2f3418`) that injects DECOY historical-coordinate fields (`historical_candidate_window_local_pt [304,50]`, `historical_candidate_frame_px [1313.5,332.333]`, `fallback_candidate_app_local_pt [42,988]`) into the geometry input; the case expects the candidate to stay the genuine frame-derived `[1297.333, 351.5]`. The updated results.json reports 44/44 cases pass, 9/9 global assertions (eligible set + `c19-historical-coordinate-injection`). I re-ran c19 myself: ELIGIBLE, candidate frame `[1297.333, 351.5]`, decoys absent — the added coverage is real and safety-positive.
- **Cause:** not this reviewer. I executed no selftest and made no repo write outside this review pair; every experiment wrote under `/tmp/rev27c-review/`. The modification is a concurrent post-freeze change: the round's own freeze record was written 09:35–09:38 and binds the old selftest/results SHAs. No updated freeze record / run-ledger entry documenting the change exists in `attempt-01/` (last entry 09:39).
- **Consequence (why this forces a new plan revision):** per the round's own freeze rule ("any byte change to the v8 tool, the selftest, the fixtures or the replay record after this record invalidates this freeze and requires a fresh self-test + fresh independent reviews"), the rev27c-1 freeze is invalidated. The plan's §7 approval precondition — reviews approving "this exact plan SHA and the bound locator/selftest SHAs" — no longer holds on disk: two of the five bound artifacts have different bytes, and a future live authorization citing Rev27c + the old SHAs would cite artifacts that no longer exist. No restoration is performed or authorized by this review (the instruction to this reviewer: restore nothing). The plan must therefore be re-issued as a new revision (e.g. rev27c-2) that re-binds the post-09:43 artifact set (or the owner restores the binding) after a fresh freeze + fresh self-test record, and both fresh reviews must then target the new bytes. The substantive design review below remains valid for the unchanged locator (`c5ad…`) and for the originally bound artifact bytes (`c94f…`/`943f…`).


## 1. Goal baseline (plain language, reconstructed from authoritative sources on disk)

Sources: the plan above; `evidence/20260921-rev27-save-all/attempt-05/selftest-attempt05/result-live-positive1.json`; `attempt-05/99-scope-status.json`; `attempt-05/locator-freeze-attempt05.json`; `attempt-05/frame-menu-post1.png` + `s6-menu-pairB1.json` + `live-window-geometry-post1.json`; frozen v7 tool copy `attempt-02/tools/locate_save_all_menu_item.py` (`ea09c1ca…`).

- **Primary outcome**: keep the route able to establish exactly ONE Save All candidate from a freshly, affirmatively observed LINE album menu — for the eventually owner-authorized single live click — without ever weakening target identity, menu identity, or geometry, and refusing everywhere else.
- **Observed failure**: in attempt-05 the menu was affirmatively observed and the target row 「儲存全部」 itself was read cleanly (word conf 96.955/96.985/96.556), but the frozen v7 locator returned `MENU_CONTENT_UNEXPECTED` (exit 5) and established NO candidate, because it required exact OCR for **all five** reference items and the non-target row 修改相簿名稱 was OCR'd as 修改相簿名般 (glyph 稱→般, conf 49.772): `reference_hits["修改相簿名稱"] = []`, `strict_order=false`, `identified_row_index=2`. This was a **false negative** — a scoped blocker `SAVE_ALL_CANDIDATE_LOCATOR_MENU_CONTENT_UNEXPECTED` — not a real menu problem. Save All dispatch stayed 0/1; chooser not observed; baseline unchanged; the menu may still be open (it must not be relied on).
- **Authorized fix (this round, zero GUI)**: an append-only v8-class locator that preserves every safety property while removing the accidental dependency on exact non-target OCR; validated by a deterministic offline selftest + offline replay against attempt-05 (must become ELIGIBLE) and earlier positive frames; then two fresh independent reviews of the exact SHAs. No live GUI input is authorized this round.
- **Essential (CORE)**: (1) target「儲存全部」exact, no fuzzy/partial/similar matching; (2) bounded tolerance only for NON-target OCR damage (exactly the attempt-05 class); (3) menu surface + window geometry stay hard and frame-SHA-bound; (4) candidate derived only from the current frame's own OCR + bbox + frame-bound window rect; (5) refusal on every ambiguity; (6) attempt-05 replay → ELIGIBLE, unique, reproducible; (7) full negative matrix refuses; (8) locator never clicks (at-most-once dispatch lives outside).
- **Supporting (design-only)**: future live route (unchanged gate chain, v8 replacing only the candidate component), menu-persistence rule (never reuse attempt-05's menu or its offline candidate). **Best-effort**: none.
- **What could block everything**: any non-ELIGIBLE verdict means no candidate, hence no click; and a wrong ELIGIBLE would be a safety failure — so the acceptance question is symmetric: close the false negative *without* admitting a wrong row/menu.
- **Status semantics**: an offline ELIGIBLE is NOT a live click authorization; this round's target status is at most `READY_FOR_REV27C_LIVE_AUTHORIZATION` after both reviews approve.

## 2. Method (what I did)

Read (fully): the plan; the v8 tool source line by line; a full `diff -u` against the frozen v7 copy (`ea09c1ca…`); the selftest script; `results.json` (all 43 cases + 9 global assertions); the replay builder + record; attempt-05's live refusal result + scope status + locator freeze + menu detector/AX geometry/frame; the rev27c freeze record; the v8 fixtures.

Ran (all offline; outputs to `/tmp/rev27c-review/`):
1. the frozen v8 CLI with `--out /tmp/...` on the three replay frames using the replay builder's own argv forms — attempt-05 (twice), attempt-13, attempt-19 — and byte-compared with the frozen replay outputs;
2. CLI spot-runs of selftest cases c01 (twice), c04, c09, c10, c15 against their fixtures, comparing verdicts/exit codes and (for c01) the stdout SHA recorded in `results.json`;
3. 20 synthetic probes of the pure `evaluate()` core (below), including two deliberate bypass attempts: a wrong-row/wrong-menu construction and target-fuzzy constructions;
4. static scans: input/write/network/GUI API grep, historical-constant grep, and a replication of the g07 forbidden-token scan + single-write-site check.

Did NOT run: the selftest script itself (it would rewrite `selftest/results.json` and `selftest/out/` in the repo — explicitly avoided); no GUI/computer-use action of any kind; no writes outside the two review files.

## 3. Pass 1 — top-down findings (does the design serve the goal?)

1. **The revision targets exactly the observed failure class.** The v7 refusal is fully explained by one non-target glyph (verified in `result-live-positive1.json`: four rows hit exactly, only 修改相簿名稱 empty, target row 2 clean). v8 replaces v7's "exact hit for every reference item + ascending hit order" cross-check with a **positional five-row structure check**: target matched with tolerance 0, non-target rows matched with at most ONE substituted character in a length-equal window. Nothing else in the text domain was relaxed. Necessity: YES; bounded: YES (constant `NON_TARGET_SUBSTITUTION_TOLERANCE = 1`, no tuning flags — v8 even removed v7's runtime `--min-overlap-px/--row-tol-*` flags so the behavior cannot be re-tuned at invocation time).
2. **No safety gate is weakened; several are added.** v7→v8 adds: popup-shape plausibility (≤30% area / ≤50% width / ≤80% height of the frame), bands-inside-bbox, adjacent-band non-overlap, target band-height range, target word-center cohesion, and a stricter candidate derivation (x = midpoint of the 2px-shrunk x-overlap with a ≥20px overlap rule; y = midpoint of the y-safe interior bounded by edge margins and 12px neighbor separation, with explicit all-inside placement checks). The only relaxation is the non-target glyph tolerance.
3. **Design economy is sound.** One constant, one loop, one new exit code (7), each new gate mapped to a named refusal verdict and covered by the case matrix; no runtime knobs; the tolerant matcher never touches the target (called with tolerance 0 for the target position and the target-identity check itself uses plain exact substring).
4. **Fail-closed posture holds end to end.** Every non-ELIGIBLE path returns before any candidate exists; binding failures are BAD_INPUT; the locator's sole write is its `--out` JSON; "offline ELIGIBLE ≠ click authorization" is stated in the plan, the replay record, and the fixtures.
5. **Honest residual.** The plan itself records the unbounded-by-proof residual (no formal proof that no other menu presents the same shape) and bounds it by the 1-glyph tolerance + the outside live-route gates. My probes confirm the boundary behaves as documented (≥2 substitutions refuse; 1 substitution per row passes — by design).

## 4. Pass 2 — bottom-up findings (files + code + execution)

1. **Source read-through.** Read-only: `argparse/hashlib/io/json/os/re/subprocess/PIL` only; `subprocess` used solely for `tesseract` (OCR and `--version`); only write site `emit()` (`--out`); no network/AX/GUI/input code (see checklist 8). Deterministic: no timestamps/randomness; fixed field order; identical inputs+argv produce byte-identical output (verified: attempt-05 ×2, c01 ×2, plus the frozen replay pairs).
2. **Binding gates.** Detector JSON must be `detect_menu_popup/MENU_DETECTED` with `post == frame arg` and `post_sha256 == frame SHA`; geometry JSON must carry the same frame SHA + window rect + scale; bbox must be a valid ascending rect inside the frame and pass popup-shape limits. Any failure → BAD_INPUT (refusals c09/c10/c11/c12/c13/c14/c17/c18 in the matrix; I re-ran c09/c10 myself with the same result).
3. **Target identity.** `TARGET_ITEM in row.cjk_text` (exact CJK substring, no tolerance) + exactly one matching row (0 → NOT_FOUND, 2+ → AMBIGUOUS). The tolerant matcher exists only in the structure loop and is passed tolerance 0 for the target. No fuzzy/similar/partial logic exists anywhere in the file.
4. **Menu identity.** Exactly five CJK item rows; positional match against `選擇項目/修改相簿名稱/儲存全部/刪除相簿/分享相簿`; the target must be precisely the 3rd item row (index equality with the uniquely identified target row). The reference order is never used to compute a position (only to check the observation).
5. **Geometry.** Item bands inside bbox (±1px rounding), adjacent bands non-overlapping, target band height ∈ [16px, 50% bbox height], target word-center spread ≤ max(8px, 25% band height); addressable region = window ∩ bbox ∩ frame (two-stage intersection with explicit None check — the pre-freeze correction documented in the plan; c15/s22 both return NOT_ADDRESSABLE; I re-ran c15); x-overlap ≥ 20px; x_safe = overlap − 2px each side must be non-empty; y_safe = [max(band.top+edge, above.bottom+12), min(band.bottom−edge, below.top−12)] with edge = max(6px, 15% band height) must be non-empty.
6. **Candidate derivation.** `frame_px = midpoint(x_safe), midpoint(y_safe)`; `screen_pt = px/scale`; `app_local_pt = screen_pt − window origin`. Every term comes from the passed frame's OCR rows, the passed bbox, and the passed frame-SHA-bound window rect. No file reads besides the three inputs; no coordinate constants in the file (g07-equivalent scan: forbidden hits `[]`, write sites 1).
7. **Diff vs frozen v7.** Strictly scoped: docstring/version/class strings, proper `os` import, row boxes kept as floats (v7 formatted strings), `cjk_text` normalization + `match_reference`, the added hard gates, removal of the three runtime tuning flags, new exit code 7, and extraction of the pure `evaluate()` core that the CLI calls (same code path). Frozen v7 copies remain byte-identical.
8. **Selftest read.** 43 cases (25 `evaluate-synthetic` + 18 `cli-real-frame`), all PASS; 9/9 global assertions PASS (including g02 refusals-never-eligible, g04 deterministic-run-twice, g05 no-fuzzy-target, g06 attempt-05 closed, g07 no historical constants/extra writes, g08 candidate is input-derived, g09 fixture frame bindings); eligible set = s01, s09, s10, c01–c04; the negative matrix covers every class the plan claims (target missing/misread/partial/twice/spanning; non-target 1-sub (single + multi-row) ELIGIBLE; 2-sub and length-change refused; count 4/6; order swap; duplicate context row; band overlap; empty y-safe; band outside bbox (x and y); whole-screen and over-wide bbox; window disjoint; x-overlap below rule; empty words; stale frame SHA; wrong-frame detector; non-detection; missing bbox; bbox outside frame; missing frame; unparseable geometry).
9. **Spot-runs (mine).** c01 stdout reproduced byte-identically (`c92f415148e19391a9343273c7a141155da597b0131f764b41fb0934fc14fa3b` = the SHA recorded in results.json for c01); c04 candidate (origin +7pt) `app_local_pt [316.167, 127.75]`, screen y unchanged — input-derived; c09 → BAD_INPUT 6; c10 → BAD_INPUT 6; c15 → NOT_ADDRESSABLE 3; attempt-05/13/19 replays byte-identical to the frozen outputs (see §0 table).
10. **Replay semantics.** attempt-05 → ELIGIBLE with candidate frame [1297.333, 351.5] / app-local [319.667, 134.75] and tiers [EXACT, TOLERANT_1_SUB, EXACT, EXACT, EXACT]; attempt-13 candidate [1313.5, 332.333] is numerically identical to the historical v7 candidate and the record states it was "compared, not used as input" (I verified the v7 historical value exists in `attempt-02/selftest/result-positive1.json`; v8 re-derives it from the frame; the geometry inputs carry `OFFLINE REPLAY INPUT ONLY`). Reproducibility caveat (documented behavior, not a defect): the output echoes input path strings, so byte-identity holds for identical argv; my re-runs used the builder's argv forms and matched byte-for-byte (a repo-relative geometry path yields semantically identical but byte-different JSON).

## 5. Checklist 1–9 (each independently confirmed)

1. **Target「儲存全部」requires affirmative exact recognition (no fuzzy matching). — YES.** Code: target matched only via `TARGET_ITEM in cjk_text` (+ exactly-one-row rule); the tolerant matcher is called with tolerance 0 for the target position. Evidence: probes X3a (`儲存全郜` → NOT_FOUND), X3b (truncated `儲存全` → NOT_FOUND), P6-style split-word case → NOT_FOUND; matrix s03/s04/s08 and replay c05 refuse similar strings; g05 passes. No similarity/partial/fuzzy code path exists in the file.
2. **Non-target OCR typo cannot alone cause a false negative (attempt-05 case ELIGIBLE; 1 substitution per non-target row, length-equal only). — YES.** Evidence: my independent CLI replay of attempt-05 → ELIGIBLE, exit 0, tiers [EXACT, TOLERANT_1_SUB, …], byte-identical ×2 (= frozen `e8f53bd3…`); own-coordinates attempt-05-class probe (probe4: `修改相簿名般`, conf 49.772) → ELIGIBLE with tiers [EXACT, TOLERANT_1_SUB, EXACT, EXACT, EXACT]; probes P3 (2 substitutions in one row) and P4 (length change) → MENU_CONTENT_UNEXPECTED; matrix s10 (three 1-sub rows) ELIGIBLE, s11/s12 refused; g06 passes.
3. **Relaxing non-target OCR cannot let a wrong menu or a wrong row pass (structure/order/target-position/geometry remain hard). — YES (with the plan's documented 1-glyph boundary).** Evidence: probe P1 (target physically at position 4) → MENU_CONTENT_UNEXPECTED; P2 (neighbors swapped) → MENU_CONTENT_UNEXPECTED; X2a (different five-item menu, ≥2 subs) → MENU_CONTENT_UNEXPECTED; X4 (target in two rows) → AMBIGUOUS; P5 (band overlap) → ROW_GEOMETRY_UNSAFE; Q1 (incohesive target words) → ROW_GEOMETRY_UNSAFE; X5c (overlap 19.8px) → NOT_ADDRESSABLE; X6 (window disjoint) → NOT_ADDRESSABLE; X7 (whole-screen bbox) → BAD_INPUT. Documented boundary probed honestly: X2b (a wrong-menu row differing by exactly 1 substituted char, `分享相片`) is still accepted — this is the deliberate tolerance, bounded to ≤1 glyph per row, and every non-text gate (popup shape, five-row positional order, exact target at exact middle, geometry) remained hard.
4. **Candidate derived from the current frame's own evidence only. — YES.** `evaluate()` inputs are only frame size, detector bbox, scale, window rect, and this frame's OCR words; all three inputs are SHA-bound (frame↔detector↔geometry). Dynamic: probe X5a (window origin +10pt) → frame/screen candidate unchanged, app-local shifted by exactly (−10, −10); c04 (+7pt) likewise; the three replay frames produce three different, frame-specific candidates; g08 passes; g07 scan shows no historical constants or extra file reads.
5. **No historical-coordinate fallback anywhere. — YES.** Static: g07-equivalent scan of the tool — `forbidden_hits: []`, single write site; no file reads besides `args.frame/detector/geometry`; no embedded coordinates (the only large literals are the v7 SHA string and `1 << 20`). Dynamic: origin-shift probes follow current inputs; the attempt-13 equality with the historical v7 candidate is explicitly a comparison recorded in the replay record, and the geometry inputs are labeled `OFFLINE REPLAY INPUT ONLY`.
6. **Attempt-05 replay candidate unique and reproducible (run ×2 byte-identical). — YES.** My CLI runs ×2: byte-identical to each other AND to the frozen `attempt-05-replay-1/2.json` (`e8f53bd3…`); one candidate only; record `determinism.attempt-05.byte_identical = true`; the 6 replay out files form 3 byte-identical pairs (verified with `cmp`/`shasum`).
7. **All ambiguous negative-matrix cases REFUSE. — YES.** At the bound revision (results.json `943f…`, 43 cases): 43/43 pass; 36 refusal-verdict cases; no refusal-class case is ELIGIBLE (g02; covers c07/c08); every case's observed verdict/exit equals its expected value (g01); no refusal case carries a candidate payload (spot-checked programmatically plus the refusals I re-ran: c09/c10/c15 + 12 of my own probes); every refusal exit code matches its documented class (2/3/4/5/6/7). The post-freeze on-disk state (`c83e…`, 44 cases incl. c19) reports 44/44 pass / 9/9 globals (verified by me) — but that state is not the plan-bound artifact (see §0-bis).
8. **Save All stays at-most-once (locator never clicks; dispatch outside it). — YES.** No input/automation capability in the file: no `pynput/Quartz/CGEvent/AppKit/osascript/click` references, no network imports (`socket/urllib/requests` absent); the only subprocess is `tesseract`; the CLI emits at most one candidate and returns. The dispatch mechanism and its 1-of-1 budget are outside this revision (unchanged Rev27 gate chain; attempt-05 already had ellipsis 1/1, Save All 0/1).
9. **Chooser is not part of this revision. — YES.** Chooser appears in the plan only as a future Gate B observation-only step (`NO chooser interaction`; §4/§8); zero "chooser" references in the v8 tool and selftest script; the replay record concerns candidate establishment only.

## 6. Independent experiments (commands and observations)

All under `/tmp/rev27c-review/`; repo untouched.

- `probe_evaluate.py` (pure `evaluate()`, my own synthetic coordinates): baseline ELIGIBLE with all 8 HARD checks pass; X2a wrong-menu (≥2 subs) → MENU_CONTENT_UNEXPECTED; X2b 1-sub boundary → ELIGIBLE (documented tolerance); X3a target 1-sub → NOT_FOUND; X3b target truncated → NOT_FOUND; X3c target + CJK suffix → ELIGIBLE (substring-containment semantics, see R2); X4 target twice → AMBIGUOUS; X5a origin +10pt → app-local shifts exactly, frame/screen invariant; X5b overlap exactly 20.0px → ELIGIBLE at x=1343.0 (boundary constant is `>=`); X5c overlap 19.8px → NOT_ADDRESSABLE; X6 window disjoint → NOT_ADDRESSABLE; X7 whole-screen bbox → BAD_INPUT.
- `probe2.py` (physically-moved rows — note a list-order swap is inert because rows are re-clustered from geometry, so I swapped y-bands instead): P1 target physically 4th → MENU_CONTENT_UNEXPECTED; P2 neighbors physically swapped → MENU_CONTENT_UNEXPECTED; P3 non-target 2-sub → MENU_CONTENT_UNEXPECTED; P4 non-target length-change → MENU_CONTENT_UNEXPECTED; P5 band overlap → ROW_GEOMETRY_UNSAFE; P6 target word dropped to another row → NOT_FOUND.
- `probe3.py`: Q1 isolated word-center spread violation (28px > 13px tolerance) → ROW_GEOMETRY_UNSAFE; Q2 non-CJK OCR noise inside the target (`儲存全.部`) → cjk_text `儲存全部` → ELIGIBLE (see R3).
- `probe4.py`: own-coordinates attempt-05-class typo (`修改相簿名般`, glyph conf 49.772 on the 修改相簿名稱 row) → ELIGIBLE, tiers [EXACT, TOLERANT_1_SUB, EXACT, EXACT, EXACT], candidate frame [1287.0, 356.6].
- c19 (post-freeze case, first-hand re-run after the incident was detected): decoy historical-coordinate fields injected into the geometry fixture are ignored → ELIGIBLE, candidate frame [1297.333, 351.5], app-local [319.667, 134.75]; no decoy value appears in the output.
- End-of-review SHA re-verification: plan/tool/replay-record still match; run_selftest.py and results.json now carry different bytes (new SHAs above) with mtimes 09:43:55/09:44:18 — the incident recorded in §0-bis.
- CLI replays (frozen tool, `--out /tmp`): attempt-05 ×2 → `e8f53bd3…` byte-identical (frozen out equal); attempt-13 → `b802113b…`; attempt-19 → `768c5cda…`; each matched the frozen replay outputs byte-for-byte with the builder's argv forms.
- CLI spot-cases: c01 ×2 byte-identical, equals results.json c01 stdout SHA `c92f4151…`; c04 → ELIGIBLE app-local [316.167, 127.75]; c09 → BAD_INPUT 6; c10 → BAD_INPUT 6; c15 → NOT_ADDRESSABLE 3.
- Static scans: input/GUI/network/write grep (only tesseract subprocess + one write site); g07-equivalent forbidden-token scan (`[]`); numeric-constant grep (no coordinate-like constants); no `__pycache__` created.

## 7. Residual risks / unknowns (honest record)

- **R1 (bounded, disclosed in the plan).** A hypothetical menu with the same five-row shape, exact 儲存全部 at the middle, and all non-target rows within 1 substituted glyph would pass. There is no formal proof this cannot exist. Bounds: ≤1 glyph per row in length-equal windows; plus everything outside the locator (fresh single-ellipsis provenance, detector binding, baseline tripwire, staging preflight, durable intent, at-most-once dispatch, post-click observation) and the fact such a menu would have to be nearly identical to the observed album menu. Mitigation is design-level; residual is recorded, not hidden.
- **R2 (inherited semantics).** Target identity uses substring containment on the CJK-only row text: a row reading e.g. 儲存全部相簿 would be accepted as the target row (probe X3c). This matches v7's containment semantics (`儲存全部 in row_text`); no substitution, insertion, or deletion of CJK characters in the target is ever accepted. No such item exists in the observed menu; the middle-position + neighbor structure further constrain it.
- **R3 (v8-only normalization, narrow).** Non-CJK characters are stripped before matching, so OCR punctuation noise inside the target occurrence no longer causes a refusal (probe Q2 `儲存全.部` → ELIGIBLE) — this is why the observed trailing "-"/"t" noise class on other rows is harmless, and it cannot admit a substituted/truncated CJK target. Worth recording because it is a (benign, intended) widening vs v7 in the noise dimension only.
- **R4 (offline-only evidence).** ELIGIBLE on historical frames confers no live authority; the attempt-05 menu may not persist; a future live round must re-derive fresh composite → ellipsis → detection → candidate under a new owner authorization citing Rev27c + the v8 SHA (menu-persistence rule). Live behavior under future OCR variation is covered by the bounded tolerance design, not by this replay.
- **R5 (reproducibility nuance).** Byte-identity of outputs requires identical argv strings because paths are echoed into the JSON; determinism per fixed inputs/argv is verified (multiple pairs). Readers comparing SHAs across differently-invoked runs should expect semantic equality but not necessarily byte equality.
- **R6 (not re-verified here, out of review scope).** The accepted-baseline digest (57 files / 17,924,900 bytes) was not re-derived by me; `git status` shows no tracked modifications and the round is append-only by construction. The claim "the whole selftest ran twice; results.json byte-identical across runs" was not reproduced by me (deliberately not re-running the script to avoid rewriting repo artifacts); per-case determinism (`deterministic: true` ×43) and my own ×2 CLI reruns support it.
- **R7 (minor documentation inconsistency, non-blocking).** `attempt-01/locator-freeze.json` states `refusal_cases: 25`, while `results.json` (the freeze-bound source of truth) reports `refusal_cases: 2` (refusal-class count) and contains 36 refusal-verdict cases (7 ELIGIBLE + 36 = 43). The substantive claims (43/43 pass, 9/9 globals, listed eligible set) agree; the 25 counter matches no observable count and should be read as a stale summary field, not as data.
- **R8 (opened during this review; drives the gate).** A concurrent post-freeze modification replaced the selftest script + fixtures + results.json (`c94f…`/`943f…` → `0dde65b3…`/`c83e2ed6…`, plus new fixture `842cb933…`) while this review was running; the freeze record and the plan's bound SHAs now disagree with disk (see §0-bis). Until a new plan revision re-binds the new bytes after a fresh freeze + fresh reviews, the rev27c-1 approval precondition cannot be satisfied and no future live authorization may cite Rev27c. This reviewer performed no restoration and made no changes to those files; the new state must be re-reviewed on its own merits in a fresh review cycle (its self-reported 44/44 + 9/9 and my c19 spot-run suggest it is consistent, but one spot-run is not a review).
- Unknowns: none that affect the checklist items; the live-route questions (menu persistence, future OCR variance) are explicitly deferred to a future authorized round by design.

## 8. Verdict

All nine checklist items PASS with direct evidence from disk + independent execution, judged against the plan-bound bytes that existed at review start: the v8 design closes the attempt-05 false negative while preserving target identity, menu identity and geometry, adding hard gates rather than weakening any, and its residual boundaries are disclosed and bounded. However, two of the five plan-bound artifacts (run_selftest.py `c94f…`, results.json `943f…`) were replaced on disk by a concurrent post-freeze modification during this review (now `0dde65b3…`/`c83e2ed6…`, new fixture `842cb933…`; see §0-bis and R8). That invalidates the rev27c-1 freeze per the round's own freeze rule and breaks the plan's §7 approval precondition for the exact bound SHAs, so this plan revision cannot be approved as the binding basis for any future live authorization. The remedy is procedural and small: issue a new plan revision (rev27c-2) that re-binds the post-09:43 artifact set after a fresh freeze + fresh self-test record, and obtain fresh independent reviews of the new bytes (the unchanged locator `c5ad…`, its design, and the negative-matrix conclusions already reviewed here would carry over as inputs, not as approvals). Final gate:

PLAN_REVISION_REQUIRED
