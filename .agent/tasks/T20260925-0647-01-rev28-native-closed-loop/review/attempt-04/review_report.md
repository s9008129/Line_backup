# Plan Review Report

## REVIEW_METADATA
- TASK_ID: `T20260925-0647-01-rev28-native-closed-loop`
- REVIEW_ATTEMPT: `04` (second, differently-seeded independent Stage-02 reviewer; attempt-01/02/03 directories were not touched. Finding IDs `RV-01…RV-05` are attempt-local.)
- REVIEWED_PLAN_REVISION: `2`
- REVIEWED_PLAN_SHA256: `84b65bc9eebb763d9ea3839db1ecfafa7f181b7fa6be92cf4216e2befceba1ed` (recomputed on disk this round; matches the expected value; 505 lines)
- PLAN_SNAPSHOT_PATH: `.agent/tasks/T20260925-0647-01-rev28-native-closed-loop/review/attempt-04/plan_snapshot.md` (byte-copy of `plan.md`; SHA-256 identical)
- Repository anchor observed: HEAD `766b22c4f8bf29b9d0a46049c75309c70593d66d` == `origin/master`, ahead 0, working tree has only untracked paths; Issue `ISSUE-2026-09-24-rev27e4-r2-ownership-provenance.md` SHA-256 `0b3c9efab209bcb4c8c9b5dcaa5fa1ca6279234df0ee3cc0984da829ead67a2d` (recomputed, match); staging `RUN-20260923-111908-01` exists, 0 entries; accepted baseline (actual on-disk spelling `album-2024-05-13_to_2024-05-17_57`) = 57 files / 17,924,900 bytes / manifest digest `b7debe929a24406a44f53708194b644a4811559cf91ad87d5a55e5da91a28fbd` reproduced with the plan's stated method / content-multiset `ee958e6467676506a1c7aaf237a4376ecc5e5083fd94aa6fd0d56d376cacdaaf` reproduced from the live files and set-equal to `baseline-content-multiset.json` (file SHA `3c932d8ccb9f4d2a7945463861fb4ebae066eadb59b8ff2702767b3a9f851bc2`); all 20 replay-fixture SHAs in `HISTORICAL_REPLAY_PLAN` byte-exact; probe citations `prototype-round1-findings.md` (`43998519…`) and `prototype-round2-outputs.txt` (`afd6c26d…`) match; SDK claim re-grepped: `pointPixelScale` declared in `SCStream.h`/`SCShareableContent.h`, `ignoreShadows`/`includeChildWindows` in `SCScreenshotManager.h`/`SCStream.h`, and no `contentScale`/`scaleFactor` symbol in either SDK.
- Reviewer runtime/model: fresh Codex CLI session (no Planner transcript). Model tag not exposed to this session (informational only).
- Anti-anchoring note: the authoritative goal (attachment `41a5dfc8-e350-429c-a01b-aea3a67e03fd/pasted-text-1.txt`, 782 lines) and an internal `GOAL_BASELINE` were established before reading the full plan; all findings below were formed before attempt-01/02 reports were consulted, and those reports are used only as an incorporation cross-check (their statements are claims, not authority).

## OWNER_VERDICT
**Goal as understood:** the owner wants exactly one more end-to-end backup of the album `2024/05/13～05/17` (57 photos) of group `旻謙允禎成長日記` from `jp.naver.line.mac`, rebuilt on a new macOS-native closed-loop architecture (ScreenCaptureKit seeing, Vision labels, a real Quartz system click, Accessibility for the native dialog), with success proven by directly observing the resulting chooser and then by 57 complete, stable files in a fresh staging folder whose content equals the already accepted baseline (`DUPLICATE_CONTENT_CONFIRMED`), leaving the baseline untouched.

**Essentials (what actually carries the outcome):** capability probe + synthetic harness calibration (including freezing the capture rule, postcondition bounds, chooser predicate, and tripwire-attribution rule), one canonical coordinate transform, fresh window-identity binding, the closed-loop Save All postcondition (directly observed chooser only — never click-return/menu-gone/panel-service presence), exactly-once confirmation into a fresh empty staging run, and filesystem/content verification against the baseline. Every pre-live gate is deliberately hard; the one Save All dispatch and the one destination confirmation are irreversible and are never retried.

**Optional / deferred (should not stop the core path):** Foundation Models diagnostics, feature-print/registration stability comparison, the AX chooser-navigation path (keyboard fallback), and the documentation/manifest deliverables. All are correctly labeled non-gating.

**What can block the whole run and why (the one real defect):** the plan's stated path for the accepted baseline folder contains a one-character error — the plan writes `album-2024-05-13_to-2024-05-17_57` (a dash after `to`) while the actual folder and the plan's *own* recorded JSON evidence use `album-2024-05-13_to_2024-05-17_57` (an underscore after `to`). As written, any automated check that opens the baseline by that path fails immediately (I reproduced this repeatedly); the live preflight ("baseline unchanged") and the independent Stage-05 recomputation are exactly such checks, so the plan cannot be executed literally on its most load-bearing identity anchor without either a blocked preflight or a silent, unreviewed path substitution. The fix is trivial — copy the exact spelling from the plan's own `baseline-content-multiset.json` and re-approve — but it must go into the plan itself.

**Complexity verdict:** proportionate. A new native engine, harness, ledger, and review set each map to a goal-mandated outcome or must-not-break invariant; no untraceable scope was found; the three extra adversarial scenarios are cheap and useful.

**Biggest remaining risk (accepted, pre-declared):** the single Save All budget can be consumed by an honest-but-unsuccessful outcome (no chooser observed, chooser seen only after the hard cap, a non-native chooser refused by the frozen predicate, or a fail-closed abort on an unattributable filesystem write during concurrent machine activity). The plan discloses each of these and forbids retries or label inflation; that is the correct posture for a one-shot run.

## GOAL_BASELINE
Reconstructed from the authoritative goal text (`41a5dfc8-…/pasted-text-1.txt`, 782 lines; read before the plan):
- **Primary objective:** autonomously redesign, implement, validate, review, and — only after all safety/calibration gates pass — execute ONE new production LINE album backup using macOS 27 native sensing/visual localization/event injection/AX/filesystem verification. Solve autonomously; human intervention only for genuinely external blockers.
- **Target:** app `jp.naver.line.mac`; group `旻謙允禎成長日記`; album `2024/05/13～05/17`; 57 photos; `禎 U+798E` and `楨 U+6968` must never be normalized/merged. The historical accepted baseline (57 files / 17,924,900 bytes / manifest digest `b7debe92…`) is READ-ONLY.
- **Repository anchors:** HEAD == `origin/master` == `766b22c4…`, ahead 0; issue `ISSUE-2026-09-24-rev27e4-r2-ownership-provenance.md` SHA `0b3c9efa…`; verify facts from disk; do not reset/overwrite historical evidence; no `__pycache__`/`*.pyc`; no push unless separately authorized.
- **Mandated architecture:** closed loop OBSERVE → LOCATE → ACT → OBSERVE DIRECT POSTCONDITION → VERIFY FILESYSTEM; the primary Save All acknowledgement is the directly observed resulting chooser (click-return, menu disappearance, panel-service presence, and logs are supporting/forensic only); ScreenCaptureKit window-scoped capture (`SCContentFilter(desktopIndependentWindow:)`); no fullscreen as primary localization source; multi-source window identity binding (bundle, pid/process instance, windowID, frame, epoch, capture SHA); Vision OCR (accurate, zh-Hant + en-US, language correction off) with identity/geometry separation and no raw-character-bbox clicking; structural locators incl. `57張照片`, album ellipsis, `儲存全部`, chooser; frame SHA for provenance and registered/normalized evidence for semantic state; Foundation Models diagnostics only (never production click authority); native Quartz actuator (`CGPreflightPostEventAccess`, `CGEventCreateMouseEvent`+`CGEventPost`, mouseMoved → revalidate → down → up) with one canonical transform carrying window origin, content rect, content scale, backing scale, capture dimensions, and no implicit 2×; synthetic AppKit harness proving capture (occlusion-surviving, stable coordinates, correct Retina scale, popup/child identification), Vision localization, transforms, Quartz topmost-popup routing, postcondition detection; AX-first for the native chooser; app activation with post-verification and recoverable focus theft; persistent execution state machine (`APP_READY`…`FINALIZED`) owning recovery with per-transition precondition/action/postcondition/timeout/bounded recovery/evidence.
- **Risk classes:** `REVERSIBLE_NAVIGATION` / `PRE_SIDE_EFFECT_ACTION` / `IRREVERSIBLE_SIDE_EFFECT` defined and independently reviewed; Save All empirically classified, never inferred from its label; destination confirmation is irreversible and exactly-once with durable intent written before confirmation.
- **Chooser/destination rules:** AX semantics first; destination `…/staging/<UNIQUE-RUN-ID>/` fresh, canonicalized, inside the approved root, empty at start, not the baseline and not ancestor/descendant of it, no symlink escape; keyboard/Go-to-Folder only after fresh chooser identity verification; confirmation exactly-once.
- **Filesystem completion:** exactly 57 completed image files, no subdirectories, no partial suffixes, no zero-byte files, all decodable, stable sizes/mtimes across repeated samples, quiescence interval, manifest SHA-256, and content hash multiset compared against the accepted baseline; identical content → `DUPLICATE_CONTENT_CONFIRMED`; never create a second accepted copy; preserve staging unless cleanup is separately authorized.
- **Transactional safety:** fresh preconditions, durable intent, single dispatch, postcondition observation, filesystem verification, append-only ledger; unknown irreversible dispatches are never blindly retried; reversible state acquisition only under the exact reviewed bounded policy.
- **Autonomy and bounds:** revise plans, run reviews, repair; ≤3 autonomous recovery cycles per identical live-state blocker; 3 consecutive reviews on the same unresolved dependency → `BLOCKED_WITH_ROOT_CAUSE`; never weaken a safety invariant to obtain approval.
- **Reviews:** seven named pre-live topic reviews plus an adversarial review of the goal's verbatim 22 scenarios (wrong window, wrong bundle, stale windowID, wrong Retina transform, occluding app, window movement, popup movement, wrong row, duplicate target OCR, OCR corruption, whole-screen false positive, click outside popup, click on neighboring row, focus theft, postcondition timeout, fake chooser-like surface, unexpected filesystem write, zero-byte burst, partial download, duplicate files, process restart, multi-display transform); production only if all required reviews approve the exact frozen implementation.
- **Production authorization envelope:** ONE new production run for exactly that album/group/count; irreversible destination confirmation max = 1; side-effecting operations max = 1 (unless the reviewed architecture proves idempotent recovery); reversible navigation per reviewed budget; no historical coordinates — every live coordinate fresh-derived.
- **Success condition:** correct album identity, native localization, reviewed actuation, chooser postcondition confirmed, unique staging selected, download dispatched exactly as authorized, 57 complete stable images, content verification PASS, accepted baseline unchanged, registry/evidence updated consistently, no unexplained side effect → `DUPLICATE_CONTENT_CONFIRMED` / `REV28_END_TO_END_LINE_ALBUM_BACKUP_VERIFIED`; never claim `BACKUP_COMPLETE` from a click, menu disappearance, chooser appearance alone, or an unverified file count.
- **Deliverables and reporting:** the listed artifacts plus a final report stating sensing API, visual API, actuation API, why the event reaches the intended window, how transforms were proved, how chooser success was directly observed, how filesystem completion was proved, whether the production run completed, whether content matched the baseline, and whether any ambiguity remains.

## GOAL_ALIGNMENT
- A1 Intent: the Plan's `PRIMARY_OUTCOME` (`DUPLICATE_CONTENT_CONFIRMED` in a fresh staging run with the baseline unchanged) is the goal's own success proof, not an implementation-completeness proxy; the acceptance chain observes the actual user outcome (one new verified backup route) rather than "code written".
- The Plan does not let a supporting detail become the project goal: ScreenCaptureKit/Vision/Quartz/AX are mandated by the goal itself; Foundation Models, registration, and the AX-chooser path are correctly subordinated (SUPPORTING/degradable) with the deterministic core carrying the outcome.
- Outcome-proving checks are present and separated: chooser directly observed (not click-return), filesystem predicate exact, baseline-unchanged invariant, independent recomputation by Stage 05. No acceptance criterion reduces to synthetic-suite PASS.
- Failure semantics match the owner's intent: a scoped honest terminal (no chooser / late chooser / content mismatch) is explicitly *not* "Done"; `DONE` requires primary-outcome achievement. No label inflation path was found.
- One goal-alignment caveat is captured by RV-01 (a wrong anchor path would make the plan's own reproducibility claim unreachable as written).

## NECESSITY_AND_TRACEABILITY
- R1–R25 map cleanly onto the goal's architecture, safety, review, envelope, and deliverable sections; each `CORE` item traces to a goal mandate or a must-not-break invariant (e.g., R9 transform ↔ goal's explicit transform record; R15/R16 ↔ postcondition/chooser sections; R18 ↔ transactional safety; R21 ↔ production envelope; R24 ↔ barrier supersession).
- `SUPPORTING` items (R6 registration/tracking, R7 Foundation Models, R23 deliverables) are genuinely non-gating and are protected from silent drops by the plan's own classification rule; none of them acquires an unjustified global veto.
- The W2-freeze obligations (capture per-state rule/tolerance; postcondition cadence/bounds; chooser-affirmation predicate incl. hosting shape and ownership semantics; tripwire-attribution rule; restart fixture) each trace to a specific attempt-02 correction (RV-01/03/07/08/09) and to the goal's "independently reviewed" obligations; they are the plan's mechanism for making the one-shot run reviewable. `X01–X03` are cheap, labeled extensions beyond the goal's 22.
- No significant untraceable work, schema expansion, or field accumulation was found; nothing in the plan fails the deletion test without losing an acceptance/safety obligation.
- Minor traceability nuance: replay/adversarial suites are labeled non-closure-gating yet act as hard preconditions to the live run — see RV-02's clarification request.

## GATE_AND_VETO_AUDIT
- Every gate has a decision that becomes unsafe without it: (a) capability probe — the whole approach depends on native APIs being actually available/trusted in the executable context; (b) harness calibration + rule freeze — the one-shot run cannot discover its own semantics safely; (c) offline replay — regression evidence before touching LINE; (d) adversarial suite — the goal's own 22-scenario mandate; (e) frozen-SHA reviews — the goal's "production only if all required reviews approve"; (f) live preflight — baseline unchanged/staging empty/TCC; (g) reconnaissance gate — LINE-specific surface facts with zero side effects, before the irreversible step.
- The irreversible dispatch has exactly one budget and a hard prerequisite chain; everything else fails locally without blocking unrelated work (the plan's explicit ordering rule). Failure is contained at the narrowest safe boundary (refusals instead of clicks; fail-closed instead of guessing).
- Global blocking is confined to the irreversible production step and to task-closure gates; no supporting/best-effort item can veto unrelated core behavior. Waivers are correctly denied to agents (`WAIVER_ALLOWED: NO`; owner-only authority), so no self-waiver path exists.
- v4.2 §7 checks: subjects are kept orthogonal (PRIMARY_OUTCOME / IMPLEMENTATION / CORE_ACCEPTANCE / REQUIRED_VERIFICATION / INDEPENDENT_ACCEPTANCE / TASK_CLOSURE); no legacy terminal schema is active; `IMPLEMENTATION_BLOCKED` is reserved; the terminal taxonomy routes indeterminate/blocked cases without fabricating PASS; `DONE` prerequisites are stated (frozen-SHA reviews, manifest, replay, adversarial, ledger, handoff, local commits, closeout audit). Broad-gate policy is explicit (`BASELINE_REQUIRED: NO`; no repository-wide gates; baseline comparison is the success predicate itself). The §7.3 field *vocabulary* deviation is RV-02; the status-mechanics fixtures listed in the v4.2 integration note are Stage-04/05 artifacts (see TESTABILITY_AND_ACCEPTANCE).
- One clarity defect (in RV-02): a `SUPPORTING`/`NO` row (replay/adversarial) is simultaneously enforced as §LIVE_RUN_ENVELOPE gates (c)/(d). The interlock is safety-justified (protect the single irreversible dispatch), but the plan should say so explicitly and distinguish live-run interlock from closure gating.

## COUPLING_AND_FAILURE_CONTAINMENT
- Independent signals are not collapsed into one all-or-nothing flag: chooser affirmation requires new-surface + native-panel AX semantics + ownership (pid/bundle/signing + start time; census as *one* ingredient), with explicit refusal fixtures; identity binding is multi-source with stale-ID invalidation; postcondition evidence keeps one evidence standard per sample.
- Optional planes degrade deterministically (Foundation Models → diagnostics; registration → deterministic comparison; AX chooser navigation → tested keyboard path) without blocking the core, exactly as the goal requires.
- No low-value adapter became a single point of failure; the filesystem tripwire fails closed only on *unattributable* writes (RV-03 records the narrower residue), and the pre-dispatch ≥10 s FSEvents baseline is a purposeful anti-false-positive control under concurrent machine activity.
- Recovery is bounded and scoped per class: reversible navigation has fresh-precondition retries with a global ceiling (12) and `ABORTED_REVERSIBLE_BUDGET_EXHAUSTED`; irreversible ops are never retried; crash recovery resumes observe-only with ledger hash-chain continuity (fixture-enforced).

## DESIGN_ECONOMY
- The new modules each pay for an outcome/invariant: sensor+perception (goal), actuator (goal), AX chooser plane (goal), harness (goal), ledger/state machine (goal), staging verifier (goal), W2 freezes (reviewability of the one-shot run), reviews (goal). No speculative future-facing abstraction, plugin framework, or state explosion was found.
- Deletion test: removing registration/tracking, Foundation Models, or the AX-chooser path costs no acceptance criterion (all correctly non-gating); removing the harness/W2 freezes or the ledger *does* cost safety/acceptance, which is why they stay. This is the right side of the economy line.
- The added 25-scenario matrix costs little (fixtures largely share the harness) and buys the goal's mandated coverage plus three cheap extensions.

## CRITICAL_PATH_AND_PRIORITY
- Sequence is correct: capability+harness first, deterministic core, offline replay, adversarial hardening, frozen reviews, reconnaissance, then the single irreversible live step, then filesystem/content verification and closeout. The one-shot irreversible step is last among live actions; supporting documentation (W6) is off the critical path.
- No priority inversion: the plan spends proportional effort on the CORE path (transform/identity/postcondition/predicate) while optional items stay cheap; acceptance (Stage 05) verifies CORE first and recomputes from disk rather than trusting Stage-04 claims.
- The W2 freeze artifacts are correctly treated as critical-path inputs to the seven reviews; post-freeze rule changes are replan+review, never implementer edits.

## REQUIREMENT_FIDELITY
- The goal's adversarial list is verbatim 22 items; the plan's `G01–G22` map 1:1 in goal order (with `G05` refined to a separate-process occluder, `G21` to crash-injection/observe-only resume, `G22` labeled fail-closed on this single-display machine), plus `X01–X03` explicitly labeled extensions = 25. Verified against the goal text, not the plan's summary.
- Goal mandates preserved: `禎/楨` non-normalization; zh-Hant + en-US OCR with correction off; identity/geometry separation; no historical coordinates; no fullscreen as primary localization; exactly-once confirmation; durable intent; fresh unique staging with the ancestor/descendant exclusion; 57/stable/decodable/17,924,900 B/content-multiset predicate; no second accepted copy; baseline read-only; no cleanup without authorization; no push; no pycache; barrier supersession reviewed before any new Save All.
- No out-of-scope requirement additions and no missing goal requirement were found; the plan is a faithful refinement of the goal.

## GROUNDING_AND_DRIFT
Independently re-verified this round: HEAD/origin anchor and ahead=0; issue SHA; staging empty; baseline count/bytes/manifest digest (method reproduced exactly: sorted `relpath\tsize\tsha256` lines each `\n`-terminated) and the name-excluded content-multiset (sorted hashes joined `\n`, no trailing newline) set-equal to the recorded JSON; all 20 replay fixtures; probe file SHAs and probe geometry claims (shadow inflation ≈56 pt/side; `ignoreShadows=true` giving exact frame×2 = 800×664 px and child-union 1040×798 px; `SCWindow.frame` 3 pt inset instability; `sourceRect` window-local semantics with `-3811` out-of-bounds); SDK symbols (`pointPixelScale` declared; `contentScale`/`scaleFactor` absent; `ignoreShadows`/`includeChildWindows` present).
**One grounding failure:** plan.md line 87 spells the accepted-baseline directory `…_to-2024-05-17_57` (dash), while the actual directory and the plan's own `baseline-content-multiset.json.source_dir` spell it `…_to_2024-05-17_57` (underscore). Byte-level: plan `5f 74 6f 2d` vs disk/JSON `5f 74 6f 5f`. I reproduced the consequence: literal-path access using the plan's spelling returns ENOENT; the correct spelling yields the matching digests. (Note: attempt-02's report repeats the same misspelling in its metadata — evidence that this typo propagates into downstream artifacts; attempts' text is not authority.) This is RV-01.

## ARCHITECTURE_AND_CONTRACTS
- The module map and architecture sections are coherent with the goal: thin OS edges + deterministic cores; one canonical transform with recorded per-capture bbox/config and an independent scale cross-check; state machine + ledger define the observable contract; the `DUPLICATE_CONTENT_CONFIRMED` predicate is stated exactly (57 files, no subdirs/partials/zero-byte, decodable/stable, 17,924,900 B, `SHA-256(sort(values).joined("\n"))` no trailing newline == `ee958e64…`), with the name-inclusive `b7debe92…` demoted to identity/tripwire only; named mismatch classes and the route-defect-vs-album-changed hypothesis split are present.
- Review binding is sound: seven topic reviews + adversarial + barrier supersession approve exact frozen SHAs; W2 artifacts are mandatory review inputs; a change to any frozen rule is a replan + fresh review.
- Load-bearing semantic contract defect (minor): the §7.3 verification-item schema uses non-canonical value vocabularies (`EVIDENCE_ROLE: DIRECT/SUPPORTING`; `CLOSURE_GATE: YES/NO`) instead of the v4.2 §7.3 enums, omits a `CHECK_ID` column, and defers `CHECK_RESULT`/`WAIVER_STATUS` wholesale — see RV-02.
- The chooser-affirmation predicate's dependency on macOS 27 panel behavior is handled by W2 calibration against a real `NSOpenPanel` plus refusal fixtures and review 6, with the LINE-specific shape unavoidably resolved only at the live step — see RV-04/RV-05 for the small wording gaps.

## DATA_SECURITY_RELIABILITY
- Read-only posture for the accepted baseline and all historical evidence is explicit and enforced by intent (do-not-touch list, "never copy staging into the accepted baseline", no cleanup without authorization).
- Exactly-once and durable-intent semantics for the irreversible dispatch/confirmation are stated and budget-counted; no blind retry of any irreversible op; crash recovery resumes observe-only with hash-chained ledger continuity; the state machine can never silently re-dispatch (fixture-asserted).
- Concurrency/attribution: the tripwire rule with a pre-dispatch environmental baseline + process census diff + timing, failing closed on unattributable writes, is a reasonable containment for the observed concurrent-machine-activity risk; the remaining disclosure gap is RV-03.
- Privacy/secret hygiene: LINE captures/OCR extracts stay in the untracked evidence dir; local commits only; no push; no secrets involved. TCC/permission state is re-verified inside the executable context at gate (f).

## IMPLEMENTATION_SEQUENCE
- W1→W7 ordering is dependency-correct and the attempt-02 sequencing obligations are honored (W1 consumes the revised bbox/tolerance rule; W4 owns the exact staging predicate before acceptance tests freeze; W5 runs the full 25-scenario matrix; W2 freezes precede reviews 4/6).
- Phase A (read-only reconnaissance, zero side-effecting dispatches, own repair/replan loop) correctly precedes Phase B and cannot consume the live budget. The live preflight rechecks LINE launchability, baseline unchanged, staging empty, ledger armed, TCC grants.
- No blocking sequencing defect found; RV-01 must be fixed before handoff so the preflight's baseline path actually resolves.

## TESTABILITY_AND_ACCEPTANCE
- Test layers are complete and layered: deterministic unit tests, harness integration with SHA-bound bundles, offline replay (fixture table with SHAs; divergence → hypothesis/test, never threshold tweaks), adversarial suite with two-run semantic determinism, live ledger/captures/AX/manifest, and Stage-05 independent recomputation from disk.
- Acceptance predicates are exact and observable (chooser predicate; 57/stable/decodable/bytes/multiset; baseline unchanged incl. digests + mtimes). `DUPLICATE_CONTENT_CONFIRMED` cannot be reached from logs/panel presence/counts alone.
- Status contract: subjects are kept orthogonal and the terminal taxonomy covers success, late affirmative, no-chooser, mismatch classes, aborts, indeterminate, and `BLOCKED_WITH_ROOT_CAUSE` with scope/subject naming; `DONE` prerequisites are explicit. The per-item `CHECK_RESULT`/`WAIVER_STATUS` and the v4.2 status-mechanics fixtures are (acceptably) deferred to Stage-04 `execution.md` and the Stage-05 report; the plan-level schema labels should still be normalized (RV-02).
- Postcondition bounds are plan-time provisional by design (re-frozen by W2 with ≥20 real-panel runs and bound by review 4); this is the right mechanism, and any later change is explicitly a replan.

## SCOPE_AND_COMPLEXITY
- Scope equals the goal's deliverables plus the three labeled extensions; no unrelated refactor, no cleanup, no push, no reclassification of attempt-07; uncommitted Rev27e WIP is preserved as evidence.
- Complexity is justified by the one-shot irreversible nature of the run and by the goal's own review/evidence mandates; the design avoids inventing runtime semantics by freezing them in W2 under review.
- The only scope-neutral defect is the anchor path typo (RV-01); all other findings are contract-precision/word-tuning items.

## FINDINGS
### RV-01
- ID: `RV-01`
- Severity: `MAJOR`
- Category: `GROUNDING`
- Affected: `plan.md` line 87 (§VERIFIED_REPOSITORY_FACTS, accepted-baseline path) and every downstream consumer of that literal path: live preflight gate (f) "baseline unchanged", §12/§VERIFICATION_AND_ACCEPTANCE "Stage 05 recomputes … both baseline digests … from disk itself", and any verification script copied from the plan.
- Evidence: plan.md line 87 bytes at the directory name: `5f 74 6f 2d` → `album-2024-05-13_to-2024-05-17_57` (dash after `to`). On-disk directory (parent readdir; exactly one `album-2024-05-13*` match) and the plan's own `evidence/20260925-rev28-native-closed-loop/baseline-content-multiset.json` `source_dir` bytes `5f 74 6f 5f` → `album-2024-05-13_to_2024-05-17_57` (underscore). Reproduced consequence: literal-path `ls`/`stat`/Python access with the plan's spelling → ENOENT; with the true spelling → 57 files / 17,924,900 B / `b7debe92…` (method-reproduced) / `ee958e64…`. [VERIFIED] (The digest and multiset claims themselves are TRUE; only the path string is wrong. The same misspelling exists in the revision-1 snapshot, so this is a carried defect, not a revision-2 regression.)
- Failure/rework mechanism: as written, the plan cannot be executed literally on its most load-bearing identity anchor. A literal implementation aborts at preflight (or fails the Stage-05 recomputation) with ENOENT — a fail-closed but outcome-blocking failure; an implementer who silently substitutes the real path is deviating from the frozen plan on a CORE verification anchor (unreviewed drift), and the plan's own claim "recomputed on disk this round = MATCH" is attached to a path that does not resolve.
- Smallest required correction: replace the path string in line 87 with the exact on-disk spelling (copy verbatim from `baseline-content-multiset.json.source_dir`); optionally harden the Phase-A/live preflight to assert that the baseline path resolves and its manifest digest equals `b7debe92…` before proceeding. Re-review the new revision.

### RV-02
- ID: `RV-02`
- Severity: `MINOR`
- Category: `SEMANTIC_CONTRACT`, `TEST`
- Affected: `plan.md` §VERIFICATION_AND_ACCEPTANCE, "Verification-item schema (v4.2 §7.3)" table (lines 435–453) and its surrounding paragraphs.
- Evidence: the table's `EVIDENCE_ROLE` values are `DIRECT`/`SUPPORTING` and `CLOSURE_GATE` values are `YES — blocks Phase B`/`YES`/`NO`, while `~/.codex/policies/workflow-routing.md` §7.3 defines `EVIDENCE_ROLE: OUTCOME | MUST_NOT_BREAK | REPOSITORY_HEALTH | DIAGNOSTIC` and `CLOSURE_GATE: HARD_CLEAN | BASELINE_DELTA | NON_GATING`; the table has no `CHECK_ID` column; `CHECK_RESULT`/`WAIVER_STATUS` are deferred to `execution.md`/Stage-05 rather than declared. Intent (what blocks what) is otherwise clear and no self-waiver path exists. [VERIFIED]
- Failure/rework mechanism: downstream status tooling/readers keyed to canonical vocabulary cannot machine-read the committed table; `EVIDENCE_ROLE: DIRECT` erases the OUTCOME vs MUST_NOT_BREAK distinction (e.g., "Baseline unchanged" is a must-not-break invariant, not an outcome check); closure-gating vs live-run-interlock semantics for replay/adversarial are conflated.
- Smallest required correction: relabel values to the canonical §7.3 enums preserving intent; add `CHECK_ID`s; declare `CHECK_RESULT: NOT_RUN` / `WAIVER_STATUS: NOT_ALLOWED` at plan level (or state exactly where they are declared); add one sentence distinguishing closure gating from the live-run safety interlock with its rationale.

### RV-03
- ID: `RV-03`
- Severity: `MINOR`
- Category: `RELIABILITY`, `SECURITY`
- Affected: `plan.md` §10 filesystem-tripwire attribution paragraph and the final-report "no unexplained side effect" obligation.
- Evidence: attributable post-dispatch writes outside the staging dir are "recorded as supporting evidence"; there is no named record/terminal/disclosure for an attributable-but-unexpected external write (e.g., LINE or another attributable actor writing directly under `~/Downloads` outside the fresh run dir). Revision 1 aborted on "any unexpected filesystem write"; revision 2 narrows this to unattributable-only (attempt-02 RV-07) with compensating controls (census diff, path pattern, FSEvents timing, ≥10 s pre-dispatch baseline, concurrent-activity caveat) — the narrowing is deliberate and reviewed, but the disclosure obligation for attributable writes is not explicit. [VERIFIED]
- Failure/rework mechanism: under the goal's "no unexplained side effect" success condition, an attributable external write could be silently absorbed as "supporting evidence" and the success terminal could omit it → an unexplained/undisclosed side effect despite the exacting evidence rules (small, but it is exactly the class of claim the goal forbids).
- Smallest required correction: name the observation (e.g., `ATTRIBUTED_EXTERNAL_WRITE_OBSERVED`) and require it to be disclosed in the ledger and final report as a side-effect observation (non-fatal), keeping the fail-closed default for unattributable writes.

### RV-04
- ID: `RV-04`
- Severity: `MINOR`
- Category: `SEMANTIC_CONTRACT`, `TEST`
- Affected: `plan.md` §11 chooser-affirmation predicate + §LIVE_RUN_ENVELOPE abort/terminal taxonomy + §ACCEPTED_TERMINAL_STATES_AND_CLOSURE.
- Evidence: the predicate is frozen by W2 calibration against a real `NSOpenPanel` (native panel semantics) and bound by review 6, but LINE's actual chooser hosting shape cannot be pre-validated before the single live dispatch (Phase A reconnaissance never opens a chooser — doing so would consume the Save All budget). If LINE's chooser deviates from the frozen native-panel-shaped predicate, the engine refuses confirmation and the single budget is consumed; the plan implies this (refusal semantics, risk #4) but does not name this refusal cause in the terminal taxonomy. [VERIFIED]
- Failure/rework mechanism: an honest indeterminate end-of-run whose recorded cause is generic rather than "frozen-predicate mismatch" weakens the forensic value and could invite misreading as a detector failure rather than an architecture assumption mismatch; a future revision could be tempted to loosen the predicate unreviewed.
- Smallest required correction: name the refusal terminal/sub-cause (e.g., `INDETERMINATE_CHOOSER_REFUSED` with cause `frozen_predicate_mismatch`) and require explicit disclosure that the native-panel-shaped assumption was not confirmed for LINE before the live step. Fail-closed behavior itself is correct; no loosening is requested.

### RV-05
- ID: `RV-05`
- Severity: `MINOR`
- Category: `SEMANTIC_CONTRACT`
- Affected: `plan.md` §11 chooser-affirmation predicate, ownership point 3 (empty pre-dispatch census case).
- Evidence: the predicate permits an empty pre-census set with ownership resting on `(1)+(2)+timing`; with an empty census the ownership evidence is strictly weaker (no process-set anchor), and the look-alike/different-process refusal fixture only covers the non-empty case explicitly. [VERIFIED]
- Failure/rework mechanism: sloppy reading of "empty set allowed" could tolerate an ambiguous new surface (same-process look-alike) instead of refusing it; the failure is contained by (1)+(2) but the refusal obligation should be unambiguous under uncertainty.
- Smallest required correction: state that in the empty-census case any residual ambiguity in (1) or (2) must be refused (no benefit of the doubt), i.e., the empty census *widens* the refusal requirement rather than relaxing it.

## REQUIRED_PLAN_CHANGES
1. `RV-01` (blocking for approval): correct the accepted-baseline path spelling in `plan.md` line 87 to the on-disk/JSON spelling `album-2024-05-13_to_2024-05-17_57` (canonical source: `baseline-content-multiset.json.source_dir`); add the preflight assertion that the baseline path resolves and its manifest digest equals `b7debe92…` before gate (f) may pass.
2. Fold `RV-02`, `RV-03`, `RV-04`, `RV-05` into the same revision: they are each cheap, none is independently approval-blocking, but a revision is already required and these reduce downstream rework/ambiguity (canonical §7.3 vocabulary + CHECK_IDs; named attributed-external-write record + disclosure; named chooser-refusal cause; tightened empty-census wording).
3. After revision: submit the new revision/hash for a fresh Stage-02 review; do not proceed to Stage 03 Handoff until that review approves the exact new SHA.

## RESIDUAL_MINOR_NOTES
- Postcondition bounds (≤150 ms/8.0 s, then ≤500 ms to a 15.0 s hard cap, ~30 s late sample) are plan-time provisional and correctly re-frozen by W2 (≥20 real-panel runs) and bound by review 4; ensure review 4 records the measured latency distribution and the chosen margin, and that any post-freeze change remains a replan (the plan already says so).
- The manifest-digest method wording ("SHA-256 of the concatenation") is ambiguous about the final newline; my reproduction matches with every line `\n`-terminated (no extra blank line). Recommend stating the byte-exact serialization.
- §13's "never re-dispatching an irreversible action without a fresh reviewed decision" vs the envelope's "no retry of any irreversible op": within this authorization, a second irreversible dispatch is out of scope (owner-only). One sentence clarifying that the envelope binds (not the generic transaction principle) would remove the tension.
- "No gate is weakened from revision 1" is substantially accurate (no CORE→SUPPORTING or blocking→non-blocking downgrades found in the revision diff), with two deliberate, reviewed narrowings: exact capture equality → W2-frozen validated tolerance (RV-01 of attempt-02), and abort-on-any-unexpected-write → abort-on-unattributable-write (RV-07 of attempt-02). Both have compensating controls; the META phrase could name them for precision.
- `G22` (multi-display transform) is correctly labeled fail-closed on this single-display machine; no multi-display hardware proof is claimed.
- Round-2 probe tooling (`markers4.swift`) detected only magenta/yellow marker clusters (cyan/green clusters empty) — a probe-tool detection artifact; the plan's geometry facts rest on image dimensions and magenta/yellow positions, so nothing load-bearing depends on it. No plan change needed; do not elevate marker-color detection to a production dependency.
- The plan's §7.3 status-mechanics fixtures named in the v4.2 integration note (canonical incident, true implementation blocker, CORE fail/blocked/not-run/not-required, replan, baseline unavailable/delta, hard-clean debt, formal waiver with original result preserved, independent-acceptance pending/environment/product-defect, contradictory-state rejection, legacy normalization, DONE prerequisites) are not enumerated at plan level; the plan names the subjects/terminals and routes closure to §7.7/§7.8/§7.10. Acceptable as Stage-04/05 obligations — ensure `execution.md`/Stage-05 carry per-item `CHECK_RESULT`/`WAIVER_STATUS` and the contradictory-state rejection fixture.
- Attempt-02's report reproduces the same misspelled baseline path in its metadata (a propagation symptom of RV-01); its claimed baseline re-verification should not be taken as evidence that the hyphen path resolves.

FINAL_STATUS: PLAN_REVISION_REQUIRED
NEXT_ACTION: Stage 01 revision mode on `T20260925-0647-01-rev28-native-closed-loop` (PLAN_REVISION 3) — fix the baseline path spelling at `plan.md` line 87 to the exact on-disk/JSON spelling `album-2024-05-13_to_2024-05-17_57` (+ preflight resolution/digest assertion), fold in RV-02…RV-05, then submit the new revision/hash for a fresh Stage-02 review; do not proceed to Handoff.
