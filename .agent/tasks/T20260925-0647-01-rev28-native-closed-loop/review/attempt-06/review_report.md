# Plan Review Report

## REVIEW_METADATA
- TASK_ID: `T20260925-0647-01-rev28-native-closed-loop`
- REVIEW_ATTEMPT: `06` (second independent Stage-02 reviewer of revision 3; the parallel `attempt-05` directory was deliberately **not** read; `attempt-01`…`attempt-04` were consulted only as prior review evidence, after my own Goal Baseline and findings were formed)
- REVIEWED_PLAN_REVISION: `3`
- REVIEWED_PLAN_SHA256: `63b25602b215a3e9514fa76db8bd34c097f4bec40f370381d218c57f62745828` (recomputed on disk this round from live bytes; matches the expected value; 513 lines / 76,747 bytes)
- PLAN_SNAPSHOT_PATH: `.agent/tasks/T20260925-0647-01-rev28-native-closed-loop/review/attempt-06/plan_snapshot.md` (byte copy taken before reading; SHA-256 identical to `plan.md`; 76,747 bytes)
- Repository anchor observed: HEAD `766b22c4f8bf29b9d0a46049c75309c70593d66d` == `origin/master`, ahead 0; `git status` shows only 12 untracked entries (Rev27e WIP + evidence), zero tracked modifications, zero staged changes. Issue `ISSUE-2026-09-24-rev27e4-r2-ownership-provenance.md` SHA-256 `0b3c9efab209bcb4c8c9b5dcaa5fa1ca6279234df0ee3cc0984da829ead67a2d` (recomputed, match). Staging `RUN-20260923-111908-01` exists, 0 entries. Accepted baseline realpath `/Users/hsiaojohnny/Downloads/LINE-Backup-PoC/album-2024-05-13_to_2024-05-17_57` = 57 files / 17,924,900 bytes; name-inclusive manifest digest reproduced = `b7debe92…` with the plan's stated trailing-newline method (negative control: no-trailing-newline variant `287d95c2…` ≠ match); content-multiset digest reproduced = `ee958e64…`; `baseline-content-multiset.json` SHA-256 `3c932d8c…` and its own 57-value sorted list independently reproduces `ee958e64…`. All 20 `HISTORICAL_REPLAY_PLAN` fixture files exist and their 8-hex prefixes match on-disk; prototype evidence `43998519…` / `afd6c26d…` match; environment claims re-verified (macOS 27.0 / 26A428, Swift 6.3.3, LINE 26.0.2 (3828) not running now); SDK symbol claims re-verified in Xcode `MacOSX26.5.sdk` (`pointPixelScale`, `SCScreenshotConfiguration`, `ignoreShadows`, `includeChildWindows`; Vision `TrackRectangleRequest`, `GenerateImageFeaturePrintRequest`).
- Reviewer runtime/model: fresh Codex CLI session (no Planner transcript, no conversation continuity). Model tag not exposed to this session (informational only).

## OWNER_VERDICT
**Goal as understood:** the owner wants one more, fully autonomous end-to-end backup of the LINE album `2024/05/13～05/17` (57 photos) of group `旻謙允禎成長日記`, rebuilt on a new macOS-native closed-loop architecture (ScreenCaptureKit seeing, Vision identity, a real Quartz system click, Accessibility for the native chooser). Success is not "Save All was clicked": it is a **directly observed native chooser**, an exactly-once confirmation into a fresh empty staging folder, then 57 complete, stable, decodable files whose content multiset equals the already accepted 57-photo baseline (`DUPLICATE_CONTENT_CONFIRMED`), with the baseline untouched and no unexplained side effect.

**Essentials:** capability probe + synthetic harness calibration (freezing the capture rule, postcondition bounds, chooser predicate, tripwire attribution), one canonical coordinate transform, fresh window-identity binding per capture epoch, the closed-loop Save All postcondition, exactly-once confirmation with durable intent, and disk-recomputed filesystem/content verification. Only the single Save All dispatch and the single destination confirmation are irreversible, each capped at one and never retried.

**Optional / deferred (correctly non-gating):** Foundation Models diagnostics, feature-print/registration comparison, the AX-driven chooser path (keyboard fallback), and the documentation/manifest deliverables. None can veto the core run except through its own reviewed safety interlock.

**What could block the whole run, and why:** the *only* hard preconditions are the gate chain (a)–(g) and, inside the run, the irreversible dispatch chain — all with fail-closed defaults. There is no self-waiver path (every gate is `WAIVER_ALLOWED: NO` / `WAIVER_AUTHORITY: NONE`). The revision-2 blocking defect — the accepted-baseline path literal `…_to-…` that did not resolve — is **genuinely fixed** in revision 3 (line 87 now reads the on-disk/JSON spelling `…_to_…`) and is additionally hardened with a fail-closed baseline-resolution rule (`realpath` + 57-file/byte assertion + both digest recomputations; any failure → `CHECK_RESULT: BLOCKED`, no skipped tripwire, no degraded comparison).

**Complexity verdict:** proportionate. Every new moving part (Swift engine, harness + separate-process occluder, ledger/state machine, staging verifier, W2 frozen rules, review set) maps to a goal-mandated capability, a must-not-break invariant, or the one-shot safety envelope; the deterministic pure-core/thin-OS-edge split keeps testability cheap. No speculative abstraction was found.

**Biggest remaining risk:** LINE's *actual* chooser shape could differ from the frozen native-`NSOpenPanel`-shaped predicate (it cannot be pre-validated in Phase A without consuming the single Save All budget). The plan handles this honestly: refusal with cause `frozen_predicate_mismatch`, no predicate loosening, single budget consumed, `PRIMARY_OUTCOME_STATUS: NOT_ACHIEVED`, never "Done". That is an acceptable, pre-declared residual risk for a one-shot authorization, not a plan defect.

## GOAL_BASELINE
Written from the authoritative goal text (`~/.codex/attachments/41a5dfc8-e350-429c-a01b-aea3a67e03fd/pasted-text-1.txt`, 782 lines) **before** reading the plan's rationale:

- **PRIMARY_OUTCOME:** autonomously redesign, implement, validate, review and — only after all safety/calibration gates pass — execute one Rev28 production run backing up exactly `旻謙允禎成長日記 / 2024/05/13～05/17 / 57` from `jp.naver.line.mac`, with the end state proven by `DUPLICATE_CONTENT_CONFIRMED` (fresh staging content multiset == accepted baseline content identity) and the accepted baseline unchanged.
- **SUCCESS_EVIDENCE:** closed loop OBSERVE → LOCATE → ACT → OBSERVE DIRECT POSTCONDITION → VERIFY FILESYSTEM. Save All success = the expected native chooser **directly observed** (never click-return, menu disappearance, panel-service presence, unified-log coincidence, raw counts). Unique staging under `/Users/hsiaojohnny/Downloads/LINE-Backup-PoC/staging/<UNIQUE-RUN-ID>/`, fresh/canonicalized/inside approved root/empty/no baseline overlap/no symlink escape. Exactly-once destination confirmation with durable intent. Then: exactly 57 completed stable decodable images, no partials/zero-byte/subdirs, quiescence, manifest + content-hash multiset equal to the accepted baseline.
- **MUST_NOT_BREAK:** accepted baseline READ-ONLY and unchanged; no second accepted production copy; maximum one irreversible dispatch per operation type (destination confirmation = 1; Save All empirically classified; unknown irreversible dispatch never blindly retried); no historical coordinates; `禎 U+798E` vs `楨 U+6968` never merged/normalized; no fullscreen capture as primary localization; Foundation Models never authorizes a click; beta Vision APIs never the sole production gate; no push; no `__pycache__`/`*.pyc`; historical evidence (attempt-07, Rev27d `SAVE_ALL_SEMANTIC_EFFECT_INDETERMINATE`) preserved; repo anchor `766b22c4…`, ahead 0.
- **NON_GOALS:** any other album/group; a second accepted production copy; cleanup/deletion of staging/baseline without separate authorization; reclassifying attempt-07; claiming BACKUP_COMPLETE from click/menu/chooser/count alone.
- **CRITICAL_PATH:** SDK/API capability proof → synthetic AppKit calibration (incl. occlusion, Retina, popup hit-testing, chooser observation) → deterministic offline tests/historical replay → adversarial suite (the goal's 22 scenarios) → independent pre-live reviews of the exact frozen implementation → read-only LINE reconnaissance → the single authorized live run → filesystem/content verification vs baseline → final report/handoff/commits.
- **AUTHORIZATION ENVELOPE:** autonomous problem solving without owner intervention except genuine external dependencies; exactly one production run for exactly the named album/group/count; reversible navigation obeys a reviewed bounded-recovery budget; three consecutive reviews on the same unresolved architectural dependency ⇒ `BLOCKED_WITH_ROOT_CAUSE`.

## GOAL_ALIGNMENT
- **A1 Intent alignment — PASS.** The plan's `GOAL_CONTRACT`, `CRITICAL_PATH` and success predicate implement exactly the baseline above: the primary acknowledgement is the directly observed chooser (§10), the end-state proof is the disk-recomputed content multiset (§12, §VERIFICATION_AND_ACCEPTANCE), and the plan repeatedly forbids the goal's forbidden substitutes ("Unified logs / panel-process presence / menu disappearance / click-returned are never sufficient"; "never `DONE` from synthetic evidence"). Acceptance criteria prove the outcome, not implementation completeness: synthetic suites, reviews and calibration are explicitly interlocks, never success.
- **A2 Necessity / traceability — PASS.** R1–R25 trace 1:1 to goal sections (closed loop, SCK sensor, identity binding, Vision OCR, locators, registration, FM diagnostics, Quartz actuator, transform, harness, AX, activation, state machine, risk classes, postcondition, chooser, filesystem predicate, transactional safety, bounded autonomy, reviews, single-run envelope, success condition, deliverables, barrier supersession, preservation). No untraceable material work was found; the goal-mandated FINAL DELIVERABLE list maps to `DELIVERABLES_MAP`.
- **A3 Blocking authority — acceptable.** The only global gates are (i) the pre-live gate chain (a)–(g), including the reviewer set, and (ii) the live-run's own irreversible chain. Both are safety-justified: they protect exactly one irreversible Save All + one irreversible confirmation with no retry. `V-08` (replay/adversarial) is `SUPPORTING` + `NON_GATING` for closure yet a hard live-run interlock — the plan states this rationale explicitly (line 445), which is the required justification. Nothing else can veto the core path.
- **A4 Coupling / containment — PASS.** Independent signals are not collapsed: chooser affirmation = new surface + native-panel AX shape + ownership (census as one ingredient, pid-reuse rejected), with refusal fixtures; identity = multi-source with stale-ID invalidation; optional planes degrade locally.
- **A5 Design economy — PASS.** Deletion test: removing FM diagnostics, registration/tracking, or the AX chooser path costs no acceptance criterion (all non-gating); removing the harness/W2 freezes, transform, ledger or verifier does cost acceptance/safety, so they pay rent. No hypothetical-future machinery found.
- **A6 Critical path priority — PASS.** W1–W5 are CORE and ordered capability→harness→offline→policy→adversarial; W6 documentation is SUPPORTING and off the critical path; W7 is the conditional live step last. No priority inversion observed.

## NECESSITY_AND_TRACEABILITY
- Classification audit: `CORE` requirements (R1–R5, R8–R22, R24, R25) each carry either a primary-outcome or must-not-break duty; `SUPPORTING` (R6 registration, R7 FM, R23 deliverables) cannot block the run; `BEST_EFFORT` items (VN OCR parity wrapper, rectangle-tracking evidence) degrade to deterministic behavior without blocking. Classifications are reasonable and consistent with the goal text.
- The one row worth naming: R25 ("historical artifacts preserved byte-identical; no pycache; no push") is CORE as a requirement, while its check `V-11` is `SUPPORTING`/`REPOSITORY_HEALTH`/`NON_GATING` (violation → stop + report). This is the correct direction of the v4.2 rule (a repository-health check is **not** promoted to product CORE because it gates closure); disclosure happens through the closeout audit. No mislabeling.
- No fabricated requirements detected; the ported v8 locator invariants and historical success/failure precedents are used as evidence/hardening, not as live data (no historical coordinates).

## GATE_AND_VETO_AUDIT
- Enumerated global gates: (a) capability probe PASS; (b) harness calibration PASS (W2 freezes); (c) replay complete; (d) adversarial suite PASS; (e) seven topic reviews + adversarial + barrier supersession APPROVED on frozen SHAs; (f) live preflight (baseline resolution + digests, staging empty, ledger armed, executable-context TCC); (g) reconnaissance gate. Post-dispatch: the postcondition window, the chooser predicate, the exactly-once confirmation, the staging predicate, and the tripwire attribution rule.
- Proportionality: each gate maps to an irreversible-dispatch safety or a decision-validity need; supporting/best-effort items are barred from vetoing unrelated work; no supporting item can veto the run except `V-08` (explicitly justified interlock) — and the goal itself mandates adversarial review before production.
- Waivers: `WAIVER_ALLOWED: NO` / `WAIVER_AUTHORITY: NONE` for every item; "Waivers: none available to the agent". No self-waiver loophole exists.
- v4.2 §7.3 table compliance: the committed table uses canonical enums only (`EVIDENCE_ROLE ∈ {OUTCOME, MUST_NOT_BREAK, REPOSITORY_HEALTH, DIAGNOSTIC}`; `CLOSURE_GATE ∈ {HARD_CLEAN, BASELINE_DELTA, NON_GATING}`; `BASELINE_REQUIRED` per row; `WAIVER_ALLOWED`/`WAIVER_AUTHORITY`/`WAIVER_STATUS` and `CHECK_RESULT: NOT_RUN` default declared for every item in the paragraph above the table; the extra `LIVE_RUN_INTERLOCK` column is declared as the plan's own non-canonical interlock). No repository-health gate is labeled product CORE. See `RV-02` for one definitional wording defect in the extra column.
- Orthogonality: `STATUS_SEMANTICS_AND_CLOSURE_ROUTING` + `ACCEPTED_TERMINAL_STATES_AND_CLOSURE` keep PRIMARY_OUTCOME / IMPLEMENTATION / CORE_ACCEPTANCE / REQUIRED_VERIFICATION / INDEPENDENT_ACCEPTANCE / TASK_CLOSURE separate; case (b) terminals are never "Done"; `DONE` requires primary-outcome achievement; `IMPLEMENTATION_BLOCKED` reserved; blocked states must name scope/subject; no legacy single-enum terminal schema is active.
- Baseline governance: no broad repository-wide gates are planned (bespoke checks only; `BASELINE_REQUIRED: NO` everywhere; no `BASELINE_DELTA`); the baseline comparison is tracked as its own subject (V-06 predicate / V-07 must-not-break), with fail-closed behavior when the reference cannot be resolved — exactly the §7.4 expectation.

## COUPLING_AND_FAILURE_CONTAINMENT
- Independent signals are not collapsed into a single validity flag; each refusal has a named verdict (`AMBIGUOUS`, `INDETERMINATE_CHOOSER_REFUSED` + causes, `NO_CHOOSER_OBSERVED`, `CHOOSER_OBSERVED_AFTER_WINDOW`, named staging mismatch classes).
- Failure containment: reversible navigation retries are bounded (≤3 per identical blocker, global ceiling 12 → `ABORTED_REVERSIBLE_BUDGET_EXHAUSTED`); irreversible ops are never retried; a crash resumes observe-only with ledger hash-chain continuity (harness fixture 9, `G21`).
- Optional planes (FM, registration, `VNRecognizeTextRequest` parity, rectangle tracking) degrade without blocking; the AX chooser path falls back to a reviewed keyboard path or stops at `CHOOSER_VERIFIED` with zero confirmation.
- The filesystem tripwire is the one place where independent signals are combined; the L1/L2/L3 ladder (§10) contains the failure at the narrowest safe boundary (L1 expected evidence; L2 attributable-unexpected disclosed, unattributable abort; L3 environmental context, attributable-to-run = scope-violation abort; baseline modification = immediate abort regardless of attribution). See `RV-01` for one envelope-wording inconsistency with the L3 rule.

## DESIGN_ECONOMY
- New moving parts: SwiftPM `rev28/` package (sensor/identity/perception/actuator/AX/postcondition/chooser/state/transaction/verify/risk/diagnostics), `rev28ctl`, harness + occluder, ledger, evidence dir, one `.gitignore` line. Each element maps to a goal capability, a must-not-break invariant, or the one-shot safety chain; pure cores + thin OS edges keep GUI-free testing cheap.
- Deletion test results: FM diagnostics → no acceptance loss (non-gating); registration/tracking → no acceptance loss (supporting); AX chooser navigation → fallback exists; harness/W2 freezes → acceptance and safety loss (kept); ledger/verifier → acceptance loss (kept); review set → goal-mandated (kept).
- The 25-scenario matrix (22 goal-mandated + 3 labeled extensions) is proportionate: fixtures are largely shared with the harness, `G22` is honestly fail-closed on this single-display machine, and the extensions (`X01` stale AX element, `X02` popup surface kind, `X03` inactive-window event) each target a named residual risk.

## CRITICAL_PATH_AND_PRIORITY
- The plan fixes/proves the CORE path first (capability + harness → engine → offline/replay → adversarial → reviews → recon → live), and its explicit ordering rule prevents unrelated steps from blocking each other; only the irreversible production step has a hard prerequisite chain (steps 1–5 + gate (g)).
- Supporting work (W6 docs, deliverable manifest) is off the critical path; the provisional postcondition bounds are re-frozen from measured harness evidence (≥20 real `NSOpenPanel` runs) with margin before the run, and post-freeze changes are semantic changes → replan, never silent extension.
- No element consumes disproportionate effort while a CORE signal is unresolved: the reviews target the exact frozen artifacts that protect the one-shot dispatch.

## REQUIREMENT_FIDELITY
- Adversarial list fidelity: I compared the goal's list (wrong window, wrong bundle, stale windowID, wrong Retina transform, occluding app, window movement, popup movement, wrong row, duplicate target OCR, OCR corruption, whole-screen false positive, click outside popup, click on neighboring row, focus theft, postcondition timeout, fake chooser-like surface, unexpected filesystem write, zero-byte burst, partial download, duplicate files, process restart, multi-display transform) against `G01`–`G22`: 1:1 in goal order, with three explicitly labeled extensions (`X01`–`X03`) = 25. Correct mapping, no unlabeled substitutions.
- Review fidelity: the goal's seven pre-live topics map to reviews 1–7; the goal's adversarial review is review 8; the goal's reconciliation-barrier supersession requirement is review 9. Reviews bind frozen SHAs; post-approval rule changes are replan+review.
- Envelope fidelity: single production run for exactly the named album/group/count; per-operation-type maxima (Save All = 1, confirmation = 1); no historical coordinates; bounded reversible recovery; abort conditions pre-declared; honest indeterminate terminals accepted and never labeled success.
- Success-condition fidelity: correct album identity, reviewed actuation, directly observed chooser, unique staging, 57 complete stable images, content verification vs baseline, registry/evidence consistency, baseline unchanged, no unexplained side effect. The exact predicate (`ee958e64…`) is the goal's content-identity comparison, with names excluded by design (fresh download token) — correctly distinguished from the name-inclusive digest `b7debe92…`, which is a baseline tripwire only.
- Identity fidelity: `禎/楨` never normalized or merged (NFC-exact matching named in §3; harness discrimination fixture planned).
- Deliverables fidelity: the goal's required outputs are all mapped (`ARCHITECTURE.md`, `CAPABILITY-MATRIX.md`, engine, harness, tests, replay, adversarial, reviews, live evidence if authorized, manifest, ledger, handoff, local commits, final-report content list).

## GROUNDING_AND_DRIFT
Recomputed / verified this round (read-only):
- Repo anchor: HEAD == `origin/master` == `766b22c4f8bf29b9d0a46049c75309c70593d66d`, ahead 0; only untracked work (12 entries); no staged or tracked modifications.
- Issue SHA `0b3c9efa…` MATCH.
- Baseline: literal in plan.md line 87 resolves via `realpath` to the same directory; 57 files; 17,924,900 bytes; name-inclusive digest `b7debe92…` reproduced with the described method (negative control differs); content-multiset `ee958e64…` reproduced from both disk and the JSON's own list; JSON SHA `3c932d8c…` MATCH; 57 unique content hashes.
- Replay fixtures: all 20 files exist and all 20 8-hex prefixes in `HISTORICAL_REPLAY_PLAN` match on-disk SHA-256. (Full-hash manifest is a W3 artifact — see `RV-03`.)
- Probe evidence: `prototype-round1-findings.md` (`43998519…`) and `prototype-round2-outputs.txt` (`afd6c26d…`) MATCH; `probe-metadata.json`, `probe-outputs.txt`, round tooling present.
- Environment: macOS 27.0 (26A428), Swift 6.3.3, LINE 26.0.2 (3828) installed and not running now; SDK `MacOSX26.5.sdk` contains `pointPixelScale`, `SCScreenshotConfiguration`, `ignoreShadows`, `includeChildWindows`, Vision `TrackRectangleRequest`/`GenerateImageFeaturePrintRequest`. The plan's statement that no `contentScale`/`scaleFactor` SDK symbol exists is consistent with the goal's capability-survey intent being satisfied via `pointPixelScale` + `NSScreen.backingScaleFactor`, both recorded and cross-checked per capture.
- Historical grounding: v8 tool `REFERENCE_ORDER` and menu reference order match the plan's ported invariants; Rev27d reconciliation artifacts contain the `SAVE_ALL_SEMANTIC_EFFECT_INDETERMINATE` classification; the accepted baseline directory is unchanged and read-only in intent; staging run dir exists and is empty.
- Prior-review bookkeeping: plan META's SHAs for revision-2 basis (`84b65bc9…`) and review reports attempt-01 (`2ad96474…`, `PLAN_APPROVED`), attempt-02 (`025c74cb…`), attempt-03 (`6e6602a9…`), attempt-04 (`7d28aef2…`) all match the on-disk artifacts. Revision-1 snapshot SHA `5575aca5…` matches attempt-01's stated binding.
- Drift vs PLAN_REVISION 1: no CORE→SUPPORTING downgrades and no removed gates found. Two deliberate narrowings are present and were previously reviewed (attempt-02 RV-01/RV-07): capture exact-equality → W2-frozen validated tolerance with fail-closed INVALID on unexplained deltas, and "abort on any unexpected filesystem write" → the L1/L2/L3 attribution ladder with disclosure obligations, process-census/path/timing attribution, ≥10 s pre-dispatch environmental baseline, and immediate abort on any baseline modification. The revision-2 blocking path typo is fixed and replaced by a fail-closed resolution rule. Scenario count grew 23 → 25 (goal-mandated mapping).

## ARCHITECTURE_AND_CONTRACTS
- Architecture matches the goal's first-principles decision: ScreenCaptureKit (`SCContentFilter(desktopIndependentWindow:)` + `SCScreenshotConfiguration`) as primary sensor with recorded per-capture bbox/scale/config; no fullscreen primary localization; separate filters for popup/child surfaces; `sourceRect` limits respected; window identity bound via bundle/pid+start-time/windowID/frame/AX/CG with capture-epoch invalidation; Vision `RecognizeTextRequest` (accurate, zh-Hant/en-US, correction off) for identity with geometry separated via structural locators; FM diagnostics-only; one Quartz actuator with revalidation and a recorded `DispatchRecord`; AX semantic-first only when unique; activation verified then re-verified.
- Load-bearing semantic contracts are explicit: state machine + terminal states; action-risk classes (with Save All executed under `IRREVERSIBLE_SIDE_EFFECT` semantics this run and an empirical class record for the future); exactly-once confirmation; the postcondition success predicate and its late/no-chooser terminals; the chooser-affirmation predicate incl. ownership/pid-reuse and empty-census refusal; the staging predicate and its named mismatch classes; the attribution ladder; the ledger/restart contract.
- Compatibility/local-only: new `rev28/**` package + evidence dir + one `.gitignore` line; no public API/schema/migration surface; SDK/Xcode pinning recorded per build; no push.
- Contracts vs goal: the two documented deviations (`contentScale`/`scaleFactor` non-existence; `VN` parity wrapper as best-effort) are backed by SDK/probe evidence and do not weaken goal semantics.

## DATA_SECURITY_RELIABILITY
- No secrets handled; evidence stays untracked/local; no push; no data migration; baseline read-only; no destructive cleanup; do-not-touch list explicit and preserved.
- Reliability: exactly-once irreversible semantics with durable intent (fsync) before confirmation; append-only hash-chained ledger; crash recovery enters observe-only and never re-dispatches; bounded recovery for reversible navigation only; timeout/refusal paths are named terminals; wrong-row risk contained by exact `儲存全部` single-row matching, menu reference-order tolerance limited to one substituted glyph on non-target rows, and safe-interior geometry (x_safe/y_safe) — all covered by the adversarial suite (`G08`, `G12`, `G13`) and reviews 1/3/8.
- Trust boundary: TCC (screen recording, post-event, accessibility) re-verified in the executable context; AX writes only against uniquely identified elements; chooser ownership authenticated (bundle/signing identity + process start time; pid reuse rejected); ambiguous/empty-census cases refuse.
- Failure/rollback: on abort the plan preserves all evidence, writes a scoped terminal state, and does not retry irreversible operations; the accepted baseline is protected by immediate-abort-on-modification.

## IMPLEMENTATION_SEQUENCE
- W1 (core, no GUI) → W2 (harness + all freezes) → W3 (locators + replay) → W4 (policy: state machine, risk, ledger, verifier, chooser driver, monitor) → W5 (adversarial hardening) → W6 (supporting docs) → W7 (conditional recon + single live run + verification + closeout). Ordered by the critical path; the frozen W2 artifacts are mandatory inputs to the seven reviews; post-freeze rule changes require replan + fresh review.
- Handoff/plan-identity continuity is preserved by binding reviews to frozen SHAs; the plan requires Stage-04 `execution.md` per-item results and Stage-05 recomputation from disk.

## TESTABILITY_AND_ACCEPTANCE
- Deterministic pure cores with unit tests; harness integration proofs with SHA-bound evidence bundles (capture matrix, Vision localization incl. `禎/楨` fixture, transforms incl. wrong-scale detection, Quartz routing to the topmost popup under a separate-process occluder, postcondition bounds from ≥20 real panel runs, AX chooser predicate incl. look-alike/empty-census refusals, focus theft, tripwire attribution, restart/observe-only with zero re-dispatches).
- Historical replay is offline, read-only, SHA-bound, and divergences are documented rather than threshold-tuned.
- Acceptance is objectively observable: `DUPLICATE_CONTENT_CONFIRMED` is a recomputable digest predicate; Stage 05 recomputes the staging multiset and both baseline digests from disk itself and modifies nothing; CORE acceptance is verified first; synthetic evidence can never imply a live result.
- §7.3 table compliance re-verified (canonical enums; per-item waiver/result fields declared; no self-waiver). Stage 04/05 status routing is inherited from the canonical §7.7/§7.8 contract and the plan's terminal taxonomy routes every case-(b) outcome to a scoped, non-success closure subject.

## SCOPE_AND_COMPLEXITY
- Scope is bounded: `rev28/**` + `evidence/20260925-rev28-native-closed-loop/**` + the task dir + one `.gitignore` line; everything else is on the do-not-touch list.
- Complexity budget: the harness/occluder pair and the W2 freezes are the main new coordination points; both are required to prove the exact things the goal demands (occlusion-surviving capture, topmost-popup routing, native-panel observation) before the one-shot run. The review set and ledger are goal-mandated. No excess.
- See `RV-04` for a small self-contradiction in the CHANGE_MAP wording about `.gitignore`.

## FINDINGS
### RV-01
- ID: `RV-01`
- Severity: `MINOR`
- Category: `SEMANTIC_CONTRACT`
- Affected: `plan.md` §LIVE_RUN_ENVELOPE abort list (line 415) vs §10 attribution ladder (`L2` line 268, `L3` line 269); same shorthand also appears in harness item 8 (line 335) and RISKS #9 (line 482).
- Evidence: §10 `L3` states that unattributable activity **outside the approved root** "is recorded as environmental context … and does not abort by itself", while the envelope aborts on "an **unattributable** post-dispatch filesystem write outside staging (§10 attribution rule)". For an unattributable write *outside the approved root* the two texts disagree; only the parenthetical pointer defers to §10. [VERIFIED wording conflict]
- Failure/rework mechanism: an implementer taking the envelope literally aborts the single-budget run on unrelated background activity under `~/Downloads` (a scenario the plan itself anticipates in RISKS #9 under concurrent machine activity); the opposite reading silently ignores the envelope's fail-closed intent. Either way the ledger/disposition text is inconsistent and the Stage-05 audit has to adjudicate.
- Smallest required correction: reword the envelope item to "an unattributable post-dispatch filesystem write in the approved root outside staging (per the §10 L1/L2/L3 ladder)" and state in one sentence that L3 unattributable activity remains non-aborting environmental context (with `G17` covering the fail-closed case).

### RV-02
- ID: `RV-02`
- Severity: `MINOR`
- Category: `SEMANTIC_CONTRACT`, `TEST`
- Affected: `plan.md` §VERIFICATION_AND_ACCEPTANCE, `LIVE_RUN_INTERLOCK` definition (line 445) vs table rows (lines 449–459).
- Evidence: the column is defined as "whether the item must pass **before** the irreversible Phase B may start", yet `V-03` (capture-geometry invariants honored in live frames), `V-04` (postcondition observation), `V-05` (chooser identity/exactly-once confirmation) and `V-06` (staging predicate after download) are marked `YES` although they can only be evaluated during or after Phase B (and `V-07` is both pre-live and closeout). [VERIFIED]
- Failure/rework mechanism: Stage-04/05 readers could misclassify live-time gates as impossible pre-live prerequisites, or treat the column as decorative; the canonical `CLOSURE_GATE` column remains correct, so this is interpretive rework risk rather than acceptance loss.
- Smallest required correction: split the column or redefine it as "part of the authorized run's gate chain" (pre-live for V-01/V-02/V-08/V-09; live-time for V-03/V-04/V-05/V-06; pre-live + closeout for V-07).

### RV-03
- ID: `RV-03`
- Severity: `MINOR`
- Category: `GROUNDING`, `TEST`
- Affected: `plan.md` §HISTORICAL_REPLAY_PLAN (fixture SHA binding paragraph).
- Evidence: the plan promises "recomputed when copied; any mismatch → STOP, the fixture is not what this plan reviewed", but the full-hash manifest is a W3 artifact that does not exist yet (`evidence/20260925-rev28-native-closed-loop/` currently contains only `baseline-content-multiset.json` and `capability-probe/`), so copy-time checks compare against a manifest generated at W3 from the same bytes — circular for any drift between plan time and W3. I independently confirmed all 20 files exist and all 20 8-hex prefixes in the plan match on-disk. [VERIFIED]
- Failure/rework mechanism: a fixture modified between plan time and W3 would be re-blessed by the W3 manifest; the only independent anchor is the 8-hex prefixes in the frozen plan snapshot/review records (~32 bits per file).
- Smallest required correction: at W3, write the full hashes into `replay/fixture-manifest.json` **and** mechanically assert each against the plan snapshot's abbreviated prefixes (fail closed on any discrepancy), so the manifest is anchored to the reviewed plan rather than self-generated.

### RV-04
- ID: `RV-04`
- Severity: `MINOR`
- Category: `SCOPE`
- Affected: `plan.md` §CHANGE_MAP.
- Evidence: "Modified: none outside these (`.gitignore` gets one line for `rev28/.build/`)" — `.gitignore` is a tracked file outside the listed additions yet is a planned modification. [VERIFIED wording]
- Failure/rework mechanism: the preservation/closeout audit (`V-11`, review 8, Stage 05) or a strict implementer could treat the `.gitignore` edit as out-of-scope tracked modification, while skipping it lets build artifacts pollute `git status`.
- Smallest required correction: list `.gitignore` explicitly under "Modified" with its single intended line.

## REQUIRED_PLAN_CHANGES
- **None blocking.** `PLAN_APPROVED` is not conditioned on `RV-01`–`RV-04`; they are low-cost consistency/provenance clarifications, all fail-closed-safe in their current form. Recommended: carry them into the Stage-03 Handoff as explicit interpretations (RV-01, RV-02) and into W3 as a mechanical check (RV-03); fold the wording fixes into the plan only if a future revision occurs for another reason (any plan edit invalidates this approval and requires a fresh Stage-02 attempt).
- Carry into Handoff (not plan changes): the §7.7/§7.8 routing table and the §7.8 immutable Stage-04 snapshot fields; per-item `CHECK_RESULT`/`WAIVER_STATUS` in `execution.md`; and the requirement that a case-(b) run leads its final report with primary outcome before closure state.

## RESIDUAL_MINOR_NOTES
- `LIVE_RUN_ENVELOPE`/§10 wording (RV-01) and the `LIVE_RUN_INTERLOCK` definition (RV-02) should be read together with §10 and the canonical §7.3 enums by Stage-04; the handoff should carry both interpretations to avoid re-litigation.
- Provisional postcondition bounds (≤150 ms/8.0 s, ≤500 ms to 15.0 s hard cap, ~30 s forensic sample) are correctly re-frozen from ≥20 measured real-panel runs and bound by review 4; ensure review 4 records the measured distribution and margin, and that any post-freeze change remains a replan.
- The frozen native-panel-shaped chooser predicate cannot be validated against LINE's actual chooser before the single live dispatch; the plan's refusal semantics (`INDETERMINATE_CHOOSER_REFUSED`, cause `frozen_predicate_mismatch`, no loosening) are the correct handling, and the `ATTRIBUTED_EXTERNAL_WRITE_OBSERVED` disclosure requirement keeps a disclosed-but-unexpected write from being silently absorbed.
- Two deliberate narrowings vs revision 1 (capture exact-equality → validated tolerance; abort-on-any-unexpected-write → unattributable-only with the L1/L2/L3 ladder and disclosure) are reviewed, compensated, and not silent weakenings; they should remain named in the handoff's GOAL_ANCHOR so a later reader does not misread them as regressions.
- `G22` (multi-display) stays `[UNVERIFIED]` on this single-display machine and is correctly fail-closed; no multi-display hardware claim is made.
- The goal's §7 status-mechanics fixtures (canonical incident, implementation-blocker, CORE fail/blocked/not-run/not-required, replan, baseline unavailable/delta, hard-clean debt, waiver preservation, independent-acceptance pending/environment/product-defect, contradictory-state rejection, legacy normalization, DONE prerequisites) are inherited from the canonical contract rather than enumerated in the plan; acceptable at plan level because the plan routes to §7.7/§7.8/§7.10 and Stage 04/05 prompts carry the contract — the handoff should carry the field list so the artifacts are unambiguous.
- The `.gitignore` one-line change (RV-04) should be treated as planned scope by the preservation audit once the wording is corrected; it does not touch any do-not-touch path.
- Parallel reviewer independence: `review/attempt-05/` was not read; `attempt-01`–`attempt-04` were consulted only after my own Goal Baseline, disk verification, and findings were complete, purely to cross-check prior-review resolution (all prior BLOCKER/MAJOR items — attempt-04 RV-01 baseline path, attempt-03 RV-01 path/fail-closed, plus the MINOR contract items — are genuinely resolved in revision 3).

FINAL_STATUS: PLAN_APPROVED
NEXT_ACTION: Stage 03 Handoff for this exact revision (PLAN_REVISION 3, SHA-256 63b25602b215a3e9514fa76db8bd34c097f4bec40f370381d218c57f62745828); carry RV-01–RV-04 as non-blocking handoff/W3 notes — no plan revision required.
