# Plan Review Report

## REVIEW_METADATA
- TASK_ID: `T20260925-0647-01-rev28-native-closed-loop`
- REVIEW_ATTEMPT: 03 (fresh context; planner transcript not read; attempt-01/attempt-02 reports were read only AFTER independent findings were formed, solely as a resolution cross-check)
- REVIEWED_PLAN_REVISION: 2
- REVIEWED_PLAN_SHA256: `84b65bc9eebb763d9ea3839db1ecfafa7f181b7fa6be92cf4216e2befceba1ed` (recomputed from disk this attempt; matches the expected value)
- PLAN_SNAPSHOT_PATH: `.agent/tasks/T20260925-0647-01-rev28-native-closed-loop/review/attempt-03/plan_snapshot.md` (byte-copy via `cmp`; SHA-256 verified equal to `plan.md`)
- Repository anchor observed: `git rev-parse HEAD` = `git rev-parse origin/master` = `766b22c4f8bf29b9d0a46049c75309c70593d66d`; `git status --porcelain` shows only untracked paths (no staged/unstaged tracked modifications).
- Reviewer runtime/model: Codex CLI independent Stage-02 reviewer, fresh context (no prior review or planner transcript).

## OWNER_VERDICT
- Goal as understood: rebuild the LINE album backup for group `旻謙允禎成長日記`, album `2024/05/13～05/17` (57 photos) on a NEW architecture that (1) sees the real LINE window with macOS 27 ScreenCaptureKit, (2) reads identity with Apple Vision, (3) clicks with real Quartz system events from freshly derived geometry, (4) proves Save All by directly observing the resulting chooser (not a click return), (5) drives a unique staging folder with exactly one irreversible confirmation, and (6) proves on the filesystem that 57 complete, stable images arrived whose content matches the already accepted baseline → `DUPLICATE_CONTENT_CONFIRMED`, without creating a second accepted copy and without touching the baseline.
- What is essential and present in this plan: single-dispatch Save All under exactly-once semantics; exactly-once destination confirmation with durable intent; fail-closed identity/transform/locator rules; frozen chooser-affirmation and content-multiset predicates; the accepted baseline stays READ-ONLY; bounded recovery and bounded reversible budget; seven pre-live reviews + adversarial suite + reconciliation-barrier supersession review before any live dispatch.
- Optional/deferred (correctly non-gating): Foundation Models diagnostics, registration/rectangle-tracking enrichment, `VNRecognizeTextRequest` parity wrapper, multi-display behavior (fail-closed here).
- What can block the whole pipeline and why: one verified grounding defect — the plan's VERIFIED_REPOSITORY_FACTS records the accepted-baseline path with a one-character error (`...05-13_to-2024-05-17_57`; a hyphen where the on-disk directory has `_`), while the plan also asserts that this exact path was "recomputed on disk this round = MATCH". Any preflight, baseline tripwire, containment check, or Stage-05 baseline recomputation that takes the plan's literal path will fail to resolve the baseline (or, if implemented leniently, become vacuous). Correction is textual, plus one fail-closed rule; no other blocking issue was found.
- Complexity verdict: proportionate. The SwiftPM engine + synthetic harness + separate-process occluder + control CLI all pay rent against explicit goal obligations (capability calibration, adversarial coverage, deterministic replay, crash/observe-only resume). No unjustified global gate, no overengineering beyond the goal's own required evidence, no priority inversion found.
- Biggest residual (non-blocking) risks: LINE-specific surface facts (popup kind, AX exposure) remain `[UNVERIFIED]` until the planned read-only reconnaissance gate; the W2 freeze must yield a production-computable capture-geometry rule (fail-closed fallback exists); the postcondition hard cap is bound to W2/review-4 calibration and may force a replan if a real panel is slower.

## GOAL_BASELINE
Reconstructed from the authoritative goal (`~/.codex/attachments/41a5dfc8-e350-429c-a01b-aea3a67e03fd/pasted-text-1.txt`, GOAL_ID `REV28-MACOS27-NATIVE-CLOSED-LOOP-LINE-BACKUP`) BEFORE reading the plan's framing:
- PRIMARY_OUTCOME: an autonomously redesigned, validated, independently reviewed Rev28 CLOSED-LOOP route (OBSERVE PRE-STATE → LOCATE → ACT → OBSERVE DIRECT POSTCONDITION → VERIFY FILESYSTEM EFFECT) that performs exactly one new production Save All run for album `2024/05/13～05/17` of group `旻謙允禎成長日記` (57 photos) and proves end-to-end success via matching content in a fresh unique staging folder (`DUPLICATE_CONTENT_CONFIRMED`), with the accepted baseline unchanged.
- SUCCESS_EVIDENCE: native visual identity of the exact album (exact codepoints: `禎 U+798E`, `楨 U+6968`; never normalized/merged); directly observed native chooser postcondition (not click-return / menu-disappearance / panel-service presence / unified-log coincidence); exactly one destination confirmation; 57 complete stable decodable image files; content-multiset match against the accepted baseline (57 files / 17,924,900 bytes / name-inclusive digest `b7debe92…`); baseline unchanged; ledger/evidence consistent; no unexplained side effect.
- MUST_NOT_BREAK: accepted baseline READ-ONLY and never a second production copy; exactly-once for irreversible operations (destination confirmation max 1; any SIDE_EFFECTING op max 1 unless idempotent recovery is proven); no blind retry of an unknown irreversible dispatch; no historical coordinate reuse (every live coordinate fresh-derived); no push; no `__pycache__`/`*.pyc`; historical evidence (attempt-07, Rev27d barrier, Rev27e WIP) preserved and never reinterpreted.
- NON_GOALS: hidden LINE semantic-event inference as primary success condition; fullscreen capture as primary localization source; Foundation Models as production click authority; weakening safety invariants to obtain approval.
- CRITICAL_PATH: capability survey + synthetic harness proofs (occlusion-surviving capture, stable coordinates, Retina scale, popup/child identification, Quartz routing to topmost popup, AX chooser, transforms) → deterministic offline tests + historical replay → adversarial review of the exact frozen implementation → one bounded live run (navigate → locate → one Save All → observe chooser → unique staging → one confirmation → download) → filesystem/content verification (`DUPLICATE_CONTENT_CONFIRMED`) → ledger/final report/local commits.
- BOUNDED AUTONOMY: ≤3 recovery cycles per identical live-state blocker; three consecutive independent reviews on the same unresolved dependency → `BLOCKED_WITH_ROOT_CAUSE`; no invariant weakening.

## GOAL_ALIGNMENT
- The plan's `PRIMARY_OUTCOME` and `GOAL_CONTRACT` match the authoritative goal; the acceptance criteria prove the outcome (direct chooser observation + content-multiset equality + baseline unchanged + exactly-once accounting), not merely implementation completeness.
- The plan does not substitute a proxy goal: unified logs / panel-process presence / click-return are explicitly demoted to forensic evidence; `DUPLICATE_CONTENT_CONFIRMED` is used exactly as the goal defines it (proof of route success without creating a second accepted copy).
- Autonomy boundaries match the goal: the agent may research/build/test/replan/re-review, and must not stop merely because a review returns REVISION_REQUIRED.

## NECESSITY_AND_TRACEABILITY
- R1–R25 map 1:1 to goal obligations (closed-loop contract; SCK sensor; identity binding; Vision OCR; structural locators; registration/tracking; FM limits; Quartz actuator; canonical transform; harness; AX; focus handling; state machine; risk classes; postcondition; chooser; filesystem predicate; transaction; autonomy; reviews; envelope; success; deliverables; barrier; preservation).
- Classifications are reasonable: all safety/identity/postcondition/predicate/review/envelope items are `CORE`; the harness/replay/adversarial suites are `SUPPORTING` but sit on the pre-live prerequisite chain (explicitly rationalized); FM/parity/registration are `BEST_EFFORT` with local degradation. The preservation audit is correctly `SUPPORTING`/`NON_GATING` (not promoted to CORE just because it gates closure).
- Deletion test applied: removing e.g. the `saveAllEmpiricalClassRecord` would drop a goal requirement (empirical classification), the separate-process occluder/restart fixture would drop goal-named adversarial coverage, and the W2 freezes would drop the reviewed-rule basis — none of the material elements is removable without failing a goal or safety obligation.
- No untraceable significant work found; X01–X03 are explicitly labeled, cheap extensions, not disguised scope.

## GATE_AND_VETO_AUDIT
- Each global veto has a stated safety/correctness rationale: identity/epoch rules (wrong window/bundle/stale windowID), transform invariants (wrong geometry), locator refusal (wrong row/click-outside), postcondition affirmative-only success (no false success), baseline tripwire (data integrity), staging containment/symlink rejection (no escape/no baseline collision), reviews before live dispatch (goal mandate).
- Supporting/best-effort items degrade locally and are non-gating where appropriate; `REPLAY/ADVERSARIAL` is `NON_GATING` yet must pass before Phase B (a prerequisite, not a closure-gating veto) — coherent.
- Waivers: `WAIVER_ALLOWED: NO`, authority the owner only; no self-waiver possible. No broad repository-wide gate is planned; `BASELINE_DELTA` is not used as a gate.
- (d) Revision-1 → revision-2 gate comparison (my own diff of the preserved revision-1 snapshot vs revision 2), for risk on the single irreversible run:
  - Capture rule: exact-equality (unimplementable against the planner's own probe evidence: `includeChildWindows=false` 792×660 px vs frame 394×326 pt; ±1–2 pt/side) → validated-tolerance, per-state rule frozen by W2, fail-closed when no frozen rule or delta beyond bound. This is a necessary, bounded correction; wrong-scale detection stays anchored to `pointPixelScale` vs `NSScreen.backingScaleFactor`; origin consistency and same-epoch re-reads retained. Judgment: NOT a risk-increasing weakening.
  - Success predicate: revision 1 compared a "SHA-256 multiset" while citing the name-inclusive manifest digest; revision 2 defines the exact name-excluded sorted-hash-list predicate against a recorded reference digest (`ee958e64…`, recomputable from `baseline-content-multiset.json`, file SHA `3c932d8c…`). Strengthened.
  - Postcondition: window extended from ≤8.0 s (+~12 s ledger-only late sample) to ≤150 ms/8.0 s then ≤500 ms to hard cap 15.0 s, with a named late-affirmative terminal state; bounds re-frozen by W2/review 4 with ≥20 real-panel runs; insufficient cap → replan. Strengthened.
  - Chooser predicate: from "panel-service ownership pid in census set" (single ingredient, unresolved hosting shape) to a 4-clause predicate where panel membership is never the sole gate, hosting shape is harness-resolved, pid reuse is rejected, and fake/ambiguous surfaces refuse. Strengthened.
  - Counters: revision-1 "ellipsis open = 1 (recovery ≤3)" self-contradiction removed; revision 2 states per-operation exactly-once for irreversible actions plus ≤3 attempts/blocker and a global reversible-dispatch ceiling of 12 with a named abort. Goal-aligned and bounded.
  - One deliberate semantic refinement: post-dispatch writes outside staging but under the monitored root are now classified via an attribution rule — attributable writes are recorded as supporting evidence; only unattributable writes abort (`ABORTED_UNATTRIBUTED_FILESYSTEM_WRITE`). Revision 1 aborted on any unexpected write. Rationale exists (concurrent machine activity was observed) and the fail-closed default is retained for unattributable writes; the attribution classifier is itself harness-tested and review 7 owns the policy. Judgment: acceptable; residual mis-attribution risk noted (RV-02 scope wording).
  - Irreversible-op gates themselves (Save All dispatch = 1, no retry, destination confirmation = 1, durable intent before confirmation, no historical coordinates) are unchanged or stricter. No gate was weakened in a way that increases risk on the single irreversible run.

## COUPLING_AND_FAILURE_CONTAINMENT
- Independent signals are not collapsed into one all-or-nothing flag: identity, geometry, OCR identity, locator verdicts, postcondition, and filesystem verification each fail closed at their own boundary; optional components (FM, parity OCR, registration/tracking evidence) degrade without stopping the core path.
- The irreversible step has a hard prerequisite chain (gates a–g) and the reconciliation-barrier supersession must pass review 9 before dispatch; a failure before `SAVE_ALL_LOCATED` costs only pre-live/reversible budget (Phase A is explicitly not a consumed live budget).
- Failure containment is scoped: `ABORTED_*`/`INDETERMINATE_*`/`STAGING_*`/`CONTENT_MISMATCH_*` are named terminal states rather than a single global stop; `BLOCKED_WITH_ROOT_CAUSE` is reserved for genuinely external dependencies after the audit threshold.
- No low-value adapter is a single point of failure; the panel-process census is deliberately one ingredient rather than the sole gate.

## DESIGN_ECONOMY
- New moving parts: SwiftPM package (pure cores + thin OS edges), harness + separate-process occluder + control CLI, evidence bundle, append-only ledger. Each maps to a goal obligation (calibration proofs, adversarial fixtures, deterministic replay, crash/observe-only resume).
- The W2 freeze mechanism is the minimum way to make the tolerance rule reviewable and bounded; it removes an unreviewable exact-equality assumption rather than adding a bypass.
- No hypothetical-future machinery detected; `PRE_SIDE_EFFECT_ACTION` remains defined (goal requires the risk-class set) even though unused this round; nothing was promoted from optional to mandatory during revision 2.

## CRITICAL_PATH_AND_PRIORITY
- Sequence fixes/proves the CORE path first: capability probe → harness calibration + freezes → engine cores → offline replay → adversarial hardening → reviews → read-only reconnaissance → single live run → filesystem/content verification → closeout.
- Supporting items (docs, capability matrix, replay report, handoff) are W6 and never block Phase B except where they are inputs to reviews (frozen artifacts), which is explicit.
- No priority inversion or noise capture found; the prior review's precision items were folded in without displacing the CORE path.

## REQUIREMENT_FIDELITY
Goal-element check (against the authoritative goal text; all `[VERIFIED]` as present unless noted):
- Native sensor plane with SCK incl. `SCContentFilter(desktopIndependentWindow:)`, child-window handling, no fullscreen as primary: present (§1, capability matrix, harness).
- Window identity binding (bundle/pid/windowID/frame/epoch/capture SHA + cross-checks; stale ID invalidates): present (§2, R3).
- Vision as authoritative visual engine (accurate, zh-Hant+en-US, correction off, NFC-exact identity, 禎/楨 discrimination fixture): present (§3, R4).
- Registration/stability evaluation incl. rectangle tracking + feature prints, non-beta sole gates: present (R6, §3, W3).
- Foundation Models limits: present (§4, R7).
- Quartz actuator with preflight/request, screen-global coordinates, mouseMoved→revalidate→down→up, single canonical transform, no implicit 2×: present (§5, R8/R9).
- Synthetic harness proving occlusion survival, stable coordinates, Retina, popup hit-testing, chooser detection, AX observation; separate-process occluder: present (§W2, R10).
- AX plane with unambiguous-element preference and refusal on ambiguity: present (§6, R11).
- Activation/focus with post-activation verification and recoverable focus theft: present (§7, R12).
- Persistent state machine with per-transition precondition/action/postcondition/timeout/recovery/evidence: present (§8, R13).
- Risk classes incl. empirical Save All classification: present (§9, R14; conservative IRREVERSIBLE_SIDE_EFFECT this run with an empirical classification record).
- Save All postcondition incl. bounded high-frequency observation, corroborating inventories, tripwire, affirmative-only success, no blind retry: present (§10, R15).
- Chooser via AX first, keyboard fallback only after fresh identity verification, unique canonicalized staging, symlink rejection, exactly-once confirmation with durable intent: present (§11, R16).
- Filesystem completion incl. 57/stable/decodable/quiescence/multiset and DUPLICATE_CONTENT_CONFIRMED semantics: present (§12, R17).
- Transactional safety incl. append-only hash-chained ledger, observe-only restart, no re-dispatch of irreversible actions: present (§13, R18).
- Bounded autonomy (3 cycles; hypothesis-differing continuation; 3-review dependency threshold; no weakening): present (R19).
- Reviews: 7 goal-named pre-live topics + adversarial matrix + barrier supersession, bound to frozen SHAs: present (`REVIEW_PLAN`).
- Production authorization envelope (exactly one run, exactly this album; confirmation max 1; side-effecting max 1; fresh coordinates): present (`LIVE_RUN_ENVELOPE`, R21).
- Success condition and no weak claims: present (R22, closure section, final-report question list).
- Deliverables incl. architecture doc, capability matrix, tests, replay results, adversarial results, review artifacts, live evidence, manifest, ledger, handoff, local commits: present (`DELIVERABLES_MAP`).
- Do-not-touch/preservation, no push, no pycache: present (`CONSTRAINTS_NON_GOALS_AND_DO_NOT_TOUCH`, R25).
- Out-of-scope check: no extra album/group, no cleanup/deletion of staging or baseline, no reclassification of attempt-07, no baseline copy creation. Clean.

## GROUNDING_AND_DRIFT
Independent read-only audit performed this attempt (all numbers recomputed by me, not taken from the plan):
- Repository anchor: HEAD = origin/master = `766b22c4f8bf29b9d0a46049c75309c70593d66d`; `git status --porcelain` shows only untracked paths (`??`), i.e. no tracked modifications. `[VERIFIED]`
- Accepted baseline on disk: I located it by directory listing rather than by the plan's literal path (see RV-01): actual name `album-2024-05-13_to_2024-05-17_57` (hex tail `...5f 74 6f 5f 32...` = `_to_2024`). Recompute: 57 files, 17,924,900 bytes; name-inclusive manifest digest (`sorted "relpath\tsize\tsha256"` lines, joined with `"\n"`, plus trailing newline) = `b7debe929a24406a44f53708194b644a4811559cf91ad87d5a55e5da91a28fbd` = MATCH. (The no-trailing-newline variant does not match; the trailing-newline method is the correct one.) `[VERIFIED]`
- Content-multiset reference: `evidence/20260925-rev28-native-closed-loop/baseline-content-multiset.json` SHA-256 = `3c932d8ccb9f4d2a7945463861fb4ebae066eadb59b8ff2702767b3a9f851bc2` = MATCH; its 57 values, sorted lexicographically and joined with `"\n"` (no trailing newline), hash to `ee958e6467676506a1c7aaf237a4376ecc5e5083fd94aa6fd0d56d376cacdaaf` = MATCH; the same digest recomputes from the on-disk baseline hashes (57 unique). Its `source_dir` field uses the correct on-disk spelling and exists. `[VERIFIED]`
- Staging `staging/RUN-20260923-111908-01/`: exists, 0 entries, 0 bytes = MATCH. `[VERIFIED]`
- Historical replay fixtures: all 20 table entries' SHA-256 prefixes recomputed against on-disk files = 20/20 MATCH (incl. the six `20260921-rev27-save-all/attempt-07/s11e-*` files and the `20260916-route/attempt-07|12|13|14|16` files); both attempt-07 trees exist and are disambiguated by explicit paths (RV-10). `[VERIFIED]`
- Probe evidence: `capability-probe/prototype-round1-findings.md` SHA-256 `43998519…` = MATCH; `prototype-round2-outputs.txt` SHA-256 `afd6c26d…` = MATCH. The cited findings are corroborated in-file: capture bbox ≠ `SCWindow.frame` (round 1: `includeChildWindows=false` 792×660 px vs 394×326 pt; round 2 `ignoreShadows=true`: 800×664 px = 400×332 pt ×2 exactly; union case 1040×798 px = 520×399 pt ×2 exactly); shadow-bearing default 1024×952 px for a 400×364 pt frame (~56 pt/side); `SCWindow.frame` unstable (uniform 3 pt inset in unsettled states, exact when settled); `sourceRect` window-local semantics; `SCScreenshotManager` needs `NSApplication`+run loop. `[VERIFIED]`
- Environment claims spot-checked: macOS 27.0 (26A428) MATCH; Swift 6.3.3 MATCH; bare `xcrun --show-sdk-path` → CLT SDK Version 27.0, `--sdk macosx` → MacOSX26.5.sdk MATCH; `ScreenCaptureKit` headers contain `pointPixelScale`, `includeChildWindows`, `SCScreenshotConfiguration`, and contain NO `contentScale`/`scaleFactor` symbol MATCH (both SDKs); LINE installed 26.0.2 (3828) MATCH. `[VERIFIED]`
- Issue anchor `ISSUE-2026-09-24-rev27e4-r2-ownership-provenance.md` SHA-256 `0b3c9efa…` = MATCH. `[VERIFIED]`
- Historical context claims: `SAVE_ALL_SEMANTIC_EFFECT_INDETERMINATE` and `RUN-20260907-154331-01` both exist in repo evidence (spot-checked); revision-1 snapshot SHA `5575aca5…` matches the plan's `PLAN_REVISION_BASIS`. `[VERIFIED]`
- Grounding drift found: RV-01 (baseline path literal). No other drift detected; the plan's other recorded facts that I checked all reproduce.

## ARCHITECTURE_AND_CONTRACTS
- (a) Validated-tolerance capture rule + W2 freeze: implementable and free of self-contradiction. Invariant 1 defines the check as `image.size.px` within a W2-frozen per-state bound of `round(expectedBBox.size × S)` where `expectedBBox` is the union of the freshly read included `SCWindow.frame`s; invariant 2 uses the recorded capture bbox `B` (not an assumed frame) with exact or frozen-symmetric-padding origin; invariant 3 anchors scale detection to `NSScreen.backingScaleFactor`; invariant 4 makes mixed-space use type-level impossible; invariant 5 refuses points within 1 pt of a safe-interior boundary. States without a frozen rule fail closed, and post-freeze rule changes are replan+review, never implementer edits. The residual implementation burden is that for `includeChildWindows=true` the engine must compute the expected union from SC enumeration in a way that matches ScreenCaptureKit's actual included set; the plan covers this as a W2 freeze obligation with fail-closed behavior, and the primary production path (`includeChildWindows=false`, `ignoreShadows=true`) is the one the probe evidence shows is exactly reproducible (frame × scale). No internal contradiction found.
- (b) `DUPLICATE_CONTENT_CONFIRMED` predicate: exact and independently recomputable. The predicate fixes: 57 image files, no subdirs/partials/zero-byte at settle, all structurally decodable, sizes+mtimes stable across ≥3 samples spanning ≥4 s with ≥5 s quiescence, total bytes 17,924,900, and `SHA-256(sort(values).joined("\n"))` (no trailing newline) == `ee958e64…` against the reference JSON (file SHA `3c932d8c…`). Filenames are excluded by design and the name-inclusive `b7debe92…` is explicitly demoted to identity/tripwire. I recomputed both sides myself; the predicate reproduces. Stage 05 recomputes the staging multiset and both baseline digests from disk and never trusts Stage-04 claims; mismatch classes (`STAGING_INCOMPLETE`, `STAGING_EXTRA_FILES`, `STAGING_DUPLICATE_CONTENT`, `CONTENT_MISMATCH_AGAINST_ACCEPTED_BASELINE`, `STAGING_UNSTABLE`) are named with a route-defect-vs-album-changed forensic split and never a success label.
- Chooser contract: the 4-clause predicate (newness vs pre-dispatch inventory; native panel semantics calibrated against a real `NSOpenPanel`; ownership via pre/post panel-process census + bundle/signing identity + process start time with pid reuse rejected; in-window observation with full evidence) is frozen by W2 before the seven reviews, permits whichever hosting shape the harness proves, keeps panel membership non-sole, and refuses fake/ambiguous surfaces. Consistent with §10's affirmative-evidence rule.
- Route terminal-state names are consistent with their evidence (`CHOOSER_VERIFIED` / `CHOOSER_OBSERVED_AFTER_WINDOW` / `NO_CHOOSER_OBSERVED`), and no state name asserts a stronger claim than its evidence.
- No public API/type/config or external contract is modified; the change set is additive (`rev28/**`, evidence dir, task dir, one `.gitignore` line).

## DATA_SECURITY_RELIABILITY
- Data integrity: baseline READ-ONLY; no second accepted copy; staging must be fresh/empty/canonicalized/inside the approved root/symlink-free/not baseline-related; append-only hash-chained ledger with fsync'd intent before the irreversible confirmation; every artifact bound by path+SHA-256.
- Reliability: single dispatch for irreversible ops; no blind retry; unknown irreversible dispatch never re-issued; crash resume in observe-only mode with ledger continuity and zero irreversible re-dispatch (harness-proven at scripted kill points); bounded patience for downloads with honest indeterminate terminal states.
- Attribution/fail-closed: unattributable post-dispatch writes abort (`ABORTED_UNATTRIBUTED_FILESYSTEM_WRITE`); pre-dispatch ≥10 s environmental baseline recorded; concurrent-activity caveat disclosed (scope wording issue: RV-02).
- Privacy/security: no secrets involved; live captures/OCR extracts stay in the untracked evidence directory and are never pushed; TCC grants re-verified in the executable context as a hard gate; no AX write against ambiguous elements; no clipboard path unless AX and keyboard both fail under review.

## IMPLEMENTATION_SEQUENCE
- W1 (no-GUI cores + probe + transform tests) → W2 (harness + occluder + freezes: capture per-state rule, latency/bounds, chooser predicate, tripwire attribution, restart fixture) → W3 (locators + replay + stability) → W4 (state machine, risk classes, ledger, verifier with the RV-02 predicate, chooser driver, postcondition monitor) → W5 (adversarial 25 + deterministic reruns) → W6 (SUPPORTING docs) → W7 (conditional Phase A reconnaissance gate → Phase B single live run → verification → report → local commits).
- The frozen W2 artifacts are mandatory review inputs before the seven reviews; reviews bind to SHAs; repairs loop back without weakening; three consecutive reviews on the same unresolved dependency → `BLOCKED_WITH_ROOT_CAUSE`.
- Replan triggers are explicit (frozen-rule change, calibration insufficiency, review REVISION_REQUIRED). No sequencing gap that could let the live run precede a gate was found.

## TESTABILITY_AND_ACCEPTANCE
- Unit/integration/replay/adversarial layers are deterministic and evidence-bound; each adversarial scenario has fixture id, expected verdict, fail-closed behavior, and evidence path; the suite must be semantically equal across two runs.
- Scenario numbering is now exact: `G01`–`G22` = the goal's 22 scenarios in goal order; `X01`–`X03` explicitly labeled extensions; canonical count = 25, consistent across R20, CRITICAL_PATH, and the matrix.
- Postcondition bounds are calibrated (real `NSOpenPanel`, ≥20 runs) and review 4 binds the frozen cadence/bounds with margin; late-affirmative semantics are explicit; no-retry is unchanged.
- v4.2 §7 status-contract audit (my verification): no repository-health check is called product CORE (preservation audit is SUPPORTING/NON_GATING); broad-gate policy is explicit (none; bespoke checks; `BASELINE_REQUIRED: NO`; no `BASELINE_DELTA` gate); waiverability/authority explicit (`WAIVER_ALLOWED: NO`; owner only; no self-waiver); Stage 04/05 routing covers CORE FAIL/`BLOCKED`/`NOT_RUN` (probe NOT_RUN → pending; FAIL → replan/blocked; postcondition names; chooser refusal → `INDETERMINATE_CHOOSER_REFUSED`; baseline delta → abort; Stage 05 blocks closure); no legacy active terminal schema conflict (v2 subjects used; legacy evidence preserved); Stage 05 reads/recomputes from disk and preserves Stage-04 facts (the §7.8 immutable snapshot fields themselves are inherited from the canonical contract, which the plan binds to — acceptable at plan level); deployment/mixed-version concern N/A. Fixture audit outcome: canonical incident/implementation-blocker/CORE-states/replan/baseline-delta/hard-clean-debt/waiver/independent-acceptance/contradictory-state/DONE-prerequisites are explicit or explicitly inherited from §7; the one weak spot is `baseline unavailable` (no explicit CHECK_RESULT rule — folded into RV-01's correction).
- Closure semantics are precise: case (b) is an accepted terminal state but never "Done"; `PRIMARY_OUTCOME_STATUS` is `NOT_ACHIEVED`/`UNKNOWN`; `TASK_CLOSURE_STATUS` follows §7.7/§7.8; `DONE` requires §7.10 (and the plan's additional closure prerequisites). No synthetic evidence can produce a live-success claim.
- Residual wording issues that affect acceptance clarity (not the acceptance predicate itself): RV-03 (counter wording) and RV-04 (§11 budget sentence).

## SCOPE_AND_COMPLEXITY
- Scope equals the goal's deliverables plus three cheap, explicitly labeled adversarial extensions; nothing outside the named album/group is touched; no cleanup/deletion/push/pycache; historical artifacts are do-not-touch.
- Complexity is concentrated at stable boundaries (one transform component, one actuator path, pure cores + thin OS edges); no scatter across layers; no over-coupling found.
- The only scope-relevant precision defects are the findings below; none expands or shrinks the authorized production run.

## FINDINGS
Prior-review resolution cross-check (non-authoritative; formed after my own findings): attempt-02's `RV-01` (exact-equality capture rule) → resolved in revision 2 §1 + invariant 1 + W1 + capability matrix (validated tolerance, W2 freeze, fail-closed; wrong-scale detection anchored independently). `RV-02` (predicate) → resolved (§12 exact predicate + recorded reference digest + mismatch classes + Stage-05 recompute). `RV-03` (late sample/bounds) → resolved (named late-affirmative terminal + calibrated bounds + replan rule). `RV-04` (counters) → resolved (per-op exactly-once + ≤3/blocker + global ceiling 12 + named abort). `RV-05` (scenario count) → resolved (22+3=25 canonical). `RV-06` (capability matrix) → resolved (round-1/2 rows + contentScale/scaleFactor survey line). `RV-07` (tripwire attribution) → resolved (attribution rule + pre-dispatch baseline + fail-closed unattributable) with a residual scope-wording nit (RV-02 below). `RV-08` (occluder/restart) → resolved (separate-process `rev28occluder` + `rev28ctl` SIGKILL points + observe-only resume assertions). `RV-09` (chooser predicate) → resolved (4-clause resolved-and-frozen predicate). `RV-10` (replay fixtures) → resolved (full relative paths; 20/20 prefixes verified). Attempt-01's non-blocking minors: postcondition latency/verdict-eligible late sample → resolved; DoD/"Done" conflation → resolved; LINE-specific reconnaissance step → resolved (Phase A gate); chooser census/pid-reuse pinning → resolved; content-mismatch terminal class → resolved; scenario count → resolved; rectangle tracking → resolved; §7.3 field table → resolved (table present). The attempt-01 residual interplay note (revalidation cap vs navigation recovery) is explicitly resolved in §9.

New findings in this review:

- ID: RV-01
  - Severity: MAJOR
  - Category: GROUNDING, HANDOFF
  - Affected plan section: `## VERIFIED_REPOSITORY_FACTS`, plan.md line 87 (accepted-baseline path); downstream: §LIVE_RUN_ENVELOPE preflight (f), §11 destination containment, §12/§VERIFICATION_AND_ACCEPTANCE Stage-05 baseline recomputation.
  - Evidence: plan.md line 87 contains the literal `album-2024-05-13_to-2024-05-17_57` (hex after `to`: `2d` = `-`), and claims it was "recomputed on disk this round = MATCH". The on-disk directory is `album-2024-05-13_to_2024-05-17_57` (hex after `to`: `5f` = `_`); `os.path.exists()` on the plan's literal path = False; exactly one candidate directory exists; the same correct spelling appears in repo history (`tests/automation_verification/run_phase2_r3_preconditions.py`, `evidence/20260921-lineage-guard-replan/attempt-01/baseline-tripwire.json`) and in the plan's own `baseline-content-multiset.json` `source_dir` (which exists). The baseline numbers/digests themselves are correct and recompute from the real directory. `[VERIFIED defect]`
  - Failure/rework mechanism: the VERIFIED_FACTS section is the plan's contract for Stage 04/05; an implementer that copies the literal path builds the baseline tripwire/containment/preflight against a non-existent directory — at best a fail-closed preflight abort (consume/rework a CRITICAL cycle; the Stage-04 handoff↔plan mismatch rule treats such inconsistencies as stop conditions), at worst, if implemented leniently (skip-if-missing), a vacuous "baseline unchanged" CORE gate plus a blocked Stage-05 baseline recomputation. The plan's own claim "recomputed on disk this round = MATCH" is not literally reproducible from the recorded path.
  - Smallest required correction: correct the line-87 literal to the on-disk spelling (or, better, bind the baseline location to `evidence/20260925-rev28-native-closed-loop/baseline-content-multiset.json` `source_dir` + `realpath` canonicalization), and add one explicit rule: a baseline path/reference that cannot be resolved or read is a hard failure (`CHECK_RESULT: BLOCKED`; no skip, no vacuous tripwire) for preflight and Stage 05.

- ID: RV-02
  - Severity: MINOR
  - Category: SEMANTIC_CONTRACT
  - Affected plan section: §10 filesystem-tripwire attribution (line 265); §LIVE_RUN_ENVELOPE abort conditions (line 409); §11 preflight (line 276).
  - Evidence: three scopes are named without a defined ladder — monitored scope "under the staging run dir and under `~/Downloads`" (line 257/409), attribution scope "elsewhere under the approved root" (line 265), and containment scope "inside the approved root" (line 276). A post-dispatch write under `~/Downloads` but outside `LINE-Backup-PoC/` is monitored but not explicitly covered by the attribution rule, so its classification (supporting vs `ABORTED_UNATTRIBUTED_FILESYSTEM_WRITE`) is undefined. `[VERIFIED wording gap]`
  - Failure/rework mechanism: ambiguous classification could either abort a good run on unrelated activity (budget loss) or leave a real side effect loosely labeled "supporting"; implementers may silently pick a reading.
  - Smallest required correction: define one explicit scope ladder `staging ⊆ approved root ⊆ monitored ~/Downloads` and state the classification/abort rule per level; keep fail-closed for unattributable writes and keep attributable writes recorded as supporting.

- ID: RV-03
  - Severity: MINOR
  - Category: SEMANTIC_CONTRACT
  - Affected plan section: §LIVE_RUN_ENVELOPE counters (line 407); §9 risk classes.
  - Evidence: the counters say `Save All dispatch = 1; destination confirmation = 1; irreversible side-effecting ops = 1` while §9 explicitly executes Save All under `IRREVERSIBLE_SIDE_EFFECT` semantics (exactly one dispatch, no retry) AND the destination confirmation is irreversible — i.e., two irreversible-classified dispatches exist. The goal's envelope wording ("For any operation classified SIDE_EFFECTING: maximum count = 1") supports a per-operation-type reading, but the plan's aggregate count can be read as a single-event count, which Stage 05 could flag as an envelope violation. `[VERIFIED ambiguity]`
  - Failure/rework mechanism: contradictory-looking ledger/acceptance accounting on the single run; avoidable status-contract churn at closeout.
  - Smallest required correction: state per-operation-type maxima explicitly (`any irreversible-classified operation: ≤1 dispatch`) or list both irreversible-classified dispatches and drop the ambiguous aggregate count.

- ID: RV-04
  - Severity: MINOR
  - Category: SEMANTIC_CONTRACT
  - Affected plan section: §11 chooser automation (line 279) vs the preceding exactly-once confirmation sentence (line 278).
  - Evidence: "All chooser actions are logged with pre/post evidence and consume the single confirmation budget." Read literally, routine navigation actions (Go-to-folder field entry, ⇧⌘G) consume the confirmation budget, which is inconsistent with "exactly one confirmation action is chosen before dispatch … the two are never both used". The safety-relevant rule (a non-effect consumes the budget; no second confirmation) is stated nearby, but the overbroad sentence invites a literal misreading. `[VERIFIED wording defect]`
  - Failure/rework mechanism: an implementer could over-count the budget and self-block before confirmation (fail-closed but breaks the run), or under-count if they disregard the sentence's safety intent.
  - Smallest required correction: reword to "chooser navigation actions are logged; the single confirmation action (AXPress or Return, chosen before dispatch) consumes the confirmation budget once, and a non-effect consumes it".

## REQUIRED_PLAN_CHANGES
1. `RV-01` (blocking, one MAJOR): fix the accepted-baseline path literal in `VERIFIED_REPOSITORY_FACTS` (on-disk spelling `album-2024-05-13_to_2024-05-17_57`, i.e. `_to_`, hex `5f 74 6f 5f`), and add the explicit fail-closed rule for a non-resolvable/unreadable baseline path or reference (`CHECK_RESULT: BLOCKED`; never skip the tripwire; preflight and Stage 05 both covered). Prefer binding the path to `baseline-content-multiset.json` `source_dir`.
2. `RV-02` (minor): define the staging/approved-root/`~/Downloads` scope ladder and the per-level attribution/abort classification.
3. `RV-03` (minor): make the irreversible counters per-operation-type explicit (or list both irreversible dispatches).
4. `RV-04` (minor): reword the §11 chooser-budget sentence so only the single chosen confirmation action consumes the confirmation budget.
5. After revision: increment `PLAN_REVISION`, then fresh re-review binds to the new revision/hash. No gate may be weakened in the revision (no loosened identity/confirmation/recovery semantics, no retry of irreversible ops, no tolerance without a recorded rule).

## RESIDUAL_MINOR_NOTES
- (a) The `includeChildWindows=true` expected-union computation is a W2 implementation burden: the engine must derive the included child set in a way that matches ScreenCaptureKit's actual inclusion; the plan's fail-closed fallback (state without a frozen rule → INVALID) is safe, but if W2 cannot freeze a production-computable union rule, those states simply cannot be used for geometry — acceptable, worth recording in the W2 artifact.
- Late-affirmative handling leaves the chooser open with zero further input (by design, disclosed). Consider noting in the ledger/final report that a modal sheet may remain for the owner to dismiss.
- The 8.0 s plan-time cadence boundary is explicitly provisional and must be bound by W2 measurement + review 4; if the measured latency makes the hard cap insufficient → replan (already stated).
- The `~30 s` late sample should remain forensics-only and must not extend any input path; wording is consistent with that (no action needed beyond RV-04's clarification).
- Stage 05's §7.8 immutable Stage-04 snapshot fields are inherited from the canonical contract rather than restated in the plan; acceptable at plan level (execution.md is referenced), but the Stage-03 handoff should carry the field list so the Stage-05 artifact is unambiguous.
- The dual OCR wrapper remains slight redundancy (BEST_EFFORT, non-gating); keep only if parity evidence is genuinely used by review 1.
- Multi-display behavior remains `[UNVERIFIED]` on this single-display machine and is correctly fail-closed with an adversarial scenario present.

FINAL_STATUS: PLAN_REVISION_REQUIRED
NEXT_ACTION: Stage 01 revision mode on `T20260925-0647-01-rev28-native-closed-loop` (PLAN_REVISION 3): apply the RV-01 baseline-path/fail-closed correction plus the RV-02–RV-04 wording clarifications, then re-review the new revision/hash; do not proceed to Handoff for revision 2.
