# Plan Review Report

## REVIEW_METADATA
- TASK_ID: `T20260925-0647-01-rev28-native-closed-loop`
- REVIEW_ATTEMPT: `02` (this task's `review/` contained no prior attempt directory; `attempt-02/` was created as instructed and nothing existing was overwritten)
- REVIEWED_PLAN_REVISION: `1`
- REVIEWED_PLAN_SHA256: `5575aca5f2c8ededa3f76ff79baf6b82eecd25353457b709982eb05e6cbcbbb9` (recomputed on disk this round; matches the expected value)
- PLAN_SNAPSHOT_PATH: `.agent/tasks/T20260925-0647-01-rev28-native-closed-loop/review/attempt-02/plan_snapshot.md` (byte-copy of the exact plan bytes; SHA-256 identical to `plan.md`)
- Repository anchor observed: HEAD `766b22c4f8bf29b9d0a46049c75309c70593d66d` == `origin/master`, ahead 0; `git status --porcelain` shows only untracked WIP/evidence (no tracked modifications); Issue `ISSUE-2026-09-24-rev27e4-r2-ownership-provenance.md` SHA-256 `0b3c9efab209bcb4c8c9b5dcaa5fa1ca6279234df0ee3cc0984da829ead67a2d` (recomputed, match); baseline `/Users/hsiaojohnny/Downloads/LINE-Backup-PoC/album-2024-05-13_to-2024-05-17_57` = 57 files / 17,924,900 bytes / manifest digest `b7debe929a24406a44f53708194b644a4811559cf91ad87d5a55e5da91a28fbd` (recomputed with the plan's own method, match); staging `RUN-20260923-111908-01` exists, 0 entries; LINE 26.0.2 (3828) installed, not running; macOS 27.0 (26A428); Xcode 26.6 (17F113); Swift 6.3.3.
- Reviewer runtime/model: Codex CLI fresh independent session (no Planner transcript, no prior review context); exact model tag not exposed to this session (informational only).

## OWNER_VERDICT
**Goal as understood:** the owner wants one more end-to-end LINE album backup of exactly `旻謙允禎成長日記 / 2024/05/13～05/17 / 57` photos, rebuilt on a new macOS-native architecture (ScreenCaptureKit seeing, Vision labeling, Quartz clicking, Accessibility for the native dialog), with success proven by *directly observing the resulting chooser* and then by *57 complete files in a fresh staging folder whose content equals the already-accepted baseline* (`DUPLICATE_CONTENT_CONFIRMED`). Only one new production Save All run is authorized.

**Essentials (what actually carries the outcome):** the capability+harness calibration, the single canonical coordinate transform, fresh window-identity binding, the closed-loop Save All postcondition (directly observed chooser, never click-return/menu-gone/panel-service presence), exactly-once destination confirmation into a fresh empty staging dir, and filesystem/content verification against the baseline.

**Optional / best-effort (should not stop the core path):** Foundation Models diagnostics, feature-print/registration stability comparison, the AX-chooser navigation path (falls back to a tested keyboard path), and the documentation/manifest deliverables.

**What can stop the whole run and why:** every gate before the live run is deliberately hard (capability probe, harness calibration, adversarial suite, seven topic reviews + adversarial review + barrier-supersession review, live preflight). After that, the one Save All dispatch and the one confirmation are irreversible and are not retried; if the chooser is not directly observed inside the bounded postcondition window, the run honestly ends indeterminate and the single-run budget is consumed. This is consistent with the goal's envelope and with the historical "returned without chooser" precedent.

**Complexity verdict:** proportionate. This is a new native engine, not a patch; the module set, ledger, and harness each pay for a required outcome or a must-not-break invariant. No significant untraceable scope found; the three extra adversarial cases beyond the goal's list are cheap and useful.

**Biggest remaining risk:** not the architecture — it is the plan's own hard-coded "exact capture size" invariant, which the Planner's newer probe evidence in the same evidence directory already contradicts (child-window union/padding; `SCWindow.frame` inset instability). As written it invites fail-closed aborts of valid frames or an unreviewed loosening of a CORE safety gate on a one-shot run. Second: the `DUPLICATE_CONTENT_CONFIRMED` verdict currently does not define its exact comparison predicate, and the only baseline digest cited is name-inclusive, which a fresh download (new timestamp in filenames) can never match.

## GOAL_BASELINE
Reconstructed from the authoritative source `/Users/hsiaojohnny/.codex/attachments/41a5dfc8-e350-429c-a01b-aea3a67e03fd/pasted-text-1.txt` (read before the plan; `782` lines), GOAL_ID `REV28-MACOS27-NATIVE-CLOSED-LOOP-LINE-BACKUP`.

- **PRIMARY_OUTCOME:** autonomously redesign, implement, validate, review and — only after all safety/calibration gates pass — execute exactly one new Rev28 production LINE album backup run for `旻謙允禎成長日記 / 2024/05/13～05/17 / 57`, using macOS 27 native sensing (ScreenCaptureKit), Vision localization, Quartz event injection, Accessibility, and filesystem verification, under the observable closed-loop contract OBSERVE→LOCATE→ACT→OBSERVE DIRECT POSTCONDITION→VERIFY FILESYSTEM EFFECT.
- **SUCCESS_EVIDENCE:** correct album identity → native visual localization → reviewed native actuation → chooser postcondition confirmed → unique staging selected → download dispatched exactly as authorized → 57 complete stable images → content verification PASS → accepted baseline unchanged → registry/evidence updated consistently → no unexplained side effect. Matching content vs the existing accepted 57-photo baseline (`DUPLICATE_CONTENT_CONFIRMED`) is valid end-to-end proof and must not create a second accepted copy. Never claim `BACKUP_COMPLETE` from a click, menu disappearance, chooser appearance alone, or an unverified file count.
- **MUST_NOT_BREAK:** accepted baseline (57 files / 17,924,900 bytes / manifest digest `b7debe92…`) is READ_ONLY; no historical coordinate reuse (every live coordinate fresh-derived); `禎 U+798E` vs `楨 U+6968` never normalized/merged; no `__pycache__`/`*.pyc`; no push; historical evidence not reset/discarded/overwritten (attempt-07, Rev27d/e artifacts, issue, frozen v4–v8 tools, staging run); line-internal inference (unified logs, panel-service presence, click-return) is supporting/forensic only, never the primary success condition; no AX write/click against ambiguous identity.
- **NON_GOALS:** another album/group; a second accepted production copy; staging cleanup without separate authorization; reclassification of attempt-07; weakening safety invariants to obtain approval.
- **CRITICAL_PATH:** capability survey/prototype → synthetic harness proving capture/occlusion/coordinates/retina/popup routing/postcondition/AX → native engine (sensor, identity, Vision, Quartz, AX, state machine, ledger, verifier) → offline replay of retained frames through Rev28 locators → adversarial hardening → independent reviews → single bounded live run → filesystem/content verification → closeout.
- **REVIEW LIST (goal, 7 named):** macOS 27 sensor/perception design; coordinate transform model; Quartz actuator; postcondition model; action-risk classification; chooser automation; transaction/recovery policy — all before production live execution, approving the exact frozen implementation.
- **ADVERSARIAL LIST (goal):** the goal text names **22** scenarios verbatim (lines 653–674): wrong window, wrong bundle, stale windowID, wrong Retina transform, occluding app, window movement, popup movement, wrong row, duplicate target OCR, OCR corruption, whole-screen false positive, click outside popup, click on neighboring row, focus theft, postcondition timeout, fake chooser-like surface, unexpected filesystem write, zero-byte burst, partial download, duplicate files, process restart, multi-display transform. (The review request speaks of "23"; the verbatim goal list is 22 — recorded as such here, and see RV-05.)
- **AUTHORIZATION ENVELOPE:** autonomous research/implementation/testing is pre-authorized; after ALL gates pass, exactly one new Rev28 production run for exactly this album/group; authorization does not transfer; irreversible destination confirmation max 1; any SIDE_EFFECTING op max 1 unless reviewed idempotent recovery is proven; reversible navigation may use its reviewed bounded recovery budget; identical live-state blocker ≤3 cycles; 3 consecutive reviews on the same unresolved dependency → `BLOCKED_WITH_ROOT_CAUSE`; "Do not weaken a safety invariant merely to obtain approval."
- **RISK-CLASS RULES (goal):** define and independently review `REVERSIBLE_NAVIGATION`, `PRE_SIDE_EFFECT_ACTION`, `IRREVERSIBLE_SIDE_EFFECT`; classify Save All empirically for the exact LINE/macOS version, never from the label; Save All may be `PRE_SIDE_EFFECT` only if *proven* to open a chooser before any filesystem write; if it can write directly, exactly-once semantics stay. If no chooser appears: do not infer failure or retry — apply only the reviewed recovery policy for the empirically established class.
- **BARRIER RULE (goal):** attempt-07 stays `SAVE_ALL_SEMANTIC_EFFECT_INDETERMINATE`; before any new production Save All, an independent review must establish why the new architecture + new authorization scope safely supersede the previous reconciliation barrier.

## GOAL_ALIGNMENT
- The Plan's `PRIMARY_OUTCOME` and success semantics match the goal: closed-loop postcondition (chooser), `DUPLICATE_CONTENT_CONFIRMED` route, baseline unchanged, single bounded run (plan §GOAL_CONTRACT, §DEFINITION_OF_DONE). No supporting tool/detail has been promoted into the project goal.
- Acceptance criteria prove the outcome, not merely implementation completeness: the plan's Stage-02→adversarial→reviews→live-run→filesystem/content sequence terminates in the goal's success conditions, and it explicitly refuses to treat synthetic PASS or click/chooser-shape as live success (plan §STATUS_SEMANTICS, §DEFINITION_OF_DONE).
- The plan correctly keeps Rev27e provenance as forensic/supporting and does not make it the sole blocker — exactly what the goal's FIRST-PRINCIPLES section demands.
- The plan's conservative Save All classification (execute under `IRREVERSIBLE_SIDE_EFFECT`: one dispatch, no retry) is coherent with the goal's rule set: the class is not proven at plan time, so the exactly-once branch applies; the goal's "if no chooser: do not retry" is honored; the run's own evidence records the empirical classification for future runs. No weakening, no label inference.
- Minor goal-alignment deltas are completeness/labeling issues only (see RV-05, RV-06): the goal's capability survey also names `contentScale`/`scaleFactor`, which the plan silently omits rather than recording as "not present in the SDK".

## NECESSITY_AND_TRACEABILITY
All 25 plan requirements trace to goal text or to a must-not-break invariant; no untraceable material work found:
- R1→closed-loop contract; R2/R3→sensor + window identity; R4/R5/R6→Vision identity, locators, stability; R7→FM role; R8/R9→Quartz + transform; R10→harness proofs; R11/R12→AX + activation; R13→state machine; R14/R15→risk classes + postcondition; R16→chooser; R17→filesystem; R18→transaction; R19→bounded autonomy; R20→reviews/adversarial; R21→envelope; R22→success condition; R24→barrier; R25→preservation/no-push/no-pycache.
- Classifications are reasonable: R6/R7 are correctly `SUPPORTING` (both degrade locally, plan §DEGRADATION); R23 (deliverables manifest) `SUPPORTING` is acceptable; R25 `CORE` is a must-not-break invariant, not a repository-health gate disguised as product outcome.
- The three extra adversarial cases (stale AX element, popup-in-main-window vs separate, inactive-target posting) are cheap and trace to real uncertainty in the goal text ("popup/child/sheet observation"; activation/focus); keep them.
- No requirement invents hypothetical future needs; the ledger/state-machine/harness all map to goal-listed deliverables and safety rules.

## GATE_AND_VETO_AUDIT
- Gates are proportionate: capability probe, harness calibration, offline replay, adversarial suite, seven topic reviews + adversarial review, barrier-supersession review, and live preflight all gate only the *irreversible production run*, which is exactly where a global gate is justified (single-shot authorization; no retry budget).
- Local degradation is preserved for `SUPPORTING` items (FM diagnostics, registration, AX chooser path → keyboard fallback), so no supporting element vetoes the core path globally. No unjustified global gate found.
- Baseline invariants are explicit hard-clean semantics (`baseline unchanged`) with a must-not-break rationale; the plan does not call repository-health checks product CORE.
- Waiver authority: plan §DEGRADATION_AND_GATE_BEHAVIOR states "Waivers: none available to the agent; every gate is explicit and reviewed" — no self-waiver possible. Good.
- Broad-gate baseline policy: no repo-wide lint/test gate is defined, so no HARD_CLEAN-vs-BASELINE_DELTA ambiguity arises; recommend (residual) that Stage 04/05 artifacts carry the §7.3 verification-item schema (`EVIDENCE_ROLE`/`CLOSURE_GATE`/`WAIVER_*`) for the material gates.

## COUPLING_AND_FAILURE_CONTAINMENT
- Independent signals are kept independent: identity, localization, postcondition, chooser, filesystem verification are separate stages with distinct verdicts; locator ambiguity refuses instead of collapsing into one shared validity flag.
- Failure containment is at the narrowest sensible boundary for perception (`INVALID` frame → re-locate), for locators (AMBIGUOUS → refuse), and for the AX plane (ambiguous element → refuse).
- Two containment defects: (1) the capture-size invariant is over-strict (RV-01) so a *valid* observation can be classified INVALID and propagate to a global abort; (2) the filesystem tripwire's "any unexpected write outside staging" abort rule is unattributed and can misfire on this machine where the probe itself documented concurrent agent activity (RV-07).
- No low-value adapter is a single point of failure: FM and registration are optional; the AX chooser path has a tested keyboard fallback; the core path depends only on ScreenCaptureKit + Vision + Quartz + filesystem, all verified locally.

## DESIGN_ECONOMY
Deletion test applied module by module: sensor/identity/perception/actuator/postcondition/chooser/verifier/ledger each fail a user acceptance criterion or a must-not-break invariant if removed — they stay. The synthetic harness pays for itself because the goal explicitly forbids touching production Save All before calibration. Hash-chained ledger + persisted state directly implement the goal's transactional-safety section. No premature abstraction found beyond `Diagnostics/DiagnosticHooks` (small, and the goal names an FM diagnostic role). Complexity is concentrated in one new SwiftPM package with thin OS edges, which is the right boundary. One dead-weight note: `PRE_SIDE_EFFECT_ACTION` may be inapplicable this round (LINE Save All cannot be proven pre-write before the single run), but the goal explicitly requires the three classes to exist and be reviewed — keep, no change needed.

## CRITICAL_PATH_AND_PRIORITY
W1→W7 order matches the goal's critical path: capability + harness first, engine core, offline validation, adversarial hardening, reviews, then the single live run, then filesystem/content verification and closeout. Supporting deliverables (W6 docs) are sequenced after hardening, not ahead of core acceptance. No priority inversion: nothing is polishing optional items while a CORE signal is failing, and the live run is gated on the whole chain.

## REQUIREMENT_FIDELITY
- Faithful: closed-loop contract, ScreenCaptureKit primary capture with `SCContentFilter(desktopIndependentWindow:)`, no fullscreen as primary, window-identity binding incl. capture epoch + SHA-256 and stale-ID invalidation, Vision exact-identity OCR without normalization, identity/geometry separation, structural locators incl. v8 Save All invariants (verified against `evidence/20260916-route/tools/v8/locate_save_all_menu_item.py`: `REFERENCE_ORDER`, `MIN_OVERLAP_PX=20`, `X_EDGE_PX=2`, `EDGE_FRACTION=0.15`, `EDGE_MIN_PX=6`, `NEIGHBOR_SEP_PX=12`, non-target substitution tolerance 1), FM diagnostics-only role, Quartz actuator with preflight/request, one canonical transform, synthetic calibration matrix, AX semantics-first, activation verification, persistent state machine, risk classes, postcondition model, chooser rules (fresh unique staging, canonicalization, no symlink escape, not baseline/ancestor/descendant, durable intent before confirmation), filesystem rules (57, no partials/zero-byte/subdirs, decodability, stability, quiescence), transactional safety, bounded autonomy, review list, envelope, success condition, deliverables, barrier supersession, preservation constraints.
- Deviations: (a) `DUPLICATE_CONTENT_CONFIRMED` comparator undefined/ambiguous (RV-02); (b) capture-size/transform invariants contradict the Planner's own newest probe evidence (RV-01); (c) goal capability survey names `contentScale`/`scaleFactor` — plan records neither presence nor absence (RV-06); (d) plan says "23 named scenarios" vs goal's 22 (and 25 in its own matrix) (RV-05); (e) postcondition-window late-sample semantics (RV-03) and chooser-affirmation predicate (RV-09) are under-specified relative to the goal's "directly observed" rule.

## GROUNDING_AND_DRIFT
Verified this round (all read-only):
1. Repository anchor: `git rev-parse HEAD origin/master` = `766b22c4f8bf29b9d0a46049c75309c70593d66d` both; `git rev-list --left-right --count origin/master...HEAD` = 0/0; only untracked entries. Matches plan §VERIFIED_REPOSITORY_FACTS.
2. Issue SHA-256 recomputed = `0b3c9efa…` (match).
3. Baseline recomputed with the plan's stated method (sorted `relpath\tsize\tsha256` lines → SHA-256): 57 files, 17,924,900 bytes, digest `b7debe92…` (match). Filenames carry a per-download date token (`LINE_ALBUM_20240513～0517_260907_*.jpg`) — directly relevant to RV-02.
4. Staging `RUN-20260923-111908-01` exists, 0 entries (match).
5. Capability probe evidence exists under `evidence/20260925-rev28-native-closed-loop/capability-probe/`; round-0 metadata/outputs confirm macOS 27.0 (26A428), Xcode 26.6 (17F113), Swift 6.3.3, TCC grants (screen capture, post-event, AX), `desktopIndependentWindow` capture (2294×1366 vs 2294×1438 with children), legacy 1920×1080 default, `NSApplication.shared` + run-loop requirement, single 1147×745@2.0 display. LINE 26.0.2 (3828) installed, not running. Historical LINE geometry claims are grounded (attempt-17: focused main `(0,33) 327×643`, `LINE` `(0,37) 1147×699`; attempt-07 screen-probe: `(0,36) 1147×699`).
6. SDK headers (Xcode `MacOSX26.5.sdk` and CLT `MacOSX27.0.sdk`): `SCScreenshotConfiguration` (macOS 26.0, `includeChildWindows` present, children included by default), `SCScreenshotManager.captureScreenshot` (26.0), `captureImage` (14.0), `captureImage(in:)` (15.2), `SCContentFilter.initWithDesktopIndependentWindow` (12.3), `pointPixelScale`, `contentRect` (14.0); Vision Swift API (`RecognizeTextRequest`, `DetectRectanglesRequest`, `GenerateImageFeaturePrintRequest`, `RecognizeDocumentsRequest`) present; CoreGraphics `CGPreflight/RequestPostEventAccess`, `CGEventCreateMouseEvent`, `CGEventPost`, `CGEventKeyboardSetUnicodeString` present. No SDK symbol named `contentScale`/`scaleFactor` (RV-06).
7. **Anchor drift found:** the plan (`plan.md` mtime 06:50) predates `prototype-round1-findings.md` (06:52) inside the very directory the plan cites as its capability evidence. Round-1 falsified the exactness of the plan's capture-size invariant (P3: `includeChildWindows=false` image `792×660 px = 396×330 pt` vs `SCWindow.frame 394×326 pt`; `includeChildWindows=true` `1038×796 px = 519×398 pt` vs union `517×396 pt`, i.e. ~1 pt per side padding) and documented `SCWindow.frame` instability (P6: 3 pt inset vs AppKit frame in unsettled states; P4: exact match when settled) plus `sourceRect` window-local semantics (P5). The plan's §1/§TFM `±0.5 px` INVALID rule therefore contradicts its own evidence base — RV-01.
8. Replay inputs exist but the plan's references are ambiguous between two different `attempt-07` trees (`evidence/20260916-route/attempt-07/` and `evidence/20260921-rev27-save-all/attempt-07/`; the `s11e-*` frames/AX/menu JSON live in the latter) — RV-10.
9. Environment row detail: bare `xcrun --show-sdk-path` resolves to CLT `MacOSX27.0.sdk` while `xcrun --sdk macosx --show-sdk-path` resolves to Xcode `MacOSX26.5.sdk`; both contain every required API. Recommend recording the exact SDK per build (residual).

## ARCHITECTURE_AND_CONTRACTS
- Control flow is sound and directly implements the goal's OBSERVE→LOCATE→ACT→OBSERVE→VERIFY chain with a persistent state machine owning recovery, not conversation continuity.
- Load-bearing contracts: epoch-bound `WindowIdentity` (no reuse across epochs; stale windowID invalidates), one transform component with explicit `W/B/S` (no implicit 2×), identity-vs-geometry separation, fail-closed locators with distinct verdicts, exactly-once irreversible ops with durable intent, hash-chained append-only ledger with observe-only crash resume.
- The Save All risk-class policy is internally coherent (plan §9): class unproven → execute as `IRREVERSIBLE_SIDE_EFFECT` (1 dispatch, no retry, postcondition + tripwire evidence records the empirical class for future runs). This satisfies the goal's "never infer from the label" and "do not retry unknown irreversible dispatch" rules; the alternative reading (retry because it might be pre-side-effect) is correctly rejected.
- Postcondition model is the right shape (direct native-chooser observation, panel-service identity as *one* ingredient + fresh census + SC/CG corroboration; logs/click-return explicitly insufficient), but two precision defects remain: late-sample semantics beyond the ≤8.0 s window (RV-03) and the hard-coded panel-service binding versus the plan's own open question about the macOS 27 chooser AX surface (RV-09).
- Chooser automation contract matches the goal (AX first, keyboard only after fresh identity verification, single confirmation, destination constraints); wording "AXPress … else Return" should explicitly mean "exactly one of the two, chosen before dispatch; no second confirmation on non-effect" (residual).
- Barrier supersession section correctly preserves attempt-07's classification and routes the supersession argument through an independent review before any live dispatch.

## DATA_SECURITY_RELIABILITY
- Data: no writes to the accepted baseline; fresh empty staging run; canonicalization, symlink-escape rejection, ancestor/descendant checks; staging evidence preserved; append-only evidence with per-file SHA-256.
- Security/trust: TCC grants verified read-only (screen recording, post-event, AX); no elevation; no network; destination confined to the approved root; the goal's privacy-sensitive captures stay inside the untracked evidence dir (plan commits locally, never pushes) — recommend stating explicitly that live-run captures/OCR extracts are kept out of any push (residual).
- Reliability: no blind retry of irreversible ops; unknown irreversible dispatch never retried; bounded recovery only for reversible navigation; crash resume observe-only; abort conditions enumerated (identity/transform invariant failure, baseline change, staging precondition failure, no chooser, unexpected writes, unverifiable focus/identity).
- Defect: tripwire abort breadth/attribution (RV-07) — with concurrent agent activity documented in the plan's own probe metadata and RISKS #3.

## IMPLEMENTATION_SEQUENCE
Waves are correctly ordered and dependency-aware. Two sequencing obligations created by this review: W1's "capture layer + bbox invariants" must consume the revised bbox rule (RV-01) rather than the current `±0.5 px` text; W4's staging verifier must define the duplicate-content predicate (RV-02) before any acceptance tests are frozen. Both are Plan-level edits, not implementer discretion, because they change CORE validity/acceptance semantics.

## TESTABILITY_AND_ACCEPTANCE
- Unit/integration/replay/adversarial/live layers are observable and evidence-bound (fixture id, expected verdict, fail-closed behavior, evidence path, deterministic reruns). Harness proofs cover capture semantics, Vision localization, transforms, Quartz routing to the topmost popup, postcondition detection incl. fake look-alike refusal, AX chooser observation, and focus theft.
- Independent acceptance (Stage 05) is correctly specified as verifying CORE first, modifying nothing, and recomputing rather than trusting Stage-04 claims. For safety, make explicit that Stage 05 recomputes the baseline delta and the staging content comparison itself (see RV-02 for the predicate it must use).
- E2E semantics are honest: `E2E_REQUIRED: YES` but strictly conditional; if gates fail the task ends in a scoped terminal state without erasing proven implementation facts — consistent with the harness v4.2 contract (orthogonal subjects, scoped blockers, no naked `BLOCKED`).
- Status-contract review (v4.2 §7): CORE vs closure gating is not conflated; repository-health items are not promoted to CORE; no self-waiver; Stage 04→05 routing leaves the legal CORE/verification/acceptance states covered (`NOT_RUN`/`BLOCKED`/`FAIL` → pending/fix/replan routing); no deployment/mixed-version concern applies. The plan does not enumerate the §7.3 verification-item schema or the §7.5/§7.6 fixture set for the material gates; that is acceptable at plan level because the canonical contract governs Stage 04/05 reports, but the frozen implementation/review set should declare `GOAL_CRITICALITY / EVIDENCE_ROLE / CLOSURE_GATE / BASELINE_REQUIRED / FAILURE_CLASSIFICATION_RULE / WAIVER_*` for the gates and carry the v4.2 fixtures (including baseline-unavailable/baseline-delta, hard-clean debt, waiver with original result preserved, contradictory-state rejection) in the acceptance plan (residual recommendation, not a blocker).

## SCOPE_AND_COMPLEXITY
Scope equals the goal's deliverables plus the three cheap adversarial extras; nothing outside the named album/group is touched; no cleanup, no push, no pycache. The added complexity is the minimum that can satisfy the goal's evidence and safety obligations. The two significant risks found are not scope creep but contract precision (RV-01, RV-02).

## FINDINGS

`RV-01` | **MAJOR** | `GROUNDING`, `SEMANTIC_CONTRACT` | Affected: plan §1 Sensor plane (line 177), §COORDINATE_TRANSFORM_MODEL invariants 1–2 (lines 273–274), harness proof #1 (line 287), W1 (line 348), capability evidence matrix (lines 92–118).
- Evidence: plan requires `image.px == bbox.size × scale (±0.5 px)` or INVALID, and `B == W` when `includeChildWindows=false`. The Planner's own newer probe evidence in the same cited directory (`prototype-round1-findings.md` P3/P4/P5/P6, `childunion.swift`, `markers2.swift`, `sourcerect2.swift`) shows: `includeChildWindows=false` image `792×660 px = 396×330 pt` vs `SCWindow.frame 394×326 pt` (≈+1 pt/side horizontally, +2 pt/side vertically); `includeChildWindows=true` image `519×398 pt` vs union `517×396 pt`; `SCWindow.frame` 3 pt inset vs AppKit frame in unsettled states; exact equality only in the settled state. The plan lists "exact meaning of SCWindow.frame vs captured image bounds" as an *open* question while simultaneously freezing an exact-equality rule — a self-contradiction inside a CORE gate.
- Mechanism: implementer follows the plan → every padded/unsettled capture is declared INVALID → fail-closed abort on a perfectly good frame → single-shot live budget lost; or implementer "fixes" the tolerance ad hoc → unreviewed change to a CORE safety/validation invariant (forbidden semantic change). It also breaks harness proof #3's wrong-scale detection design if the tolerance is loosened without a replacement rule.
- Smallest required correction: revise §1/§TFM to a validated-tolerance rule grounded in round-1 evidence — expected image = `union(SCWindow frames) × pointPixelScale` plus a recorded, bounded padding (per-side delta ≤ ~2 pt on this machine), with the actual image origin/offset recorded per capture and used by the transform; fail-closed only on unexplained deltas; require the W2 harness to resolve and freeze the exact per-state rule before the frozen-SHA reviews; keep wrong-scale detection anchored to an independent scale source (`NSScreen.backingScaleFactor` / `pointPixelScale`) plus the bounded size check. Update the capability matrix to cite round-1 findings.

`RV-02` | **MAJOR** | `SEMANTIC_CONTRACT`, `TEST` | Affected: plan §12 Filesystem completion (lines 252–253), R17 (line 58), CRITICAL_PATH step 7 (line 76), success condition (line 383).
- Evidence: the verdict rule is "Compare multiset vs the accepted baseline (57 files / 17,924,900 bytes / digest `b7debe92…`). Equal multiset → `DUPLICATE_CONTENT_CONFIRMED`". The cited digest is the *name-inclusive* manifest digest (sorted `relpath\tsize\tsha256`, verified). Baseline filenames embed a per-download date token (`LINE_ALBUM_20240513～0517_260907_*.jpg`, from the 2026-09-07 run). A fresh LINE download carries a new token, so a relpath-inclusive comparison can never match on a fresh run, while a content-only multiset comparison is the semantically correct equality the goal intends ("content identity"). The plan never defines the exact predicate (what is compared, ordering, duplicates, partial overlap) nor the baseline-side content-multiset reference value.
- Mechanism: literal name-inclusive comparison → false negative (`INDETERMINATE_*`) despite a genuinely identical backup, on the one authorized run; or implementer/verifier improvises a content comparator → unreviewed acceptance semantics deciding the final verdict.
- Smallest required correction: state the exact predicate: exactly 57 files, no subdirs/partials/zero-byte, all decodable, stable; total bytes 17,924,900; **sorted SHA-256 content multiset equality** (57 hashes) with filenames excluded; and record the baseline content-multiset digest computed in this task as the reference (keeping `b7debe92…` as the baseline identity/manifest artifact only). Define mismatch reporting (e.g. 56/57 → scoped mismatch, not success; duplicates inside staging → mismatch) and require Stage 05 to recompute both sides.

`RV-03` | MINOR | `SEMANTIC_CONTRACT` | Affected: plan §10 Save All postcondition (line 233).
- Evidence: "total ≤8.0 s, plus one late sample at ~12 s for the ledger only"; "No affirmative within the window → `NO_CHOOSER_OBSERVED` → scoped indeterminate; no retry". If the late sample affirmatively shows the chooser (or a direct filesystem write), the terminal name `NO_CHOOSER_OBSERVED` becomes misleading/contradictory, and there is no evidence-based rationale for 8.0 s (historical chooser latency was never recorded; plan §RISKS #4 accepts the miss case).
- Mechanism: evidence/state contradiction in the one run's terminal artifacts; unnecessary false-indeterminate risk on a single-shot budget.
- Smallest required correction: define late-affirmative handling explicitly (e.g. terminal `CHOOSER_OBSERVED_AFTER_WINDOW` — indeterminate with cause, zero further input, disclosed in the final report) and either justify the 8.0 s bound with evidence or predeclare a bounded extension (same cadence cap, hard cap ≤15 s) that still forbids retry.

`RV-04` | MINOR | `SEMANTIC_CONTRACT` | Affected: plan §LIVE_RUN_ENVELOPE (line 340) vs §9 (lines 223–230).
- Evidence: "ellipsis open = 1 (recovery ≤3 within policy)" is self-contradictory; §9 allows ≤3 attempts per identical reversible blocker, and the goal permits reversible navigation to follow its reviewed recovery budget. Save All dispatch/confirmation = 1 is correct and coherent.
- Mechanism: implementer aborts a recoverable miss (wasting the budget) or treats the counter as ≤3 and later evidence looks like an envelope violation.
- Smallest required correction: unify the counters: Save All dispatch = 1; chooser confirmation = 1; destination = 1; irreversible ops = 1; no irreversible retry; ellipsis/menu re-open ≤3 under the reviewed `REVERSIBLE_NAVIGATION` recovery policy (and state the total-navigation ceiling).

`RV-05` | MINOR | `TEST`, `SCOPE` | Affected: R20 (line 61), CRITICAL_PATH step 4 (line 73) vs §ADVERSARIAL_TEST_MATRIX (line 306).
- Evidence: the goal list has 22 named scenarios; the plan says "23 named scenarios"; the plan's own matrix enumerates 25 (22 + 3 extras). All 22 goal scenarios are covered (mapped 1:1), so this is documentation precision, not a coverage gap.
- Smallest required correction: renumber to the goal's 22 with stable scenario IDs + explicitly labeled extras, and point each at its fixture/expected verdict (as §ADVERSARIAL line 308 already requires).

`RV-06` | MINOR | `GROUNDING` | Affected: §ENVIRONMENT_AND_CAPABILITY_MATRIX (lines 92–118).
- Evidence: matrix cites only round-0 probe outputs; round-1 findings (child union padding, `SCWindow.frame` instability, `sourceRect` semantics, popup-as-separate-window) are absent. The goal's capability survey also names `contentScale`/`scaleFactor`; neither exists in the SDK headers (the scale property is `pointPixelScale`), and the plan neither records nor explains this.
- Mechanism: understated capability matrix → W1/W2 rediscover known facts (time) or assume wrong semantics (RV-01).
- Smallest required correction: fold round-1 findings into the matrix and add one line recording the survey outcome for the goal-named properties (`contentScale`, `scaleFactor` → not present; `pointPixelScale` used; `NSScreen.backingScaleFactor` for display).

`RV-07` | MINOR | `SECURITY`, `RELIABILITY` | Affected: §10 tripwire (line 237), §LIVE_RUN_ENVELOPE abort (line ~341).
- Evidence: "any creation/modification under the staging run dir and under `~/Downloads`" + abort on "any unexpected filesystem write outside staging", with no attribution rule; the plan's own probe metadata and RISKS #3 document concurrent agent/session activity on this machine.
- Mechanism: an unrelated process writing under `~/Downloads` during the 8–12 s post-dispatch window aborts the run for a non-side-effect, consuming the single authorization; or implementers narrow the rule ad hoc (unreviewed semantics).
- Smallest required correction: define "unexpected" by attribution (own actions / target app / panel-service flow vs unrelated machine activity), keep writes inside the fresh staging dir as the primary chooser-effect signal, keep fail-closed for unattributable writes under the approved roots, record attribution evidence, and disclose concurrent-activity caveats in the ledger.

`RV-08` | MINOR | `TEST` | Affected: §SYNTHETIC_HARNESS_CALIBRATION_PLAN (line 284), §ADVERSARIAL_TEST_MATRIX (line 306).
- Evidence: the occluder is "a second NSWindow (occluder) fully over the main window" (same process), which is weaker than the goal's "occluding app" scenario at the OS level; "process restart (state machine resume in observe-only mode)" has no fixture/mechanism (who kills the engine, at which point, and what asserts no re-dispatch).
- Smallest required correction: add a separate-process occluder (helper app or real third-party window) for capture-survival and event-routing proofs; specify the engine-restart fixture (ctl crash-injection at scripted points → resume from persisted `state.json` in observe-only mode; assert ledger continuity and zero irreversible re-dispatch).

`RV-09` | MINOR | `SEMANTIC_CONTRACT` | Affected: §10 affirmative-evidence rule (line 239), §11 chooser identity-first rule (line 242), open questions (line 117).
- Evidence: affirmation requires "AX sheet/window bound to the panel-service process whose identity is established by the fresh census + timing", while the plan simultaneously lists the macOS 27 chooser AX surface as an open question. If macOS 27 hosts the chooser differently (in-process or a different service), the frozen predicate can produce a false `NO_CHOOSER_OBSERVED`, or the implementer loosens it ad hoc.
- Smallest required correction: state the affirmation as properties resolved-and-frozen in W2 (native open/save-panel semantics: role/subrole/buttons/path affordance; unambiguous ownership/identity; fresh observation window; SC/CG corroboration), explicitly allowing the harness-proven hosting shape, and freeze the exact predicate before the seven reviews.

`RV-10` | MINOR | `GROUNDING`, `HANDOFF` | Affected: §HISTORICAL_REPLAY_PLAN (lines 295–303), §RECONCILIATION_BARRIER_SUPERSESSION references.
- Evidence: "attempt-07 frames/AX JSON … attempt-07 menu evidence" is ambiguous between `evidence/20260916-route/attempt-07/` and `evidence/20260921-rev27-save-all/attempt-07/` (the `s11e-*` frame/AX/menu JSON live in the latter; `attempt-12/13/14/16` frames live in `evidence/20260916-route/`).
- Mechanism: replay fixtures misidentified or a "missing" fixture declared when it exists in the other tree.
- Smallest required correction: enumerate replay fixtures with full relative paths + expected SHA-256 (the plan already demands SHA-bound fixtures elsewhere).

## REQUIRED_PLAN_CHANGES
1. `RV-01` (blocking): revise the capture-size/bbox and coordinate-transform invariants to incorporate the round-1 probe evidence and the recorded padding/offset rule; cite `prototype-round1-findings.md`; require W2 to resolve/freeze the exact per-state rule before the frozen-SHA reviews; keep fail-closed on unexplained deltas; preserve wrong-scale detection through an independent scale check.
2. `RV-02` (blocking): define the exact `DUPLICATE_CONTENT_CONFIRMED` predicate (57 files, no subdirs/partials/zero-byte, decodable, stable; total bytes 17,924,900; sorted SHA-256 content-multiset equality, filenames excluded) and record the baseline content-multiset reference digest in the plan; state that `b7debe92…` is the baseline identity/manifest artifact only; define mismatch reporting.
3. Incorporate the same-revision clarifications `RV-03`–`RV-10` (late-sample terminal semantics + window rationale; envelope counters; scenario IDs/count; capability-matrix update incl. `contentScale`/`scaleFactor`; tripwire attribution; separate-process occluder + restart fixture; chooser-affirmation predicate; replay fixture paths).
4. After revision: `PLAN_REVISION` increments; re-review binds to the new revision/hash. No gate may be weakened in the revision (no tolerance without a recorded rule, no loosened identity/confirm semantics, no retry of irreversible ops).

## RESIDUAL_MINOR_NOTES
- Record the exact SDK used per build (bare `xcrun --show-sdk-path` → CLT `MacOSX27.0.sdk`; `xcrun --sdk macosx` → Xcode `MacOSX26.5.sdk`); both contain every required API.
- Clarify §11 wording "AXPress … else Return" as "exactly one confirmation action chosen before dispatch; a non-effect consumes the budget; never both".
- Plan says the run's evidence records Save All's empirical class; ensure the ledger names that classification record explicitly (so a future bounded-recovery decision has a reviewed basis).
- Consider a one-line statement that live-run captures/OCR extracts remain in the untracked evidence dir and are never pushed.
- The plan's `E2E_REQUIRED: YES` is conditional by design; ensure the final report uses the harness v4.2 subjects and scoped blockers (no naked `BLOCKED`, no `DONE` from synthetic evidence).
- `PRE_SIDE_EFFECT_ACTION` may remain unused this round; its definition is still required by the goal and should stay in the reviewed set.

FINAL_STATUS: PLAN_REVISION_REQUIRED
NEXT_ACTION: Stage 01 revision mode on `T20260925-0647-01-rev28-native-closed-loop` (PLAN_REVISION 2) — apply the blocking RV-01/RV-02 corrections plus the RV-03–RV-10 clarifications, then re-review the new revision/hash; do not proceed to Handoff.
