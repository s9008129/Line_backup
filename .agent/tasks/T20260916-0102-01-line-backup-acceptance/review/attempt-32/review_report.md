# Plan Review Report

## REVIEW_METADATA
- TASK_ID: T20260916-0102-01-line-backup-acceptance
- REVIEW_ATTEMPT: 32 (fresh independent Stage-02 review; single write target = `review/attempt-32/review_report.md`; `review/attempt-30/` and `review/attempt-31/` are prior reviewers' directories, read only)
- REVIEWED_PLAN_REVISION: 22
- REVIEWED_PLAN_SHA256: `426fe6a1c758e96667e970021a80e20b68396c5af6a4ccaf65672772c514734b` (2,041 lines / 278,141 bytes; recomputed from `plan.md` before the full read and re-verified byte-for-byte after writing this report; identical to the task-provided hash, so this review proceeded).
- SNAPSHOT / MINIMALITY BASELINE: `review/attempt-31/plan_snapshot.md` (`466bda4ad79897cf5f6395beafc0a70c57d99ed4dc15f4b78328fb5c69b68190`, the byte snapshot of Rev21) read only, used as the diff baseline. `diff` shows the Rev22 delta confined to header lines 4, 11–14, 21–22 and the new `## Revision 22 changes` block (new lines 25–188, i.e. §22.1–§22.9; one pre-existing blank line reused at new line 103); every other line is byte-identical, which independently confirms §22.9's "Unchanged by this revision" list.
- REPOSITORY ANCHOR: branch `master`, HEAD `a68f890` (the Rev22 commit), worktree clean apart from the pre-existing untracked `evidence/20260917-freeze-set/` and `evidence/20260917-owner-briefing/` (neither touched).
- REVIEWER RUNTIME: fresh Codex CLI Stage-02 session; model identity not exposed to this session (informational only).
- READ-ONLY HONORED: no GUI input of any kind, no network use, no product code / tool / evidence / `plan.md` mutation, no commit. All reads were shell/file reads; the only write anywhere is this report.
- AUTHORITATIVE GOAL SOURCES (baseline reconstructed from these only): session transcript `/Users/hsiaojohnny/.codex/sessions/2026/09/17/rollout-2026-09-17T19-59-26-01a0af3c-6693-72b2-abf2-095a3317e9a4.jsonl` — line 1034 (`response_item`, role `user`, the owner's request, verbatim `請深度梳理上下文後，用白話、一般人或非技術人員可以理解的方式告訴我，為什麼這一次做的測試失敗了，以及它的瓶頸是什麼。`), line 1048 (`response_item`, role `assistant`, the option presentation carrying the reply example, re-read this review), line 1063 (`response_item`, role `user`, `$.payload.content[0].text` = `1 選 B；2 用更正版紀錄`); and `evidence/20260917-owner-decisions/owner-decisions-rev22.json` (tracked; SHA-256 `9d6b72e09f13e70418885b14269f6c04c89515d7b11abda1416f0684dc6cfd64`). The Plan's own summary of the goal was not accepted as the baseline.
- PRIOR GATES (hashes re-verified this review): `review/attempt-30/review_report.md` `92d5be16aa21ce0a07b6dbe0210ab00cf6d995198ecb59599146ed59d8284729` and `review/attempt-31/review_report.md` `c80a14da9a6449226052230c8c7a635e8b2eb997ed4cf67dfb0dda22f6ab0aa5`; both approve Rev21 at `466bda4a…` only. No approval exists for Rev22 before this review.

## OWNER_VERDICT
（白話，給非技術主持人）您回答的「1 選 B；2 用更正版紀錄」被照著 1:1 做，沒有多做、也沒有少做。路線 B ＝ **開一份新修訂（Rev22）＋一道全新的第四號一次性授權**，把上次停住的「先點開目標相簿 → 再看一眼相簿內的 ⋮」重新走一次；但這次「判斷畫面對不對」的眼睛換成 macOS 內建 Vision 讀取器（它已在同一張冷凍畫面上讀出「57張照片」、信心 1.00，而舊引擎讀成「75」）。**不點任何選單項目、不用鍵盤、不碰 chooser、不下載、不改您的 57 張照片與任何正式資料**；兩個動作各只有一次、零重試。紀錄方面＝採用先前已備妥的「更正版紀錄」（v1.1），原檔一字不改；我用程式獨立重跑比對：舊版不合格、更正版合格。若執行時畫面不在「相簿列表」，這次**不會送出任何輸入**，只記錄後請您恢復畫面再續；跑完後若有明確結果且紀錄合格，才照原本 §20.3 已寫好的規則結案；若不是，照實記錄、立刻回報，**不會自行宣布「這條路不需要」**（那是您保留的決定）。真正會擋結案的只有 5 個 CORE（紀錄合格、兩次輸入各只有一次、S5 必須用 v4 讀取器、明確結果必須是機器可觀察、舊證據不可被動）；螢幕探測是支撐性、永不擋。審查結論：找到 5 條不影響安全、授權範圍或驗收的 MINOR（見 FINDINGS），不需要再改計畫就能交付 Handoff；本次審查只核准 Rev22 這個版本與這個雜湊值。

## GOAL_BASELINE
Independent reconstruction from the authoritative sources above, written before adopting the Plan's framing:
1. PRIMARY outcome (task-level, result ①, unchanged): safely establish whether the existing 57-image destination is a valid backup of the exact LINE source group `旻謙允禎成長日記`, album `2024/05/13～05/17`, and otherwise stop without any ambiguous or duplicate production transaction; result ② (reusable automation path proven through the real entry) was closed for the Rev21 wave by `e2e/attempt-06`. [VERIFIED]
2. State that produced the question: after `e2e/attempt-06`, result ① still had exactly two scoped CORE blockers — `SOURCE_CORRESPONDENCE` (BLK-01: the v1 record's `answer.part_2.confirmed_album` fails the §16.4 exact-equality on one leaf) and `CUA_ROUTE_DECISION` (BLK-02: attempt-05 stopped at S5 with `TARGET_MISMATCH`, album-card input spent 1/1, ⋮ input unspent, owner decision required). [VERIFIED: `e2e/attempt-06/e2e_report.md` `14b44c94…`; `result.md`; attempt-05 ledger `17b17203…`]
3. Owner's words: line 1034 asks for a plain-language account of why the test failed and what the bottleneck is (a request for explanation, not itself an authorization). Line 1048 presents the two questions — 路線 **A** 記成「這條路不需要」關閉卡點 ／ **B** 開新修訂＋再授權一次「⋮ 只觀察」（不點任何選單項目；新修訂會把驗證改用新 Vision 讀取器），建議 B；來源紀錄 **(i)** 用您先前已同意的「更正版紀錄」 ／ **(ii)** 您重新給一次精確答案，建議 (i) — with the reply example 「1 選 B；2 用更正版紀錄」. Line 1063 is the sole real user answer: `1 選 B；2 用更正版紀錄`. [VERIFIED]
4. Governing intent: branch (b) of §20.3 — one further corrected route attempt under a NEW `PLAN_REVISION`, a new one-shot gate and a fresh review, with S3/S5/S6 verification bound to the v4 Vision reader (§21.6's obligation) — plus adoption of the corrected versioned record v1.1 as BLK-01's remedy; the corrected route's target remains the album-level ⋮ *inside* the target album, whose directed precondition is opening the album card (owner's earlier correction, transcript lines 6206/6224, still governing and quoted in `gate-3-authorization.json` `05db1678…`). [VERIFIED]
5. Must-not-break: no menu-item activation (Save All named), no chooser, no keyboard/AX write, no download; the 57 files and all formal config/state/registry/run-log read-only; at-most-once inputs, zero retry; `禎` U+798E and `楨` U+6968 never merged/normalized; zero conversation images (paths + SHA-256 only); no `ROUTE_NOT_NEEDED` (owner chose B; that closure stays owner-reserved). [VERIFIED from line 1048's option text + §20.1 hard boundaries + §21.1's owner-reserved route note]
6. Success/failure semantics: one corrected attempt-06; `AFFIRMATIVE` (machine-observable at the album level) **and** a passing §16.4 check over v1.1 → the §20.3 closure tuple; any non-`AFFIRMATIVE` → honest record with zero side effects, route stays a scoped CORE blocker, owner notified; nothing auto-runs. Bounded autonomy: ≤5 read-only observation windows, immediate notify on stop, no long loops. [VERIFIED from §20.3 + line 1048]

## PASS A — TOP-DOWN (goal alignment, necessity, priority, gate proportionality, containment, coupling, design economy)

### GOAL_ALIGNMENT
- Every Rev22 change traces to the owner's two decisions or to a §21.6 obligation: the new revision + gate-4 + fresh review (§20.3(b)); S3/S5/S6 bound to v4 (§21.6 line 283: "the new revision must bind its S5 verification to the v4 reader and carry a new one-shot gate"); v1.1 adoption (decision 2 = option (i)); `ROUTE_NOT_NEEDED` explicitly excluded (owner chose B). [VERIFIED: plan lines 4–22, 26–188; diff vs Rev21 snapshot]
- The wave deliberately does **not** attempt to advance the album-data result beyond what was authorized: §22.5's closure tuple is §20.3's exact tuple and the disclosed axis facts (Registry=FAIL, State=`LEGACY_PROVENANCE_LIMITED`, product-level Source=`UNRESOLVED`) are reported, never promoted. [VERIFIED: §22.5 vs §20.3 lines 414–419]
- No goal drift found: the diff proves nothing else in the plan changed, so no previously-frozen CORE/scope decision was silently rewritten. [VERIFIED: `diff review/attempt-31/plan_snapshot.md plan.md` = 5 hunks, all header + new §22]

### NECESSITY_AND_TRACEABILITY
- `REQ-V22-1` (v1.1 → `SOURCE_CORRESPONDENCE=CONFIRMED` re-derived by Stage 05) is necessary: it is BLK-01's only owner-chosen remedy; the alternative (a fresh precise answer, option (ii)) was not chosen. [VERIFIED]
- `REQ-V22-2` (one corrected route attempt under the v4 reader) is necessary: the route is one of the two remaining CORE blockers, and `e2e/attempt-06` explicitly required "a new plan revision + review + a new gate" for any option-B continuation (its ROUTING_DECISION and RESIDUAL_RISK sections). [VERIFIED]
- `NFR-V22-1..3` (frozen prior bytes; at-most-once inputs; read-only destination/formal data + no normalization) are the safety envelope the owner's authorization rests on (line 1048: 「不點任何選單項目」; the standing append-only and read-only rules). `NFR-V22-4` (screen probe) is correctly SUPPORTING/no global veto because attempt-05 already proved the run continues with `screen_scope=UNAVAILABLE`. [VERIFIED: attempt-05 ledger `resume.screen_scope`; §20.2 S2; §22.6 row]
- No CORE item exists without a traceable owner/contract source, and no supporting item was promoted to a gate. [VERIFIED]

### GATE_AND_VETO_AUDIT
- The only gates in this wave are HARD_CLEAN CORE checks plus one NON_GATING supporting check; §22.5 states "No supporting item may block the wave (base L117 rule); conversely no supporting pass may mask a CORE failure" — proportionate. [VERIFIED: §22.5, §22.6]
- Gate-4 is explicitly **not** a reset/extension of gate-1/2/3: §22.2 "It is not a reset and not an extension of gate-3 or of attempt-05's spent budget: attempt-05's single navigation input stays spent and its frozen verdict stands"; the fresh ledger's parent is attempt-05's final ledger `17b17203…`. [VERIFIED]
- Global veto proportionality: nothing outside the six rows can block; the route itself remains a scoped CORE blocker (subject-scoped, not task-wide) until a machine-observable result exists. No self-waiver exists anywhere (§22.6 waiver NO / authority NONE on all rows). [VERIFIED]

### COUPLING_AND_FAILURE_CONTAINMENT
- Coupling is minimal: the wave touches no product code, command, fixture or status vocabulary; it reuses the frozen detector/v2 locator byte-identically, binds the v4 tool set by SHA, and parents the new ledger to attempt-05's. Failure of the GUI attempt cannot corrupt product/data state because the write surface is empty (no state/config/run-log/destination writes; no download). [VERIFIED: §22.3; diff; attempt-05 no-side-effects record]
- Failure containment is fail-closed at every step: preflight surface failure → zero inputs, `NO_TARGET_ON_FRAME`, resume-eligible; S3/S5/S6 refusals stop before the next input; v4 reader refusal outcomes (`binary_missing`/`build_failed`/`nonzero_exit`/`unparsable`/`timeout`) stop the run; a stop after input #1 never re-authorizes input #2. [VERIFIED: §22.3 preconditions/steps/resume rule]
- Worst realistic live outcome (Q7 below): an opened album and an opened album-level menu on the owner's screen — the same class the owner authorized — with zero menu-item activation and zero data side effects. No credible path to a production Save-All or a destination mutation exists in this design. [VERIFIED]
- Residual coupling risk: the GUI run depends on the live surface/OS state (window binding, permission prompts). This is handled by stop-and-notify + resume, not by retry. [VERIFIED]

### DESIGN_ECONOMY
- Deletion test: removing gate-4, the v4 S3/S5/S6 substitutions, the v1.1 adoption, or any of the five CORE rows removes either the owner's authorization basis or a required closure fact; nothing else is built. No new machinery beyond the one required attempt. [VERIFIED]
- The plan reuses the established gate/runbook/ledger pattern (gate-3 → gate-4; v3 runbook → v4 runbook; same budgets/stop rules) instead of inventing a new workflow — consistent with the repository's own precedent. [VERIFIED]
- The one unavoidable semantic addition (a second GUI subject for the album-data result) is carried in the header/ACCEPTANCE_MODE and §22.5, i.e. exactly where a Stage-03/05 reader will look. [VERIFIED]

### CRITICAL_PATH_AND_PRIORITY
- Rev22 is a strict subset of the shortest path to closing result ①'s two remaining blockers: (1) adopt v1.1 so the §16.4 check can pass; (2) run the single corrected attempt-06 so `CUA_ROUTE_DECISION` can resolve. No deferred work (production Save-All, bridge, migration) was pulled forward. [VERIFIED]
- `e2e/attempt-06` (Rev21 wave) already consumed the wave acceptance number; Rev22 correctly assigns its own independent acceptance to `e2e/attempt-07` and forbids GUI input in that acceptance (§22 header + §22.5). This matches §21.6's "a later route-closure acceptance takes the number after it". [VERIFIED]

## PASS B — BOTTOM-UP (grounding, contracts, safety, sequencing, verification)

### REQUIREMENT_FIDELITY
- §22.3's substitutions are exactly the reader-layer change the v4 README documents ("Scales are unchanged from v3: 3x for frame reads, 10x for the count region"; same verdicts/exit codes + one `reader` field) — no gating rule, threshold or verdict was loosened. [VERIFIED: `tools/v4/README.md`; §21.3/§21.4; v4 tool SHAs re-verified byte-identical: `vision_reader.py` `22a4e9ef…`, `locate_album_card.py` `bb52aff1…`, `verify_album_open.py` `ffa82aed…`, `locate_album_ellipsis.py` `b77e3d51…`; helper source `4fc9fa2b…`; v4 self-test `5ad2be10…` (16 cases, `cases_failed 0`, PASS); v3 self-test `17840e91…` (13/13); detector `6ae9c250…` + frozen detector self-test `d8ffc129…`)]
- The v1.1 adoption is the exact remedy §16.4 sanctions ("a corrected re-authoring of the same preserved answer under [Rev19 §19.1's] single-leaf rule — is a new versioned file"); matching rules themselves are unchanged. [VERIFIED: §16.4 lines 934–937; §22.4]
- All six §22.6 rows carry the full Status Contract v2 field set with waiver NO / authority NONE. [VERIFIED]

### GROUNDING_AND_DRIFT
- All plan-cited hashes re-verified byte-identical by this reviewer: v4 tool set, helper source, both self-test summaries, detector + its self-test, attempt-05 ledger `17b17203…`, `album-open-verify.json` `ffa5d963…` (verdict `TARGET_MISMATCH`, count read `75`, changed_fraction `0.646749`), `vision-ocr-crosscheck.json` `d3ebbaed…` (Vision reads `57張照片` conf 1.00 on the frozen post frame `4cb8a6b4…`; frozen tesseract `75`; probe crops read `27`/`5`), gate-3 `05db1678…`, attempt-05 runbook `b78f1c91…`, `e2e/attempt-06/e2e_report.md` `14b44c94…`, v1/v1.1 records `2cd7eccd…`/`a8c10551…`, gate-answer `03ffff57…`. [VERIFIED]
- The crosscheck is correctly used as supplementary non-authoritative evidence; the plan does not let it substitute for the frozen chain (§22.7 tells the owner plainly it is the basis for the reader swap, and §22.3 keeps attempt-05's verdict frozen). [VERIFIED]
- Two grounded drifts found (both MINOR, see FINDINGS): the owner-decisions artifact's "did not spend" wording (RV-32-1) and the §22.6 rows not being physically inside the canonical matrix (RV-32-2), plus carried residuals (RV-32-4, RV-32-5).

### ARCHITECTURE_AND_CONTRACTS
- No architecture or contract change: no product enum, no persistence field, no status vocabulary, no routing fixture; the only contract touched is the §16.4 record *instance* (v1.1), under the contract's own versioned-file allowance. [VERIFIED: diff; §16.4; §22.4]
- `user_fact_v1_matches` (src/line_backup_acceptance/common.py:533) remains the single oracle; §22.4 cites it and this reviewer re-ran it (raw result in Q3). [VERIFIED]

### DATA_SECURITY_RELIABILITY
- Data safety: destination and all formal state read-only; no re-download; no file writes of any kind; frames stay in /tmp with bytes/size/SHA-256 recorded only; zero conversation images. [VERIFIED: §22.3 S11 + NFR-V22-3; attempt-05 precedent]
- Privacy/least privilege: the GUI surface is used for observation only; the ledger enforces `menu_item_budget 0`, `keyboard_input_budget 0`, `app_acquisition_input_budget 0`, `conversation_images 0`, `retry_budget 0`, `click_count_per_input 1`. [VERIFIED: §22.3 precondition 3]
- Reliability: append-only evidence; attempt-05 artifacts never overwritten/re-scored; deterministic tool chain (v4 self-test incl. 5× byte-identical determinism); fail-closed refusal paths stop the run. [VERIFIED]

### IMPLEMENTATION_SEQUENCE
- Stage 03 must compile a fresh handoff bound to TASK_ID + Rev22 + the approved SHA carrying: the two decisions; the two at-most-once inputs + budgets; the freeze set (gate-4, v4 runbook, v4 tools/helper/self-test SHAs); the S3/S5/S6 substitutions; the resume rule; the §22.5 closure tuple; `e2e/attempt-07` as the acceptance; and the owner-reserved route closure. The ordering (freeze gate-4 + runbook + ledger before any input, then the run, then execution-rev22.md, then independent acceptance) is executable and CORE-first. [VERIFIED: §22.3; §20.2 pattern]
- The only sequencing wrinkle is the stale §Closure literals still saying "Revision 21 (21 at this writing)" (RV-32-3) — documentary; the header and §22 sections are unambiguous. [VERIFIED: plan lines 2027/2029]
- `e2e/attempt-07` does not exist yet and `evidence/20260916-route/attempt-06/` does not exist yet — the wave has not pre-run anything. [VERIFIED: directory listings]

### TESTABILITY_AND_ACCEPTANCE
- Every CORE claim has an observable check with raw evidence: v1.1 matcher True (Q3), one-shot budget counters in a fresh ledger, S5 verdict from the frozen v4 verifier with the required `reader` block, `AFFIRMATIVE` only via machine-observable menu evidence, and byte-immutability of prior evidence. [VERIFIED: §22.6 vs §22.3]
- Acceptance mode is truthfully INTEGRATION for both subjects; the GUI attempt is a bounded observation, never reported as production E2E; C4-style demonstration semantics cannot flip frozen verdicts (Rev21 rule carried). [VERIFIED: header ACCEPTANCE_MODE; §21.6; §22.5]
- Residual test-design detail: the stop-before-S11 artifact policy is not restated (RV-32-4) — handoff-level clarification suggested. [VERIFIED]

### SCOPE_AND_COMPLEXITY
- Scope fence: exactly one corrected GUI attempt + one record adoption; no menu item, no chooser, no keyboard, no download, no product change, no fixture change; budgets ≤2 inputs / ≤5 windows / ≤5 captures. Nothing over-broad found. [VERIFIED]
- Complexity earns its keep: the only "new" things are the four literals for this wave (gate-4, runbook, v4 bindings, execution-rev22.md) and they exist because the owner's own option B names a new revision + new gate. [VERIFIED]

## JUDGEMENT_QUESTIONS
Q1 — Is Rev22 a faithful and MINIMAL instantiation of §20.3 branch (b) and §21.6 ("if the owner later chooses B, the new revision must bind its S5 verification to the v4 reader and carry a new one-shot gate")? Does it add anything unauthorized or omit anything required?
Answer: Faithful and minimal. Evidence: §20.3(b) requires "one further corrected attempt under a NEW PLAN_REVISION, a new gate and a fresh review — never inside Rev20" (plan lines 419–420) — Rev22 provides exactly PLAN_REVISION 22 (line 4), gate-4 + a fresh v4 runbook frozen pre-run (§22.3 item 2, `evidence/20260916-route/attempt-06/gate-4-authorization.json`), and this fresh review round; §21.6 requires S5 bound to the v4 reader and a new one-shot gate (line 283) — §22.3 substitutes S3/S5/S6 to v4 (four SHA-pinned v4 files + helper + 16/0 self-test, all re-verified) and carries gate-4 with budgets 1/1/0 (precondition 3). Minimality is proven by the diff against the Rev21 snapshot: only the header (lines 4, 11–14, 21–22) and the new §22 block (new lines 25–188) changed; §22.9's "unchanged" list is literally true. Unauthorized additions: none — OOS-V22-1..4 forbid menu items/chooser/Save All, destination writes/re-download, `ROUTE_NOT_NEEDED`, and any input beyond the two. Required omissions: none — the new revision, new gate, fresh review, S5→v4 binding, §20.3 closure tuple (§22.5), owner notification on non-AFFIRMATIVE, and attempt-05 immutability are all present.
Q2 — Does §22.2's reading of `1 選 B；2 用更正版紀錄` over-reach? Judge the recorded alternative reading and whether the two-input set (album-card click + album-level ⋮) is properly derived from the owner's authority.
Answer: No over-reach. The owner's reply answers the presented option B text verbatim-referenced in the same answer (`1 選 B`), and that option (transcript line 1048) says 「開新修訂＋再授權一次「⋮ 只觀察」（不點任何選單項目；新修訂會把驗證改用新 Vision 讀取器）」. The ⋮ target was already owner-directed to be the album-level ⋮ *inside* the album ("你應該先點進去「2024/0513~0517」這本相簿"; "要進入該相簿裡面才會有「⋮」", lines 6206/6224, recorded in `gate-3-authorization.json` `05db1678…` and §20.1), and under the frozen verification contract an already-open album cannot be verified on a single frame — the v4 album-open verifier consumes `<pre> <post>` and requires changed_fraction ≥5% (v4 README usage line; §22.3 S5 with the frozen `--delta 12`), so a ⋮-only-from-current-view design cannot produce `ALBUM_OPEN_VERIFIED`. The two-input corrected route is therefore the only design consistent both with the owner's corrected target and with the frozen S5 gate; the album-card click is the directed precondition, not an added liberty. The plan also records the alternative reading honestly (§22.2 third bullet: if only ⋮ were meant, the two-input attempt must not be sent) and makes it supersedable by a newer explicit owner correction — and attempt-05's spend history is stated correctly there ("attempt-05's single navigation input stays spent"). One documentary error sits in the evidence artifact, not in the Plan (§22.2/§22.3): RV-32-1.
Q3 — Does §22.4's v1.1 adoption satisfy §16.4 exactly, and does it validly close BLK-01's remedy without rewriting or normalizing anything? Independent re-run of the matcher on both records, raw output.
Answer: Yes. §16.4 (lines 934–937) explicitly allows "a corrected re-authoring of the same preserved answer under [Rev19 §19.1's] single-leaf rule — is a new versioned file"; Rev19 §19.1 (lines 461–503) is that authorization, with the owner reply `1.可以` documented in the authoring note `1fbddc4b…`. This reviewer re-ran the oracle read-only: `python3` importing `line_backup_acceptance.common.user_fact_v1_matches` (src/line_backup_acceptance/common.py:533; file SHA-256 `df9fe0bc…`) with each record's own `app_identifier`/`raw_requested_group`/`fingerprint` produced, verbatim: `source-identity-user-fact.confirmed.v1.json -> False` and `source-identity-user-fact.confirmed.v1.1.json -> True`. Bytes/SHA: v1 1,746 B `2cd7eccd…` (unchanged; mtime Sep 17 09:11), v1.1 1,741 B `a8c10551…`. Leaf audit: 27 leaves in both; exactly one differing leaf `.answer.part_2.confirmed_album` `2024/05/13～2024/05/17` → `2024/05/13～05/17`; U+FF5E appears 3× in both and ASCII `~` 0× in both; the single evidence entry re-hashes to `human-gate-answer-20260917.json` (1,438 B `03ffff57…`). No rewriting, no normalization, no merge of 禎/楨 (the matcher compares the group string byte-for-byte). §22.4 correctly defers closure to Stage 05's fresh re-derivation rather than asserting it, and keeps ANOM-01 non-gating. Valid remedy for BLK-01, subject to that Stage-05 re-derivation.
Q4 — Are §22.3's preconditions, stop rules, budgets and resume rule consistent with §20.2 and with the attempt-05 facts? Is anything unfalsifiable, self-waiving, or over-broad? Is any required artifact (route-result.json, manifest.json, run-ledger final state) missing?
Answer: Consistent. §22.3 precondition 1 mirrors §20.2 precondition 1 (album-list state, target card `2024/05/13～05/17` count 57, no overlay; `NO_TARGET_ON_FRAME`, ≤5 read-only windows, notify, zero inputs). Precondition 2's freeze set mirrors §20.2 precondition 2 one version up (gate-4 + v4 runbook + v4 tools/self-test + reused detector/v2 locator; v3 set frozen and not used for verdicts). Precondition 3's fresh ledger parent `17b17203…` (re-verified: that is exactly attempt-05's final ledger hash) and budgets match §20.2's plus the §22.3 additions (menu 0 / keyboard 0 / click_count 1 / retry 0 / conversation_images 0; ≤5 windows; ≤5 captures ≈4 s — the last two from §20.2 S2/S9). Consistency with attempt-05 facts: ledger `ledger_state` `CLOSED_AFTER_INPUT_1`; `final_counts` navigation 1 / album_card 1 / ellipsis 0; `run_status` `STOPPED_AT_S5_TARGET_MISMATCH`; `route_result_sha256` null; the resume block records the zero-input abort resumed on the same ledger with both budgets unspent, and the terminal `next_action` states the ⋮ authorization remained conditioned on `ALBUM_OPEN_VERIFIED` — §22.3's resume rule ("zero inputs spent → same ledger may be resumed; a run that spends input #1 and then stops does not re-authorize input #2") reproduces exactly that. Falsifiability: every precondition/stop is a frame/AX/ledger observable; S5's verdicts are enumerated with frozen thresholds; the reader `conf` is explicitly never a gate; budgets are counters. Self-waiving: none — all six §22.6 rows are waiver NO/authority NONE, and §22.5's semantic choices reproduce §20.3/the owner's decision rather than waiving anything. Over-broad: no — ≤2 inputs, zero menu/keyboard/download/destination writes. Artifacts: when S11 is reached, the inherited set (route-result.json, FINAL run-ledger with `route_result_sha256`, manifest.json, per-step JSONs, execution-rev22.md) is complete; if the run stops before any route outcome — attempt-05's actual situation, where the ledger documents "No route-result.json was written because no route outcome exists" — the stop is recorded in the ledger with events/final counts/stop reason, and §22.3 does not restate that policy → RV-32-4 (MINOR, handoff-level).
Q5 — Is §22.5's closure tuple exactly §20.3's, with no promoted axis fact and no self-decided ROUTE_NOT_NEEDED?
Answer: Yes. §20.3 (lines 414–419) declares: AFFIRMATIVE + §16.4(v1.1) → `PRIMARY_OUTCOME_STATUS=ACHIEVED`, `IMPLEMENTATION_STATUS=COMPLETE`, `CORE_ACCEPTANCE_STATUS=PASS`, `REQUIRED_VERIFICATION_STATUS=PASS`, `INDEPENDENT_ACCEPTANCE_STATUS=PASS`, `TASK_CLOSURE_STATUS=DONE`, `BASELINE_REGRESSION_DELTA=UNCHANGED`, with the three axis facts (Registry=FAIL, State=`LEGACY_PROVENANCE_LIMITED`, product-level Source=`UNRESOLVED`) reported as axis facts and never promoted. §22.5 reproduces all seven values and all three axis facts verbatim in substance, and defers the re-derivation to Stage 05. Non-AFFIRMATIVE: "recorded honestly with zero side effects; the route stays a scoped CORE blocker; the owner is notified immediately; `ROUTE_NOT_NEEDED` is **not** taken (the owner chose B)" — no self-decided closure, and a preflight surface failure is "not a route result and changes no status; it is a `resume`-eligible stop". No promoted fact or self-decision found.
Q6 — Is any check in §22.6 mis-classified (CORE vs SUPPORTING) or waivable by the agent that runs it?
Answer: No. The five CORE rows each protect a load-bearing fact: the record that closes BLK-01 (`V22_SOURCE_RECORD_V1_1_CONFIRMED`), the at-most-once safety envelope the authorization depends on (`V22_ROUTE_ATTEMPT_06_SINGLE_ONESHOT`), the §21.6 obligation (`V22_S5_BOUND_TO_V4_READER`), the anti-forgery rule for route resolution (`V22_AFFIRMATIVE_MACHINE_OBSERVABLE_ONLY`), and history/data immutability (`V22_PRIOR_EVIDENCE_IMMUTABLE`). The single SUPPORTING row (`V22_SCREEN_SCOPE_PROBE`, NON_GATING) matches NFR-V22-4 and attempt-05's fact that the run continued with `screen_scope=UNAVAILABLE`; per §22.5 it can neither block nor mask. All rows are `Waiver allowed: NO` / `Authority: NONE`, so nothing is waivable by the agent that runs it — including the supporting row, which simply never gates. No misclassification found. (Placement of these rows is a separate documentation point: RV-32-2.)
Q7 — Failure containment: worst realistic outcome on the owner's live machine, and is it within the owner's authorization?
Answer: The worst realistic outcome is: one album-card left click opens the target album (attempt-05 already did exactly this), and one album-level ⋮ left click opens the album-level menu on screen; the menu may be left open and the owner is told to close it (§22.3 S11; no keyboard input is ever sent). If a locator misfires, the consequences are bounded to a single left click at a frame-derived point inside the LINE window (opening an album or a ⋮-like element whose menu is never activated) or no effect, after which the frozen S5/S6 rules stop the run before the next input. Guaranteed-absent side effects: menu-item activation (Save All named), chooser use, keyboard/AX writes, downloads, state/config/run-log writes, destination writes, conversation images (paths + SHA-256 only). The owner-decision artifact itself records the same worst case ("an opened album and an opened album-level menu on the owner's screen — the same class of observation-only side effect the owner authorized for one ⋮ input under gate-3"). Authorization basis: line 1063 `1 選 B` + line 1048's option text (再授權一次「⋮ 只觀察」, 不點任何選單項目) + the corrected-route direction of lines 6206/6224. Within authorization. Residual live risks (window un-bindable, OS permission prompt) are handled fail-closed (zero inputs, notify, resume), not by retry.

## FINDINGS
All findings are MINOR, non-gating; none affects goal alignment, authorization scope, safety, compatibility, rollback or acceptance.

- `RV-32-1` — [MINOR][GROUNDING] `evidence/20260917-owner-decisions/owner-decisions-rev22.json` `decisions.item_1.authorizes[2]` (SHA `9d6b72e0…`) describes the two inputs as ones "that attempt-05 did not spend", but attempt-05 spent input #1: its ledger records `route_outcome.inputs.album_card: "SPENT (1/1)"`, `final_counts.navigation_inputs/album_card_inputs = 1`, `inputs.sent = 1`, and `ledger_state: CLOSED_AFTER_INPUT_1` (`17b17203…`); only the ⋮ input was unspent (`ellipsis: "UNSPENT (0/1)"`). The Plan's own §22.2 states it correctly ("attempt-05's single navigation input stays spent and its frozen verdict stands"), and §22.3's gate-4 is a new one-shot authorization (the established gate-2/gate-3 pattern), so the authorization and budgets are unaffected. Mechanism: the artifact cited as Rev22's authority contains one sentence that contradicts the ledger and the Plan; a Stage-03/05 reader quoting it could misstate the spend history or mistakenly question the "not a reset" claim. Smallest correction: an append-only correcting note (and one sentence in the Stage-03 handoff): gate-4's input #1 is a newly authorized one-shot under the owner's branch-B decision — not a reset or extension; only the ⋮ input was unspent when attempt-05 stopped.
- `RV-32-2` — [MINOR][GROUNDING] The six V22 check rows are specified in §22.6 (plan lines 156–161) but are **not** physically present in the `## Canonical Status Contract v2` matrix table (the Rev21 rows `VISION_READER_TOOLCHAIN_V4` etc. are physically at plan lines 1955–1958), while §22.8's bullet reads "§Canonical Status Contract v2 check matrix: six rows added (§22.6)" — the same phrasing Rev21 §21.8 used for four rows that *were* physically inserted. Mechanism: a Stage-05 session that treats the canonical matrix as the standing checklist would miss these six rows, though §22.6 itself carries the full field set and the wave's semantics reference it. Smallest correction: carry the six rows verbatim in the Stage-03 handoff (or append them to the matrix in a future in-place touch). Documentary only.
- `RV-32-3` — [MINOR][SEQUENCING] The `## Closure and sequencing` section still binds only the Rev21 wave and says "the current PLAN_REVISION at handoff time (21 at this writing)" and "this Revision 21" (plan lines 2027/2029), and the Rev21-vs-Rev22 numbering for the route attempt vs the acceptance attempt is only disambiguated by §22.3/§22.5/§21.6 together. Rev20 §20.4/Rev21 §21.8 corrected such literals in place when revising; Rev22 §22.8 did not update these. Mechanism: a handoff compiler reading only the closure section could bind the wrong revision or omit the Rev22-specific bindings; the header (`PLAN_REVISION: 22`), §22.5 and §22.8 make the correct reading available, so this is documentary. Smallest correction: Stage 03 binds its handoff explicitly to TASK_ID + Rev22 + this approved hash (and lists this wave's bindings), and a future in-place touch updates those literals.
- `RV-32-4` — [MINOR][TEST] §22.3 inherits §20.2 S11 ("stop and write the artifact set") but does not restate attempt-05's documented stop-artifact policy for runs that stop before any route outcome — no `route-result.json`/`manifest.json` is written; the ledger records the stop with events/final counts/stop reason (`17b17203…` `ledger_state_note`: "No route-result.json was written because no route outcome exists before any input"). Mechanism: a Stage-04/05 reader could expect `route-result.json` even on a mid-run stop and debate artifacts instead of recording; the required-artifact set is otherwise complete (FINAL ledger with `route_result_sha256` + manifest + per-step JSONs + `execution-rev22.md` when S11 is reached). Smallest correction: one Stage-03 handoff sentence pinning the policy for both stop shapes.
- `RV-32-5` — [MINOR][GROUNDING] (carried, still uncorrected) Header line 23 `PHASE_0_BASELINE_COMMITTED` states "handoff H3.0 §3 anchors re-computed 15/15 match", but the committed `evidence/20260917-vision-reader/phase0/baseline.json` `handoff_sha_table` holds 16 rows of which 14 carry a recorded SHA-256 and 2 carry `recorded_sha256: null`, with `handoff_sha_mismatches: []` (re-verified this review). Mechanism: an unreconcilable count in otherwise-solid baseline evidence; Stage 05 could re-derive a different number. Smallest correction: read "14/14 recorded SHA-256 anchors (0 mismatches; 2 informational rows without SHA)" in a future in-place touch or in the Stage-03 handoff note.

## REQUIRED_PLAN_CHANGES
None. No BLOCKER or MAJOR finding exists: the revision is a faithful, minimal instantiation of §20.3(b) and §21.6; the v1.1 adoption satisfies §16.4 exactly; §22.3 is consistent with §20.2 and attempt-05's facts; §22.5 reproduces §20.3's closure tuple without promotion or self-decision; §22.6 contains no misclassified or agent-waivable check; and the worst-case live outcome stays inside the owner's authorization. RV-32-1…5 are non-gating and are best absorbed into the Stage-03 handoff (smallest path), with RV-32-1/RV-32-2/RV-32-5 optionally corrected in a later in-place plan touch; none requires a revision before Stage 03.

## RESIDUAL_MINOR_NOTES
- `result.md` (SHA `7c0f3ab6…`, mtime 2026-09-17 13:36) is still the Rev18-era closing artifact: it names `e2e/attempt-05` as the closing attempt and quotes plan Rev18 `22a5e500…`; it was not refreshed by `e2e/attempt-06`'s wave acceptance and does not mention the Rev21 wave. The Rev22 plan does not claim otherwise; Rev22's Stage 05 (the closing role for a DONE outcome) must regenerate `result.md` when it re-derives the tuple.
- Prior-review residuals still standing and not touched by Rev22: RV-30-1 (= RV-32-5 above), RV-30-2's route-numbering supersession sentence (now largely moot: §22.3 uses the route number 06 and §22.5's acceptance is 07), RV-30-3..6 and RV-31-1..9 (v4-wave precision notes) remain non-gating handoff fodder.
- The Rev22 header's `REV22_WAVE_STATUS` line carries the task-level tuple (IMPLEMENTATION COMPLETE / CORE BLOCKED / REQUIRED_VERIFICATION PASS) with `INDEPENDENT_ACCEPTANCE PENDING` / `TASK_CLOSURE IN_PROGRESS` and is explicitly labelled "to be re-derived by Stage 05" — no pre-claim of the wave's own result was found (the wave adds no implementation subject).
- The task e2e numbering is monotonic: attempts 02–06 exist; §22.5/§22.3 correctly assign the next unused acceptance to `e2e/attempt-07` while the route run uses the route-space number 06 (`evidence/20260916-route/attempt-06/`, not yet created).
- A parallel reviewer may hold another attempt directory for this same Rev22 pair; this review neither read nor touched it, and this approval binds this exact revision/hash only.
- No hidden blocker or unverified load-bearing claim remains; every hash cited in this report was recomputed in this session.

NEXT_ACTION: Stage 03 Handoff compiling `handoff.md` bound to TASK_ID T20260916-0102-01-line-backup-acceptance, PLAN_REVISION 22, SHA-256 426fe6a1c758e96667e970021a80e20b68396c5af6a4ccaf65672772c514734b, carrying RV-32-1…5 as non-gating notes plus the six §22.6 rows verbatim; do not edit `plan.md` for this approval (any later edit invalidates it).
FINAL_STATUS: PLAN_APPROVED
