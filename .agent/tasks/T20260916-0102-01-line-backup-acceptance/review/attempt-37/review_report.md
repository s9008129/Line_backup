# Plan Review Report — Stage 02 (fresh independent), attempt-37

## REVIEW_METADATA

- TASK_ID: T20260916-0102-01-line-backup-acceptance
- REVIEW_ATTEMPT: 37 (single write target = this file; `review/attempt-37/` created fresh and verified absent before writing; attempts 30–36 read-only, untouched — including attempt-36, whose report was neither read nor consulted; independence is by fresh context, not by comparison)
- REVIEWED_PLAN_REVISION: 24
- REVIEWED_PLAN_SHA256: `40eb01980c7e11f96b4b12c2db8f53728852f1ab6ca452dfb5e865fd5435a003` — 2,330 lines / 313,461 bytes. Re-computed at review start (before the full read), again at 2026-09-18 10:55 +0800 (Asia/Taipei) immediately before this report was written, and once more immediately after writing (the reviewer's closing act; result reported with this file's own SHA-256). All identical to the mandated value. The committed blob at `92a167e` re-hashes to the same value, and the worktree plan path is byte-identical to it (`git hash-object` match; `git status --porcelain` shows no tracked modification — only the two new review dirs as untracked). The worktree content is therefore the frozen Rev24 candidate.
- PLAN_SNAPSHOT_PATH: none — this review is write-limited to this report file; the triple hash re-verification is the substitution.
- Repository anchor: branch `master`, HEAD `5d6e2b1`; plan frozen at commit `92a167e`; no commit, no network, no GUI (zero clicks/keyboard/AX/window activation/capture), zero images in any output.
- Write set: this report only. Everything else read-only: `plan.md`, product code, tools, evidence, prior reviews, task `handoff.md`, `result.md`, formal config/state/run-log (not opened), destination (not opened; verified only via the frozen baseline record).
- Inputs (reading order for this reviewer): `plan.md` Rev24 header + §24.1–§24.9 first (the wave's own claims), then §23.1–§23.2 + §22.1–§22.9 + §20.2/§20.3 + §19.1/§19.3 + requirement table + closure + owner view (the preserved machinery), then the full diff `git diff 192e6f0 92a167e`, then `evidence/20260918-owner-decisions/owner-decisions-rev24.json` (9,872 B, `be1f5173de9219bfba2a547223542dde17c6e6a23f653274122f0d8dcbe2360d`), then the raw transcript JSONL (`01a0af3c…`) lines 2291/2365/2405/2412/2420/2427/2428, then `evidence/20260916-route/attempt-06/{run-ledger.json (906c1433…, 28,390 B),gate-4-authorization.json (e32ccd88…, 8,979 B, 17 keys),album-ellipsis-locate.json (NO_ELLIPSIS_FOUND),route-runbook.md}`, then the frozen toolchain (`tools/v4/vision_reader.py 22a4e9ef…`, `tools/v4/locate_album_card.py bb52aff1…`, `tools/v4/verify_album_open.py ffa82aed…`, `tools/v4/locate_album_ellipsis.py b77e3d51…`, `tools/v4/README.md ed3ebf0a…`, `tools/selftest/v4/selftest-summary.json 5ad2be10…` 16/0 PASS, `tools/selftest/v3/selftest-summary.json 17840e91…` 13/13, `tools/vision/vision_ocr.swift 4fc9fa2b…`, `tools/detect_menu_popup.py 6ae9c250…`, `tools/selftest/selftest_menu_popup.py db091703…` 4,018 B, `tools/selftest/selftest-summary.json d8ffc129…` 2,946 B), then the frozen frames (`route5r_frame_post.jpg 4cb8a6b4…`, `route5r_frame_pre.jpg 3d926e7d…`), baseline (`ab6747f2…`), v1.1 record (`a8c10551…` 1,741 B), `result.md` (`bd00d9c9…` CORE_ACCEPTANCE_BLOCKED), `execution-rev22.md` (`c9b59080…`), task `handoff.md` (`19c9c6d3…` 175 L / 37,582 B), reviews 34 (`21c4bbb5…`) + 35 (`975142c3…`), e2e/attempt-07 report (`6416a5f4…` 38,483 B), and H4.0 (`ba870b4b…` 43,749 B) + GOAL (`3c724e7b…` 10,558 B) as cross-checks only.
- Verification method: every SHA below was re-hashed from disk in this review with `python3 -B` (no `__pycache__`; verified absent afterwards); every transcript byte-count/SHA was re-extracted from the raw JSONL `payload.content[0].text` UTF-8 bytes; every geometric number was re-derived from the frozen JSON (no trust in the plan's arithmetic); the gate-4 key set was re-listed (17 keys, exact match to §24.4's list); the Rev24 diff was re-walked hunk by hunk.
- Transcript results (this review): L2291 243 B `3fc89cbc9388a176fd92f47a00e0cae7bde24d51286a084584fa73da16d0ae8e` exact; L2365 60 B `2e729d0339bc1574d9869b7054da586259f7dde584fb6ef85c28135796850364` exact; L2405 1,931 B `14905585890cb158957b0e20e34abe6ec8bac26439c8b18187d3f58076ff8752` exact (B-excerpt 470 B `5674299eb0fc6d11f7fb507086bdf6215dffe06df44477aeff96387f273f9eda` verified inside the decisions file); L2412 304 B `caf2f7d789c3429fa9bc4e956ea1da6782c50afd4f6983ce952e565160ca4038` + L2420 430 B `a8caf86dc8e92338777157dae54904a689525a8fe653f1f7b08f3311ca7383b9` both followed by `turn_aborted` (superseded, correctly excluded from authority); L2427 882 B `a600ac4c9af8e3e44327f4566f5388985a79658b43a6eeb435915edec6f9e9ec` exact and byte-identical to the decisions file's `answer_verbatim` (re-hashed here); L2428 echo `client_id 143b072f…` consistent. Order L2291 → L2365 → L2405 → L2412(aborted) → L2420(aborted) → L2427 is chronological and matches §24.1.
- Geometry results (this review): post frame `4cb8a6b4…` 327×643; title `[15,83,204,111]`; v4 band `x[206,326] y[77,121]`; ⋮ triple `[[304.5,44.0],[304.5,49.5],[304.5,55.0]]` middle y 49.5 → 27.5 px above band top (77), 33.5 px above strip top (83) → `above_album_title_band`, ineligible under v4; other triples `inside_text` on `57張照片` / `2024.05.18`; verdict `NO_ELLIPSIS_FOUND`. Header-depth check: `header y1=max(0,83-7)=76`, `y0=max(0,76-60)=16` → y∈[16,76] contains middle y 49.5 with 27 px headroom to 76 and 33.5 px to strip top 83 — exactly the §24.3 window; `x0=204+2=206` contains x 304.5. The v4 fixture blind spot (control dots inside the synthetic strip) was confirmed by reading `selftest/v4/run_selftest.py album_frame()`.
- Diff results (this review): `git diff 192e6f0 92a167e` touches only the header block, the new `## Revision 24 changes` section, and one §22.3 sentence (detector self-test disambiguation). No hidden change to budgets, gates 1–4, ledgers, tools, fixtures, vocabulary, closure tuple, or read-only fences. §24.8's literal list is complete.
- Absence checks (this review): `tools/v5/` absent, `attempt-07/` absent, `e2e/attempt-08/` absent — all expected (Stage 04/05 not started); no approval exists for Rev24 before this review (34/35 bind Rev23 `4337e2b5…` only).
- Invalidation: this review approves exactly PLAN_REVISION 24 at the SHA-256 above and nothing else; any later edit invalidates it.

## OWNER_VERDICT

（白話版，給非技術主持人）

- 上次不是照片壞、不是眼睛壞：是「找 ⋮ 的格子畫錯」。程式只在標題列裡找，LINE 把 ⋮ 放在標題列上面約 33 像素，所以程式誠實說找不到、什麼都沒點。這次只把格子改成「標題列上方、往上 60 像素」，其他規則（一次只找一個、找不到就停、開相簿一次＋點 ⋮ 一次、不點任何選單、不重試）全部照舊。
- 為什麼限制 60：留一點餘裕（約 27 像素）但不無限往上，避免掃到不相干的按鈕、避免把一張正常畫面誤判成有兩個 ⋮。標題列本身這次明定為不合格（那裡全是文字，旧的誤判都從那裡來）。「把兩區聯集」的寬鬆做法已寫明不用、理由也有。
- 流程保障：先用「上次那張已保存的真實畫面」離線證明「新規則找得到、舊規則還是找不到」，證明完成才准凍結一次性授權（gate-5：同上一輪 17 欄格式，綁定您的 B 決策＋Rev24 雜湊＋兩份複審），凍結後才准碰真機。ledger 接續上一輪 FINAL（906c1433…），不斷鏈、不改舊檔。
- 我獨立重算的部分：6 行對話逐字雜湊全對（含兩則您自己中斷的草稿，確認未被當授權）；26 個凍結檔 SHA 全對；失敗幾何（標題、band、⋮ 座標、33.5 差值、文字區候選）重算一致；diff 只動 header＋新 §24＋一處 detector 文件字；gate-4 確為 17 鍵、預算 12 鍵鏡射一致；e2e/attempt-07 六列（5 PASS＋1 非關卡 FAIL）與「⋮ 未送、零副作用」一致。
- 執行當天：LINE 停在相簿列表、目標卡可見；跑時不動滑鼠鍵盤；若選單彈出，agent 只看不點，結束後由您關。畫面不符則零輸入停止（可重排）。
- 結論：可核准（綁定 Rev24＋雜湊 40eb0198…）；無阻擋性問題。

## Top-down review

1. Goal alignment: the wave's CORE set (REQ-V24-1 corrected attempt-07 once under gate-5 bound to v5; REQ-V24-2 durable offline replay ELIGIBLE+[304.5 dots]+[304,50] with v4 still NO_ELLIPSIS_FOUND; NFR-V24-1 additive-only v5; NFR-V24-2 at-most-once mirror; NFR-V24-3 destination/formal-state read-only + 禎/楨 never merged; NFR-V24-4 SUPPORTING probe) is necessary and sufficient for the stated objective (machine-observable AFFIRMATIVE-or-honest-negative for the album-level ⋮ reachability). No goal creep: OOS-V24-1..5 explicitly exclude menu activation (Save All named), re-download, ROUTE_NOT_NEEDED (A not chosen, owner-reserved), extra GUI, and in-Revision retry. The three separately reported results and the append-only/history-immutable posture are preserved.
2. Authority chain: L2427 B (882 B) → decisions file `be1f5173…` → §24.1/§24.4 `context_authorization` + `option_presentation_transcript` L2405 → gate-5 `authority_artifact` + `plan_binding` (Rev24 hash + reviews 36/37) → attempt-07's two ordered inputs. The chain is closed: gate-5 is pre-granted for the next conversation (owner's own instruction) and must not be re-asked; only the recorded escalation conditions stop the run. The alternative-reading guard prevents scope laundering (different target/album/menu path → read-only stop).
3. Safety envelope: at-most-once is enforced at three independent layers — budgets (`retry 0`, `click_count_per_input 1`, `menu_item 0`, `keyboard 0`, `conversation_images 0`, observation ≤5, captures ≤5), runbook (S6 ELIGIBLE-only permission; five non-ELIGIBLE verdicts + reader failure → no ⋮ input; historical coordinates banned as live input; S6-no-fallback-to-v4), and gate (missing/late gate or missing `906c1433…` parent = TASK_REGRESSION; post-input-#1 stop closes the round). Fail-closed is never relaxed; no semantic drift from gate-4.
4. Closure soundness: §24.5's joint condition (attempt-07 AFFIRMATIVE machine-observable AND §16.4 CONFIRMED re-derived by e2e/attempt-08 from fresh evidence → full DONE tuple; any non-AFFIRMATIVE → honest record, scoped blocker retained, owner notified, no ROUTE_NOT_NEEDED, no auto-branch, NEW revision for any retry; preflight surface failure → resume-eligible, not a route result) preserves §20.3 tuple semantics and the owner-reserved closure. Stage 05 is correctly constrained to zero GUI and no product-code change.
5. Compatibility/rollback: NFR-V24-1 + V24_PRIOR_EVIDENCE_IMMUTABLE freeze every v1–v4 byte, both self-tests, attempts 01–06, e2e 02–07, gates 1–4, ledgers, archived handoffs and result history; rollback is by non-creation (a failed attempt-07 routes to a NEW revision, never to amending Rev24). No migration, no config/state/run-log write, no third-party dependency.

## Bottom-up review

1. Position rule (§24.3): v4 band formula retained for the strip; new header formula with `--header-depth` 60 is exactly parameterized; eligibility (exactly-one ∧ ¬inside_text ∧ ¬in-group-band ∧ inside-header) plus the three named ineligible classes partition the outcome space with no gap (above-header / in-strip / below-strip); exit codes 0/2/3/4/5/6 unchanged; GROUP_LEVEL_ONLY preserved. The 60-px bound is justified (33.5 observed, ~27 headroom, AMBIGUOUS risk above, fail-closed beyond). The rejected union is documented with a real-frame reason (strip triples are glyph runs; union widens without support and raises the wrong-coordinate risk class). Correctness risk is contained by the durable replay gate.
2. Budgets/gate/runbook/ledger: gate-4's 17 keys re-listed here match §24.4's required set key-for-key; the 12 mirrored budget keys match §24.3 precondition 3; the S6 command shape (expect-start/end, title-bbox from S5, expect-group-title, header-depth 60, out path) is fully specified; S3/S5 remain on frozen v4 bytes; S8 one left click at the v5 click_point; S9–S10 observe+transcribe with the frozen detector (reader-block exempt, consistent with the Rev23 RV-33-1 fix); S11 durable ledger+execution; parent `906c1433…` (parent `17b17203…`) verified; stop-artifact policy retained. No coordinate, retry, menu, chooser, keyboard, AX, download or state write escapes the envelope.
3. Verification matrix (§24.6): nine rows — V24_RULE_REPLAY_COMMITTED_FRAME (v5 ELIGIBLE + exact dots + [304,50]), V24_V4_BASELINE_FROZEN (v4 still NO_ELLIPSIS_FOUND + no v1–v4 byte change), V24_V5_SELFTEST (≥20 cases, 0 failed, corrected positive + four named negatives + 5× determinism + tool SHAs), V24_ROUTE_ATTEMPT_07_SINGLE_ONESHOT (one card + one ⋮, no retry/menu/chooser/keyboard/AX, no post-precondition input), V24_S6_BOUND_TO_V5 (S6 verdict+point from frozen v5, reader block present, S3/S5 on v4, detector exempt, point from current frame), V24_AFFIRMATIVE_MACHINE_OBSERVABLE_ONLY (OCR-only/human-only/incomplete AFFIRMATIVE = TASK_REGRESSION), V24_PRIOR_EVIDENCE_IMMUTABLE, V24_GATE5_FROZEN_BEFORE_INPUT (gate+runbook+skeleton committed pre-input, `906c1433…` parent), V24_SCREEN_SCOPE_PROBE (SUPPORTING/NON_GATING). Each CORE row is HARD_CLEAN with a falsifiable baseline and NO waiver — acceptance cannot be gamed.
4. Header literals (§24.8): PLAN_REVISION 24; ACCEPTED_BY_USER +L2427 B; ACCEPTANCE_MODE +e2e/attempt-08 third subject; PRIOR_REVIEW_ATTEMPT 34,35; PRIOR_REVIEW_GATE Rev23 `4337e2b5…` dual approval (reports re-read, zero BLOCKER/MAJOR) + Rev21 chain + Rev22-dual-never-satisfied + no-approval-for-Rev24; REV22_WAVE_STATUS final Stage-05 state (UNKNOWN/COMPLETE/BLOCKED/PASS/PASS/CORE_ACCEPTANCE_BLOCKED per `6416a5f4…`, six rows 5 PASS + 1 SUPPORTING FAIL, sole blocker CUA_ROUTE_DECISION, S6 NO_ELLIPSIS_FOUND, ⋮ never sent, zero side effects — all re-confirmed); REV24_WAVE_STATUS claims nothing; §22.3 detector literal (`db091703…` 4,018 B script vs `d8ffc129…` 2,946 B summary) verified as a documentation-only fix with no semantic change.
5. Prior-evidence handling: Rev23 `4337e2b5…` stays frozen history (34/35 approvals untouched); attempt-05/06 verdicts and spent budgets stand (gate-4 spent, never extended); v5 applies only to attempt-07's fresh frames (no re-score of frozen verdicts); destination/formal-state read-only, no re-download, 禎/楨 never merged — all retained (§24.9 unchanged list verified against the diff).

## Budget and boundary cross-check (mandated)

- Inputs: album-card 1, ellipsis 1, navigation 1, app_acquisition 0, menu_item 0, keyboard 0, retry 0, click_count_per_input 1, conversation_images 0, observation windows ≤5, captures ≤5 within ~4 s of input #2 — mirrored from gate-4, no inflation, no new kind. A stop after input #1 never re-authorizes input #2; a preflight surface failure spends no input and is resume-eligible (not a route result).
- Boundaries: zero conversation images; /tmp-or-evidence-only frames; at-most-once; zero menu-item (Save All named); zero chooser/keyboard/AX-write/app-acquisition; un-frozen-no-input; historical coordinates never live; S6 never v4; S3/S5 never non-v4; destination + formal config/state/run-log read-only; 禎/楨 never merged; append-only for all v1–v4/v3-v4-selftests/attempts-01–06/e2e-02–07/gates-1–4/ledgers/archived handoffs/result history; no third-party dependency; no `__pycache__` (python `-B`); no self-exemption; three-result separation. All fourteen hold without exception in the text; no waiver path exists for any CORE item.

## FINDINGS

- No BLOCKER findings. No MAJOR findings.
- ID: RV-37-1 | Severity: MINOR | Category: ROBUSTNESS NOTE
  - Affected: plan.md §24.3 header-band `x0: tx1+2` (inherited from the v4 strip formula).
  - Evidence: [VERIFIED] on the real frame `tx1=204` → `x0=206`, and the ⋮ at x 304.5 is safely inside. The `+2` inset is retained byte-identically from v4 by design (detection constants unchanged). A future frame with a wider title (tx1 near the right edge) could narrow the header window, but that frame would then fail closed (NO_ELLIPSIS_FOUND) rather than mis-click — the safe direction.
  - Failure mechanism: none in this wave (fail-closed); noted only so a future revision does not "fix" the inset without a real-frame basis.
  - Smallest correction (non-blocking; future revision only): none required now; if ever touched, require a real-frame justification plus the same durable replay gate.
- ID: RV-37-2 | Severity: MINOR | Category: WORDING
  - Affected: plan.md §24.6 V24_V5_SELFTEST row ("including the corrected-geometry positive and the four v5-specific negatives of §24.3").
  - Evidence: [VERIFIED] §24.3 lists the four negatives plus a separate 5× determinism replay of the real-geometry positive; the matrix row's "including" is satisfied by all five, but a strict reader could count the determinism replay as outside "the four".
  - Failure mechanism: none for acceptance (Stage 04 must produce ≥20 cases with 0 failed including all five; the row's baseline is met either way). Purely a counting-clarity note.
  - Smallest correction (non-blocking; future revision only): reword to "the corrected-geometry positive, its 5× determinism replay, and the four v5-specific negatives".

## REQUIRED_PLAN_CHANGES

None for approval. Rev24's correction is bounded and pre-proven by a durable gate; its authority is byte-exact; its budgets/gate/closure preserve the proven Rev22/Rev23 machinery; its matrix makes approval falsifiable. The two new MINORs (RV-37-1 robustness note, RV-37-2 counting clarity) are non-gating and touch neither safety nor acceptance. Any edit to plan.md — including folding RV-37-1/2 into the text — creates a new revision and requires a fresh independent review before Stage 03 handoff (approval binds to this hash only).

## RESIDUAL_MINOR_NOTES

- RV-34-1 and RV-36-1/36-2 (if filed) are documentary and do not interact with this review's findings; Rev24 does not depend on them.
- Stage 04 must prove both replay rows from durable evidence (not from the /tmp prototype): v5 ELIGIBLE with the exact dots/point + v4 NO_ELLIPSIS_FOUND on `4cb8a6b4…` with the S5 title bbox. The prototype is not evidence.
- Stage 03 must archive the current `handoff.md` (`19c9c6d3…`) to `handoff-history/handoff-plan-r23-<YYYYMMDD>-<HHMMSS>.md` before recompiling; it must carry the §24 handoff notes (header formula + 60 + rejected union + historical-coordinate ban + S6-no-fallback + stop-artifact policy + series-homonym note).
- The v5 README must record the corrected geometry (with the real-frame numbers 304.5/49.5/77/83/33.5), the rejected union, the depth-60 rationale, and the offline-replay commands.
- The detector exemption (reader-block) is intentional and consistent with the Rev23 RV-33-1 resolution; Stage 04 must not "fix" it by adding a reader block to the detector.

## NEXT_ACTION

Stage 03 may compile `handoff.md` bound to TASK_ID T20260916-0102-01-line-backup-acceptance, PLAN_REVISION 24, SHA-256 `40eb01980c7e11f96b4b12c2db8f53728852f1ab6ca452dfb5e865fd5435a003`, carrying RV-37-1/2 and the residual notes as non-gating items, plus the nine §24.6 rows verbatim; do not edit `plan.md` for this approval (any later edit invalidates it). Stage 04 then builds `tools/v5/` + `selftest/v5/`, reproduces both replay rows as durable evidence, and freezes gate-5 + the Rev24 runbook + the ledger skeleton (parent `906c14330605122e9b600359892d4400171d62564707d50af26ef8d8bb32fa2c`) before any input; the route attempt-07 runs once under the two-input envelope; Stage 05 (`e2e/attempt-08`) re-derives both closure facts from fresh evidence with no GUI input of its own.

PLAN_APPROVED
