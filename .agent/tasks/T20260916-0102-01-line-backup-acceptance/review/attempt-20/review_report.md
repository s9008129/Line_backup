# Plan Review Report

## REVIEW_METADATA
- TASK_ID: T20260916-0102-01-line-backup-acceptance
- REVIEW_ATTEMPT: 20
- REVIEWED_PLAN_REVISION: 16 (CANDIDATE)
- REVIEWED_PLAN_SHA256: 61e1676439b9bb2a5314bd68f0a7d14a0eeb4d9b11af62230e5591c50139384e
- PLAN_SNAPSHOT_PATH: .agent/tasks/T20260916-0102-01-line-backup-acceptance/review/attempt-20/plan-snapshot.md (178069 bytes; `cmp` byte-identical to the reviewed plan.md before any canonical change; same SHA-256)
- Repository anchor observed: /Users/hsiaojohnny/Documents/ChatGPT/Line_backup (read-only review: no product CLI, no test driver, no download, no GUI/computer-use, no admin-command; all repository facts below are static reads and hashes)
- Reviewer runtime/model: Codex CLI, fresh Stage-02 context (exact model id not exposed; informational only)

Preflight: SHA-256 of the current `plan.md` recomputed and equals `61e16764…384e` (178069 bytes, 1205 lines); the snapshot was copied before any canonical change and verified byte-identical. Reviewed inputs: global `~/.codex/AGENTS.md` (no repo-local AGENTS.md exists), `02_plan_review_prompt.md`, `plan-review-gate.md`, `goal-alignment-design-economy.md`, `workflow-routing.md` §7, the goal attachment `/Users/hsiaojohnny/.codex/attachments/210176d8-…/pasted-text-1.txt`, `handoff.md` H2.1 §0–§14, `evidence/20260916-user-fact/source-identity-user-fact.json`, the two Rev15 reviews (`review/attempt-18`, `review/attempt-19`), the full Rev16 plan, and the repository anchors named below. No other review attempt was modified; nothing outside `review/attempt-20/` was written.

## OWNER_VERDICT
The goal this plan serves: prove — safely, with rerunnable and independently re-readable evidence — whether the existing 57-image destination is a real backup of the exact requested LINE album (group 「旻謙允禎成長日記」, 禎 U+798E, 2024/05/13–05/17), and repair the R1–R7 holes in the backup process itself through the real operator CLI, without touching formal state/config/photos and without re-downloading anything.

Essential (CORE): the R1–R7 repairs executed end-to-end through the real entry with literal, rerunnable argv; independent oracles that never read a product PASS line as truth; exactly the two authorized human gates (one part-2 user-fact question allowing 「不知道」, one current-target ellipsis observation) and nothing else; 禎 and 楨 never merged/normalized; the three results reported separately (album data / reusable capability / overall closure); fixture or offline PASS never masquerading as GUI end-to-end success.

Optional/deferred: production Save-All/download, bridge repair, migration, OCR and historical cleanup; bridge/service diagnosis only if causal evidence proves necessity.

Global verdict: no BLOCKER is open, but five material defects (RV-1…RV-5) remain capable of invalidating the flagship loop, the R1/R4 proofs, the R4 fix's own contract, the CONFIRMED user-fact path, or the legacy-state semantics — so this revision is not approvable as written. The rest of Rev16 is a genuine, traceable response to the attempt-18/19 findings: 15 of the 21 prior findings are fully closed (A-RV-2/3/6/7/8/9/11/13, B-RV-1/3/4/5/6/7/8); 5 are half-closed — the normative fix exists but contradicting body text remains (A-RV-1, A-RV-4, A-RV-5, A-RV-10, B-RV-2); 1 (A-RV-12) is not closed.

Complexity verdict: proportionate to a CRITICAL acceptance harness; Rev16 adds ten normative subsections that each answer a named prior finding and no new scope. The economy cost is concentrated in the five open items and in mechanical staleness (old paragraphs not updated where §16 supersedes them).

Biggest remaining risk: the wave goes green on the new fixtures while the flagship Case-01 argv, the verification-evidence chain, and the legacy/prepare semantics are still contradictory as literaled — i.e., another round of pass numbers or ad-hoc on-the-fly patching instead of the verification the user explicitly demanded.

## GOAL_BASELINE
Reconstructed before reading the Plan's framing, from the authoritative `/goal` attachment, `handoff.md` H2.1 §7–§8 (plus §0–§6, §9–§14), and `evidence/20260916-user-fact/source-identity-user-fact.json` (re-read in this review):
- Primary goal: make the verification real — reproduce and fix `handoff.md` §7 R1–R7 through the true entry points and produce rerunnable, independently re-readable tests and evidence. The user's ultimate goal is fully automatic backup with no human intervention; this wave must not inflate PASS counts or documentation.
- Scope: exactly one album — LINE app `jp.naver.line.mac`, group 「旻謙允禎成長日記」, album 2024/05/13–05/17, expected 57 images. Never re-download for testing. Formal config, state, run-log and the 57 photos stay read-only unless the user gives a separate precise gate.
- Source identity (blocker 1): the requested group is 禎 (U+798E); formal config/state carry 楨 (U+6968). Merging, normalizing or rewriting the canonical key is prohibited, as is inferring identity from equal dates/counts/hashes. Only two legal resolutions: an authoritative exact source join, or one precisely preserved user fact (original question, original answer, supplier and time, evidence SHA-256; the handoff §5 screenshot may be used to ask the one part-2 question).
- Recorded user fact (2026-09-16): part 1 answered 「正確是「禎」」 (the visible title string is 禎); part 2 — whether the 57 files are that album's backup — is UNANSWERED. So the album's source cannot be CONFIRMED today; the legally correct present result is UNRESOLVED/stop, and the CONFIRMED path exists only for a future precisely-authored record.
- GUI gate (blocker 2): after all non-GUI work, exactly one precise gate permitting exactly one current-target ellipsis observation for live post evidence; it authorizes no menu item, Save All, chooser, keyboard shortcut, state write or download; historical ellipsis budget is spent.
- Three results reported separately; result 1 AND result 2 both required for `complete`; offline/fixture PASS may not impersonate GUI end-to-end success. DONE forbids promoting UNRESOLVED/LEGACY/ CONTRADICTED.
- Hard prohibitions: no admin/authorization-triggering commands (sfltool, sudo, TCC); no AXPress/AX write, coordinate guessing, OCR-only pass or sandbox workaround; no re-dispatch on an unknown dispatch outcome; wrong album ⇒ stop immediately; `/private/tmp` is not durable storage.
- Evidence rules: every attempt keeps complete inputs, argv/env (minus secrets), stdout/stderr/exit, tested program hashes, independent side-effect counters, before/after state, artifact manifest (SHA-256+bytes), all independently readable; attempts are append-only.

## GOAL_ALIGNMENT
- `PRIMARY_OUTCOME` (plan.md:750, Rev16 §16.8) now names both wave results (album-data result and the proven reusable automation path) and forbids claiming either from offline/fixture PASS — this matches the baseline and closes the attempt-19 B-RV-7 gap.
- The three results (plan.md:705–719), the DONE prerequisites (plan.md:1199), and the stop path (plan.md:1201: PRIMARY_OUTCOME_STATUS=UNKNOWN with source/core BLOCKED, no DONE) match the baseline's honesty rules; UNRESOLVED/LEGACY_PROVENANCE_LIMITED cannot be promoted to closure.
- 禎/楨 separation is preserved everywhere checked: plan.md:400–413 (UTF-8 code-point equality, never merged/normalized/folded), §16.4's `raw_persisted_group` as evidence-only, and the real-state read path keeps the 楨 keys untouched.
- The plan keeps the one-shot human gates and the read-only fence: no production Save-All/download; formal state read-only; §16.5's legacy read contract explicitly keeps the real formal state readable without rewriting it.
- Caveat (RV-4): the v1 user-fact record has no `group_key` key while plan.md:400's general verification clause still requires the parsed artifact's own `group_key` to equal the request's, and §16.4 never authorizes `raw_requested_group` as the substitute anchor; the 24a CONFIRMED oracle is therefore not decidable from the text as written.
- Caveat (RV-2): plan.md:838's surviving universal sentence ("must validate … at every load") directly contradicts §16.5's read-only legacy contract for the very real state the plan must accept read-only.

## NECESSITY_AND_TRACEABILITY
- F1–F8 repairs trace to reproduced R1–R7 gaps; Rev16's ten subsections each trace to named attempt-18/19 finding IDs (§16.1→A-RV-1; §16.2→A-RV-2/B-RV-5; §16.3→A-RV-3; §16.4→A-RV-4/B-RV-2; §16.5→A-RV-12; §16.6→A-RV-7/A-RV-8/A-RV-5/B-RV-1; §16.7→A-RV-6/A-RV-9/A-RV-10/B-RV-6/B-RV-8; §16.8→B-RV-3/B-RV-4/B-RV-7; §16.9→A-RV-13; §16.10→A-RV-11).
- New literal rows 20–25 (plan.md:1023) are traceable: 20→R1 parent-kill proof; 21→MISSING_DISPATCHER guarantee; 22→R4 finalize payloads; 23→prepare storage faults; 24→the only CONFIRMED path; 25→the real-state legacy shape.
- CORE/SUPPORTING labels remain explicit (BRIDGE_READINESS SUPPORTING/NON_GATING; DOCUMENTATION_RETENTION_HEALTH supporting with reproducibility rationale). No untraceable material work was found, and no scope creep: §16.x changes are product/test semantics inside the declared F1–F8 surface.
- The two defects in this dimension are traceability of *semantics*, not of work: RV-2 (legacy mutation semantics stated two ways) and RV-4 (which equality anchors apply to a v1 record).

## GATE_AND_VETO_AUDIT
- Source integrity remains non-waivable and justified by the user's blocker 1; waiver paths require named authority plus the seven fields and preserve the original FAIL; no self-waiver path was found.
- The real-state read path is the correct kind of gate: it reports (LEGACY_PROVENANCE_LIMITED, exit 4) rather than refusing to load — but only if §16.5 governs; plan.md:838's universal "at every load" veto would refuse the same state, which is the RV-2 defect (unjustified global veto contradicting the plan's own mandated acceptance).
- §16.5's `prepare`-append exception is a gate inversion: it permits appending to a state whose only strict failures are the three enumerated legacy shapes (which the real state's shape satisfies — verified below), while row 25b requires the opposite refusal for the same state. Either the exception is dead text or the row's oracle is wrong; the plan does not choose.
- No other unjustified global gates were found; fixture-root restrictions and `fixture:`/test-mode containment are proportionate.

## COUPLING_AND_FAILURE_CONTAINMENT
- The legacy read contract (when read per §16.5) correctly contains failure at the narrowest boundary: read-only ops report and exit 4, never rewrite; mutation ops refuse; the real formal state stays untouched by this wave.
- Case isolation is sound: one canonical state file per case root, authority first, writes confined to evidence-dir; the §16.3 chain is meant to contain the R4 failure (finalize trusting JSON) at the finalize boundary, but its mechanics are under-specified (RV-3).
- The §16.5 ambiguity (RV-2) is a containment defect: one reading mutates a legacy state (appending a strictly-valid run while preserving legacy runs), the other refuses; the acceptance row (25b) and the exception cannot both hold.
- Case-failure routing (plan.md:1057) is not extended to cases 20–25 (RV-11), so the R1 proof's failure class is unstated.

## DESIGN_ECONOMY
- Deleting §16 would not return the plan to a valid state — each subsection answers a real prior finding — but the revision would be cheaper as in-place corrections of the named stale paragraphs instead of a layered supersession section. As written, at least eleven stale or contradicting body statements remain (RV-1, RV-2, RV-6…RV-10); each is a place where Stage 03/04 must resolve two normative texts.
- No over-engineering beyond the CRITICAL requirement was found; the 25-case matrix, the verifier/status/authority/legacy rows and the evidence-root pinning are all justified by the reproduced R1–R7 and the v4.2 status contract.

## CRITICAL_PATH_AND_PRIORITY
- The critical path is: authority constants/layout (§16.1) → in-process dispatch window with the real parent-kill proof (case 20) → finalize chain + case 22 → flagship Case-01 closed loop → verifier/status/legacy rows → production verify pass (attempt-04) → Stage-05 acceptance.
- Priority is not inverted: fixture proof is bounded before the human gate; the production pass is last; no supporting item gates the core.
- Residual priority risk: the three contract items on the critical path (RV-1 argv literals, RV-3 chain, RV-5 Case-20 mechanics) are exactly the ones that would force rework of the core path if implemented as literaled.

## REQUIREMENT_FIDELITY
- One album only, no re-download, formal state/config/run-log and the 57 photos read-only, no production Save-All this wave, exactly two human gates, no admin commands, no AX/OCR shortcuts, wrong album ⇒ stop: all preserved and consistent with the baseline. No out-of-scope requirement was found.
- The plan's own "no pre-announced fault flags to manufacture safety" rule (goal attachment, Phase 2) is honored by §16.2 (real SIGKILL, no `--crash-after-dispatch`).

## GROUNDING_AND_DRIFT
- Program hashes: all eight modules under `src/line_backup_acceptance/` hash exactly as recorded in handoff §4 (`authority.py a98964e3…`, `cli.py a5bbbbf6…`, `common.py 59a85c47…`, `status.py 20a95f7c…`, `transaction.py c486edbe…`, `verifier.py b38ee6d5…`, `__init__.py`, `__main__.py`). No anchor drift.
- `authority.py` today: `CASE_ROOTS` enumerates only `-01…-12`; test-mode transaction requires `state == root/"state.json"` and rejects `--config`/`--run-log`; verifier test mode is allowed only under `/private/tmp/line-backup-acceptance-verifier`; verify-only always requires canonical `config/line_backup_config.json` + `state/backup_state.json`. Rev16 §16.1's mandated product changes (roots −01…−25, canonical children required, verifier test mode extended to case roots) are consistent with these rules and are declared as changes — not yet implemented, which is expected at plan stage.
- `transaction.py` invokes `--dispatcher` as a subprocess (`subprocess.run`, transaction.py:88–92) — relevant to RV-5.
- `verifier.py` today writes `result.json` (keys include `mode`, `filesystem_status`, `registry_status`, `source_status`, `state_status`, `overall_status`, `failure_class`, `exit_code`, `fingerprint`, `group_key`, `destination`, `state_revision`; no `run_id`, no `verification_evidence`) and then `manifest.json` (`{"schema_version":1,"artifacts":[{path,bytes,sha256}…]}`, excluding only `manifest.json` itself). §16.3 is therefore a product extension — implementable in principle, but see RV-3 for its unresolved mechanics.
- `schemas/schemas.json`: `$defs/calibration` is `additionalProperties:false` with exactly 8 keys; `$defs/intent.required` includes `calibration`; `$defs/run.required` does NOT include `contract_revision` (optional) and `run.intent` is `oneOf [null, $defs/intent]`.
- Real formal state (read-only parse, revision 39, 5 runs, 4 verified_albums, all 楨 keys): the ONLY strict-schema failure is `RUN-20260907-132845-01` carrying `intent.calibration.first_row_point`/`row_step` (enumerated legacy shape `calibration.extra_keys`). Four runs lack `contract_revision` (schema-optional ⇒ not a strict failure); `RUN-20260907-152217-01` has `intent: null` (schema-legal). This grounds RV-2's contradiction: the case-25 copy described at §16.5 satisfies the prepare-append exception, yet row 25b expects refusal.
- Post-fix evidence roots named by §16.10 are unused on disk: `evidence/20260916-auto-verification/` has only attempt-01, `evidence/20260916-baseline/` only attempt-01, `evidence/20260916-acceptance/` has attempt-01/02 (attempt-03 free), `evidence/20260916-product-verify/` has attempt-01…03 (attempt-04 free), and `e2e/` has attempt-02/03 (attempt-04 free). A-RV-11's fix is grounded. (Stale: plan.md:822 still points the product verify command at `attempt-01`, RV-10.)
- `tests/automation_verification/` contains `verify_evidence.py` and no `run_all.py` — matching §16.9's corrected file list (A-RV-13 grounded).

## ARCHITECTURE_AND_CONTRACTS
- The dispatch-window architecture (prepare: intent 0→1, adapter invocation, post-dispatch record 1→2, all in one uninterrupted process) is coherent with the state machine and with row-4 recovery; the parent-kill case is the right proof shape (RV-5 concerns its aftermath wording/mechanism only).
- The binding architecture (external artifact + hash reference + no product-authored trust) is sound; §16.4's v1 record fixes the previously unpinned CONFIRMED path — except for the `group_key` anchor conflict (RV-4).
- The verification-evidence chain (result ↔ manifest ↔ artifacts) is the correct boundary for the R4 defect, but as literaled it contains a self-referential hash requirement and a result↔manifest write-order cycle that need an explicit canonicalization convention (RV-3).
- The legacy contract's two clauses (mutation-strict vs the prepare exception) conflict with each other and with row 25b (RV-2); plan.md:838's universal validation sentence must be explicitly superseded.
- The 25-case matrix and the four new case families are architecturally consistent with the existing drivers; the case-20 fixture adapter is the only place where the plan asserts a process-tree fact that contradicts the product's actual invocation mechanism.

## DATA_SECURITY_RELIABILITY
- No secrets are involved; the plan never writes formal state/config/run-log or the photos; the fixture layout isolates every case under `/private/tmp` with its own state and evidence.
- Lock semantics are correctly grounded: `locked_state` uses `state.parent/".line-backup-state.lock"` with `fcntl.flock`, released by process death; the case-20 remainder claim must be scoped to the state directory (RV-5).
- No admin/authorization-triggering command, AX write, coordinate guess, OCR-only pass or sandbox workaround appears anywhere in the plan text checked; escalation triggers are preserved.
- 禎/楨 is never normalized in any matching path checked (only byte-for-byte equality, with `raw_persisted_group` explicitly evidence-only).

## IMPLEMENTATION_SEQUENCE
- Rev16 does not change the declared sequence; the added rows and contracts are all within the same wave. The sequence is implementable once RV-1…RV-5 are resolved, and the plan's own rule (any semantic-contract change ⇒ PLAN_REVISION increment + fresh review) is stated in §16.8.
- Recommended ordering for Rev17 corrections: (1) case argv/layout literals, (2) legacy validation scope, (3) verification-evidence chain mechanics, (4) user-fact anchor, (5) case-20 mechanics, then the mechanical minors in one pass — before any Handoff is compiled.

## TESTABILITY_AND_ACCEPTANCE
- The wave's acceptance surface is comprehensive: 25 literal case roots, the verifier matrix, the authority negatives, the 18-row status fixture table plus §16.7's nineteenth `baseline-worsened` row, and the legacy real-state-shape case 25. The v4.2 status-contract fixtures required by the role prompt are covered by the table (canonical incident, implementation blocker, CORE fail/blocked/not-run/not-required, replan, baseline unavailable/delta, hard-clean debt, waiver preserving the original result, Stage-05 pending/environment/product-defect, contradictory-state rejection, legacy normalization, DONE prerequisites), pending the text fixes below.
- Oracles are computed before product output and the drivers never implement transitions — the right shape.
- Open acceptance items: (a) Case-01's literal argv cannot execute/achieve its claimed results as written (RV-1); (b) the §16.3 chain's mechanics are not implementable as literaled, so both the flagship finalize and rows 22a–d lack a decidable contract (RV-3); (c) row 24a's CONFIRMED oracle is ambiguous against plan.md:400 (RV-4); (d) the Case-20 aftermath oracle contradicts its own mandated artifacts and asserts a false process-tree claim (RV-5); (e) row 25b contradicts §16.5's exception (RV-2); (f) case-07's body count (RV-6), the status manifest count (RV-7), the finalize grammar (RV-8), the §16.6 oracle-assertion claim (RV-9), and the stale roots/paths (RV-10) are mechanical fixes; (g) failure routing for cases 20–25 is unstated (RV-11).

## SCOPE_AND_COMPLEXITY
- No scope creep: §16.x work is confined to the declared F1–F8 repair surface plus the acceptance rows the two prior reviews required. No new dependency, service or abstraction was introduced.
- Complexity is justified by the CRITICAL class; the remaining cost is textual precision, not design. The recommended revision should correct the named paragraphs in place rather than add another normative layer.

## FINDINGS

### Prior-finding closure audit (Rev15 findings A-RV-1…13, B-RV-1…8)

- A-RV-1 (BLOCKER) — **HALF-CLOSED** → RV-1. §16.1 (plan.md:31–64) defines the canonical layout, extends verifier test mode to case roots, deletes the "test mode omits config/run-log" rule, and states "All literal Case-01 argv in this plan are superseded by this shape". But the literal argv the plan still calls "exact" remain non-executable: plan.md:1014's verify-only carries the canonical children but no `--test-mode`; plan.md:1013/1015/1016/1017 carry `--test-mode` but omit the now-required `--config`/`--run-log`. The supersession is a generic rule, not corrected literals, and plan.md:1041 says the driver's literal argv arrays come from those literals.
- A-RV-2 (MAJOR) — **CLOSED** (with RV-5): §16.2 adds `CASE_ROOT_20` (`parent-kill-in-dispatch-window`) with a real SIGKILL inside the dispatch window, no `--crash-after-dispatch`, bounded barrier observation, after-kill state oracle, and a fresh-resume recovery oracle; this matches handoff §8 R1 ("父程序硬中斷 … 不設 --crash-after-dispatch").
- A-RV-3 (MAJOR) — **CLOSED** (with RV-3): §16.3 adds the chain contract and `CASE_ROOT_22` rows 22a–d (hand-authored PASS/57 without chain, real FAIL/0, `{}`, another run's result) with `INVALID_VERIFICATION_EVIDENCE`/`VERIFICATION_RUN_MISMATCH` exit 4 and no-write assertions.
- A-RV-4 / B-RV-2 (MAJOR) — **HALF-CLOSED** → RV-4: §16.4 pins the exact v1 record (key set, part-2 positive form, evidence re-hash, stored path, operator authorship) and rows 24a–d; but the record has no `group_key` while plan.md:400 still requires the parsed artifact's own `group_key` to equal the request's, and §16.4 never states that `raw_requested_group` is the substitute anchor.
- A-RV-5 (MINOR) — **HALF-CLOSED** → RV-6: §16.6 says the winner performs exactly two guarded replacements; plan.md:1051 (case 07) still says "with one replacement".
- A-RV-6 (MINOR) — **CLOSED**: §16.7 `CASE_ROOT_23` with the test-only `--storage-fault-slot intent|dispatch` selector and 23a–c oracles; `--storage-fault` (both choices) already exists in the product.
- A-RV-7 / B-RV-1 (MINOR/MAJOR) — **CLOSED** (with RV-9): one grammar `reconcile:<relpath>:<sha256>` now appears in place (plan.md:355, 379) with the `<kind>`-bearing form and "§15.2 base rules" phrase gone; §16.6 records the single grammar. §16.6's sentence that case-02/03/19 oracles assert the persisted string form is not reflected in those oracle texts (RV-9).
- A-RV-8 (MINOR) — **CLOSED**: §16.6 pins the prepare refusal precedence (authority → MISSING_SOURCE_EVIDENCE → MISSING_DISPATCHER → INVALID_SOURCE_EVIDENCE → precondition refusals → PREPARED) and the binding-path conversion rules.
- A-RV-9 (MINOR) — **CLOSED**: §16.7 pins the Case 18 pre-state (`active_writer_id=null`, `context_lock=null`) and makes `--verification-json` outcome-conditional for Case 19/10B (grammar text still stale → RV-8).
- A-RV-10 (MINOR) — **HALF-CLOSED** → RV-7: §16.7 adds the nineteenth `baseline-worsened` row; plan.md:1162 still says the manifest "enumerates the 18 rows by ID" and its list omits `baseline-worsened`.
- A-RV-11 (MINOR) — **CLOSED** (with RV-10's path note): §16.10 names all five post-fix roots and none is used on disk (verified); plan.md:822 still points the product-verify command at `attempt-01`.
- A-RV-12 (MAJOR) — **NOT CLOSED** → RV-2: §16.5 scopes validation to replaced payloads/new RC2 records and defines the read-only legacy contract, but plan.md:838's universal "at every load … refused with no write" sentence still stands, and §16.5's own `prepare`-append exception contradicts row 25b on the real-state shape.
- A-RV-13 (MINOR) — **CLOSED**: §16.9 corrects the module list (`verifier.py`, no `verify.py`; existing modules to be modified; `run_all.py` new, `verify_evidence.py` extended) — matches the repository listing.
- B-RV-3 (MINOR) — **CLOSED**: §16.8 plus both in-place Registry-axis statements now say "resolves to a same-state run with `workflow_outcome=VERIFIED`" and keep `EXACT` as a State-axis value only (plan.md:468, 972).
- B-RV-4 (MINOR) — **CLOSED**: §16.8's closure wording ("increments `PLAN_REVISION` on the same TASK_ID, invalidates prior approvals and any handoff, and repeats independent review") and the in-place closure section agree.
- B-RV-5 (MINOR) — **CLOSED**: §16.2's equivalence bullet states the byte-shaped equivalence between case 20 and the case-02 crash state and the shared row-4 recovery.
- B-RV-6 (MINOR) — **CLOSED** (with RV-8): §16.7 makes `--verification-json` outcome-conditional and pins the SAFE_ABORT behavior; the old grammar lines still present it unconditionally.
- B-RV-7 (MINOR) — **CLOSED**: plan.md:750's `PRIMARY_OUTCOME` names both wave results; DONE (plan.md:1199) is consistent.
- B-RV-8 (MINOR) — **CLOSED**: §16.7 `CASE_ROOT_21` (`missing-dispatcher`) asserts `MISSING_DISPATCHER` exit 2, no write, no counter line.

### RV-1 — MAJOR — TEST / SEMANTIC_CONTRACT (A-RV-1 half-closure: the flagship argv is still non-executable as written)
- Affected: §16.1 (plan.md:31–64), the literal Case-01 argv (plan.md:1013–1017, esp. 1014), plan.md:1041 (driver literal arrays), plan.md:298–299.
- Evidence: plan.md:1014: "The exact Case-01 verifier argv pair is … `verify-only --project-root /private/tmp/line-backup-acceptance-case-01 --config …/config/line_backup_config.json --state …/state/backup_state.json … --evidence-dir …/evidence/verify-1 ; and the byte-identical argv with --evidence-dir …/evidence/verify-2`" — no `--test-mode`. plan.md:1013/1015/1016/1017 carry `--test-mode` but omit `--config`/`--run-log`, which §16.1 now requires ("The three children are required and must equal the canonical children exactly"; "Transaction test mode therefore reads `--project-root CASE_ROOT_NN --config … --run-log … --state … --test-mode`"). plan.md:1041: "Cases 01–04 and 06, 08–10, 11–12 use the exact CLI grammar above with the literal `/private/tmp/line-backup-acceptance-case-NN/...` paths".
- Mechanism: (a) the transaction literals would be refused `INVALID_AUTHORITY` exit 2 once the product enforces the required canonical children — the flagship prepare/commit/finalize/resume never runs; (b) the verify literal without `--test-mode` is not refused by today's authority (the canonical children satisfy it) but silently runs in production mode, where `fixture:` bindings are unverifiable — run 2's claimed `source CONFIRMED / overall PASS` and finalize's `BINDING_UNVERIFIED` gate can never pass. Either way the flagship closed loop fails the moment it is encoded from these literals. §16.1's "All literal Case-01 argv in this plan are superseded by this shape" is a generic rule; the corrected commands exist nowhere in the plan, and the plan's own driver protocol says the literal arrays come from those literals. The six authority-negative rows (plan.md:978) keep their expected class only by accident (missing children and cross-root mismatch both yield `INVALID_AUTHORITY`).
- Smallest correction: replace plan.md:1013–1017 (and the "-01 replaced" shape reference) with corrected literal argv carrying the three canonical children plus `--test-mode` for every transaction form and `--test-mode` for both verify runs; or delete the concrete literals and state that the driver argv arrays are exactly "line 1013–1017's operations + §16.1's canonical children + `--test-mode`". Update plan.md:298–299's deleted rule.

### RV-2 — MAJOR — SEMANTIC_CONTRACT / COMPATIBILITY (A-RV-12 not closed: legacy validation is stated two contradictory ways)
- Affected: §16.5 (plan.md:165–194), plan.md:838, row 25b (plan.md:193).
- Evidence: §16.5: "Strict `$defs/run`/`$defs/state` validation applies to (i) every payload the product is about to replace and (ii) every new RC2 record. It does **not** gate reading an existing authority state." Then: "Mutation operations … load state strictly: a state whose runs are not all strictly valid is refused `INVALID_STATE_LEGACY` exit 4 with no write, **except that `prepare` may append to a state whose only strict failures are the three enumerated legacy shapes** — in that case the replacement must preserve every legacy run object with identical keys and values … and the new run must be strictly valid." Row 25b: "`transaction prepare` against the same state → `INVALID_STATE_LEGACY` exit 4, no write, no counter line." plan.md:838 still stands unchanged: "Every run and every registry entry must validate against the versioned skill schema … `$defs/run` / `$defs/state` … at every load and before every replacement; a payload that fails validation is refused with no write."
- Mechanism: the real formal state's only strict failure is the enumerated `calibration.extra_keys` shape (verified above), and `CASE_ROOT_25` is described as a copy of exactly those shapes — so §16.5's exception entitles `prepare` to append to the same state that row 25b requires it to refuse. An implementer must choose between the exception and the row; either the exception is dead text or the row's oracle is wrong. Independently, plan.md:838's universal "at every load" veto would refuse the real state that §16.5/25a must accept read-only — exactly the A-RV-12 failure mechanism, still live in the body.
- Smallest correction: pick one semantics and make it singular — either delete the `prepare`-append exception (mutation of any legacy-failing state is refused, matching 25b and the read-only wave) or rewrite 25b to the exception's expectation; and explicitly amend plan.md:838 (name it) so validation scope is exactly §16.5's (i)/(ii) plus the declared exception.

### RV-3 — MAJOR — SEMANTIC_CONTRACT / TEST (§16.3's verification-evidence chain is not implementable as literaled)
- Affected: §16.3 (plan.md:90–111), the finalize contract it extends, `verifier.py`'s actual result/manifest shapes.
- Evidence: §16.3 requires `result.json` to carry `verification_evidence{manifest_path, manifest_sha256, manifest_bytes, result_sha256, result_bytes}`, requires the manifest to be "beside it … whose own bytes/sha256 equal the values recorded in `result.json`, and which lists every artifact of that verify run with `path`, `bytes`, `sha256`", and requires finalize to "recompute … the sha256/bytes of the exact result bytes it read, the sha256/bytes of the manifest, and the sha256/bytes of every listed artifact at its recorded path. Any mismatch is refused." Today `verifier.py` writes `result.json` with no `run_id` and no chain, then `manifest.json` listing every evidence file except itself (including `result.json`, by name).
- Mechanism: (a) `result_sha256`/`result_bytes` inside `result.json` cannot equal the hash/size of `result.json`'s own final bytes — a file cannot contain its own SHA-256; a canonicalization (which bytes each recorded hash covers, and how self-fields are excluded) is required and is nowhere specified; (b) the manifest "lists every artifact", which includes `result.json`, while `result.json` records the manifest's hash — the two bindings cannot both be computed over final bytes in one write order without a second canonicalization rule; (c) verify-only is never told how `run_id` is derived (no `--run-id` argument exists; fixtures with no or multiple matching runs are unspecified), yet `run_id` matching is load-bearing for 22d's `VERIFICATION_RUN_MISMATCH`; (d) "A payload that merely looks like a result (hand-written or copied) therefore cannot finalize anything" overclaims — a hand-authored, self-consistent result+manifest+artifact chain passes any hash recomputation; the chain proves internal consistency and completeness, not verifier authorship.
- Smallest correction: state the exact canonical hash convention (which serialization each of the five recorded values covers, with the self-referential fields excluded and both product and verify_evidence.py bound to it), state whether `result.json` is listed in its own manifest (or exclude it), and pin verify-only's `run_id` selection rule; scope the anti-forgery sentence to "no evidence chain" rather than "hand-written".

### RV-4 — MAJOR — SEMANTIC_CONTRACT (A-RV-4/B-RV-2 half-closure: the v1 record's equality anchors conflict with plan.md:400)
- Affected: §16.4 (plan.md:113–163), plan.md:400–413 (verification clause), the 24a CONFIRMED oracle (plan.md:158–163).
- Evidence: §16.4's exact v1 record contains `raw_requested_group`, `raw_persisted_group`, `app_identifier`, `fingerprint`, etc. — no `group_key`; its "Matching rules — all exact" bind `raw_requested_group` byte-for-byte, `app_identifier`, and `fingerprint`, and say "A record with a missing key, an unknown key, or any failed equality yields `UNRESOLVED`". plan.md:400 still reads: "parsed content matching exactly — its own `group_key` and `app_identifier` equal the request's … and its own `fingerprint` equal to the request's; for `user_fact:` the artifact must be the preserved user-fact record in the Rev13 format, recording `source_correspondence_result: CONFIRMED` … `raw_requested_group` equal …". §16.4 supersedes the Rev15 base-resolution table but never names or amends the "own `group_key`" clause.
- Mechanism: under plan.md:400 as written, the v1 record fails the group-key equality ("missing key" ⇒ UNRESOLVED), so 24a cannot yield `source_status=CONFIRMED` and the only legal path to resolving the real album's source is undecidable; an implementer must guess whether `raw_requested_group` substitutes for the `group_key` anchor — a semantic choice made silently in implementation. Otherwise the record contract is complete, exact and honest (no normalization of 禎/楨; positive/negative rows 24a–d computable before product output).
- Smallest correction: add one sentence to §16.4 (or amend plan.md:400): "for `user_fact:` v1 records the equality anchors are `raw_requested_group`, `app_identifier` and `fingerprint`; no `group_key` key is required or consulted".

### RV-5 — MAJOR — TEST (Case 20's aftermath asserts a false mechanism and an over-broad remainder)
- Affected: §16.2 (plan.md:66–88), `transaction.py:88–95`.
- Evidence: §16.2: "sends `SIGKILL` to the prepare process (the adapter runs in-process, so the kill ends it too)"; "the only filesystem remainder permitted is the empty control file `CASE_ROOT_20/state/.line-backup-state.lock`"; the adapter "blocks for up to 300 s waiting for `CASE_ROOT_20/release.barrier`, which the driver never creates". The product invokes the adapter as a child process (`subprocess.run([ns.dispatcher, "--counter", …, "--outcome", …])`).
- Mechanism: on macOS a `SIGKILL`ed parent leaves the adapter subprocess orphaned; no parent-death signal exists in this mechanism, so the kill does not end the adapter. The aftermath oracle therefore (a) asserts an unverifiable/ false process-tree fact, (b) omits the orphan's fate (it remains blocked up to 300 s; its termination behavior is unspecified), and (c) as literally written ("only filesystem remainder") is contradicted by the same section's mandated `dispatch-started.barrier` and one-line `dispatch-counter.jsonl` unless scoped to `CASE_ROOT_20/state/`. The state-machine core is sound and deterministic: the adapter writes the counter+barrier before blocking, the rev-1 replacement strictly precedes the adapter invocation, no write is in flight at kill time, and `flock` is released by parent death (the child does not inherit the lock fd).
- Smallest correction: replace the parenthetical with the true mechanism; pin the fixture adapter's orphan behavior (e.g., it observes the parent's death and exits, or the driver records and reaps/kills it after the aftermath snapshot); and scope the remainder sentence to "within `CASE_ROOT_20/state/`, besides the counter/barrier files the protocol mandates".

### RV-6 — MINOR — TEST (case-07 replacement count still contradicted in place)
- A-RV-5 half-closure. plan.md:1051: "requires exactly one `PREPARED` exit 0 and exactly one `CONFLICT_ACTIVE_RUN` exit 4, with one replacement …" contradicts §16.6's "the winner performs exactly **two** guarded replacements (intent 0→1, dispatch record 1→2) and the loser exactly zero". Smallest correction: change line 1051 to "two replacements by the winner, zero by the loser".

### RV-7 — MINOR — TEST (status manifest count/ID list stale)
- A-RV-10 half-closure. plan.md:1162: "The literal manifest enumerates the 18 rows by ID … `candidate`, … `done`." — omits `baseline-worsened`, which §16.7 adds as the nineteenth row. Smallest correction: update the count and the ID list.

### RV-8 — MINOR — COMPATIBILITY (finalize grammar still unconditional for `--verification-json`)
- B-RV-6 half-closure. plan.md:830 and plan.md:1010 list `--verification-json PATH` for finalize without the outcome-conditional rule §16.7 pins (required for VERIFIED, optional for SAFE_ABORT). Smallest correction: annotate both grammar statements.

### RV-9 — MINOR — GROUNDING (§16.6's oracle-assertion claim is not present in the oracles)
- plan.md:201: "The case-02/03/19 oracles assert the exact persisted string form of the reference they produce." The case-02 (plan.md:1046), case-03 (plan.md:1047) and case-19 (plan.md:1056) oracle texts contain no such assertion (only §16.6 itself names `reconcile:` besides the two grammar statements). Smallest correction: add the one-line assertion to those oracles or drop the claim.

### RV-10 — MINOR — GROUNDING (stale roots/paths after §16.1/§16.7/§16.10)
- plan.md:298–299 ("case roots `-01`…`-19` … uses the same arguments minus `--config`/`--run-log`"), plan.md:506 ("case roots `-01`…`-19`"), and plan.md:822 (product verify `--evidence-dir …/20260916-product-verify/attempt-01`, already used on disk, whereas §16.10 pins `attempt-04`). These are explicitly superseded by §16.1/§16.7/§16.10 but remain as contradicting text; the planned-file list heading is likewise stale versus §16.9. Smallest correction: mechanical replacement in one pass.

### RV-11 — MINOR — TEST / SEQUENCING (case-failure routing not extended to cases 20–25)
- plan.md:1057: "Case 01/04/06/08/09/10/11/12 and 13–19 failures are product/test regressions; Case 02/03/05/07 failures stop recovery/concurrency acceptance and prohibit production use." No class is assigned to cases 20–25, including case 20 (the R1 proof) and case 22 (the R4 negatives). Smallest correction: assign 20/23 to the recovery/product-regression classes and 21/22/24/25 to product/test regressions.

## REQUIRED_PLAN_CHANGES
1. RV-1: emit corrected literal Case-01 (and case-NN-shape) argv — canonical children + `--test-mode` — or delete the concrete literals and bind the driver arrays to §16.1's shape; update plan.md:298–299.
2. RV-2: make the legacy validation semantics singular (exception vs 25b) and explicitly amend plan.md:838's "at every load" sentence to §16.5's scope.
3. RV-3: pin the chain's canonical hash convention, the result/manifest listing rule and write order, and verify-only's `run_id` selection; scope the anti-forgery claim.
4. RV-4: state that `raw_requested_group` is the `group_key` equality anchor for `user_fact:` v1 records (amending plan.md:400).
5. RV-5: correct the Case-20 mechanism claim, pin the orphan's fate, and scope the remainder oracle to the state directory.
6. Minors in one mechanical pass: RV-6 (case-07 count), RV-7 (19th status row), RV-8 (finalize grammar), RV-9 (§16.6 oracle claim), RV-10 (stale roots/paths/attempt-01), RV-11 (case-failure routing for 20–25).
7. After the edits: increment `PLAN_REVISION` to 17 on the same TASK_ID, re-snapshot byte-identically, keep review attempts 01–19 and 21 untouched, and submit the new hash for a fresh Stage 02 review before any Handoff.

## RESIDUAL_MINOR_NOTES
- No product CLI, test driver, download, GUI/computer-use tool or admin-command was executed in this review; all product behavior claims are static reads of the hashed sources plus the schema and the read-only real-state parse. The two prior reviews' reproduction evidence was not re-run.
- Explicitly superseded leftovers noted but not counted as findings: the "-01…-19" supersession by §16.1/§16.7 is normative even where the phrase is not literally "through -19"; the authority-negative rows at plan.md:978 will still return `INVALID_AUTHORITY` under the required-children rule (same class, different first check).
- The plan's remaining size (1205 lines) is acceptable for CRITICAL; the recommended revision should correct the named paragraphs in place and avoid adding a further supersession layer.
- This review created only `review/attempt-20/plan-snapshot.md` and this report; nothing else inside or outside the task tree was written.

FINAL_STATUS: PLAN_REVISION_REQUIRED
NEXT_ACTION: Stage 01 revision mode on TASK_ID T20260916-0102-01-line-backup-acceptance: produce PLAN_REVISION 17 closing RV-1…RV-5 (and the RV-6…RV-11 minors in one mechanical pass), keep review attempts 01–19 and 21 untouched, re-snapshot the new plan byte-identically, and submit the new SHA-256 for a fresh Stage 02 review; no Handoff may be compiled from Rev16.
