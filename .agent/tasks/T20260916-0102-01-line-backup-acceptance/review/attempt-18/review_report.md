# Plan Review Report

## REVIEW_METADATA
- TASK_ID: T20260916-0102-01-line-backup-acceptance
- REVIEW_ATTEMPT: 18
- REVIEWED_PLAN_REVISION: 15 (CANDIDATE)
- REVIEWED_PLAN_SHA256: e22373ee04894d2625c9f8c240276b099a66ca52f7b4d6127b4028f4eb0741e4
- PLAN_SNAPSHOT_PATH: .agent/tasks/T20260916-0102-01-line-backup-acceptance/review/attempt-18/plan-snapshot.md (154021 bytes; `cmp` byte-identical to the reviewed plan; same SHA-256)
- Repository anchor observed: /Users/hsiaojohnny/Documents/ChatGPT/Line_backup (read-only review; no product CLI, test driver, download, or GUI execution)
- Reviewer runtime/model: Codex CLI, fresh Stage-02 context (exact model id not exposed; informational only)

Preflight: SHA-256 of the current `plan.md` recomputed before review and equals `e22373ee04894d2625c9f8c240276b099a66ca52f7b4d6127b4028f4eb0741e4`; the snapshot was copied before any canonical change and verified byte-identical (154021 bytes both).

## OWNER_VERDICT
The goal this plan serves: prove, safely and provably, whether the existing 57-image destination is a real backup of the exact requested LINE album (group 「旻謙允禎成長日記」 禎 U+798E, 2024/05/13–05/17), and fix the R1–R7 holes in the backup process itself, without touching formal state/photos and without re-downloading anything.

Essential (CORE): the R1–R7 repairs through the real CLI with literal, rerunnable argv; independent oracles that never read product PASS lines as truth; exactly one human gate (the part-2 user-fact question allowing 「不知道」, plus one ellipsis observation, and nothing else); 禎 and 楨 never merged; three results reported separately (album data / reusable capability / overall closure).

Optional/deferred: production Save-All/download, bridge repair, migration, OCR, historical cleanup (plan.md:946). Supporting: bridge/service diagnosis only if causal evidence proves necessity.

Global blockers that would stop the system: none intended, and the plan keeps its stop paths safe. But five material defects must be fixed before approval: (1) Case 01 — the flagship closed loop — cannot execute as its own literal argv is written (BLOCKER, RV-1); (2) the post-fix proof for R1 no longer includes the genuine parent hard-kill the user and handoff require (RV-2); (3) the finalize verification-JSON refusals (the R4 fix) have no literal case rows (RV-3); (4) the only path that can ever confirm the real album's source — a future CONFIRMED user-fact artifact — has an unpinned, self-inconsistent schema (RV-4); (5) "validate every run against the skill schema at every load" contradicts the required legacy read-only acceptance of the real formal state, which does not satisfy that schema (RV-12).

Complexity verdict: proportionate to a CRITICAL acceptance harness carrying 17 prior review attempts; the dispatch-window relocation actually simplifies the state machine. The economy cost is concentrated in the five defects above — places where normative text is over-broad or duplicated exactly where precision matters most.

Biggest remaining risk: the wave going green on fixtures while the flagship closed loop is unsatisfiable as literaled and the genuine-kill R1 path stays unproven — i.e., another round of PASS numbers instead of verification, which the user explicitly rejected.

## GOAL_BASELINE
Reconstructed from the authoritative sources before reading the Plan's framing: the user's `/goal` instruction (`/Users/hsiaojohnny/.codex/attachments/210176d8-3c5b-4400-8443-d74a5ffcaa65/pasted-text-1.txt`), `handoff.md` H2.1 §0–§14 (including §7 R1–R7 and §8 Phase 0–7), and `evidence/20260916-user-fact/source-identity-user-fact.json` (5530 bytes; structure re-verified in this review).

- Primary goal: make the verification real — reproduce and fix handoff §7 R1–R7 through the real entrypoints and produce rerunnable, independently re-readable tests and evidence. The user's ultimate goal is fully automatic backup with no human intervention; this round must not inflate PASS counts or documentation.
- Scope: exactly one album — LINE app `jp.naver.line.mac`, group 「旻謙允禎成長日記」, album 2024/05/13–05/17, 57 images. Never re-download the 57 images for testing. Formal config, state, run-log and the 57 photos stay read-only unless the user gives a separate precise gate.
- Source identity (blocker 1): the requested group is 禎 (U+798E); formal config/state carry 楨 (U+6968). Merging, normalizing, or rewriting the canonical key is prohibited, as is inferring identity from equal dates/counts/hashes. Only two legal resolutions: an authoritative exact source join, or one precisely preserved user fact (original question, original answer, supplier and time, evidence SHA-256; the handoff §5 screenshot may be used to ask the one question).
- Recorded user fact (2026-09-16): part 1 answered 「正確是「禎」」 — the visible title string is the 禎 string; part 2 (whether the 57 files are that album's backup) is `UNANSWERED`; `source_correspondence_result_at_recording: UNRESOLVED`; `merge_prohibited: true`. Album attribution is therefore unresolved at baseline and may only be resolved by a part-2 answer or an authoritative join.
- GUI (blocker 2): after all non-GUI work is complete, exactly one precise gate — with LINE already showing the exact target, authorize exactly one current-target ellipsis observation input, capture immediate post-observation evidence, stop. It authorizes no menu item, Save All, chooser, keyboard shortcut, state write, or download; historical ellipsis credit (2/2) is exhausted.
- Phases: 0 read-only baseline → 1 isolate/archive old evidence (no deletes/moves) → 2 reproduce R1–R7 via real entrypoints (each test declaring claim / entrypoint / external oracle / decision effect; never pre-announced fault flags to manufacture safe behavior) → 3 CRITICAL loop (plan revision bump on the same task, fresh independent review, recompiled handoff, fresh implementer) → 4 minimal complete fix (never change expectations or relax authority to pass) → 5 risk-appropriate regression plus true integration (the fake adapter may replace external I/O only and must never manufacture VERIFIED) → 6 the one GUI gate if needed → 7 independent acceptance and result. Prior approvals (Rev13, review/attempt-16) do not carry over.
- Evidence rules: per attempt — complete inputs; argv/env with secrets stripped; stdout/stderr/exit; tested-program hashes; independent side-effect counter; before/after state; artifact manifest (SHA-256 + bytes); everything independently readable; append-only (existing evidence/attempt/result.md never overwritten); `/private/tmp` is never long-term storage.
- Closing semantics: three separately reported results — album data, reusable automation capability, overall closure; complete only when the first two both hold; offline or fixture PASS must never masquerade as GUI end-to-end success.
- Hard prohibitions and escalation: no admin/authorization-triggering commands (sfltool, sudo, TCC tools); no AXPress/AX write, coordinate guessing, OCR-only pass, sandbox workaround; no re-dispatch on an unknown dispatch outcome; wrong album ⇒ stop immediately. Escalation triggers include an independent review again finding self-certification or cross-test pollution.

## GOAL_ALIGNMENT
- `PRIMARY_OUTCOME` (plan.md:491) matches the baseline clause 1 exactly: establish whether the existing 57-image destination is a valid backup of the exact source group, and otherwise stop without any ambiguous or duplicate production transaction.
- `CORE_REQUIREMENTS` (plan.md:493-499) match the baseline phases and scope: prove-or-stop on exact source correspondence; read-only filesystem/inventory/state/registry/intent/writer reconciliation; a real reusable transaction process (no test-only model); the shortest safe route decision; independent acceptance; production Save-All explicitly out of this wave.
- The plan does not let fixtures claim production status: E2E rationale (plan.md:10), three-result separation (plan.md:444-458), and DONE prerequisites (plan.md:940) all require real evidence for closure.
- Stop path is safe: unresolved source identity ⇒ `PRIMARY_OUTCOME_STATUS=UNKNOWN`, source/core check BLOCKED, no DONE (plan.md:942).
- 禎/楨 handling matches blocker 1: UTF-8 code-point equality, never merged/normalized/folded (plan.md:144-146); part 1 resolves only the visible-title character, not the 57-file correspondence (plan.md:260-262, 448-451); the requested key may legally differ from config.group_key with the resulting SOURCE=UNRESOLVED (plan.md:662-664).
- Caveat: the only path that can ever confirm the real album's source (a CONFIRMED user-fact artifact after a part-2 answer) is under-pinned and inconsistent with the preserved artifact's actual shape — RV-4. The plan's safety posture holds, but goal attainment for the real album cannot be implemented or tested as specified.

## NECESSITY_AND_TRACEABILITY
- Every F1–F8 repair traces to a reproduced R1–R7 gap (plan.md:284-297): F1/F8 → in-prepare dispatch window and reconciliation-only resume (§15.1); F2 → schema-legal external binding (§15.2); F3 → `--source-evidence` record and refusal classes (§15.3); F4/F5 → verifier/status matrix additions (§15.4); F6 → driver ownership isolation (§15.5); F7 → revision binding (§15.6).
- §15.5's full driver list (plan.md:241-246) traces to the reproduced R6 destruction; covering every driver, not just the two that caused it, is justified by "same root cause twice" risk.
- The 18-row status fixture table (plan.md:876-905) traces to the v4.2 status contract; the six orthogonal status fields are required by the baseline closing semantics.
- Non-gating/supporting items are explicitly labeled and justified: `BRIDGE_READINESS` SUPPORTING/DIAGNOSTIC/NON_GATING (plan.md:865); `DOCUMENTATION_RETENTION_HEALTH` SUPPORTING with a scoped rationale tied to independent-acceptance reproducibility (plan.md:866).
- Requirement-to-verification traceability gaps (findings, not scope creep): R1 parent hard-kill proof (RV-2), R4 finalize negatives (RV-3), CONFIRMED user-fact positive/negative path (RV-4), prepare storage-fault semantics (RV-6).
- No untraceable material work was found; Cases 13–19 all map to defined refusal classes.

## GATE_AND_VETO_AUDIT
- Gates and their rationale: prepare refusals `MISSING/INVALID_SOURCE_EVIDENCE`, `MISSING_DISPATCHER` (plan.md:195-197); resume adapter-option rejection (plan.md:750); commit precondition `CONFLICT_UNRESOLVED_DISPATCH` (plan.md:751); finalize binding gate `BINDING_UNVERIFIED` (plan.md:151, 344-349); verifier exit classes 1/2/4 (plan.md:656, 664); source integrity non-waivable (plan.md:928).
- Proportionality: `SOURCE_CORRESPONDENCE` is OUTCOME/CORE/HARD_CLEAN and non-waivable — justified directly by the user's blocker 1 (no merge, no inference). `BASELINE_REGRESSION_DELTA` is CORE MUST_NOT_BREAK with explicit WORSENED=hard-failure semantics (plan.md:864, 868). `DOCUMENTATION_RETENTION_HEALTH` is supporting with a real correctness rationale (independent reproducibility of acceptance) and a waiver path that preserves the original FAIL (plan.md:890, 905, 928) — proportionate.
- No self-waiver path found: waivers require named authority and the seven fields of plan.md:928, and core source/data-integrity failures are non-waivable.
- Defect: plan.md:579 ("Every run and every registry entry must validate against … `$defs/run` / `$defs/state` … at every load and before every replacement; a payload that fails validation is refused with no write") is an unintended global veto that refuses the very formal state the plan then mandates accepting read-only as LEGACY (plan.md:658-664) — RV-12.
- Minor: when both `--source-evidence` and the adapter pair are absent, the plan does not state which refusal class wins (RV-8).

## COUPLING_AND_FAILURE_CONTAINMENT
- The single in-process dispatch window inside `prepare` plus reconciliation-only `resume` removes the cross-process ambiguity that caused R1; resume can never reach the dispatcher (plan.md:31-105, 750).
- Failure is contained at the narrowest boundaries: guarded compare-and-commit; one-way ambiguity barrier for UNKNOWN (plan.md:74, 788); no retry interpretation of an unknown dispatch; per-axis verifier statuses with distinct exits 1/2/4; formal state read-only (plan.md:452-453, 936); fixture bindings only under test mode (plan.md:130-132).
- Self-certification (adversarial focus 2): no path found where the product reads its own output as an oracle. The side-effect counter is written by the injected adapter and read independently, and the product never reads it (plan.md:743, 782); the binding artifact is operator/driver-authored and re-hashed at its external recorded path against pre-recorded bytes (plan.md:144-164, 350-364); `reconcile:` evidence is product-authored but explicitly cannot satisfy source-CONFIRMED (plan.md:120-123); case oracles are computed before product execution (plan.md:764); status output is marked `scenario_table_non_acceptance` and may not be cited as Stage-05 evidence (plan.md:236, 903). This substantively closes the user's repeat-escalation trigger for self-certification.
- Cross-test pollution (adversarial focus 3): §15.5 pins an ownership-marker/no-default-removal protocol for every driver and script that creates or removes a shared root, with both-order read-back (plan.md:241-253); case roots are literal `-01`…`-19`; barriers, counters, and evidence dirs are per-case. Residual: the on-disk phase-2 scripts still `shutil.rmtree` shared roots without markers (verified: `tests/automation_verification/fixtures.py:55`, `harness.py:185`, `run_phase2_r1_crash_window.py:41`, `r2:31`, `r3:64/311/416`, `r4:85`, `r5:141/306/309`, `r6:226`, `r7:73/142`) — the §15.5 protocol must land before those are re-run. That is an implementation obligation the plan itself owns, not an unclosed design defect.
- One shared lock covers acquire/resume/commit/finalize/duplicate-skip/cleanup with deterministic barriers rather than timing (plan.md:740).

## DESIGN_ECONOMY
- Moving the dispatch window into `prepare` and demoting `resume` to reconciliation-only is a net simplification of the state machine versus resume-dispatch plus crash flags; it is the smallest design that closes F1/F8 (plan.md:31-105).
- The `--source-evidence` record is one new artifact, required by F3/R3(a) and by the need for operator-supplied external provenance; it pays for itself.
- The driver ownership protocol is required by the reproduced R6 destruction; it pays for itself.
- Deletion test: Cases 13–19 all trace to refusal classes; the 18 status rows are required by v4.2; verifier rows trace to R5/R6-class defects and the binding negative-control matrix. Nothing found removable without failing an acceptance or safety obligation.
- Complexity verdict: proportionate overall. The avoidable cost is textual: duplicated/over-broad normative statements (plan.md:575 vs 755/584/786; plan.md:579 vs 658-664; plan.md:120 vs 96) inflate implementer risk without adding value; fixing them by making one statement canonical and deleting the stale duplicates reduces, not increases, complexity.

## CRITICAL_PATH_AND_PRIORITY
- Ordering is correct: preserve/reproduce evidence first, then fixes, then baseline pre-artifact before any product edits (plan.md:870-874), re-run the wave, both-order read-back (plan.md:251-253), real read-only verify-only pass (plan.md:563), and the human gate strictly last ("after all non-GUI work is complete", plan.md:429).
- CORE items precede supporting items; `BRIDGE_READINESS` is non-gating unless route-specific necessity is proved; route can be `ROUTE_NOT_NEEDED` with explicit rationale if exact provenance is already proven and no side effect is needed (plan.md:837, 942).
- No priority inversion found. RV-2/RV-3 are coverage gaps on the CORE path, not ordering errors.
- RV-11: the post-fix wave's durable evidence root/attempt number is not explicitly named (only `evidence/20260916-baseline/attempt-01/…` is pinned at plan.md:874, and phase-2 attempt-01 exists on disk).

## REQUIREMENT_FIDELITY
- Scope fidelity: one album only; no re-download (plan.md:10, 606); formal state/photos read-only (plan.md:452-453, 936); no production writes in this wave (plan.md:936). No scope deviation found.
- 禎/楨 fidelity: exact code-point equality, never merged/normalized/folded (plan.md:144-146); part 1 resolves only the visible-title character (plan.md:260-262); the 57-file correspondence stays UNRESOLVED (plan.md:448-451, 662-664); the preserved artifact's `merge_prohibited` and `raw_persisted_group` are treated as evidence only, never to satisfy equality (plan.md:148-155).
- Three-result closure fidelity: separately reported results and DONE only when album + capability hold (plan.md:444-458, 940); UNRESOLVED/LEGACY cannot be promoted to closure (plan.md:940).
- Human gate fidelity (adversarial focus 6): exactly one request containing exactly two items — the part-2 question allowing 「不知道」 and one ellipsis observation — with an explicit no-authorization list, exhausted historical budget, and no state write/download/production transaction (plan.md:429-442, 816, 823-824). No smuggled authorization found. 禎 and 楨 are never merged. Caveat: the CONFIRMED user-fact artifact that the part-2 answer is supposed to produce is under-pinned (RV-4), and Case 18/19 pre-state semantics are under-specified (RV-9).
- Evidence-rule fidelity: per-attempt raw inputs, argv/env, stdout/stderr/exit, program hashes, independent counter, before/after state, manifest read-back, both-order read-back, append-only attempts, /private/tmp working-space-only (plan.md:250-253, 764-766, 907-912).
- Deviations found are internal inconsistencies only: RV-1, RV-4, RV-12 (plus minors).

## GROUNDING_AND_DRIFT
Verified in this review (static reads/hashes only; no product execution):
- Formal state at `/Users/hsiaojohnny/Documents/Codex/2026-09-07/line-album-backup-line-backup-state/state/backup_state.json`: SHA-256 `e9313a563bf298d4b1e9ae243c5d3d404ad1333f69cb71b156e68589a8ec2f59`, 48146 bytes, revision 39, 5 runs, 4 verified_albums, `current_run_id`/`active_writer_id` null — matching the plan's baseline claims.
- Program defects the plan describes are real in the working tree: `transaction.py:14` writes the `[1,1]` "test-mode adapter calibration fixture" and `transaction.py:16` writes `source_provenance`/`owner_id` (both violate `$defs/run`); `verifier.py:172` trusts a `source_authority` string; `verifier.py:206` inspects only `samples[0]`; `common.py:75-84` locks with `fcntl.flock`.
- Schema ground truth: `$defs/calibration` is `additionalProperties:false` with exactly the 8 keys `confidence, dot_spacing, ellipsis, evidence, observed_at, save_all_point, screenshot_height, screenshot_width` (all required); `$defs/intent` requires `calibration`; `verified_albums` item keys are exactly `{group_key, fingerprint, verified_run_id, destinations, source_kind, evidence}`; `$defs/run`/`$defs/state` are `additionalProperties:false`.
- Real state vs schema (grounds RV-12): 4 of 5 runs have no `contract_revision`; `RUN-20260907-132845-01`'s `intent.calibration` carries forbidden `first_row_point`/`row_step`; `RUN-20260907-152217-01` has no `calibration` at all (fails `$defs/intent`'s required key). A strict "refuse any payload that fails validation at every load" therefore refuses the real authority, contradicting plan.md:658-664.
- Preserved user-fact artifact: top-level `question`/`answer` objects; `source_correspondence_result_at_recording: "UNRESOLVED"`; `facts.app_bundle: "jp.naver.line.mac"`; no `fingerprint` anywhere; no `app_identifier`; supplier/time are `supplied_by`/`recorded_at_local` — grounds RV-4 against the plan's field list at plan.md:147-155.
- Phase-2 evidence exists on disk with the reproduced verdicts (e.g. `phase2-r1/observation.json` → `REPRODUCED_DUPLICATE_DISPATCH`, with kill-state and counter artifacts) and `readback-verification.json` verdict PASS with zero problems — grounds the plan's Reproduced table (plan.md:284-290) and the claim that these scripts must be re-run under the §15.5 protocol.
- No other anchor drift found: plan.md:813-815 ledger hashes, plan.md:279 program hashes against handoff §4, and the §15.6 self-references now read 15 (plan.md:255-259). The only stale artifact list is RV-13.

## ARCHITECTURE_AND_CONTRACTS
- Fixed call graph is coherent and singular: `cli.main` → verifier (`inspect_filesystem`/`bind_registry_state_source`) or `transaction prepare/resume/commit/finalize/duplicate-check` → source-evidence load/validate → shared precondition evaluator → guarded compare-and-commit → dispatcher adapter once inside prepare's dispatch window; `status` → `status.evaluate`. Tests invoke the CLI subprocess and no second transition model exists (plan.md:581).
- RC2 contract: separate `intent_state`/`dispatch_state`/`trigger_outcome`/`dispatch_outcome`, `contract_revision=1.0-rc2` for new runs, legacy records read-only normalized (plan.md:578-579, 662).
- Schema-legal persistence: no `owner_id`/`source_provenance` fields; ownership via `state.active_writer_id` + `intent.owner_execution_id`; binding references live only in the checkpoint-shaped `events[].evidence` string and `verified_albums[].evidence` (plan.md:112-117). This resolves R4's "schema forbids the field" problem without smuggling test-only data into production records.
- Binding grammar `join:<relpath>:<sha256>` | `user_fact:<relpath>:<sha256>` | `fixture:<relpath>:<sha256>`, with `reconcile:` restricted to reconciliation evidence that can never satisfy source-CONFIRMED (plan.md:119-123, 130-139). Defect: the reconciliation grammar is stated twice with different shapes (RV-7), the absolute/relative conversion and both-missing tie-break are not fully pinned (RV-8), and the validation scope contradicts the legacy profile (RV-12).
- Duplicate precedence is explicit and non-interchangeable: `duplicate-check` → `SKIP_DUPLICATE`; matching terminal `resume` → `SKIP_TERMINAL` (plan.md:745-746, 485). No substitution allowed in oracles.

## DATA_SECURITY_RELIABILITY
- Trust boundaries: formal authority read-only; external binding artifact never copied/rewritten by the product (plan.md:133-139); path containment with realpath, no `..`/symlinks, base resolution pinned for `join:`/`user_fact:` (workspace root) and `fixture:` (project root) (plan.md:125-132); evidence written only under `--evidence-dir`; no secrets in argv/env captures.
- Reliability: guarded CAS plus shared lock; uncertainty barrier written before any UNKNOWN interpretation; READBACK_UNCERTAIN leaves the exact committed payload and a fresh resume returns SKIP_TERMINAL without replay (plan.md:742); resume idempotent (plan.md:104). Storage faults are specified as separate from dispatcher faults.
- Residual gaps: lock/control-file behavior after a genuine parent death inside the dispatch window is unspecified and untested (RV-2); prepare's own two guarded replacements have no declared storage-fault semantics/rows (RV-6).

## IMPLEMENTATION_SEQUENCE
- Sequence is coherent with the CRITICAL harness: preserve/reproduce → minimal fixes → rerun → read-back (both orders) → real verify-only → human gate last (plan.md:284-297, 429, 600-620, 930-936). Stage 03/04/05 boundaries are stated and consistent (plan.md:932-934).
- Baseline pre-artifact is captured before product edits with a literal command, environment fingerprint, and INCOMPLETE-not-PASS routing (plan.md:870-874) — correct.
- RV-13: the "Planned implementation files, to be created only after this plan is independently approved" list (plan.md:543-556) is stale versus the repository: it names `src/line_backup_acceptance/verify.py` (the real module is `verifier.py`, imported at `cli.py:9`), lists already-existing files as to-be-created (`__init__.py`, `cli.py`, `transaction.py`, `status.py`) and omits existing `authority.py`, `common.py`, `__main__.py`; `tests/run_all.py` and `tests/verify_evidence.py` are treated as deliverables (plan.md:246) but do not exist. Small correction, but it must be fixed so Stage 04 does not create a duplicate module or a false file plan.

## TESTABILITY_AND_ACCEPTANCE
- Credit: literal root names and fully expanded literal argv with a driver rejection of ellipses/nonexistent roots (plan.md:571, 754-764); oracles computed before product execution and never a product PASS line (plan.md:764); post-state/counter/stdout/stderr/exit/hash/manifest read-back after every process (plan.md:766); both-order ownership read-back (plan.md:251-253); the 18-row executable status fixture table with all six statuses, scoped blocker fields, baseline fields and waiver fields, plus the `evidence_basis == "scenario_table_non_acceptance"` non-acceptance oracle (plan.md:876-905, 236, 903); Stage-05 snapshot fields (plan.md:907-912); DONE prerequisites (plan.md:940). The v4.2 checklist fixtures (canonical incident/implementation blocker/replan/CORE fail-blocked-not-run-not-required/baseline unavailable/hard-clean debt/waiver preserving original result/independent acceptance blocked and product defect/contradictory state/legacy normalization/DONE prerequisites) are all present.
- Defects: RV-1 (Case-01 closed loop unsatisfiable as literaled: verify-only authority rule requires canonical `config/` + `state/backup_state.json`, but the literal argv uses `case-01/state.json` with no verifier test-mode allowance for case roots — INVALID_AUTHORITY exit 2 before any result.json exists; the commit/finalize argv then point at that nonexistent file, and the claimed run-2 `source CONFIRMED` cannot occur); RV-2 (no genuine parent-SIGKILL row post-fix; handoff §8 R1 explicitly requires 父程序硬中斷 without `--crash-after-dispatch`); RV-3 (`INVALID_VERIFICATION_EVIDENCE`/`VERIFICATION_RUN_MISMATCH` defined only at plan.md:348, with no case row feeding finalize a forged PASS/57, FAIL/0, `{}` or another run's JSON — exactly the R4 payloads); RV-5 (Case 07 "one replacement" vs the two-replacement prepare); RV-6 (no prepare storage-fault row); RV-9 (Case 18 pre-state ownership and Case 19 SAFE_ABORT verification-json semantics); RV-10 (no executed WORSENED baseline row), RV-11 (post-fix evidence root unnamed).

## SCOPE_AND_COMPLEXITY
- Scope is bounded and deferred items are explicit (plan.md:946): no production Save-All/download (plan.md:10, 606, 823, 936), no bridge repair without causal necessity, no migration/OCR/historical cleanup. No scope creep found.
- Complexity is justified by the CRITICAL class and the adversarial history; no unjustified global gates and no over-engineering beyond the five defects' textual over-breadth. The one scope-adjacent risk is RV-4 (the album-result path is under-pinned), which affects goal attainment rather than scope.

## FINDINGS

### RV-1 — BLOCKER — TEST (satisfiability of the flagship closed loop)
- Affected: §15.1 grammar/authority rule (plan.md:575), test-only form (plan.md:572), §15.2 positive-control sentence (plan.md:164), literal Case-01 verifier argv (plan.md:755), Case-01 expected results (plan.md:584, 786).
- Evidence: plan.md:755 runs `verify-only --project-root /private/tmp/line-backup-acceptance-case-01 --config /private/tmp/line-backup-acceptance-case-01/config/line_backup_config.json --state /private/tmp/line-backup-acceptance-case-01/state.json …` with no verifier `--test-mode`. The authority rule (plan.md:575) requires verify-only `--config`/`--state` to be the canonical children `project_root/config/line_backup_config.json` and `project_root/state/backup_state.json`, and permits verifier `--test-mode` only for the literal `/private/tmp/line-backup-acceptance-verifier` root; a mismatch returns `INVALID_AUTHORITY` exit 2 before any read. The transaction test-mode form uses `--state project_root/state.json` and omits config/run-log (plan.md:572), and no case-root config/run-log fixture is defined anywhere.
- Mechanism: verify run 1 cannot execute as written (state path not canonical, and no case-root config exists) — it exits 2 with no `evidence/verify-1/result.json`; the literal commit/finalize argv (plan.md:756-757) reference that nonexistent file; the claimed run-2 `filesystem PASS / registry PASS / source CONFIRMED / state EXACT / overall PASS` (plan.md:786) and the §15.2 claim that the positive control "is the valid external binding in case 01's closing loop" (plan.md:164) are unsatisfiable, and §15.1's sentence that Case 01 runs "all under CASE_ROOT_01 with the literal state" (plan.md:584) contradicts the authority rule. A literal driver would either fail the wave for the wrong reason or be patched ad hoc, invalidating the oracle — the exact "PASS numbers instead of verification" failure the user prohibited.
- Smallest correction: define one canonical case-root verify layout (`case_N/config/line_backup_config.json`, `case_N/state/backup_state.json`, `case_N/state/run_log.md`), make the transaction test-mode state path the same canonical file (or explicitly extend the verifier test-mode authority to the literal case roots with a read-only canonical path mapping), and update plan.md:572, 575, 584, 755, 786 to one consistent story where both invocation families read the same state bytes.

### RV-2 — MAJOR — TEST (R1 post-fix proof loses the genuine hard-kill)
- Affected: Case 02 (plan.md:76, 585, 787), §15.1 crash semantics (plan.md:74-76), R1 row (plan.md:284).
- Evidence: handoff §8 R1 (handoff.md:224) requires the normal adapter side effect to be followed by 父程序硬中斷 before/around the commit, then a fresh resume, explicitly **without** `--crash-after-dispatch`; the plan's own pre-fix reproduction used a real SIGKILL (plan.md:284; on-disk `phase2-r1/` contains kill-state and counter artifacts). Post-fix, the only R1-family row is Case 02, which uses `--crash-after-dispatch` with the parent alive and a deliberate skip of the rev-2 commit; no case kills the parent process inside the prepare dispatch window (no SIGKILL/kill row exists anywhere in the plan).
- Mechanism: the fixed design's central claim — a genuine parent death between the side effect and the rev-2 dispatch-record commit leaves exactly one dispatch, recoverable by reconciliation-only resume without re-dispatch — is never executed post-fix. In addition, the plan never states the expected lock/control-file state after parent death (the implementation locks with `fcntl.flock` at `common.py:75-84`), so a real operator kill can land in an untested state class. This also conflicts with the user's Phase-2 rule against using pre-announced fault flags to manufacture safe behavior, and with the explicit R1 wording.
- Smallest correction: add a post-fix case that SIGKILLs the parent during `prepare` inside the dispatch window (counter line persisted, rev-2 commit not yet durable, barrier-controlled timing), then runs a fresh `resume --no-dispatch` with an oracle of exactly one barrier replacement, no second counter line, and `CONFLICT_UNRESOLVED_DISPATCH` on commit; pin the expected lock/control-file aftermath for the killed parent.

### RV-3 — MAJOR — TEST (R4 finalize negatives have no case rows)
- Affected: F3 finalize validation (plan.md:344-349), case table Cases 01–19.
- Evidence: `INVALID_VERIFICATION_EVIDENCE` and `VERIFICATION_RUN_MISMATCH` occur exactly once in the plan (plan.md:348). No case row feeds finalize a forged PASS/57 JSON, a FAIL/0 JSON, `{}`, or another run's JSON — the exact payloads R4 reproduced (plan.md:287). Case 09/10 use a real product verifier result; Cases 13–19 cover source-evidence, duplicate, reconciliation and unresolved-dispatch refusals only.
- Mechanism: the R4 fix ("finalize must validate the supplied verification JSON before any write") is specified but not regression-tested; a future regression that re-trusts arbitrary JSON would pass the wave and re-create the reproduced defect silently.
- Smallest correction: add literal finalize rows (or extend Case 10 pre-states) for each R4 payload with exact class, exit 4, no revision change and no registry entry; include one `VERIFICATION_RUN_MISMATCH` row (schema-valid JSON for a different run).

### RV-4 — MAJOR — GROUNDING (also TEST: the only real-album confirmation path is unpinned)
- Affected: §15.2 `user_fact:` rule (plan.md:147-155), sole-closure basis (plan.md:487), DONE prerequisites (plan.md:940), human-gate part-2 flow (plan.md:432-435).
- Evidence: the plan requires the preserved user-fact record "in the Rev13 format, recording `source_correspondence_result: CONFIRMED` with the exact question, answer, supplier and time, `raw_requested_group` equal to the requested key's group string exactly, `app_identifier` equal, and `fingerprint` equal". The preserved artifact (`evidence/20260916-user-fact/source-identity-user-fact.json`, 5530 bytes) has top-level `question`/`answer` objects, `source_correspondence_result_at_recording: "UNRESOLVED"`, `facts.app_bundle: "jp.naver.line.mac"`, and **no** `fingerprint`, **no** `app_identifier`, and **no** `source_correspondence_result` field; supplier/time are `supplied_by`/`recorded_at_local`. "Rev13 format" is not reproduced anywhere in this revision, and no other file defines a CONFIRMED variant. Case 01's positive control is `fixture:` in test mode, so no positive `user_fact:` case exists.
- Mechanism: the one legal real-world resolution for blocker 1 (a part-2 answer recorded as a CONFIRMED user fact) must be authored in a schema that the plan itself does not pin and whose field list does not match the only preserved example; an implementer must invent the accepted schema — risking refusal of a legitimately answered fact, acceptance of malformed evidence, or an oracle that cannot be computed ahead of the product. Since exact source correspondence is the sole closure basis (plan.md:487, 940), this is the goal-critical path for the real album.
- Smallest correction: pin the exact CONFIRMED user-fact artifact schema (field names, nesting, how `fingerprint` and `app_identifier` are derived, supplier/time fields mapped to the preserved names), state who authors/authorizes it and where it is stored relative to the workspace base, and add one positive and one negative literal case (valid CONFIRMED → Source CONFIRMED only through the full external re-hash path; missing/UNANSWERED → UNRESOLVED).

### RV-5 — MINOR — TEST (Case 07 replacement-count oracle)
- Affected: Case 07 (plan.md:790) vs §15.1 (plan.md:65-67) and Case 01 (plan.md:786).
- Evidence: a successful prepare performs two guarded replacements — intent `0→1`, then the dispatch record `1→2`; Case 07's oracle says "one replacement".
- Mechanism: an implementer or driver could validate one replacement as the expected count and either fail a correct implementation or encode a wrong oracle for the concurrency case.
- Smallest correction: state that the winner performs exactly two guarded replacements and the loser zero, and align the oracle text.

### RV-6 — MINOR — TEST (prepare storage-fault semantics absent)
- Affected: test-only grammar (plan.md:572), storage-fault rows Case 08/09 (plan.md:791-792).
- Evidence: `--storage-fault` is declared for the test-mode form without operation scoping, but only commit (`WRITE_BEFORE_REPLACE`, Case 08) and finalize (`READBACK_UNCERTAIN_AFTER_REPLACE`, Case 09) have rows; prepare's own two guarded replacements have no declared storage-fault semantics or cases.
- Mechanism: the storage-fault analogue of the R1 window (dispatch performed, rev-2 commit fails) is untested and its expected ambiguity-barrier/recovery behavior is undefined; a prepare write failure could be misclassified.
- Smallest correction: declare which storage faults prepare accepts and add rows for both replacement slots with exact expected classes and barrier/recovery oracles.

### RV-7 — MINOR — SEMANTIC_CONTRACT (reconciliation grammar stated twice, differently)
- Affected: plan.md:96-97 vs plan.md:120-121.
- Evidence: one text uses `reconcile:<kind>:<relpath>:<sha256>`; the other uses `reconcile:<relpath>:<sha256>`.
- Mechanism: the persisted reconciliation `evidence` reference is schema-adjacent; two grammars mean the product and its verifier can disagree about a valid reference.
- Smallest correction: keep exactly one grammar (the kind-bearing form if the kind is persisted) and delete the other.

### RV-8 — MINOR — GROUNDING (path conversion and refusal tie-break unpinned)
- Affected: §15.2 base rules (plan.md:125-129, 133), §15.3 record shape (plan.md:180-181), §15.3 refusal order (plan.md:195-197).
- Evidence: `--source-evidence`'s `binding.artifact.path` may be "absolute or workspace-relative", but the conversion to the persisted `<relpath>` is not stated; `MISSING_SOURCE_EVIDENCE` and `MISSING_DISPATCHER` are listed in one bucket with no precedence when both are absent.
- Mechanism: two implementations can persist different-but-"valid" references, or return different refusal classes for the same invocation, breaking oracle equality.
- Smallest correction: pin the absolute→relpath conversion rule (and reject anything that is not expressible as a normal relative path under the base) and state the exact precedence when both inputs are missing.

### RV-9 — MINOR — TEST (Case 18/19 pre-state and SAFE_ABORT semantics under-specified)
- Affected: Case 18 (plan.md:764), Case 19 (plan.md:764, 788), finalize grammar (plan.md:751).
- Evidence: Case 18's `NEEDS_RECONCILIATION` refusal requires a non-terminal fingerprint barrier without a live owner, or `CONFLICT_ACTIVE_RUN` (listed first in the refusal order at plan.md:195-197) would fire first; the plan does not pin the pre-state's `active_writer_id`/lock. Case 19 passes `--outcome SAFE_ABORT --expected-revision 2` while the grammar requires `--verification-json` for finalize; SAFE_ABORT in an UNKNOWN-dispatch state has no stated verification-json semantics (required/validated/ignored).
- Mechanism: two literal rows can produce different refusal classes depending on fixture details that are not written down, weakening the oracle.
- Smallest correction: pin Case 18's pre-state ownership/lock fields and Case 19's verification-json requirement/semantics for SAFE_ABORT.

### RV-10 — MINOR — TEST (no executed WORSENED baseline fixture)
- Affected: `BASELINE_REGRESSION_DELTA` (plan.md:864, 868), executed fixtures (plan.md:903, 905).
- Evidence: `baseline_delta=WORSENED` is defined as a hard failure, but the executed fixture list covers only `baseline-unchanged` (UNCHANGED) and `baseline-unavailable` (INPUT_UNAVAILABLE).
- Mechanism: the hard-failure branch of the baseline gate is never exercised; a status implementation that maps WORSENED to INCOMPLETE or PASS would not be caught.
- Smallest correction: add one executable WORSENED fixture row with its scoped blocker/routing oracle.

### RV-11 — MINOR — TEST (post-fix evidence root unnamed)
- Affected: §15.5 evidence rule (plan.md:250-251), baseline path (plan.md:874), wave evidence plan.
- Evidence: only `evidence/20260916-baseline/attempt-01/…` is named; the post-fix wave's durable evidence root/attempt number is not explicitly named (phase-2 attempt-01 exists on disk today).
- Mechanism: durable evidence could be written to an ad-hoc path, weakening independent re-readability and attempt provenance.
- Smallest correction: name the exact durable evidence root and attempt IDs for the post-fix wave.

### RV-12 — MAJOR — COMPATIBILITY (also GROUNDING: every-load validation contradicts the legacy profile)
- Affected: plan.md:579 and §15.2 (plan.md:108-110) vs the legacy acceptance profile (plan.md:658-664).
- Evidence: the plan requires every run and registry entry to validate against `$defs/run`/`$defs/state` "at every load and before every replacement; a payload that fails validation is refused with no write". Schema ground truth: `$defs/calibration` is `additionalProperties:false` with exactly 8 keys, and `$defs/intent` requires `calibration`. The real formal state fails this: `RUN-20260907-132845-01` carries forbidden `first_row_point`/`row_step` in `intent.calibration`; `RUN-20260907-152217-01` has no calibration at all; 4 of 5 runs have no `contract_revision`. Yet plan.md:658-664 mandates that verify-only accept this state read-only as `STATE=LEGACY_PROVENANCE_LIMITED` with no init/rewrite, expecting Filesystem=PASS/Registry=FAIL/Source=UNRESOLVED/State=LEGACY, exit 4.
- Mechanism: a literal implementation of plan.md:579 refuses the real authority at load (`INVALID_*` instead of the mandated legacy read), so either the mandated real verify pass fails or the implementer silently weakens validation/special-cases legacy — a semantic contract change made outside review. This also conflicts with the schema's own legacy branch intent and with the plan's "input precondition" framing (plan.md:579 is scoped to fixtures but written as universal).
- Smallest correction: scope full-schema validation to replacement payloads and new RC2 records; define an explicit read-only legacy parse contract (allowed missing/extra fields, normalization to UNKNOWN/null, never rewritten); add the real formal-state shape as a legacy fixture; keep replacement-side validation strict.

### RV-13 — MINOR — GROUNDING (stale planned-file list)
- Affected: plan.md:543-556 (planned implementation files), plan.md:246 (driver list).
- Evidence: the list names `src/line_backup_acceptance/verify.py`, which does not exist (the real module is `verifier.py`, imported at `cli.py:9`); it lists already-existing files as to-be-created (`__init__.py`, `cli.py`, `transaction.py`, `status.py`) and omits existing `authority.py`, `common.py`, `__main__.py`; `tests/run_all.py` and `tests/verify_evidence.py` are treated as deliverables but are absent.
- Mechanism: Stage 04 could create a duplicate `verify.py` module or a wrong file plan, wasting a wave or splitting the verifier boundary.
- Smallest correction: correct the file list to the real module names and mark already-existing files as to-be-modified rather than created.

## REQUIRED_PLAN_CHANGES
1. RV-1 (BLOCKER): define one canonical case-root verify layout and reconcile plan.md:572/575/584/755/786 into a single executable story where transaction and verify-only read the same state bytes; re-check every Case-01 argv against the authority rule before approval.
2. RV-2: add a post-fix genuine parent-SIGKILL case inside the prepare dispatch window (no `--crash-after-dispatch`), with fresh-resume oracle and pinned lock/control-file aftermath.
3. RV-3: add literal finalize verification-JSON negative rows for the R4 payloads (forged PASS/57, FAIL/0, `{}`, other run's JSON) plus a `VERIFICATION_RUN_MISMATCH` row.
4. RV-4: pin the CONFIRMED user-fact artifact schema, author/authority, storage path, and positive/negative case rows.
5. RV-12: scope schema validation to replacement/new records and define the read-only legacy parse contract with a real-state fixture.
6. Minors in one batch: RV-5 (replacement count), RV-6 (prepare storage faults), RV-7 (one reconciliation grammar), RV-8 (path conversion + tie-break), RV-9 (Case 18/19 pre-states), RV-10 (WORSENED fixture), RV-11 (post-fix evidence root), RV-13 (file list).
7. After the edits: bump PLAN_REVISION on the same TASK_ID, re-run the byte-identical snapshot workflow, keep attempts 01–18 untouched, and submit the new hash for a fresh Stage 02 review before any Handoff.

## RESIDUAL_MINOR_NOTES
- The on-disk phase-2 scripts still remove shared roots without ownership markers (`shutil.rmtree` in `fixtures.py`, `harness.py`, `run_phase2_r1/r2/r3/r4/r5/r6/r7*.py`); the §15.5 protocol must be implemented before they are re-run. The plan owns this obligation; it is not an unclosed design defect.
- This review executed no product code; all repository findings are from static reads, hashes and the existing attempt-01 evidence records. Program-hash equality with handoff §4 is accepted as recorded in that evidence, not re-run here.
- plan.md:164 ("the positive control is the valid external binding in case 01's closing loop") remains vacuously true only after the RV-1 fix.
- The plan's remaining size (947 lines) is acceptable for CRITICAL; the recommended revision should delete stale duplicate normative statements rather than add new ones, keeping the goal-alignment and status sections intact.

FINAL_STATUS: PLAN_REVISION_REQUIRED
NEXT_ACTION: Stage 01 revision mode on TASK_ID T20260916-0102-01-line-backup-acceptance: produce PLAN_REVISION 16 closing RV-1 (BLOCKER), RV-2/RV-3/RV-4/RV-12 (MAJOR) and the RV-5…RV-13 minors, keep review attempts 01–18 untouched, then submit the new plan hash for a fresh Stage 02 review.
