# Plan Review Report — Stage 02 (fresh independent), attempt-36

## REVIEW_METADATA

- TASK_ID: T20260916-0102-01-line-backup-acceptance
- REVIEW_ATTEMPT: 36 (single write target = this file; `review/attempt-36/` created fresh and verified absent before writing; attempts 30–35 read-only, untouched; attempt-37 not read and not written by this review)
- REVIEWED_PLAN_REVISION: 24
- REVIEWED_PLAN_SHA256: `40eb01980c7e11f96b4b12c2db8f53728852f1ab6ca452dfb5e865fd5435a003` — 2,330 lines / 313,461 bytes. Re-computed at review start (before the full read), again at 2026-09-18 10:40 +0800 (Asia/Taipei) immediately before this report was written, and once more immediately after writing (the reviewer's closing act; result reported with this file's own SHA-256). All identical to the mandated value. The committed blob at `92a167e` re-hashes to the same value, and `HEAD` (`5d6e2b1`) contains that commit in its history with a clean worktree for the plan path, so the worktree content is the frozen Rev24 candidate. `git status --porcelain` at review start showed only the two new empty review dirs (36/37) as untracked; no tracked modification.
- PLAN_SNAPSHOT_PATH: none — this review is write-limited to this report file (per the review mandate); the hash re-verifications are the substitution.
- Repository anchor: branch `master`, HEAD `5d6e2b1`; plan frozen at commit `92a167e`; no commit, no network, no GUI (zero clicks/keyboard/AX/window activation/capture), zero images in any output.
- Write set: this report only. Everything else read-only: `plan.md`, product code, tools, evidence, prior reviews, `handoff.md`, `result.md`, destination (untouched; verified only via the frozen baseline record).
- Inputs (in the mandated reading order): session transcript `/Users/hsiaojohnny/.codex/sessions/2026/09/17/rollout-2026-09-17T19-59-26-01a0af3c-6693-72b2-abf2-095a3317e9a4.jsonl` lines 2291, 2365, 2405, 2412, 2420, 2427, 2428 (extracted byte-exact with PYTHONDONTWRITEBYTECODE=1); `evidence/20260918-owner-decisions/owner-decisions-rev24.json` (9,872 B, `be1f5173de9219bfba2a547223542dde17c6e6a23f653274122f0d8dcbe2360d`); `plan.md` Rev24 (header, §24.1–§24.9, §23.1–§23.2, §22.1–§22.9, §20.2/§20.3, §19.1/§19.3, requirement table, closure, owner view, plus a full-diff audit of the Rev24 edit `git diff 192e6f0 92a167e`); `review/attempt-34/review_report.md` (`21c4bbb551070dbdfc7c155536c4651abc9492970311b739cf506dd1d2b8a57c`, PLAN_APPROVED) and `review/attempt-35/review_report.md` (`975142c3ad79098141a74e4bd1cebf107d8e307fc65ec42ef4c781e3d8c8582c`, PLAN_APPROVED); `handoff-rev24-agent-autonomous-2026-09-18.md` (`ba870b4bdc574aec5406d9820adf906ffc13962e3e999100628eb965e91b7046`, 43,749 B) §2–§11 as the §11 hash table; `GOAL-rev24-next-conversation.md` (`3c724e7bd87823c7e34aec52085eeca2123050e73750497a671168fad35e68ab`) only to confirm it does not add authority beyond the plan; `.agent/.../e2e/attempt-07/e2e_report.md` (`6416a5f45d2d39bc056a4729a4a4b097eab4dca801cd417d41aea8d6936aba07`, 38,483 B); `evidence/20260916-route/attempt-06/{run-ledger.json,album-ellipsis-locate.json,gate-4-authorization.json,route-runbook.md}`; `evidence/20260916-route/tools/v4/*.py` + `tools/detect_menu_popup.py` + both selftest summaries + helper source.
- Freeze-set verification: all 26 §11 anchored SHAs re-hashed from disk with `PYTHONDONTWRITEBYTECODE=1` / `python3 -B`. All 26 reproduce byte-for-byte: plan `40eb0198…`, owner-decisions `be1f5173…`, H4.0 `ba870b4b…`, GOAL `3c724e7b…`, v4 tools `22a4e9ef…` / `bb52aff1…` / `ffa82aed…` / `b77e3d51…` + v4 README `ed3ebf0a…`, v4 self-test `5ad2be10…` (16 cases, `cases_failed 0`, `result PASS`), v3 self-test `17840e91…` (13/13), helper `4fc9fa2b…`, detector `6ae9c250…`, detector self-test script `db091703…` (4,018 B), detector self-test summary `d8ffc129…` (2,946 B) confirmed as distinct files (the §24.8 literal correction is factually correct), gate-4 `e32ccd88…` (8,979 B, 17 keys), attempt-06 FINAL ledger `906c1433…` (28,390 B, `parent_ledger_sha256 17b17203…`, `plan_binding.plan_sha256 4337e2b5…`, `state CLOSED_AFTER_INPUT_1`), attempt-05 ledger `17b17203…`, post frame `4cb8a6b4…`, pre frame `3d926e7d…`, baseline `ab6747f2…`, v1.1 record `a8c10551…` (1,741 B), `result.md` `bd00d9c9…` (`CORE_ACCEPTANCE_BLOCKED`), `execution-rev22.md` `c9b59080…`, task `handoff.md` `19c9c6d3…` (175 lines / 37,582 B), reviews 34/35 reports as above. `evidence/20260916-route/tools/v5/` and `evidence/20260916-route/attempt-07/` both verified absent (expected: Stage 04 not started). No `__pycache__` created by this review (checked).
- Transcript byte-checks (raw, this review): L2291 user 243 B `3fc89cbc…` exact (the three-layer question); L2365 user 60 B `2e729d03…` exact; L2405 assistant 1,931 B `14905585…` exact (A/B presentation; the B excerpt 470 B `5674299e…` verified inside the owner-decisions artifact); L2412 user 304 B `caf2f7d7…` and L2420 user 430 B `a8caf86d…` both present and both followed by `turn_aborted` markers (superseded drafts, correctly never used as authority); L2427 user 882 B `a600ac4c…` exact and equal to the decisions artifact's `answer_verbatim` bytes (re-hashed here: 882 B → `a600ac4c…`); L2428 echo is the UserMessage echo of the same bytes (client_id `143b072f…` per H4.0 §5.4). Chronology: L2291 → L2365 (08:02:00) → L2405 (08:02:44) → L2412 (08:06:14, aborted) → L2420 (08:06:20, aborted) → L2427 (08:06:40, decision). The plan's §24.1 attribution (lines/ordinals/timestamps/bytes/hashes) matches all six values. The decisions artifact's `session_id 01a0af3c…`, `session_transcript` path, `answer_echo_transcript.line 2428`, and `superseded_draft_answers` entries all match the raw JSONL.
- Attempt-06 geometry re-verification (offline, read-only): `album-ellipsis-locate.json` re-read here — `frame_sha256 4cb8a6b4…`, `frame_size [327,643]`, `title_bbox [15,83,204,111]`, `verdict NO_ELLIPSIS_FOUND`; triple[0] `dots [[304.5,44.0],[304.5,49.5],[304.5,55.0]]` with `region above_album_title_band` and middle `(304.5,49.5)`; band arithmetic `y0=max(0,83-6)=77`, strip top `ty0=83`, offset `77-49.5=27.5` from band top and `83-49.5=33.5` from strip top — exactly the §24.3 numbers. The remaining triples are `inside_text` overlapping `57張照片` (conf 1.00). The v4 self-test fixture `album_frame()` was re-read: its control dots sit inside the synthetic strip (cy≈47 vs title y≈32), confirming the §24.3 blind-spot diagnosis (fixture, not reader). S5's `ALBUM_OPEN_VERIFIED` on the same run is not re-litigated here (accepted via e2e/attempt-07).
- Rev24 delta audit: `git diff 192e6f0 92a167e -- …/plan.md` → header hunk (PLAN_REVISION 23→24; ACCEPTANCE_MODE +e2e/attempt-08 third subject; ACCEPTED_BY_USER +line-2427 B; PRIOR_REVIEW_ATTEMPT 30–33→34,35; PRIOR_REVIEW_GATE +Rev23 dual approval +no-approval-for-Rev24; REV22_WAVE_STATUS final Stage-05 correction; REV24_WAVE_STATUS added) + the new `## Revision 24 changes` block (§24.1–§24.9) + one §22.3 precondition-2 literal fix (detector self-test script vs summary disambiguation). No other line changed; no new budget kind, gate kind, tool binding beyond §24.3, authority, requirement, closure semantic or owner-visible behavior. The §24.8 literal list is complete and accurate against the diff.
- Disclosures (/tmp only, outside the repo, all offline/read-only): `/tmp/rv36/*.json|*.txt` transcript extracts, `rev24.diff`, `plan-sha-*.txt`. No repo file other than this report was written.
- Invalidation: this review approves exactly PLAN_REVISION 24 at the SHA-256 above and nothing else. Any edit to `plan.md` after that hash invalidates this review; the approval cannot be inherited by a later revision.

## OWNER_VERDICT

（白話版，給非技術主持人）

- 這版在做什麼：上次測試停在「找不到 ⋮ 就安全停住」（相簿有打開、57 張備份沒被動、⋮ 沒被點）。這版只修一條「找 ⋮ 的位置規則」：從「只在標題那一行裡找」改成「到標題行上方、往上看 60 像素的帶狀區找」；找到「剛好一個」才算過，其他（找不到、找到兩個、只在群組層找到、標題找不到、畫面壞掉）一律停。其他全部沒變：還是開相簿一次、點 ⋮ 一次、只觀察不點任何選單項目、每次恰點一下、不重試。
- 為什麼是往上 60：真實畫面的 ⋮ 在標題上方約 33 像素；60 是刻意留約 27 像素餘裕的有界視窗。再往上就不再猜（直接停），避免掃到無關按鈕、避免把良性畫面誤判成「有兩個 ⋮」。標題行本身反而列為不合格（那裡全是日期與張數文字，放行它會重新引入文字誤判風險）。被拒的替代方案（標題行＋上方聯集）已在計畫裡寫明拒絕理由。
- 授權與預算：沿用上一輪同一套「恰兩個輸入」預算（卡片一次、⋮ 一次；重試 0、選單 0、鍵盤 0、對話圖片 0）；新的 gate-5 是上一張 gate-4 的同格式（17 個鍵）一次性授權，綁定您的 line-2427 決策、Rev24 精確雜湊與本輪兩份複審；必須在碰畫面之前事先凍結並 commit，否則不得輸入。ledger 沿用上一輪 FINAL 為 parent（906c1433…），不是重開一條鏈。
- 我這次獨立重跑的驗證：對話紀錄 6 行逐字比對（決策行 882 B、選項行 1,931 B、兩則中斷草稿、前後兩個提問，雜湊全對；時間序合理；中斷草稿確實未被當授權）；26 個凍結檔案全部重新算 SHA（全對；v5 與 attempt-07 尚不存在，符合「還沒開始」）；上一輪失敗畫面的幾何重算（標題 [15,83,204,111]、band 上緣 77、⋮ middle 49.5、差 33.5，上方分類正確；其餘候選確在文字區）；Rev24 diff 確認只動 header＋新 §24＋一處 detector 文件用字更正，沒有偷加工作；gate-4 確為 17 鍵、attempt-06 ledger parent 鏈正確、e2e/attempt-07 六列（5 PASS＋1 非關卡 FAIL）與「⋮ 未送、零副作用」一致。
- 什麼會卡住整套：只有 CORE 失敗才會；執行當天 LINE 必須停在相簿列表且看得到目標卡，否則零輸入停止（可重排，不是路線結果）。任何需要第三個輸入、重試、點選單、鍵盤、拿歷史座標當 live 輸入的情境，一律停。
- 結論：這版可以核准，核准綁定「Rev24 + 雜湊 40eb0198…」；沒有發現任何會擋住交付或驗收的問題。

## GOAL_BASELINE

Reconstructed only from the authoritative sources above, before adopting the Plan's framing:

1. PRIMARY outcome (result ①; unchanged at task level): safely establish whether the existing 57-image destination is a valid backup of the exact LINE source (group `旻謙允禎成長日記`; album `2024/05/13～05/17`), and otherwise record honestly without any ambiguous or duplicate production transaction; the destination and all formal config/state/run-log stay read-only; no re-download.
2. Reusable capability (result ②): the corrected v5 locator + its self-test + its re-runnable offline replay constitute the capability subject; the v4 reader, card locator, open verifier and frozen tesseract detector are reused unchanged.
3. Overall closure (result ③): governed by §20.3 + §24.5 — AFFIRMATIVE (machine-observable, attempt-07) AND §16.4 CONFIRMED (re-derived by Stage 05 e2e/attempt-08 from fresh evidence) jointly advance the closure tuple; any non-AFFIRMATIVE leaves the route a scoped CORE blocker; ROUTE_NOT_NEEDED is owner-reserved and was not chosen (A not chosen); a further attempt needs a NEW revision.
4. Non-goals: no product-code change, no fixture/status-vocabulary change, no menu-item/chooser/keyboard/AX/download/state-write authorization, no second live use of any historical coordinate, no fallback of S6 to v4, no amendment of Rev24 after a failed attempt-07.

## CLAIM_FALSIFICATION (the Rev24 claims)

- (a) Owner authority B — CONFIRMED. L2427 882 B `a600ac4c…` is the only operative authority; L2412/L2420 are aborted drafts (turn_aborted verified); L2405 presents A (record ROUTE_NOT_NEEDED and close) and B (new revision + fresh review + one new observe-only authorization) and A was not chosen. §24.1's verbatim/excerpt bytes/hashes/ordinals/timestamps match the raw JSONL and the decisions artifact. The alternative-reading clause (§24.2: a different target/album/menu path stops read-only) correctly bounds misreading.
- (b) Attempt-06 root cause — CONFIRMED. Numbers re-derived above match §24.3 exactly; the v4 rule, band formula, constants (delta 45, flat-tol 25, flat-fraction 0.6, max-area 16, max-side 5, text-margin 6), exit codes (0/2/3/4/5/6) and the fixture blind-spot claim all verify against the frozen bytes and JSON.
- (c) v5 corrected rule — SOUND and BOUNDED. Census/dot/triple/text-blocking + constants unchanged (byte-identical claim is a Stage-04 implementation obligation, correctly stated as a requirement, not pre-claimed as done); title strip formula identical to v4; header formula `header={x0:tx1+2,x1:width-1,y1:max(0,ty0-7),y0:max(0,ty0-7-header_depth)}` with `--header-depth` default 60 is precisely specified; eligibility (exactly-one + not-inside_text + not-in-group-band + inside-header) and the three non-eligible classifications (`in_title_strip` / `above_header_band` / `below_album_title_band`) are mutually exclusive and fail-closed; the union alternative is explicitly rejected with a real-frame-grounded reason; the 60-px rationale (33.5 observed + ~27 headroom; deeper risks AMBIGUOUS_ELLIPSIS; farther is NO_ELLIPSIS_FOUND, never guessed) is proportionate. The planning-time `/tmp` prototype numbers (`ELIGIBLE`, `[[304.5,44.0],[304.5,49.5],[304.5,55.0]]`, `[304,50]`, v4 still NO_ELLIPSIS_FOUND) are stated as prototype-only and correctly re-required as durable §24.6 evidence before gate-5 — no pre-claim.
- (d) v5 self-test (≥20 cases, 0 failed, four v5-specific negatives + 5× determinism + README) — ADEQUATE as a gating requirement. The 16-case re-expression against corrected geometry plus the four named negatives (strip→NO_ELLIPSIS_FOUND; beyond-depth→NO_ELLIPSIS_FOUND; left-of-right-edge→NO_ELLIPSIS_FOUND; real-geometry 5× replay) directly cover the corrected semantics and the two boundary risks (strip-text false positive; header overshoot). Binding the summary to the v5 tool SHAs prevents silent substitution.
- (e) Gate-5 one-shot pattern — SOUND. 17-key mirror of gate-4 verified (gate-4 re-read here: exactly the 17 keys §24.4 lists); `prior_gate e32ccd88…` marked spent; `authority_artifact be1f5173…` + Rev24 hash; `plan_binding.review_attempts 36/37` (this review is one of the two; the other is not prejudged); `context_authorization` L2427 + `option_presentation_transcript` L2405 with bytes/hashes; `decided_interpretation` (target album-level ⋮; two ordered inputs; nothing_else; worst-case menu left open); 12 mirrored budgets; `not_authorized` (Save All named; chooser/keyboard/AX/third-or-retried input/download/destination-or-state write/non-current-frame coordinate); pre-input freeze-and-commit with TASK_REGRESSION on missing/late gate or missing `906c1433…` parent binding. No second live authorization is created; resume rule (zero-input resume-eligible; post-input-#1 stop closes the round) preserves at-most-once.
- (f) Route attempt-07 S1–S11 substitutions — COMPLETE and SAFE. S3/S5 stay on frozen v4 bytes; S6 bound to v5 with the exact command shape (`--expect-start 2024/05/13 --expect-end 2024/05/17 --title-bbox <S5 bbox> --expect-group-title` + `--header-depth 60 --out …/attempt-07/album-ellipsis-locate.json`); S6 ELIGIBLE is the sole ⋮-input permission and its click_point the sole coordinate source; historical coordinates explicitly banned as live input; all five non-ELIGIBLE verdicts + reader failure → no ⋮ input; every S3/S5/S6 JSON must carry the v4 reader block with the detector exempt (consistent with the Rev23 RV-33-1 fix); S8 exactly-one left click; S9–S10 observe+transcribe as attempt-06; S11 durable ledger+execution; stop-artifact policy (no route-result/manifest on pre-outcome stop) retained; ledger parent `906c1433…` (whose parent `17b17203…` verified). S6-fallback-to-v4 explicitly banned — the exact failure class that would silently reintroduce the bug.
- (g) Closure/gating/veto (§24.5) + matrix (§24.6) + OOS (§24.1) — CONSISTENT. The AFFIRMATIVE-plus-CONFIRMED joint condition, the honest non-AFFIRMATIVE path (scoped blocker, notify, no ROUTE_NOT_NEEDED, no auto-branch, NEW revision for any retry), the preflight surface-failure resume rule, and gate-5 as the only new gate all preserve §20.3 tuple semantics. The nine §24.6 rows (eight CORE HARD_CLEAN + one SUPPORTING NON_GATING probe) each state a falsifiable baseline and a TASK_REGRESSION failure class with no waiver; V24_RULE_REPLAY_COMMITTED_FRAME + V24_V4_BASELINE_FROZEN jointly force the durable pre-input proof; V24_S6_BOUND_TO_V5 + V24_ROUTE_ATTEMPT_07_SINGLE_ONESHOT jointly enforce the input envelope; V24_AFFIRMATIVE_MACHINE_OBSERVABLE_ONLY bars OCR-only/human-only/incomplete AFFIRMATIVE; V24_PRIOR_EVIDENCE_IMMUTABLE + V24_GATE5_FROZEN_BEFORE_INPUT protect history and ordering. OOS-V24-1..5 correctly exclude menu activation, re-download, ROUTE_NOT_NEEDED, extra GUI, and in-Revision amendment.
- (h) Header/§24.8 literals — ACCURATE. PRIOR_REVIEW_ATTEMPT 34,35; PRIOR_REVIEW_GATE Rev23 `4337e2b5…` dual approval (reports `21c4bbb5…`/`975142c3…`, zero BLOCKER/MAJOR — re-read here) with the Rev21 chain and the Rev22 dual-gate-never-satisfied note intact, plus no-approval-for-Rev24; REV22_WAVE_STATUS final state matches e2e/attempt-07 (six rows, 5 PASS + 1 SUPPORTING FAIL; CUA_ROUTE_DECISION sole blocker; attempt-06 S6 NO_ELLIPSIS_FOUND, ⋮ never sent, zero side effects — all re-confirmed); REV24_WAVE_STATUS correctly claims nothing; §22.3 detector-literal correction (`db091703…` 4,018 B script vs `d8ffc129…` 2,946 B summary) verified byte-for-byte above. ACCEPTANCE_MODE +e2e/attempt-08 third subject with zero-GUI constraint preserves the §23.1 series-homonym rule (route attempt-07 ≠ e2e attempt-07).
- Delta confinement — CONFIRMED. See the diff audit in REVIEW_METADATA: header + new §24 block + one §22.3 literal; no scope growth, no hidden budget/gate/tool/closure change.

## FINDINGS

- No BLOCKER findings. No MAJOR findings.
- ID: RV-36-1 | Severity: MINOR | Category: PRECISION
  - Affected: plan.md §24.3 planning-time prototype sentence ("on the committed post frame `4cb8a6b4…` invoked with `--title-bbox 15,83,204,111`, the corrected rule yields exactly one eligible candidate").
  - Evidence: [VERIFIED] the title bbox is correctly the S5-given bbox on that frame, but the sentence does not state the `--expect-group-title` value used in the prototype invocation (the Stage-04 S6 command includes `--expect-group-title 「旻謙允禎成長日記」`). The omission does not change the prototype's verdict (group bbox was null on the real frame), but a Stage-04 implementer reproducing the prototype byte-exactly must guess whether the flag was passed.
  - Failure mechanism: at most a one-line runbook clarification in Stage 04; no authorization, budget, safety or acceptance effect. The durable §24.6 replay row governs acceptance, not the prototype sentence.
  - Smallest correction (non-blocking; for a future revision only, which would itself need fresh review): append the prototype's group-title flag state (e.g. "with `--expect-group-title` unset/null, matching the frame's null group bbox").
- ID: RV-36-2 | Severity: MINOR | Category: DOCUMENTATION
  - Affected: plan.md §24.4 `recorded_in`/`recorded_at_local` description for gate-5 (mirrors gate-4's keys without naming the attempt-07 directory prefix in that bullet; the prefix appears in §24.3 precondition 2).
  - Evidence: [VERIFIED] gate-4's own `recorded_in` carries the attempt-06 path; the Rev24 runbook will carry the attempt-07 path. The split-brain is only a reading inconvenience.
  - Failure mechanism: none for execution (Stage 04 freezes `evidence/20260916-route/attempt-07/gate-5-authorization.json` per §24.3); purely editorial.
  - Smallest correction (non-blocking; future revision only): name the attempt-07 gate path inside the §24.4 bullet.

## REQUIRED_PLAN_CHANGES

None for approval. Rev24 resolves the attempt-06 root cause with a bounded, falsifiable correction; grounds its authority byte-exactly in the owner's B decision; mirrors the proven gate-4/budget/closure machinery without semantic drift; and forces the durable offline proof before any input. The two new MINORs (RV-36-1/2) are editorial, non-gating, and do not affect goal alignment, implementation, safety, compatibility, rollback or acceptance. Any edit to plan.md — including folding RV-36-1/2 into the text — creates a new revision and requires a fresh independent review before Stage 03 handoff (approval binds to this hash only).

## RESIDUAL_MINOR_NOTES

- RV-34-1 (the §22.1 line-1034 parenthetical) is retained as history and is unaffected by Rev24; Rev24 does not re-open it.
- The v5 directory is additive-only per NFR-V24-1; Stage 04 must copy (not move) the three v4 bytes and record their SHAs in the v5 README. Any byte difference in those copies is TASK_REGRESSION under V24_PRIOR_EVIDENCE_IMMUTABLE.
- Stage 03 must carry §24.1–§24.5's handoff notes (header band formula + depth 60 + rejected union + historical coordinates banned as live input + S6-no-fallback + stop-artifact policy + route/e2e series-homonym note); Stage 05 (`e2e/attempt-08`) must remain zero-GUI and must re-derive both closure facts from fresh evidence.
- The screen-scope probe stays SUPPORTING/NON_GATING per NFR-V24-4 and V24_SCREEN_SCOPE_PROBE; a failed probe never blocks and is never reported as CORE.
- Pre-existing untracked briefing files under `evidence/20260917-owner-briefing/` (if any remain) are unrelated to this review and were left untouched; Stage 03 must not bind them.

## NEXT_ACTION

Proceed to Stage 03 handoff compilation bound to TASK_ID `T20260916-0102-01-line-backup-acceptance` + PLAN_REVISION 24 + SHA-256 `40eb01980c7e11f96b4b12c2db8f53728852f1ab6ca452dfb5e865fd5435a003`, carrying the §24 handoff notes and the series-homonym note. Stage 04 builds `tools/v5/` + `selftest/v5/`, reproduces both §24.6 replay rows as durable evidence, and freezes gate-5, the Rev24 runbook and the ledger skeleton (parent `906c14330605122e9b600359892d4400171d62564707d50af26ef8d8bb32fa2c`) before any input; the route attempt-07 then runs once under the two-input envelope; Stage 05 consumes `e2e/attempt-08` with zero GUI input of its own. If the planner folds RV-36-1/2 into the text, that new revision requires its own fresh independent review before handoff.

PLAN_APPROVED
