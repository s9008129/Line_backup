# Handoff — LINE album acceptance: Rev22/23 corrected route attempt-06 (v4 reader) + v1.1 record adoption

## TASK
- TASK_ID: T20260916-0102-01-line-backup-acceptance
- STATUS: READY_FOR_IMPLEMENTATION
- PLAN_PATH: .agent/tasks/T20260916-0102-01-line-backup-acceptance/plan.md
- PLAN_REVISION: 23
- PLAN_SHA256: 4337e2b5c105901ce7c56956ecea5469894df2d2a29f099068c979584c71b6b2
- REVIEW_REQUIRED: YES
- REVIEW_REPORT: review/attempt-34/review_report.md (fresh Stage-02 review, PLAN_APPROVED, SHA-256 21c4bbb551070dbdfc7c155536c4651abc9492970311b739cf506dd1d2b8a57c) + review/attempt-35/review_report.md (fresh independent review, PLAN_APPROVED, SHA-256 975142c3ad79098141a74e4bd1cebf107d8e307fc65ec42ef4c781e3d8c8582c); both bound to Rev23 at `4337e2b5…`; MINOR findings only (RV-34-1, RV-35-1/2/3 — carried below as non-gating notes); zero BLOCKER/MAJOR
- REVIEWED_PLAN_REVISION: 23
- REVIEWED_PLAN_SHA256: 4337e2b5c105901ce7c56956ecea5469894df2d2a29f099068c979584c71b6b2
- INDEPENDENT_ACCEPTANCE_REQUIRED: YES
- E2E_REQUIRED: NO (plan.md:9-10 header rationale: the album subject is a read-only verify-only pass over an existing destination — no production download is authorized; the bounded, owner-gated live GUI route observation is not full user-journey E2E. Acceptance = `e2e/attempt-07` re-deriving both closure facts from fresh evidence; Stage 05 runs zero GUI input of its own.)
- ACCEPTANCE_MODE: per plan header (plan.md:11): INTEGRATION for the Rev21 wave (already consumed: `e2e/attempt-06`); Rev22 adds the owner-gated live GUI route observation (route attempt-06) as a second subject of the same album-data result; its independent acceptance is `e2e/attempt-07`, which must re-derive both closure facts from fresh evidence (§22.5) and must not run any GUI input of its own.
- Fresh Implementer required: YES
- Planner/Reviewer transcript required: NO

## GOAL_ANCHOR
PRIMARY_OUTCOME (owner decision `1 選 B；2 用更正版紀錄`, transcript-bound in `evidence/20260917-owner-decisions/owner-decisions-rev22.json` `0664b5fb…`; plan.md §22.1-§22.5): execute owner decision B exactly once — the corrected route attempt-06 under gate-4 with the v4 reader (S3/S5/S6 stop-or-continue verdicts bound to the frozen v4 tools; the frozen tesseract detector reused byte-identically and exempt), plus adoption of the corrected record v1.1 for the §16.4 check. Two at-most-once inputs only: (1) an album-card left click opening the target album (raw group 旻謙允禎成長日記; title `2024/05/13～05/17`; count 57), (2) an album-level ⋮ left click inside the opened album after `ALBUM_OPEN_VERIFIED`. click_count 1 each; retry 0; observation only. NEVER click any menu item (Save All named explicitly); no chooser, no keyboard, no AX writes, no destination/formal-state writes, no re-download, zero images in conversation.
SUCCESS_EVIDENCE: attempt-06 completes under the committed pre-freeze bundle with exactly its two authorized inputs and a machine-observable `AFFIRMATIVE` at the album level (a new AX menu element or the frozen detector's `MENU_DETECTED`) → `CUA_ROUTE_DECISION=PASS` for the corrected route; AND the §16.4 exact-equality check over `source-identity-user-fact.confirmed.v1.1.json` (`a8c10551…`) re-derives `CONFIRMED` → `SOURCE_CORRESPONDENCE=CONFIRMED`, closing BLK-01. Stage 05 `e2e/attempt-07` (fresh, zero GUI) independently re-derives BOTH facts from fresh evidence and writes `result.md`; only if both hold is the §22.5/§20.3 closure tuple expected: PRIMARY ACHIEVED / IMPLEMENTATION COMPLETE / CORE PASS / REQUIRED_VERIFICATION PASS / INDEPENDENT_ACCEPTANCE PASS / TASK_CLOSURE DONE, `BASELINE_REGRESSION_DELTA=UNCHANGED`.
MUST_NOT_BREAK (NFR-V22-1..4; §22.3; §22.6): every v1/v2/v3 tool byte, the v3 self-test, the attempt-01..05 artifacts, gates 1-3, both prior ledgers, the archived handoffs, the 57-file destination and all formal config/state/registry/run-logs stay byte-identical; `禎` U+798E and `楨` U+6968 are never merged or normalized; at-most-once inputs (click 1/input, retry 0, menu_item 0, keyboard 0, conversation_images 0); fail-closed stops; the frozen tesseract detector is reused byte-identically (never re-implemented or replaced); no menu-item activation, no chooser, no production Save-All/download, no destination or formal-state write; attempt-05's frozen `TARGET_MISMATCH` verdict is never overwritten or re-scored; the disclosed axis facts (Registry=FAIL, State=`LEGACY_PROVENANCE_LIMITED`, product-level Source=`UNRESOLVED`) are reported as axis facts and are never promoted.
ROUTE (owner decision; unchanged): attempt-05 remains stopped (`STOPPED_AT_S5_TARGET_MISMATCH`, `owner_decision_required: true`); the owner chose B, and this handoff grants exactly §22.3's two inputs under gate-4 and nothing more. A non-AFFIRMATIVE outcome is recorded honestly with zero side effects and leaves the route a scoped CORE blocker; `ROUTE_NOT_NEEDED` is never taken; route closure belongs to the owner alone and no agent may take it.

## CRITICAL_PATH
The Rev21 wave is delivered and was independently accepted (commit `287e2f5`; `e2e/attempt-06` report `14b44c94…`) — preserved, not repeated. This wave's live critical path (max two GUI inputs; everything else read-only):
1. Pre-freeze bundle (CORE prerequisite; §22.3 preconditions 2-3): `evidence/20260916-route/attempt-06/` — created by a concurrent writer during this handoff's compilation (23:09-23:10 +0800, uncommitted at compile time); W7 re-hashes and verifies it, then COMMITS it BEFORE any input (never overwrite committed bytes; finalize append-only if incomplete): `gate-4-authorization.json` (cites `evidence/20260917-owner-decisions/owner-decisions-rev22.json` `0664b5fb…`, the verbatim decision, the two-input scope), `route-runbook.md` (S1-S11 with §22.3's S3/S5/S6/S10 substitutions; budgets; capture bounds), `run-ledger.json` (`schema_version 1`; `parent_ledger_sha256` = attempt-05's final ledger `17b17203…`; budgets `album_card 1 / navigation 1 / ellipsis 1 / retry 0 / menu_item 0 / keyboard 0 / app_acquisition 0 / conversation_images 0`; `click_count_per_input 1`).
2. Preflight (S1-S3; zero input): binding read on a fresh frame + the v4 `locate_album_card` (`bb52aff1…`) — `ELIGIBLE` required; else record `NO_TARGET_ON_FRAME`, notify the owner, zero inputs, stop (resume-eligible). The S2 screen-scope probe is SUPPORTING/non-gating (`screen_scope=UNAVAILABLE` never blocks; `PERMISSION_PROMPT_OBSERVED` → SAFE_ABORT, no input).
3. Input #1 → S5 (CORE gate): exactly one album-card metadata left click at the v4 `click_point`; then the v4 `verify_album_open` (`ffa82aed…`) on the pre/post frames — continue ONLY on `ALBUM_OPEN_VERIFIED` (exit 0); `TARGET_MISMATCH` / `NO_EFFECT` / `INCONCLUSIVE` stop the run before input #2.
4. S6 (CORE gate): the v4 `locate_album_ellipsis` (`b77e3d51…`) on the same post frame — continue ONLY on `ELIGIBLE` (exactly one candidate in the album-title row band); `NO_ELLIPSIS_FOUND` / `AMBIGUOUS_ELLIPSIS` / `GROUP_LEVEL_ONLY` → no input #2.
5. Input #2 → S7-S10 (CORE): exactly one album-level ⋮ left click at the S6 point; then immediate read-only captures (app frame + AX state; up to 5 `screencapture -x` within about 4 s) and the frozen `detect_menu_popup.py` (`6ae9c250…`) on every frozen pre/post pair (screen pre-menu vs each post capture; app pre vs app post) + AX re-read. `AFFIRMATIVE` only for a new AX menu element or `MENU_DETECTED`; OCR-only/human-report-only/incomplete → `UNKNOWN`, never AFFIRMATIVE.
6. S11 (CORE): write `route-result.json` / final `run-ledger.json` / `manifest.json` / the durable §7.11 execution record `execution-rev22.md` (per §20.2 S11 also `album-card-locate.json`, `album-open-verify.json`, `album-ellipsis-locate.json`, `screen-probe.json`, `menu-analysis.json`); commit; notify the owner (the menu may be left open — the owner closes it; no keyboard input is ever sent). A stop before any route outcome writes no `route-result.json`/`manifest.json` — the final ledger records events, final counts and stop reason (carried note below).
7. Stage 05 (fresh, zero GUI) at `e2e/attempt-07` (§22.5): re-derives BOTH closure facts from fresh evidence — the §16.4 check over v1.1 and the attempt-06 route result — snapshots the Stage-04 facts from `execution-rev22.md`, and writes `result.md`. Stage 04 must not pre-claim its outcome.

## SEMANTIC_INVARIANTS
- Exactly one semantic delta carries into this wave: the reader of the official chain is the v4 Vision tool set. S3/S5/S6 stop-or-continue verdicts come only from the frozen v4 tools (`vision_reader.py` `22a4e9ef…`; `locate_album_card.py` `bb52aff1…`; `verify_album_open.py` `ffa82aed…`; `locate_album_ellipsis.py` `b77e3d51…`), whose JSON carries the v4 `reader` block (`helper.resolved_from`, `helper.binary_sha256`, per-call `outcome`, `exit_code`, `stdout_sha256`); the frozen tesseract-based `detect_menu_popup.py` (§20.2 S10; `6ae9c250…`) is reused byte-identically and explicitly exempt; the v3 screen-probe JSON is exempt (RV-35-3 note below).
- Reader failure semantics: `binary_missing` / `build_failed` / `nonzero_exit` / `unparsable` / `timeout` reaches the tool's frozen refusal path (non-zero exit, no guessed value) and stops the run before the next input. Empty stdout = helper outcome `ok` + zero words = recorded UNREADABLE, never a successful read; it refuses only where the rule requires a readable value (e.g. a title region); an unreadable count alone follows frozen v3 parity and does not refuse (verbatim note below).
- S5 verdicts (§22.3/§20.2 S5): `ALBUM_OPEN_VERIFIED` = target date title in the post frame AND frame-diff fraction ≥5% at the frozen `--delta 12`; a readable non-target/mismatching count → `TARGET_MISMATCH`; no surface change → `NO_EFFECT`; no readable title → `INCONCLUSIVE`. S6 `ELIGIBLE` = exactly one candidate inside the album-title row band (the row of the verified title, to the right of it); group-title-row candidates are never eligible. S10 `AFFIRMATIVE` is machine-observable only; the observed menu is checked against the documented item order (Select items / Rename album / Save All / Delete album / Share album) as a recorded check only — never used to compute or click a row.
- At-most-once and resume semantics (NFR-V22-2; §22.3 resume rule): input #1 and input #2 are each sent at most once, each only if every precondition holds at that moment; retry 0; a stop after input #1 never re-authorizes input #2; a zero-input stop (preflight surface failure, capability unavailable) is resume-eligible with the same unspent authorization once the owner restores the surface.
- §22.4 source-record adoption: `source-identity-user-fact.confirmed.v1.1.json` (`a8c10551…`, 1,741 B) becomes the effective record for `source_status`; v1 (`2cd7eccd…`, 1,746 B) is never rewritten/edited/deleted; §16.4's matching rules are unchanged (no normalization, no relaxation); exactly one leaf differs (`answer.part_2.confirmed_album`: `2024/05/13～2024/05/17` → `2024/05/13～05/17`, a duplicated year; both U+FF5E, no ASCII tilde) and the other 26 leaves are identical; `user_fact_v1_matches` (`common.py:533`) → v1 `False`, v1.1 `True`. `ANOM-01` (gate-answer back-reference mismatch `603ab720…`/1,585 B vs `2cd7eccd…`/1,746 B) stays recorded and non-gating.
- §22.5 closure semantics: the closure tuple is reached only when attempt-06 returns `AFFIRMATIVE` AND the §16.4 check over v1.1 passes (both re-derived by Stage 05 from fresh evidence); any non-AFFIRMATIVE outcome is recorded honestly with zero side effects, leaves the route a scoped CORE blocker, notifies the owner immediately, and takes neither closure branch automatically (`ROUTE_NOT_NEEDED` never taken). A preflight surface failure spends no input, is not a route result and changes no status. No supporting item may block the wave; no supporting pass may mask a CORE failure.
- Frozen bytes (re-hash before reuse; any byte change = TASK_REGRESSION): v4 tools + helper source `vision_ocr.swift` `4fc9fa2b…` + v4 self-test summary `5ad2be10…` (16 cases, `cases_failed 0`); v3 tools `500fcadb…`/`80504262…`/`60e3120a…` + v3 self-test `17840e91…`; v2 locator `8c8b6fc7…`; detector `6ae9c250…` + its self-test `d8ffc129…`; gates 1-3 (gate-1 = the human-gate answer `03ffff57…`; gate-2 `f3d17790…`; gate-3 `05db1678…`); attempt-05 final ledger `17b17203…`; `album-open-verify.json` `ffa5d963…` (`count_digits_read "75"`, verdict `TARGET_MISMATCH`); `vision-ocr-crosscheck.json` `d3ebbaed…` (SUPPLEMENTARY_NON_AUTHORITATIVE, never promoted); v1.1 `a8c10551…`; v1 `2cd7eccd…`; human-gate answer `03ffff57…`; the 57-file destination and all formal config/state/run-logs; `e2e/attempt-06` report `14b44c94…`; the archived handoffs (`handoff-history/`). Attempt-05's frozen verdict and artifacts are never re-scored or re-interpreted; the v4 reader applies only to attempt-06's own fresh frames; frames stay in `/tmp` (bytes/size/SHA-256 recorded only).
- Orthogonal Status Contract v2 (plan.md:1981-2076): six orthogonal subjects; no field rewrites another; `Done` means overall closure only; verification blockers never collapse into IMPLEMENTATION_BLOCKED; `BASELINE_REGRESSION_DELTA=WORSENED` → REQUIRED_VERIFICATION=FAIL, closure FIX_REQUIRED (never INCOMPLETE/PENDING_REQUIRED_VERIFICATION); a missing/unreadable pre-baseline → REQUIRED_VERIFICATION=INCOMPLETE, never PASS.
- Execution-record naming (§22.3 S11 addition): this wave's durable §7.11 record is `execution-rev22.md`; `execution.md`, `execution-rev19.md`, `execution-rev21.md` stay untouched.

## BEST_EFFORT_DO_NOT_GATE
- `NFR-V22-4` (SUPPORTING/non-gating): the S2 screen-scope probe (`screen_scope=UNAVAILABLE` never blocks), the v4 README and the build record; a failed probe is recorded, never a CORE failure.
- `ANOM-01` stays recorded and non-gating; `ANOM-02` likewise — neither is repaired in this wave.
- Freeze set is Rev22-scoped: `evidence/20260917-freeze-set/rev22-freeze-set.json` (`fea6eb7a…`) records `plan.md` at Rev22's `426fe6a1…` by design; do not refresh mid-wave; compare the plan row against the freeze-era revision (RV-35 residual).
- `baseline.json`'s 14/14 anchors are a baseline-time snapshot; the plan.md (Rev20) and handoff (Rev20) rows have legitimately advanced; `BASELINE_REGRESSION_DELTA` keys on the frozen signature classes (v3 tools, v3 self-test, route evidence, frames), so no false delta arises.
- RV-35 residual notes (non-gating, no plan edit): the `owner-decisions-rev22.json` 1025 excerpt is a marked join of three byte-exact fragments; the `.agent/.../e2e/` series starts at `attempt-02`; `evidence/20260917-owner-briefing/owner-explanation-rev22-audit.md` (committed at `8253d7d`, `433b082a…`) is unrelated and must not be bound.

## CARRY_NOTES_NON_GATING (verbatim; apply them, but do not gate the wave on them)
- empty-stdout note (refined by RV-35-1): "empty stdout = helper outcome `ok` + zero words = recorded UNREADABLE, never a successful read; it refuses only where the rule requires a readable value (e.g., a title region); an unreadable count alone follows frozen v3 parity and does not refuse."
- budget taxonomy (RV-35-2): "budgets are album_card 1 / navigation 1 / ellipsis 1 / retry 0 / menu_item 0 / keyboard 0 / app_acquisition 0 / conversation_images 0; input #1 (the album-card click) is the run's single navigation input and is counted under BOTH navigation_inputs and album_card_inputs (attempt-05 precedent); this adds no liberty — no extra navigation input is authorized."
- reader-block scope (RV-35-3): "only the three v4-tool JSONs (locate_album_card, verify_album_open, locate_album_ellipsis) must carry the v4 reader block; the frozen tesseract-based detector JSON (menu-analysis.json) and the v3 screen-probe JSON are exempt."
- stop-artifact policy (RV-32-4): "a run that stops before any route outcome writes no route-result.json/manifest.json; the final ledger records events, final counts and stop reason (attempt-05 precedent)."
- series note: "evidence/20260916-route/attempt-NN is the route-run series; .agent/tasks/<TASK_ID>/e2e/attempt-NN is the acceptance series — route attempt-06 ≠ e2e/attempt-06 (consumed by the Rev21 wave, report 14b44c94…); this wave's Stage 05 is e2e/attempt-07."
- RV-34-1 (non-gating, note only; do NOT edit plan.md): "§22.1's parenthetical about transcript line 1034 is imprecise — L1034 is the owner's plain-language request answered at L1048; it did not produce the L1025 option presentation."

## DEFERRED_NOT_THIS_TASK
- Any menu-item activation (Save All named explicitly), any chooser, any production Save-All route/download; any destination write or re-download (OOS-V22-1/OOS-V22-2).
- `ROUTE_NOT_NEEDED` recording (OOS-V22-3; the owner chose B and that closure stays owner-reserved).
- Any GUI input beyond §22.3's two inputs (OOS-V22-4); any v3/tesseract reader substitution for S3/S5/S6; bridge repair/reinstall; TCC changes; legacy migration/normalization; historical cleanup.
- A future route/production wave: any new attempt or plan change requires a NEW revision, a new gate and a fresh review (never inside Rev23).

## REPO_ANCHOR
- Project root: /Users/hsiaojohnny/Documents/ChatGPT/Line_backup
- Branch: master
- Anchor HEAD: 8253d7d (Rev23 dual-review intake — attempt-34 Carver + attempt-35 Noether both PLAN_APPROVED). Chain: plan Rev23 = 192e6f0; owner-decisions + reviews intake = c301e5e; Rev21 wave = 287e2f5; Rev21 acceptance (`e2e/attempt-06`) = 92202a4; Phase 0 baseline = adf1829; H3.0 = 1343b55; attempt-05 input #1 = e53f67b; Vision crosscheck = 4d6c307.
- Relevant dirty state: none at compile time (`git status --porcelain` clean before this regeneration); this regeneration adds only `handoff-history/handoff-plan-r21-20260917-230851.md` (byte-identical copy of the superseded Rev21 handoff `df595831…`) and rewrites `handoff.md`; it is pending the orchestrator's commit.
- Drift since plan/review: none — plan.md SHA-256 re-verified at `4337e2b5…` (2,093 lines / 284,897 bytes), byte-identical to the reviewed revision; both review reports re-verified (attempt-34 `21c4bbb5…`; attempt-35 `975142c3…`). Stage 04 stops on any handoff↔plan mismatch.

## CURRENT_STATE_DELTA
- Rev23 approved: review/attempt-34 (`21c4bbb5…`, PLAN_APPROVED) and review/attempt-35 (`975142c3…`, PLAN_APPROVED), both bound to Rev23 at `4337e2b5…`; MINOR findings only (RV-34-1; RV-35-1/2/3) — carried above as non-gating notes; no plan edit is permitted for them (an edit voids both approvals and this handoff).
- Owner decisions committed at `c301e5e`: `evidence/20260917-owner-decisions/owner-decisions-rev22.json` (`0664b5fb…`; decision `1 選 B；2 用更正版紀錄`; `item_1` = BRANCH_B with the two-input `authorizes` list + `not_authorized`; `item_2` = OPTION_I v1.1 adoption; CORR-01 wording fix present).
- Freeze set: `evidence/20260917-freeze-set/rev22-freeze-set.json` (`fea6eb7a…`; 71 artifacts; Rev22-scoped per the non-gating note above).
- THE v4 TOOL SET EXISTS AND IS FROZEN (committed in `287e2f5`): `evidence/20260916-route/tools/v4/` — `vision_reader.py` `22a4e9ef…`, `locate_album_card.py` `bb52aff1…`, `verify_album_open.py` `ffa82aed…`, `locate_album_ellipsis.py` `b77e3d51…`, `README.md` `ed3ebf0a…`; the v4 self-test `evidence/20260916-route/tools/selftest/v4/selftest-summary.json` `5ad2be10…` (16 cases, `cases_failed 0`). The Vision helper source `evidence/20260916-route/tools/vision/vision_ocr.swift` `4fc9fa2b…` stays frozen (never edited). W7 verifies, it does not rebuild.
- Frozen chain (must stay byte-identical): v3 tools `500fcadb…` / `80504262…` / `60e3120a…`; v2 side tools `detect_menu_popup.py` `6ae9c250…` + its self-test summary `d8ffc129…`, `locate_card_ellipsis.py` `8c8b6fc7…`; v3 self-test summary `17840e91…`; attempt-05 `run-ledger.json` `17b17203…`; `album-open-verify.json` `ffa5d963…` (`count_digits_read "75"`, `TARGET_MISMATCH`); `vision-ocr-crosscheck.json` `d3ebbaed…` (SUPPLEMENTARY_NON_AUTHORITATIVE, never promoted); gates 1-3 (`03ffff57…` / `f3d17790…` / `05db1678…`); attempt-05 has no `route-result.json`/`manifest.json` (stop-artifact precedent).
- Route state: attempt-05 stopped at S5 (`STOPPED_AT_S5_TARGET_MISMATCH`, `owner_decision_required: true`); exactly one input was sent (album card, once); the ⋮ was never sent; attempt-05's spent budget stays spent — gate-4 is a NEW, independent one-shot authorization (no reset, no extension; §22.2).
- `evidence/20260916-route/attempt-06/` was created by a concurrent writer during this handoff's compilation (23:09-23:10 +0800; `gate-4-authorization.json`, `route-runbook.md`, `run-ledger.json`; ledger `state=FROZEN_BEFORE_INPUTS`, parent `17b17203…`, budgets 1/1/1 + zeros; gate-4 cites `0664b5fb…`), uncommitted at compile time — W7 verifies and commits it before any input (never overwrite committed bytes). `evidence/20260916-route/agent-e2e/` exists (Rev21 wave; frozen). `.agent/tasks/<TASK_ID>/e2e/` holds attempts 02-06; the next unused number is 07 (this wave's Stage 05); route attempt-06 ≠ `e2e/attempt-06` (series note above).
- Frozen execution records present and untouchable: `execution.md` (Rev18 wave), `execution-rev19.md`, `execution-rev21.md`; this wave writes `execution-rev22.md` (does not exist yet).
- The header status block remains the result ① tuple (PRIMARY UNKNOWN / IMPLEMENTATION COMPLETE / CORE BLOCKED / REQUIRED_VERIFICATION PASS / INDEPENDENT_ACCEPTANCE PASS / CLOSURE CORE_ACCEPTANCE_BLOCKED) plus the `REV21_WAVE_STATUS` (closed; accepted at `e2e/attempt-06`) and `REV22_WAVE_STATUS` (not started; nothing pre-claimed) lines; Stage 05 re-derives the wave tuple from fresh evidence — never assumed.
- The attempt-05 ledger's `handoff_sha256` = `ab88e496…` (the superseded Rev20 handoff); historical binding only — Stage 05 freshness must not treat it as current.
- Formal read-only compatibility profile unchanged: for the requested 禎 key vs the persisted 楨 key the expected real-command axes are Filesystem=PASS, Registry=FAIL, Source=UNRESOLVED, State=LEGACY_PROVENANCE_LIMITED, Overall=UNKNOWN, exit 4; disclosed axis facts, never contradictions.

## MUST_READ_PLAN
Rev23 §23.1-§23.2 + header (plan.md:1-76; §23.1 at 39, §23.2 at 70); Rev22 §22.1-§22.9 (plan.md:77-240; §22.1 at 89, §22.2 at 121, §22.3 at 134, §22.4 at 177, §22.5 at 192, §22.6 at 204 with the six rows at 208-213, §22.7 at 215, §22.8 at 224, §22.9 at 236); Rev20 §20.1-§20.3 as the step base (plan.md:361-480; S1-S11 at 403-458 apply unchanged with §22.3's substitutions; §20.3 closure routing at 459); §16.4 matching contract (plan.md:973-1024); §19.1 v1.1 authoring rule (plan.md:513-554); §19.3 `ROUTE_NOT_NEEDED` (plan.md:596-625; NOT taken this wave); §21.6 e2e series rule (plan.md:330-337); requirement table (plan.md:1746-1761); Runtime route, ledger authority, GUI gate, truthful E2E (plan.md:1949-1980); Canonical Status Contract v2 (plan.md:1981-2076); Closure and sequencing (plan.md:2077-2089); Owner view (plan.md:2091-2093).

## SETTLED_DO_NOT_REOPEN
- Rev23 §23.1-§23.2: the review-response fixes (RV-33-1 row scope + the RV-33-2/3/4 corrections, header bookkeeping) and nothing else; the dual approval binds only Rev23 at `4337e2b5…`; any plan edit — including folding RV-34-1/RV-35-* in — creates a new revision and needs fresh review before handoff.
- Rev22 §22.1-§22.9: the owner's two decisions and their reading, the §22.3 two-input envelope, the budgets, the gate-4 pattern, the v4 bindings, the v1.1 adoption and the §22.6 rows.
- Rev21 §21.x: the Vision wave stands closed and accepted (`e2e/attempt-06` report `14b44c94…`; frozen set 21 anchors UNCHANGED).
- Rev20 §20.1-§20.3 (gate-3, attempt-05 artifacts) and Rev19 §19.1/§19.3 (v1.1 record, `ROUTE_NOT_NEEDED` preconditions) stand as preserved history with the Rev23 `e2e/attempt-07` rebinding; Rev18 §18.1-§18.6 and the Rev17/Rev16 decisions stand as corrected.
- The route closure decision (A/B) is owner-reserved; demonstration semantics never flip a frozen route verdict; no supersession layer is added.

## REVERIFY_ON_START
- Recompute plan.md SHA-256 + byte length; require exact match to PLAN_SHA256 `4337e2b5…` (2,093 lines / 284,897 bytes), else STOP.
- Re-read both review reports; confirm each ends with its single gate line `PLAN_APPROVED` bound to Rev23 at `4337e2b5…` (attempt-34 `21c4bbb5…`; attempt-35 `975142c3…`) with no BLOCKER/MAJOR.
- `git status --porcelain` / `git log --oneline -6`: confirm no plan/product/review edits after `8253d7d` (this handoff regeneration + its archive are the only expected pending changes until committed).
- Re-hash before use: the v4 tool set (4 SHAs) + helper source `4fc9fa2b…` + v4 self-test `5ad2be10…`; the v3 tool set + v3 self-test `17840e91…`; v2 locator `8c8b6fc7…`; detector `6ae9c250…` + self-test `d8ffc129…`; attempt-05 `run-ledger.json` `17b17203…`, `album-open-verify.json` `ffa5d963…`, crosscheck `d3ebbaed…`; gates 1-3; v1.1 `a8c10551…`, v1 `2cd7eccd…`, human-gate answer `03ffff57…` — all must remain byte-identical.
- Verify the attempt-06 pre-freeze bundle (created concurrently 23:09-23:10 +0800, uncommitted at compile time): re-hash its three files, check §22.3's pins (`0664b5fb…`; parent `17b17203…`; budgets 1/1/1 + zeros; v4 SHAs), and COMMIT it before any input; if absent or incomplete, finalize it append-only (never overwrite a committed artifact). Confirm `execution-rev22.md` does not exist before W7 writes; `execution.md` / `execution-rev19.md` / `execution-rev21.md` stay untouched.
- Mutable read-only facts: formal DATA_CONFIG / DATA_STATE / DATA_RUN_LOG bytes and SHA-256; DATA_DESTINATION inventory (57 files, 17,924,900 bytes) — captured read-only, never mutated; the owner's live surface (album-list state, target card visible) is a live precondition captured read-only at run time. A missing/unreadable pre-baseline is REQUIRED_VERIFICATION=INCOMPLETE, never PASS.

## TRIGGERED_POLICIES
- workflow-routing.md (§7 Status Semantics v2, §7.4 baseline, §7.6 blockers, §7.7 Stage 04 routing, §7.8 Stage 05, §7.10 DONE, §7.11 artifacts)
- testing-verification.md
- goal-alignment-design-economy.md
- git-change-hygiene.md
- security-privacy.md
- debugging-recovery.md
- high-risk-change.md

## FIRST_ACTION
Execute W7-PREFREEZE, zero input (§22.3 preconditions 2-3): the `evidence/20260916-route/attempt-06/` pre-freeze bundle (gate-4/runbook/ledger) was created by a concurrent writer during this handoff's compilation — re-hash, verify (gate-4 cites `0664b5fb…`; ledger parent `17b17203…`; budgets 1/1/1 + zeros; `click_count_per_input 1`; `retry_budget 0`; v4 SHAs pinned) and COMMIT it BEFORE any input; if absent or incomplete, finalize it append-only (never overwrite a committed artifact). Contents required either way: `gate-4-authorization.json` (the verbatim decision `1 選 B；2 用更正版紀錄`, the two-input scope, the budgets), `route-runbook.md` (S1-S11 with §22.3's S3/S5/S6/S10 substitutions; the carried notes; capture bounds), `run-ledger.json` (parent `17b17203…`). Verify (not rebuild) the v4 set + helper + v4 self-test SHAs. Only after the bundle is committed: preflight S1-S3 on a fresh frame (zero input). No GUI input before that.

## IMPLEMENTATION_WAVES
1. W7-PREFREEZE (CORE prerequisite; zero GUI): FIRST_ACTION above; verify the concurrent gate-4/runbook/ledger bundle (SHA pins/budgets/parent), finalize append-only if incomplete, and commit before any input.
2. W7-ROUTE-ATTEMPT-06 (CORE; the only GUI in this wave): S1-S11 under §20.2 with §22.3's substitutions; exactly two at-most-once inputs; record every step's evidence; reader-block requirement scoped per the carried note; write the S11 artifact set + `execution-rev22.md`; commit. A zero-input stop is resume-eligible; a stop after input #1 never re-authorizes input #2.
3. Stage 05 (fresh) at `e2e/attempt-07` (zero GUI; §22.5): snapshots the Stage-04 facts from `execution-rev22.md` (STAGE_04_REPORTED_IMPLEMENTATION_STATUS / CORE_ACCEPTANCE / REQUIRED_VERIFICATION + STAGE_04_EXECUTION_ARTIFACT_SHA256 = `execution-rev22.md` + TASK_ID / PLAN_REVISION / HANDOFF_SHA256 / timestamp / evidence path); re-derives both closure facts from fresh evidence; writes `result.md`. Stage 04 must not run or pre-claim Stage 05.

## ACCEPTANCE_CONTRACT
Required per-check fields: CHECK_ID | COMMAND/SCENARIO | GOAL_CRITICALITY | EVIDENCE_ROLE | CLOSURE_GATE | BASELINE_RULE | FAILURE_ROUTING | WAIVER_ALLOWED | WAIVER_AUTHORITY, plus CHECK_RESULT and WAIVER_STATUS; no agent approves its own waiver. The six §22.6 rows are carried VERBATIM below (wave-scoped; the canonical matrix body does not repeat them):
| CHECK_ID | Criticality | Evidence role | Gate | Baseline | Failure classification | Waiver allowed | Authority | Check result | Waiver status |
|---|---|---|---|---|---|---|---|---|---|
| V22_SOURCE_RECORD_V1_1_CONFIRMED | CORE | OUTCOME | HARD_CLEAN | YES | §16.4 must re-derive CONFIRMED over the v1.1 record and over no other record; any normalization or merge of 禎/楨, or any byte change to the v1 file, is TASK_REGRESSION | NO | NONE | NOT_RUN | NOT_ALLOWED |
| V22_ROUTE_ATTEMPT_06_SINGLE_ONESHOT | CORE | OUTCOME | HARD_CLEAN | YES | more than one album-card click, more than one ⋮ click, any retry, any menu-item/chooser/keyboard input, or any input spent after a failed precondition is TASK_REGRESSION | NO | NONE | NOT_RUN | NOT_ALLOWED |
| V22_S5_BOUND_TO_V4_READER | CORE | OUTCOME | HARD_CLEAN | YES | attempt-06's S3/S5/S6 stop-or-continue decisions must come from the frozen v4 verifier/tools and their JSON must carry the v4 reader block; the frozen tesseract-based detect_menu_popup.py (§20.2 S10, reused byte-identically per §22.3) is explicitly exempt from the v4-reader requirement; a tesseract-based S3/S5/S6 stop-or-continue verdict, or a missing reader block in any v4 tool JSON, is TASK_REGRESSION | NO | NONE | NOT_RUN | NOT_ALLOWED |
| V22_AFFIRMATIVE_MACHINE_OBSERVABLE_ONLY | CORE | OUTCOME | HARD_CLEAN | YES | an OCR-only, human-report-only or incomplete observation reported as AFFIRMATIVE is TASK_REGRESSION | NO | NONE | NOT_RUN | NOT_ALLOWED |
| V22_PRIOR_EVIDENCE_IMMUTABLE | CORE | MUST_NOT_BREAK | HARD_CLEAN | YES | any byte change to a v1/v2/v3 tool, the v3 self-test, an attempt-01..05 artifact, gates 1-3, either ledger, an archived handoff, or the destination/formal state is TASK_REGRESSION | NO | NONE | NOT_RUN | NOT_ALLOWED |
| V22_SCREEN_SCOPE_PROBE | SUPPORTING | DIAGNOSTIC | NON_GATING | NO | a failed screen probe records `screen_scope=UNAVAILABLE`; it never blocks and is never reported as a CORE failure | NO | NONE | NOT_RUN | NOT_ALLOWED |
- Wave acceptance: attempt-06's recorded outcome under the frozen pre-freeze bundle (plus `execution-rev22.md`) + Stage 05's independent re-derivation of both closure facts. Expected closure tuple ONLY on AFFIRMATIVE + §16.4 CONFIRMED over v1.1 (§22.5/§20.3); prior CORE carry-forward checks (SOURCE_CORRESPONDENCE, CUA_ROUTE_DECISION, STATUS_CLOSURE_CONTRACT, BASELINE_REGRESSION_DELTA, INDEPENDENT_ACCEPTANCE) re-derived with fresh evidence by Stage 05.
- Baseline rule: `BASELINE_REGRESSION_DELTA=UNCHANGED` over the frozen signature classes (v1/v2/v3 tools, v3 self-test, route evidence incl. attempt-01..05, gates, ledgers, archived handoffs, the destination and formal state); any byte change = TASK_REGRESSION; `WORSENED` → FAIL/FIX_REQUIRED; missing/unreadable pre-baseline → REQUIRED_VERIFICATION=INCOMPLETE, never PASS.
- Failure routing: a wave CORE failure keeps result ② open; a route-only gap is a scoped CORE blocker (owner-reserved); required-verification debt is INCOMPLETE, never PASS; baseline-worsened is a hard failure (FIX_REQUIRED).
- Stop-shape: a run that stops before any route outcome writes no `route-result.json`/`manifest.json`; the final ledger records events, final counts and stop reason (attempt-05 precedent).
- E2E: `E2E_REQUIRED=NO` per the plan header; the acceptance is INTEGRATION-level and must never be reported as live/production E2E; Stage 05 runs zero GUI.

## STOP_AND_ESCALATE_IF
- Handoff↔plan revision/hash mismatch → STOP.
- Plan hash drift or ANY plan edit after this handoff → STOP: it invalidates both Rev23 approvals and this handoff (fresh review required).
- Any semantic change (validity / requiredness / enums / gating / error / fallback / precedence / authority / retry / closure meaning) → stop, write escalation evidence, replan; never a bounded executor fix (§22.9; Rev16 §16.8).
- If the v4/cua capability is unavailable → the run stops zero-input (no v3/tesseract substitution for S3/S5/S6 verdicts), notify the owner; resume-eligible with the same unspent authorization.
- If the surface is not the album-list state (target card visible; no menu/dialog/chooser/photo-viewer/permission overlay) → stop zero-input, record `NO_TARGET_ON_FRAME` honestly, notify the owner immediately (no long loops); resume-eligible. After the S2 probe, `PERMISSION_PROMPT_OBSERVED` → SAFE_ABORT, no input.
- A stop after input #1 never re-authorizes input #2 (at-most-once; retry 0); a zero-input stop does not consume the authorization.
- 5 cumulative failures reached → stop before starting a new attempt, write the stop reason + evidence, notify the owner; no long retry.
- Any menu-item activation (Save All named explicitly), chooser, keyboard input, AX write, production download, or destination/formal-state write needed to proceed → STOP (OOS-V22-1/2/4; no production authorization exists).
- Any byte change to a v1/v2/v3 tool, the v3 self-test, an attempt-01..05 artifact, gates 1-3, either ledger, an archived handoff, the destination or formal state → TASK_REGRESSION (hard failure), never INCOMPLETE.
- A reader outcome `binary_missing` / `build_failed` / `nonzero_exit` / `unparsable` / `timeout` → the frozen refusal path (non-zero exit, no guessed value); stop before the next input; never a silent continue.
- An `AFFIRMATIVE` claimed from OCR-only / human-report-only / incomplete observation → STOP (V22_AFFIRMATIVE_MACHINE_OBSERVABLE_ONLY).
- Any 禎/楨 merge/normalization attempt, any attempt to take `ROUTE_NOT_NEEDED` or to decide route closure, or any re-scoring/overwrite/re-interpretation of attempt-05's frozen verdict → STOP.
- A `baseline-worsened` delta → hard failure FIX_REQUIRED; never INCOMPLETE/PENDING_REQUIRED_VERIFICATION.

## OWNER_VIEW（白話，給非技術主持人；非 gating）
- 這一版（Rev23，雙複審通過）＝您選的 **B**：會再走一次「開相簿 → 看一眼相簿裡的 ⋮」；這次判斷畫面的「眼睛」是 macOS 內建的 Vision 讀取器（v4 工具組，同一張冷凍畫面上讀出「57張照片」信心 1.00，舊引擎讀成「75」）。兩個動作各只做一次、不重試，過程中不會點任何選單項目（含「儲存全部」）、不開選擇視窗、不打鍵盤，只觀察。
- 您選的「更正版紀錄」：會用 v1.1 版本重做「一字不差」檢核（原檔一字不改）；通過才把來源對應記為已確認。
- 兩件事都成立才會整體結案（相簿層級選單被機器證實 + v1.1 檢核通過）；任一不成立就照實記錄、立刻回報，不會自行放行，也不會用 A 方案（記成「不需要」）。
- 執行時需要您把 LINE 停在「相簿列表」、目標相簿卡片（`2024/05/13～05/17`、57 張）可見。若不在這個畫面，這次不會送出任何輸入，只會記錄後請您復原畫面再續（同一份授權可續用）。
- 最大風險：若這次仍看不到相簿層級的 ⋮ 選單，路線維持卡點、立即回報；獨立驗收（`e2e/attempt-07`）全程不碰滑鼠鍵盤，只重新核對證據。

## HISTORICAL_TASK_DEPENDENCIES
- NONE for generic context. The root `/Users/hsiaojohnny/Documents/ChatGPT/Line_backup/handoff.md` is historical/non-authoritative background only (it predates this TASK_ID) and must not be used as a plan or contract.
- Prior handoffs of this task are archived (keep as history; never execute from any of them):
  - `handoff-history/handoff-plan-r13-20260917-021523.md` (Rev13-bound);
  - `handoff-history/handoff-plan-r18-20260917-154007.md` (Rev18-bound);
  - `handoff-history/handoff-plan-r19-20260917-165617.md` (Rev19-bound);
  - `handoff-history/handoff-plan-r20-20260917-204314.md` (Rev20-bound);
  - `handoff-history/handoff-plan-r21-20260917-230851.md` (Rev21-bound; superseded by this handoff; archived 2026-09-17 23:08 +0800, byte-identical to the superseded handoff `df595831…`).
- Frozen execution records: `execution.md` (Rev18 wave), `execution-rev19.md`, `execution-rev21.md`; this wave adds `execution-rev22.md`. The attempt-05 run-ledger's `handoff_sha256` (`ab88e496…`) is a historical binding to the Rev20 handoff only.

TASK_ID: T20260916-0102-01-line-backup-acceptance
HANDOFF_PATH: .agent/tasks/T20260916-0102-01-line-backup-acceptance/handoff.md
PLAN_REVISION: 23
STATUS: READY_FOR_IMPLEMENTATION
NEXT_STAGE: 04_IMPLEMENT
HANDOFF_SHA256: dde0380161897bdf208a525100e7a6ca7970c0f5173607b882474b9eef18f179 | HANDOFF_BYTES: 37297 (SHA-256 and byte count of all file bytes above this footer line; verify: `sed '$d' handoff.md | shasum -a 256` → this SHA-256, `sed '$d' handoff.md | wc -c` → this byte count)
