# Handoff — LINE album acceptance and reusable transaction process

## TASK
- TASK_ID: T20260916-0102-01-line-backup-acceptance
- STATUS: READY_FOR_IMPLEMENTATION
- PLAN_PATH: .agent/tasks/T20260916-0102-01-line-backup-acceptance/plan.md
- PLAN_REVISION: 20
- PLAN_SHA256: 4919d87148c68ea3d70cbb9abd258edbd0bfa0b55be5123f7202251e557db17c
- REVIEW_REQUIRED: YES
- REVIEW_REPORT: review/attempt-28/review_report.md (dual fresh review; second report review/attempt-29/review_report.md)
- REVIEWED_PLAN_REVISION: 20
- REVIEWED_PLAN_SHA256: 4919d87148c68ea3d70cbb9abd258edbd0bfa0b55be5123f7202251e557db17c
- INDEPENDENT_ACCEPTANCE_REQUIRED: YES
- E2E_REQUIRED: NO (plan.md:9-10, 1606; the primary object is an existing destination that must not be redownloaded and no production download is authorized; the corrected run's two at-most-once inputs are observation-only and not full user-journey E2E; no fixture/offline result may be reported as production E2E.)
- ACCEPTANCE_MODE: Independent Stage 05 acceptance (attempt-06) of the real CLI, the formal read-only verify-only result, the R1–R7 repair evidence package, the Rev19 §19.1 v1.1 record, and the Rev20 §20.2 corrected route attempt-05 run and artifacts. Attempt-06 binds to the then-current revision (20) and re-derives closure per §20.3 from fresh evidence (plan.md:1606, 1730-1734; §20.3).
- Fresh Implementer required: YES
- Planner/Reviewer transcript required: NO

## GOAL_ANCHOR
PRIMARY_OUTCOME (both results CORE; plan.md:1288-1294; TARGET_ALBUM plan.md:1333): (1) safely establish whether the existing 57-image destination is a valid backup of the exact LINE source group '旻謙允禎成長日記' / album '2024/05/13～05/17' — otherwise stop with no ambiguous or duplicate production transaction; and (2) leave the reusable automation path itself proven through the real operator CLI — the R1–R7 repairs executed end-to-end with rerunnable, independently readable evidence. Neither result may be claimed from offline or fixture PASS; paperwork is not a substitute.
SUCCESS_EVIDENCE: the product CLI emits separate Filesystem/Registry/Source/State/Overall outcomes with a SHA-256/bytes artifact manifest; SOURCE_CORRESPONDENCE is exactly CONFIRMED | UNRESOLVED | CONTRADICTED (only an authoritative exact join or one precise preserved user fact may CONFIRM — plan.md:1417-1442, 1738; the Rev19 §19.1 v1.1 record is this wave's basis, subject to Stage 05 attempt-06 re-verification); the same product transaction CLI runs in subprocess restart/fault/race tests with independent counter/state oracles (plan.md:1526-1603).
MUST_NOT_BREAK (plan.md:1306-1313): formal config/state/registry/intent/run-log and the 57 photos are read-only — never redownload, rewrite, migrate, or normalize; '禎' U+798E and '楨' U+6968 stay separate strings/keys and are never merged; no AXPress/AXUIElementPerformAction/AX write/guessed coordinate/OCR-only acceptance/AppleScript-hidden-AX workaround; no Save-All retry after an UNKNOWN dispatch; DATA_PROJECT_ROOT authority stays explicit and immutable; UNKNOWN is never converted to zero/NOT_ATTEMPTED without affirmative non-dispatch proof; no test may pass by asserting a value it wrote itself.
ROUTE LEG (Rev20 §20.1-§20.3; plan.md:32-140): the corrected route observation sends exactly two at-most-once inputs, in order — one album-card metadata left click (this run's single navigation input), then, only after the frozen album-open verifier returns `ALBUM_OPEN_VERIFIED`, one album-level ⋮ left click — then observation only; zero retry, zero conversation images, no keyboard input, no menu item, no Save All, no chooser, no state/config/run-log write, no AX write, no guessed or historical coordinate. AFFIRMATIVE resolves the corrected route and the route stops being a scoped CORE blocker (no menu-item activation; production Save-All stays outside every wave); non-AFFIRMATIVE is recorded honestly with zero side effects, the route stays a scoped CORE blocker, the owner is notified immediately, and closure may only proceed as `ROUTE_NOT_NEEDED` under the owner's explicit decision with §19.3's four preconditions re-verified by Stage 05, or by opening a NEW revision — never automatically.

## CRITICAL_PATH
Base-wave items 1–5 (plan.md:1395-1403) and the product implementation are delivered and evidenced (evidence/20260916-baseline/attempt-02, evidence/20260916-auto-verification/attempt-02, evidence/20260916-stage05/attempt-04 and attempt-05, execution.md) and preserved, not repeated. The Rev19 wave is likewise delivered and preserved (v1.1 record + route attempt-04; execution-rev19.md; §19.1-§19.3). The live critical path for this revision:
1. Pre-freeze wave before ANY GUI input (CORE; offline; plan.md:60-84): freeze `evidence/20260916-route/attempt-05/gate-3-authorization.json` (the §20.1 `[DECIDED]` interpretation with the verbatim owner messages, timestamps, session/transcript references, the exact scope and the budgets), the v3 runbook `evidence/20260916-route/attempt-05/route-runbook.md`, and the v3 tools with their frozen self-tests under `evidence/20260916-route/tools/`: `locate_album_card.py` (album-card metadata locator), `verify_album_open.py` (album-open verifier), `locate_album_ellipsis.py` (album-level ⋮ locator); reuse attempt-04's `detect_menu_popup.py` and the v2 card-⋮ locator `locate_card_ellipsis.py` unchanged with their SHAs recorded (the v2 locator's verdict is corroborating, never blocking, and it never supplies the click point); create the fresh attempt-05 `run-ledger.json` (`parent_ledger_sha256` = attempt-04's final ledger `df0e9c44…`; `album_card_input_budget=1`, `ellipsis_input_budget=1`, `app_acquisition_input_budget=0`, every other input class 0, `click_count=1` per input; zero retry; zero conversation images). All of it committed before the run.
2. The single corrected route attempt-05, only after the owner presents the target card in the album-list state (app `jp.naver.line.mac`, group 旻謙允禎成長日記, album 2024/05/13～05/17, 57 images): S1 binding read → S2 one screen-scope probe (accept criteria; `UNAVAILABLE` non-fatal; `PERMISSION_PROMPT_OBSERVED` → SAFE_ABORT, no input) → S3 album-card locate ELIGIBLE → S4 one album-card metadata left click → S5 `verify_album_open.py`; only `ALBUM_OPEN_VERIFIED` (target date title found in the post frame + substantive surface change, frame-diff ≥5%) permits continuing; any non-verified verdict stops before the ⋮ input → S6 album-level ⋮ locate (exactly one candidate inside the album-title row band; group-title-row candidates never eligible) → S7 screen pre-menu capture → S8 one album-level ⋮ left click → S9 immediate post observation (app frame + AX state; ≤5 screen captures within about four seconds; timestamps/bytes/SHA-256 only) → S10 detection and transcription (AFFIRMATIVE only via a new AX menu element or the frozen detector's new region + ≥2 transcribed menu strings; OCR-only/human-report-only/incomplete capture → UNKNOWN, never AFFIRMATIVE) (plan.md:60-118).
3. Write the artifact set plus the durable §7.11 execution record: `route-result.json`, `run-ledger.json` (FINAL with the counts and `route_result_sha256`), `manifest.json`, `album-card-locate.json`, `album-open-verify.json`, `album-ellipsis-locate.json`, `screen-probe.json`, `menu-analysis.json`, plus `execution-rev20.md`; frames stay in /tmp (bytes/size/SHA-256 recorded only); prior attempts and frozen records are never touched; the menu may be left open and the owner is notified to close it (S11, plan.md:114-119).
4. Stage 05 attempt-06 (fresh) re-derives closure per §20.3: AFFIRMATIVE → the corrected route is resolved and `CUA_ROUTE_DECISION` is PASS for the corrected route; non-AFFIRMATIVE → recorded honestly with zero side effects, the route stays a scoped CORE blocker and the owner is notified immediately; closure only as `ROUTE_NOT_NEEDED` under the owner's explicit decision (§19.3's four preconditions re-verified + attempt-05's evidence) or under a NEW revision — never automatic (plan.md:120-140).

## SEMANTIC_INVARIANTS
- Orthogonal axes, never collapsed: Filesystem / Registry / Source / State / Overall; intent / dispatch / trigger / writer / terminal ownership remain separate. Registry follows only the §16.8 axis rule (plan.md:773-784 as corrected by §18.1 at 318-328): a unique readable match with `verified_run_id` null (imported-evidence form, e.g. matrix `legacy-record`) is Registry PASS; the `CASE_ROOT_25` real-state copy (楨 U+6968 never matches the requested 禎 U+798E album) and any unreadable/non-matching run are Registry FAIL. Provenance failures never flip the registry axis (plan.md:634-685, 686-722).
- Stable results: exit 0 only when all required acceptance axes pass; exit 4 for a completed, safely rejected negative/blocked result; exit 2 invalid invocation/configuration/authority before any read/write; exit 1 internal/read/command/artifact error (plan.md:1443-1454). Refusal classes (SKIP_DUPLICATE, SKIP_TERMINAL, CONFLICT_*, NEEDS_RECONCILIATION, MISSING_DISPATCHER, MISSING_SOURCE_EVIDENCE, INVALID_STATE_LEGACY, READBACK_UNCERTAIN, …) are fixed and may not be renamed, merged, or reordered (plan.md:1526-1603).
- Validation scope: a run without contract_revision / RC2-only intent fields / full provenance is a readable LEGACY record (STATE=LEGACY_PROVENANCE_LIMITED, SOURCE=UNRESOLVED), never initialized or rewritten; validation at every load must not contradict the read-only legacy contract (plan.md:1455-1462 as corrected by §17.3 at 429-449).
- Finalize is the only terminal path; `--verification-json` is outcome-conditional (required for `--outcome VERIFIED`, optional for `SAFE_ABORT`); a commit refuses with `CONFLICT_UNRESOLVED_DISPATCH` exit 4 no-write unless the loaded run carries the completed successful dispatch record (plan.md:329-333, 1526-1603).
- Status Contract v2 fields stay orthogonal: PRIMARY_OUTCOME_STATUS | IMPLEMENTATION_STATUS | CORE_ACCEPTANCE_STATUS | REQUIRED_VERIFICATION_STATUS | INDEPENDENT_ACCEPTANCE_STATUS | TASK_CLOSURE_STATUS; no field rewrites another subject; `Done` means overall closure only (plan.md:1636-1727, 1728-1741). Verification blockers never collapse into IMPLEMENTATION_BLOCKED.
- Baseline: `baseline_delta=WORSENED` is a must-not-break violation → REQUIRED_VERIFICATION=FAIL, closure FIX_REQUIRED, blocker BASELINE_REGRESSION_DELTA/FAIL/TASK_REGRESSION — never INCOMPLETE/PENDING_REQUIRED_VERIFICATION; a missing/unreadable pre-baseline is REQUIRED_VERIFICATION=INCOMPLETE, never PASS (plan.md:1661, 1665; workflow-routing §7.7 rule 8).
- Rev19 §19.1 (plan.md:174-214): the v1.1 corrected record is the same preserved answer re-authored under owner authorization — a versioned instance only; §16.4's matching rules themselves are unchanged (no normalization, no relaxation); v1 and the one-shot gate artifact are never rewritten; the authoring-note stays outside the record body; ANOM-01/ANOM-02 are recorded, not repaired. The passing §16.4 check is the task-level source-correspondence basis for closure, subject to attempt-06; it does not claim the product CLI prints Source=CONFIRMED for the real destination (plan.md:1455-1462, 1738).
- Rev20 §20.1-§20.3 (plan.md:32-140): gate-3 is a third, fresh one-shot authorization independent of the exhausted attempt-03/attempt-04 budgets (no reset, no extension) and authorizes exactly the two corrected inputs (album-card entry, then album-level ⋮), each sent only if every §20.2 precondition holds at the moment of that input, each at-most-once, zero retry; the alternative (narrower) reading is recorded honestly and a newer explicit owner correction supersedes at any time; AFFIRMATIVE only from machine-observable evidence (new AX menu element, or the frozen detector's new-region + ≥2 transcribed menu strings); OCR-only, human-report-only or incomplete capture → UNKNOWN; the observed menu is checked against the documented item order as a recorded check, never used to compute or click a row; non-AFFIRMATIVE leaves the route a scoped CORE blocker and closure is owner-routed (§19.3's four preconditions + attempt-05's evidence for `ROUTE_NOT_NEEDED`, or a NEW revision) — never automatic. Attempt-04's artifacts remain history and never established the album route.

## BEST_EFFORT_DO_NOT_GATE
- Bridge/service readiness is SUPPORTING/DIAGNOSTIC and non-gating unless a route-specific experiment proves necessity (requirement table plan.md:1412; check row BRIDGE_READINESS plan.md:1662).
- DOCUMENTATION_RETENTION_HEALTH is SUPPORTING/REPOSITORY_HEALTH, waivable only by the project owner within the exact task-evidence scope (plan.md:1663).
- The route observation is a bounded one-shot evidence input for the CORE check CUA_ROUTE_DECISION — not a production step; its failure/absence closes nothing by itself and never proves a bridge necessary (plan.md:1604-1635; §20.3).
- Non-gating residual review notes (from review/attempt-28 and review/attempt-29; advisory only — do NOT gate, do NOT reopen, do NOT create a new wave, and do NOT edit plan.md for any of them: the plan must stay byte-identical at `4919d871…`, because any plan edit invalidates both approvals):
  - RV-28-1 | MINOR | GROUNDING | §20.1: gate-3 is a Plan-authored formalization; advisory — when writing `gate-3-authorization.json`, cite per input (transcript line 5091 「2.要，授權給你」, the 「再點一次 ⋮」 grant, for the album-level ⋮ input; lines 6206/6224 for the directed album-card entry input) and label gate-3 as the corrected execution of that grant. Non-gating.
  - RV-28-2 | MINOR | TEST | §20.2 S7/S9: S7/S9 omit the "when screen_scope=AVAILABLE" phrasing Rev19 carried (S2 makes `UNAVAILABLE` non-fatal; the run stays window-scoped); advisory — treat S7/S9 captures as conditional on `screen_scope=AVAILABLE`, keep the app-pre/app-post comparison as the detector basis under `UNAVAILABLE`, and do not improvise captures under UNAVAILABLE. No safety impact; fail-closed AFFIRMATIVE rule unchanged. Non-gating.
  - RV-28-3 | MINOR | SEMANTIC_CONTRACT | §20.2 S10 / §20.3: the documented item-order check is recorded but not wired into route resolution; advisory — a transcription that lacks the documented items (notably Save All) is reported as a scope fact toward the owner-decision/new-revision branch rather than silent route resolution. Mitigations already in force; production re-establishes the route under its own revision/gate. Non-gating.
  - RV-28-4 | MINOR | HANDOFF | plan.md:1624: "The single Human Gate" sentence enumerates three independent gates; the parenthetical ("none of the three gates resets or extends another's budget") disambiguates. Reader confusion only; no requirement change. Non-gating.
  - RV-28-5 | MINOR | GROUNDING: four numeric cross-references inside the preserved §19 records point at Rev19 line numbering (+144 drift: 1189→1333 TARGET_ALBUM; 1594→1738 DONE source-correspondence clause; 1162/1311-1318→1306/1455-1462 read-only legacy formal state; 1490/1596→1634/1740 ROUTE_NOT_NEEDED clauses). Named anchors (TARGET_ALBUM, DONE, §19.3 by name) preserve the resolution path; no instruction or verdict affected. Non-gating.
  - RV-29-1 | MINOR | GROUNDING: critical-path item 7 (plan.md:1403) still reads "one ellipsis GUI input only"; §20.2 and the updated route section (plan.md:1606, 1619-1621) control — the corrected run's two inputs (one directed album-card navigation input + one album-level ⋮) are authorized; do not refuse the corrected run from the stale literal. Non-gating.
  - RV-29-2 | MINOR | GROUNDING: plan.md:1398/1415 retain "Rev15" self-references; the binding meaning is "the current revision at handoff time" (§15.6, header, §16.8). This handoff binds Rev20; a Rev15-bound handoff is impossible because §16.8 and the header void earlier approvals and Stage 03/04 enforce plan-revision/hash matching. Non-gating.
  - RV-29-3 | MINOR | GROUNDING | §20.3: the non-AFFIRMATIVE enumeration (plan.md:126-127) omits S5's `INCONCLUSIVE` and S2's `PERMISSION_PROMPT_OBSERVED`; the operative clause is "Non-AFFIRMATIVE" and both route to the same blocker branch via SAFE_ABORT — enumeration incomplete only. Non-gating.
  - RV-29-4 | MINOR | GROUNDING | §20.2 precondition 2: the v2 card-⋮ locator's corroboration role lacks a named step; its verdict is corroborating-only, never blocking, and never supplies the click point (its output point is recorded only). Non-gating.
  - RV-29-5 | MINOR | SECURITY | §20.2 S11: the observed album-level menu may be left open (no close input is authorized); advisory — make the S11 owner notification explicit that the menu may contain 儲存全部/Save All and must be closed by the owner without activating any item; optionally record the documented safe-close deviation in the route result. No authorization change. Non-gating.
  - RV-29-6 | MINOR | TEST | §20.2 precondition 2: the plan requires the v3 tools "with their frozen self-tests" but not explicitly that the summary passed and was recorded before any input; advisory — record "frozen and self-tested (summary recorded, all pass) before any input". Non-gating.

## DEFERRED_NOT_THIS_TASK
- Production Save-All/download, bridge repair/reinstall, TCC changes, legacy migration/normalization, OCR, historical cleanup, distributed exactly-once protocol, new persistence schema, unrelated bridge/controller refactor (plan.md:1315-1320, 1604-1635).
- Any production transaction against the formal project; any write to formal config/state/registry/run-log; any redownload of the existing 57 files (plan.md:1306-1313).
- Any further GUI input beyond the corrected attempt-05's two at-most-once inputs (menu item, Save All, chooser, keyboard) needs a new gate + new revision + fresh review; a later production Save-All flow must re-establish the route under its own new plan revision and gate with its own budget before it may act (plan.md:60-118, 1604-1635; §19.3, §20.3).

## REPO_ANCHOR
- Project root: /Users/hsiaojohnny/Documents/ChatGPT/Line_backup
- Branch: master
- Anchor HEAD: 1c8777c (Rev20 dual-approval commit; planner Rev20 = 72d6ed0; route attempt-04 artifacts (W4) = 8655c25; Rev19 W1–W3 = 2983f44; Rev19 handoff = 661999a)
- Relevant dirty state: none at compile time (`git status --porcelain` clean before this regeneration); this regeneration itself adds/modifies only `handoff.md` and `handoff-history/handoff-plan-r19-20260917-165617.md` and is pending the orchestrator's commit.
- Drift since plan/review: none — plan.md SHA256 re-verified at 4919d87148c68ea3d70cbb9abd258edbd0bfa0b55be5123f7202251e557db17c (233,123 B / 1,744 lines), byte-identical to review/attempt-28/plan_snapshot.md and review/attempt-29/plan_snapshot.md; Stage 04 stops on any handoff↔plan mismatch.

## CURRENT_STATE_DELTA
- The Rev19 wave executed and is evidenced (`execution-rev19.md`; its §1 reports, not independently verified by Stage 05: PRIMARY `UNKNOWN` / IMPLEMENTATION `COMPLETE` / CORE `PASS` (scoped) / REQUIRED_VERIFICATION `PASS` / INDEPENDENT_ACCEPTANCE `PENDING` / CLOSURE `READY_FOR_INDEPENDENT_ACCEPTANCE`). The plan header keeps the Stage-05-attempt-05-verified six-axis snapshot (`evidence/20260916-stage05/attempt-05/closure-arithmetic.json`): PRIMARY `UNKNOWN`, IMPLEMENTATION `COMPLETE`, CORE `BLOCKED`, REQUIRED_VERIFICATION `PASS`, INDEPENDENT_ACCEPTANCE `PASS`, CLOSURE `CORE_ACCEPTANCE_BLOCKED`.
- The §19.1 v1.1 record exists (passed its fail-closed construction gate): `evidence/20260916-user-fact/source-identity-user-fact.confirmed.v1.1.json` — 1,741 B / `a8c10551…` — plus its authoring-note; §16.4 check 16/16 (per execution-rev19.md; Stage 05 attempt-06 re-verifies — it is a re-verification gate, not a re-authoring). v1 (`2cd7eccd…`, 1,746 B) and the frozen gate artifact (`03ffff57…`, 1,438 B) untouched.
- Route attempt-04 executed exactly once (one ⋮ input at the album-list card control): `route_status=AFFIRMATIVE` for that control (5/5 screen-scoped captures `MENU_DETECTED`; transcribed items 上傳項目 / 修改相簿名稱 / 分享相簿 / 刪除相簿; no download entry; zero side effects); final ledger `df0e9c44…` (11,463 B) is attempt-05's `parent_ledger_sha256`. Frozen attempt-04 records: route-runbook v2 `30253892…`, gate-2-authorization `f3d17790…`, route-result `6f71c5e7…`, menu-analysis `81d430bd…`, screen-probe `912e962b…`. The owner then corrected the target (transcript lines 6206/6224): the ⋮ that matters is inside the album; Rev20 re-specifies the corrected route.
- `evidence/20260916-route/attempt-05/` does NOT exist yet; `gate-3-authorization.json`, the v3 runbook, and the v3 tools (`locate_album_card.py`, `verify_album_open.py`, `locate_album_ellipsis.py`) are Stage 04 deliverables of this revision. In-tree frozen tools to reuse unchanged: `detect_menu_popup.py` (`6ae9c250…`, 6,339 B) + selftest summary (`d8ffc129…`, 2,946 B), `locate_card_ellipsis.py` (`8c8b6fc7…`, 8,943 B).
- Stage-05 attempt-06 has NOT run; it binds to the then-current revision (20) and re-derives closure per §20.3. Expected tuple only if `AFFIRMATIVE` and the §16.4 check over the v1.1 record passes (never assumed here): PRIMARY `ACHIEVED`, IMPLEMENTATION `COMPLETE`, CORE `PASS`, REQUIRED_VERIFICATION `PASS`, INDEPENDENT_ACCEPTANCE `PASS`, CLOSURE `DONE`, BASELINE_REGRESSION_DELTA `UNCHANGED`.
- Formal read-only compatibility profile (plan.md:1455-1462) unchanged: for the current requested 禎 key vs persisted 楨 key the expected real-command axes are Filesystem=PASS, Registry=FAIL, Source=UNRESOLVED, State=LEGACY_PROVENANCE_LIMITED, Overall=UNKNOWN, exit 4 (config/schema failure is the only exit-2 case). These are disclosed axis facts, never contradictions.
- Residual non-gating review notes RV-28-1…5 and RV-29-1…6 are carried under `BEST_EFFORT_DO_NOT_GATE` (advisory; no new waves; no plan edits).

## MUST_READ_PLAN
Rev20 §20.1–§20.5 (plan.md:22-163); Rev19 §19.1–§19.5 (plan.md:165-307; §19.1 174-214, §19.2 216-255, §19.3 257-285); §16.4's matching rules as corrected by §17.4 (plan.md:634-685, 450-458); goal contract (plan.md:1286-1321 — PRIMARY_OUTCOME 1288, CORE_REQUIREMENTS 1290, SUCCESS_EVIDENCE 1298, MUST_NOT_BREAK 1306, NON_GOALS 1315); authoritative roots and product boundary (plan.md:1322-1394 — TARGET_ALBUM 1333, DATA_DESTINATION 1329); critical path (plan.md:1395-1404); requirement/current-evidence/closure table (plan.md:1405-1416); source/registry/legacy outcome contract (plan.md:1417-1442); product verifier oracle and failure containment (plan.md:1443-1454); current formal config/legacy compatibility profile (plan.md:1455-1462); runtime route, ledger authority, GUI gate, truthful E2E (plan.md:1604-1635); Canonical Status Contract v2 + 13-row check table + baseline rules + status fixtures (plan.md:1636-1727 — check table 1649-1663, baseline rule 1665); closure and sequencing (plan.md:1728-1741 — DONE 1738). Carry-forward context, read only if a check needs its exact literals: axis-correct fixture matrix (plan.md:1463-1525); transaction product contract and case protocol (plan.md:1526-1603).

## SETTLED_DO_NOT_REOPEN
- Rev20 §20.1 (plan.md:32-58): gate-3 — the corrected target, the two-input scope, at-most-once/zero retry, the recorded alternative reading, independence from the attempt-03/04 budgets, and owner supersession.
- Rev20 §20.2 (plan.md:60-118): the corrected attempt-05 preconditions, sequence S1–S11, budgets (`album_card_input_budget=1`, `ellipsis_input_budget=1`, every other class 0) and hard boundaries; the documented item-order check is recorded, never used to compute or click a row.
- Rev20 §20.3 (plan.md:120-140): route resolution and closure routing after attempt-05; the expected closure tuple is re-derived by Stage 05 attempt-06 from fresh evidence, never assumed.
- Rev19 §19.1–§19.3 (plan.md:174-285): the v1.1 record contract, attempt-04's preserved history, and the `ROUTE_NOT_NEEDED` rationale + its four preconditions.
- Rev18 §18.1–§18.6 (plan.md:309-359) and Rev17 §17.1–§17.10 / Rev16 §16.1–§16.10 decisions stand as corrected; no supersession layer is added.
- The fresh Stage-02 approvals (review/attempt-28 + review/attempt-29) bind only Rev20 at SHA `4919d871…`; any later plan edit increments PLAN_REVISION, voids this handoff and requires fresh review (plan.md:1728-1741; §20.4-§20.5).

## REVERIFY_ON_START
- Recompute plan.md SHA-256 and byte length; require exact match to PLAN_SHA256 (1,744 lines / 233,123 bytes), else STOP.
- Re-read both review reports (review/attempt-28 `d9bf574f…`, review/attempt-29 `540fda1a…`) and confirm each ends `FINAL_STATUS: PLAN_APPROVED` for PLAN_REVISION 20 at `4919d871…` with no BLOCKER/MAJOR; confirm both plan_snapshot.md files re-hash to `4919d871…`.
- `git status --porcelain` and `git log --oneline -5`: confirm no plan/product/review edits after `1c8777c` (this handoff regeneration is the only expected pending change until the orchestrator commits it).
- Frozen parent before the attempt-05 ledger: attempt-04 final ledger `df0e9c44…` (11,463 B; the new ledger's `parent_ledger_sha256`).
- Reused-unchanged tools before any input: `detect_menu_popup.py` `6ae9c250…` (6,339 B) + selftest summary `d8ffc129…` (2,946 B); `locate_card_ellipsis.py` `8c8b6fc7…` (8,943 B); the v3 tools must be frozen and self-tested before any input.
- The v1.1 record `a8c10551…` (1,741 B) and its §16.4 basis are Stage-05 attempt-06 re-verification inputs, not to be re-authored.
- Mutable read-only facts: formal DATA_CONFIG / DATA_STATE / DATA_RUN_LOG bytes and SHA-256; DATA_DESTINATION inventory (57 files, 17,924,900 bytes); current screen/window state and whether the owner has the target card visible in the album-list state. Recapture via the read-only baseline command if any doubt; a missing/unreadable pre-baseline is REQUIRED_VERIFICATION=INCOMPLETE, never PASS (plan.md:1661, 1665).
- Preserved prior-wave records (read-only): the Rev18-wave `execution.md`; `execution-rev19.md`; `e2e/attempt-05` FINAL_GATE `ACCEPTED_WITH_SCOPED_BLOCKER`; all prior route attempts (01–04) and Stage-05 attempts (01–05) roots.

## TRIGGERED_POLICIES
- workflow-routing.md (§7 Status Semantics v2, §7.4 baseline, §7.6 blockers, §7.7 Stage 04 routing, §7.8 Stage 05, §7.10 DONE, §7.11 artifacts)
- testing-verification.md
- security-privacy.md
- high-risk-change.md
- goal-alignment-design-economy.md
- debugging-recovery.md
- git-change-hygiene.md

## FIRST_ACTION
Execute the pre-freeze wave before ANY GUI input (CORE; offline; no GUI, no formal writes; plan.md:60-84): write and freeze, committed before the run — (a) `evidence/20260916-route/attempt-05/gate-3-authorization.json` recording the §20.1 `[DECIDED]` interpretation together with the verbatim owner messages and timestamps (lines 6206 and 6224; the grant at line 5091), the session/transcript references, the exact scope and the budgets (plan.md:32-58); (b) the v3 runbook `evidence/20260916-route/attempt-05/route-runbook.md`; (c) the v3 tools with their frozen self-tests under `evidence/20260916-route/tools/` — `locate_album_card.py` (album-card metadata locator), `verify_album_open.py` (album-open verifier), `locate_album_ellipsis.py` (album-level ⋮ locator); (d) recorded reused-unchanged SHAs for attempt-04's detector `detect_menu_popup.py` (`6ae9c250…`) and the v2 card-⋮ locator `locate_card_ellipsis.py` (`8c8b6fc7…`), whose verdicts are corroborating-only, never blocking, and never supply the click point; (e) the fresh `evidence/20260916-route/attempt-05/run-ledger.json` with `parent_ledger_sha256` = attempt-04's final ledger `df0e9c44…` and budgets `album_card_input_budget=1`, `ellipsis_input_budget=1`, `app_acquisition_input_budget=0`, every other input class 0, `click_count=1` per input, zero retry, zero conversation images.

## IMPLEMENTATION_WAVES
Order is CORE-first; details remain in the plan.
1. W5-PREFREEZE (CORE; plan.md:60-84): FIRST_ACTION above — all frozen inputs written and committed before any input, including the fresh attempt-05 ledger chained to the attempt-04 final ledger.
2. W5-ROUTE-ATTEMPT-05 (CORE; plan.md:60-118; only after the owner presents the exact target card in the album-list state): S1–S11 exactly as §20.2 — one album-card metadata left click at the S3 current-frame-derived point; the second input (album-level ⋮ at the S6 point) is sent only after `verify_album_open.py` returns `ALBUM_OPEN_VERIFIED`; every non-verified verdict stops before it; `NO_TARGET_ON_FRAME` / `NO_ELLIPSIS_FOUND` / `AMBIGUOUS_ELLIPSIS` / `GROUP_LEVEL_ONLY` / `PERMISSION_PROMPT_OBSERVED` → record, notify the owner immediately, stop (at most five read-only observation windows; never a long loop); zero retry; zero conversation images; AFFIRMATIVE only via machine-observable evidence (new AX menu element or detector new region + ≥2 transcribed menu strings); no keyboard/menu item/Save All/chooser/state-config-run-log write/AX write/guessed or historical coordinate/scrolling/re-entry.
3. W5-DELTA-RECORD (CORE deliverable; plan.md:115-118; workflow-routing §7.11): write `route-result.json`, `run-ledger.json` (FINAL with the counts + `route_result_sha256`), `manifest.json`, `album-card-locate.json`, `album-open-verify.json`, `album-ellipsis-locate.json`, `screen-probe.json`, `menu-analysis.json`, plus the durable §7.11 execution record `execution-rev20.md` (six orthogonal statuses, matrix/results, scoped blockers, next action, evidence references, PLAN_REVISION/HANDOFF identity, artifact hashes); frames stay in /tmp (bytes/size/SHA-256 only); prior attempts and frozen records never touched; no formal writes; no download.
- After W1–W3, Stage 05 attempt-06 (fresh) re-derives closure from fresh evidence per §20.3; Stage 04 must not pre-claim its outcome. BRIDGE_READINESS and DOCUMENTATION_RETENTION_HEALTH remain SUPPORTING; menu-item/Save-All/chooser/keyboard stay outside every wave.

## ACCEPTANCE_CONTRACT
Required per-check fields: CHECK_ID | COMMAND/SCENARIO | GOAL_CRITICALITY | EVIDENCE_ROLE | CLOSURE_GATE | BASELINE_RULE | FAILURE_ROUTING | WAIVER_ALLOWED | WAIVER_AUTHORITY. The plan's authoritative 13-row policy table is plan.md:1649-1663 (status vocabularies plan.md:1640-1645; baseline rules plan.md:1665); every check additionally carries CHECK_RESULT and WAIVER_STATUS; no agent approves its own waiver.
- CORE first: VERIFIER_FALSE_POSITIVE_REPRO, VERIFY_REAL_DESTINATION, VERIFY_NEGATIVE_FIXTURES, TRANSACTION_RESUME_CORE, TRANSACTION_COMMIT_CORE, FORMAL_STATE_READONLY_RECONCILIATION, SOURCE_CORRESPONDENCE, CUA_ROUTE_DECISION, STATUS_CLOSURE_CONTRACT, INDEPENDENT_ACCEPTANCE, BASELINE_REGRESSION_DELTA — all HARD_CLEAN except BASELINE_REGRESSION_DELTA (BASELINE_DELTA); all WAIVER_ALLOWED=NO / WAIVER_AUTHORITY=NONE. The six long-standing checks carry forward from prior Stage-04/05 attempts with freshness; this revision's open items are CUA_ROUTE_DECISION (route attempt-05 + §20.3 routing), SOURCE_CORRESPONDENCE (v1.1 record re-check at attempt-06), INDEPENDENT_ACCEPTANCE (attempt-06), STATUS_CLOSURE_CONTRACT and BASELINE_REGRESSION_DELTA (re-derived with fresh evidence; expected UNCHANGED).
- Expected orthogonal status fields (Status Contract v2; re-derived by Stage 05 attempt-06, never assumed): if `AFFIRMATIVE` and the §16.4 check over the v1.1 record passes → PRIMARY_OUTCOME_STATUS `ACHIEVED`, IMPLEMENTATION_STATUS `COMPLETE`, CORE_ACCEPTANCE_STATUS `PASS`, REQUIRED_VERIFICATION_STATUS `PASS`, INDEPENDENT_ACCEPTANCE_STATUS `PASS`, TASK_CLOSURE_STATUS `DONE`, BASELINE_REGRESSION_DELTA `UNCHANGED`; the disclosed axis facts (Registry=FAIL, State=`LEGACY_PROVENANCE_LIMITED`, product-level Source=`UNRESOLVED`) are reported as axis facts and never promoted (plan.md:134-138). If non-AFFIRMATIVE: recorded honestly with zero side effects; the route stays a scoped CORE blocker and the owner is notified immediately; closure may only proceed as `ROUTE_NOT_NEEDED` under the owner's explicit decision with §19.3's four preconditions (plan.md:257-285) re-verified by Stage 05 plus attempt-05's evidence, or by opening a NEW revision — never automatically; that state is a route-scoped CORE blocker, never `IMPLEMENTATION_BLOCKED` — implementation is COMPLETE and verification blockers never collapse into it (plan.md:1640-1645, 1728-1741).
- Baseline rule: pre-change artifact SHA-256/bytes/argv/cwd/env fingerprint; the post-change rerun of the same argv must show `baseline_delta=UNCHANGED`; WORSENED → FAIL/FIX_REQUIRED; missing/unreadable pre-baseline → REQUIRED_VERIFICATION=INCOMPLETE, never PASS (plan.md:1661, 1665, 1526-1603).
- Stage 04 durable record: a workflow-routing §7.11 execution record is mandatory for this wave's material runs — `execution-rev20.md` with the six statuses, matrix/results, scoped blockers, next action, evidence references, PLAN_REVISION/HANDOFF identity and artifact hashes (plan.md:115-118, 1732); preserve the frozen Rev18 `execution.md` and `execution-rev19.md`; never overwrite frozen evidence.
- Stage 05 carry-forward/freshness: before acceptance, attempt-06 snapshots STAGE_04_REPORTED_IMPLEMENTATION_STATUS, STAGE_04_REPORTED_CORE_ACCEPTANCE_STATUS, STAGE_04_REPORTED_REQUIRED_VERIFICATION_STATUS, STAGE_04_EXECUTION_ARTIFACT_SHA256 plus TASK_ID, PLAN_REVISION, HANDOFF_SHA256, execution timestamp and evidence path; it freshness-checks the execution record and must never downgrade proven implementation because acceptance could not run (workflow-routing §7.8; plan.md:1732).
- Degraded-mode semantics: source UNRESOLVED / LEGACY_PROVENANCE_LIMITED keeps the album-data result UNKNOWN/BLOCKED with no DONE while still reporting filesystem findings; a route-only gap is a scoped CORE blocker unless `ROUTE_NOT_NEEDED` is validly rationalized under §19.3's four preconditions plus the owner's explicit decision (§20.3); required-verification debt is INCOMPLETE, never PASS (plan.md:1417-1442, 1728-1741; §19.3).
- E2E: E2E_REQUIRED=NO; the controlled corrected run (two at-most-once inputs, observation only) is not full user-journey E2E; no fixture/offline result may be reported as production E2E (plan.md:9-10, 1606).

## STOP_AND_ESCALATE_IF
- Any change to semantic validity/requiredness/enums/gating/error/fallback/precedence/authority/retry/closure meaning → stop, write escalation evidence, replan; never improvise (plan.md:1306-1321, 1734).
- Handoff↔plan revision/hash mismatch, or any plan edit after this handoff → STOP (plan.md:1728-1741).
- Pre-freeze: any v3 tool self-test failure → do not run the attempt (advisory RV-29-6: record the frozen self-test summary as passed before any input) (plan.md:60-84).
- Attempt-05 preconditions: target card not on the current surface, or a surface already showing a menu/dialog/chooser/photo viewer/permission prompt → `NO_TARGET_ON_FRAME`: record, use at most five read-only observation windows, notify the owner immediately, stop (plan.md:62-67).
- Attempt-05: `PERMISSION_PROMPT_OBSERVED` → SAFE_ABORT without sending any input (S2, plan.md:86-90).
- Attempt-05: `verify_album_open.py` non-verified verdict (`TARGET_MISMATCH` / `NO_EFFECT` / `INCONCLUSIVE`) → stop before any ⋮ input; record, notify the owner, stop (S5, plan.md:96-99).
- Attempt-05: `NO_ELLIPSIS_FOUND` / `AMBIGUOUS_ELLIPSIS` / `GROUP_LEVEL_ONLY` at S6 → no ⋮ input; record, notify the owner, stop (S6, plan.md:100-103).
- Attempt-05: any second input at either control, retry, guessed or historical coordinate, menu-item selection, Save All, chooser interaction, keyboard input, or state/config/run-log write → hard stop, preserve evidence; any such further input needs a new gate + new revision + fresh review (plan.md:60-118, 1624, 1734; §20.3).
- AFFIRMATIVE must remain machine-observable; OCR-only, human-report-only, or incomplete capture → record UNKNOWN, never AFFIRMATIVE (S10, plan.md:108-113).
- §20.3: non-AFFIRMATIVE outcomes may not be converted to closure automatically; `ROUTE_NOT_NEEDED` requires the owner's explicit decision + all four §19.3 preconditions re-verified by Stage 05; otherwise the route stays a scoped CORE blocker and closure may only proceed under a NEW revision (plan.md:120-140, 257-285).
- Any 禎/楨 mismatch, missing exact-source proof, first-match/ambiguous registry match, or attempted merge/normalization → scoped CORE blocker; never merge or rewrite (plan.md:1417-1442).
- Any failed/uncertain authority validation, state replacement, read-back, revision/owner check or unresolved dispatch barrier → safe abort, no retry, preserve evidence (plan.md:1526-1603).
- Any ambiguous runtime dispatch or undocumented Save-All/menu identity → no Save-All retry; stop at the Human Gate pattern only after all automatic work is complete, specifying the exact app/group/album/count, the permitted inputs and the safety purpose (plan.md:1226-1240, 1604-1635).
- Bridge/service repair, TCC changes, reinstall, new permissions, deployment or production download are outside this handoff; prepare them only after route-specific causal proof and a later approved revision (plan.md:1315-1320, 1604-1635).
- A `baseline-worsened` delta is a hard failure (FIX_REQUIRED), never INCOMPLETE/PENDING_REQUIRED_VERIFICATION (plan.md:1665; workflow-routing §7.7 rule 8).

## HISTORICAL_TASK_DEPENDENCIES
- NONE for generic context. The root /Users/hsiaojohnny/Documents/ChatGPT/Line_backup/handoff.md is historical/non-authoritative background only (it predates this TASK_ID) and must not be used as a plan or contract (plan.md:1730).
- Prior handoffs of this task are archived (keep as history; never execute from any of them):
  - `handoff-history/handoff-plan-r13-20260917-021523.md` (Rev13-bound);
  - `handoff-history/handoff-plan-r18-20260917-154007.md` (Rev18-bound; superseded);
  - `handoff-history/handoff-plan-r19-20260917-165617.md` (Rev19-bound; superseded by this handoff; archived 2026-09-17 16:56 +0800, byte-identical to the superseded handoff `361d4b27…`).
