# Handoff — LINE album acceptance: Rev21 macOS-Vision reader wave (v4) with preserved album/route state

## TASK
- TASK_ID: T20260916-0102-01-line-backup-acceptance
- STATUS: READY_FOR_IMPLEMENTATION
- PLAN_PATH: .agent/tasks/T20260916-0102-01-line-backup-acceptance/plan.md
- PLAN_REVISION: 21
- PLAN_SHA256: 466bda4ad79897cf5f6395beafc0a70c57d99ed4dc15f4b78328fb5c69b68190
- REVIEW_REQUIRED: YES
- REVIEW_REPORT: review/attempt-30/review_report.md (fresh Stage-02 review, PLAN_APPROVED, SHA-256 92d5be16aa21ce0a07b6dbe0210ab00cf6d995198ecb59599146ed59d8284729) + review/attempt-31/review_report.md (parallel fresh review, PLAN_APPROVED, SHA-256 c80a14da9a6449226052230c8c7a635e8b2eb997ed4cf67dfb0dda22f6ab0aa5); both bound to Rev21 at `466bda4a…`, zero BLOCKER/MAJOR
- REVIEWED_PLAN_REVISION: 21
- REVIEWED_PLAN_SHA256: 466bda4ad79897cf5f6395beafc0a70c57d99ed4dc15f4b78328fb5c69b68190
- INDEPENDENT_ACCEPTANCE_REQUIRED: YES
- E2E_REQUIRED: NO (plan.md:10-11, §21.5-§21.6, 1735; the Rev21 wave's acceptance is an offline replay over already-frozen frames — INTEGRATION level, never reported as live E2E. The album-data subject keeps its prior rationale: a read-only verify-only pass over an existing destination; no production download is authorized.)
- ACCEPTANCE_MODE: INTEGRATION for the Rev21 wave (offline replay over the durable frozen frames + the v4 tool set; §21.6). The album-data subject keeps its previous E2E decision (plan.md:11).
- Fresh Implementer required: YES
- Planner/Reviewer transcript required: NO

## GOAL_ANCHOR
PRIMARY_OUTCOME for this wave (owner-directed 2026-09-17; H3.0 §1; plan.md §21.1): (1) the OCR reader actually used by the official route-verification chain (the three v3 tools under `evidence/20260916-route/tools/`) becomes a macOS-native Vision reader (`VNRecognizeTextRequest`, system frameworks only) delivered as a new versioned v4 tool set with every v3 byte frozen; and (2) a bounded, offline, re-runnable AI-agent acceptance test (C1-C5) proves the replacement is effective on the key fields (date title, photo count) and that the attempt-05 `75 ≠ 57` blocker is a reader artifact, not a data problem. The album-data result ① (57-file destination validity) is not advanced by this wave; its prior facts are preserved.
SUCCESS_EVIDENCE: v4 tool set present with its 16-case self-test green (`result: PASS`, `cases_failed: 0`); C1-C5 green in one agent-e2e attempt on the four durable frozen frames with raw readings + SHA-256 (C1 post count `57`; C2 5× byte-identical helper stdout + per-tool JSON; C3 title `2024/05/13~05/17` on pre + post; C4 v4 `verify_album_open.py` on (pre, post) → `ALBUM_OPEN_VERIFIED` exit 0 labelled `DEMONSTRATION_ONLY`; C5 s1/s2 `57`); append-only attempt evidence; Stage-05 independent re-run at `e2e/attempt-06`; every prior v2/v3/route artifact byte-identical (`baseline_delta=UNCHANGED` keyed on the durable frame copies).
MUST_NOT_BREAK (plan.md §21.1 NFR-VR-1, §21.4-§21.6, §21.9): zero GUI input / zero capture this wave (no click, no ⋮, no menu item — Save All named explicitly — no chooser, no keyboard, no AX write, no production Save-All/download); v2/v3 tools, v3 self-test artifacts, attempt-01..05 route evidence, run-ledgers, gate authorizations, formal config/state/run-log, and the 57 photos stay read-only / byte-identical; `禎` U+798E and `楨` U+6968 are never merged or normalized; no third-party dependency; the helper source `vision_ocr.swift` is never edited; zero conversation images (paths + SHA-256 only); fail-closed semantics are never loosened (a reader failure reaches a documented refusal path — never a guess, never a crash); supplementary evidence (the Vision crosscheck) never overturns the frozen attempt-05 `TARGET_MISMATCH` verdict.
ROUTE (owner-reserved; unchanged): attempt-05 remains stopped (`STOPPED_AT_S5_TARGET_MISMATCH`, `owner_decision_required: true`). Route closure A `ROUTE_NOT_NEEDED` / B new revision + new one-shot ⋮ gate belongs to the owner alone; Rev21 grants nothing over the route, C4 can never set `CUA_ROUTE_DECISION` or authorize the ⋮, and no agent may take this decision.

## CRITICAL_PATH
Base-wave, product implementation, Rev19 wave and Rev20 route attempt-05 are delivered and preserved (evidence/20260916-baseline/attempt-02, evidence/20260916-auto-verification/attempt-02, evidence/20260916-stage05/attempt-04+05, execution.md; execution-rev19.md; attempt-05 artifacts) — not repeated. The Rev21 wave's live critical path (offline; zero GUI):
1. W6-V4-TOOLCHAIN (CORE; plan.md §21.3-§21.4): create `evidence/20260916-route/tools/v4/` — `vision_reader.py`, `locate_album_card.py`, `verify_album_open.py`, `locate_album_ellipsis.py`, `README.md` — as v3 logic with only the reader layer replaced; create `tools/selftest/v4/` (`run_selftest.py`, `selftest-summary.json`, `README.md`); run the 16-case matrix to `result: PASS` / `cases_failed: 0`; record the v4-vs-v3 structural diff audit (only the reader path/lines differ) into the v4 self-test evidence; pin expectations to the frozen v3 literals (summary `17840e91…` + the v3 run_selftest source) plus the named reader-layer expectations.
2. W6-AGENT-E2E (CORE; plan.md §21.5): `evidence/20260916-route/agent-e2e/` — `README.md`, `runner/run_agent_e2e.py`, `attempt-01/{summary.json, raw/*, SHA256SUMS}`; deterministic orchestrator (no model calls in the loop); run C1-C5 (5 repeats for C2) on the four durable frames; bounded loop with the pinned 5-failure counter semantics; C4 labelled `DEMONSTRATION_ONLY` next to the frozen v3 result (`TARGET_MISMATCH` exit 4, count 75); record the 3x/6x/10x count sweep as raw evidence.
3. W6-DELTA-RECORD (CORE; plan.md §21.4 last bullet, §21.6; workflow-routing §7.11): write the durable execution record `execution-rev21.md` (six orthogonal statuses, check matrix/results, scoped blockers, Stage-04 snapshot fields, artifact hashes) and the post-change byte-identity re-verification (v2/v3 tools, v3 self-test summary `17840e91…`, all route evidence, four frames) with `baseline_delta=UNCHANGED`; `execution.md` and `execution-rev19.md` stay untouched.
4. Stage 05 (fresh) independent acceptance at `e2e/attempt-06` (§21.6): re-runs the same frozen inputs, snapshots the Stage-04 facts from `execution-rev21.md`, re-derives the tuple; INTEGRATION mode, never live E2E. A later route-closure acceptance (if ever authorized under a new revision) takes the next number after 06 (07).

## SEMANTIC_INVARIANTS
- Exactly one semantic delta: which reader produces the OCR token stream. Everything downstream (`REQ-VR-2`) is byte-for-byte v3: all gating rules, verdicts, exit codes, refusal classes and geometric parameters (frame reads 3x LANCZOS; count region 10x; every bbox/margin/band/click-point rule).
- Reader contract (plan.md §21.3): v3-shaped word records `{text, conf, x, y, w, h}` in original-frame coordinates (pixels divided by scale); token unit = the Vision line box, no re-tokenization; count normalization = v3 `digits()` + v3 classification (MATCH / MISMATCH / AMBIGUOUS_READ never refuses / UNREADABLE never refuses); `conf` is recorded evidence and never a gate; no case folding, no width normalization, no `禎`/`楨` merge, no historical coordinates.
- Helper resolution + failure semantics (pins RV-31-7 / RV-31-8): `$VISION_OCR_BIN` unset → may build once from the frozen Swift source (path/size/SHA recorded in the emitted result); `$VISION_OCR_BIN` set but invalid → no rebuild, empty words, refusal path. The reader must emit an outcome record (`ok | binary_missing | build_failed | nonzero_exit | unparsable | timeout`) with exit code/stderr recorded in the tool JSON, so "reader failed" is distinguishable from "no text". Fail-closed: helper missing/unbuildable, non-zero exit, load failure, timeout, unparsable → no words / empty region → `TARGET_TITLE_NOT_FOUND(2)`, `BAD_FRAME(6)`, or a recorded `UNREADABLE` count that never refuses; never a crash, never a guessed value.
- Determinism / C2 (pins RV-30-5 / RV-31-6): the emitted v4 tool JSON contains no timestamp and no process-unique path; C2's byte-identity applies to helper stdout + per-tool JSON only — the attempt summary (`summary.json`) is intentionally NOT part of the byte-identity comparison (it carries timestamp/argv/helper path by design). The tool-JSON half of C2 is first measured at Stage 04.
- v4 self-containment and copy-not-edit: the three v4 tools load the v4 `locate_album_card.py` from their own directory; v4 never imports v3 modules; v3 files remain byte-identical history (`DEC-VR-1`: copy, never in-place edit). Stage 04 records the v4-vs-v3 structural diff audit (pins RV-31-2 / RV-31-9): only the reader path/lines differ; v4 expectations = the pinned v3 literals + explicit new reader-layer expectations.
- Bounded loop (REQ-VR-4; pin RV-31-1): each check runs once per attempt (no intra-attempt retry); `failures_total` = the sum of failed check results across attempts, persisted by reading prior attempt summaries; at 5 cumulative failures the loop stops before starting a new attempt, writes the stop reason + evidence, and the owner is notified; every attempt is append-only (JSON + SHA-256), never rewritten or deleted; a later run continues at the next attempt number.
- Orthogonal product axes, never collapsed: Filesystem / Registry / Source / State / Overall; intent / dispatch / trigger / writer / terminal ownership remain separate. Registry follows only the §16.8 axis rule (plan.md:896, corrected by §18.1 at 441): a unique readable match with `verified_run_id` null (imported-evidence form) is Registry PASS; the `CASE_ROOT_25` real-state copy (楨 U+6968 never matches the requested 禎 U+798E album) and any unreadable/non-matching run are Registry FAIL. Provenance failures never flip the registry axis (§16.4 at 757, §17.3 at 552).
- Stable results: exit 0 only when all required acceptance axes pass; exit 4 for a completed, safely rejected negative/blocked result; exit 2 invalid invocation/configuration/authority before any read/write; exit 1 internal/read/command/artifact error. Refusal classes (SKIP_DUPLICATE, SKIP_TERMINAL, CONFLICT_*, NEEDS_RECONCILIATION, MISSING_DISPATCHER, MISSING_SOURCE_EVIDENCE, INVALID_STATE_LEGACY, READBACK_UNCERTAIN, …) are fixed and may not be renamed, merged, or reordered.
- Validation scope: a run without contract_revision / RC2-only intent fields / full provenance is a readable LEGACY record (STATE=LEGACY_PROVENANCE_LIMITED, SOURCE=UNRESOLVED), never initialized or rewritten; validation at every load must not contradict the read-only legacy contract (§17.3 at 552 as corrected by §18.1 at 441).
- Finalize is the only terminal path; `--verification-json` is outcome-conditional (required for `--outcome VERIFIED`, optional for `SAFE_ABORT`); a commit refuses with `CONFLICT_UNRESOLVED_DISPATCH` exit 4 no-write unless the loaded run carries the completed successful dispatch record.
- Status Contract v2 fields stay orthogonal (plan.md:1765-1860): PRIMARY_OUTCOME_STATUS | IMPLEMENTATION_STATUS | CORE_ACCEPTANCE_STATUS | REQUIRED_VERIFICATION_STATUS | INDEPENDENT_ACCEPTANCE_STATUS | TASK_CLOSURE_STATUS; no field rewrites another subject; `Done` means overall closure only; verification blockers never collapse into IMPLEMENTATION_BLOCKED.
- Baseline: `baseline_delta=WORSENED` is a must-not-break violation → REQUIRED_VERIFICATION=FAIL, closure FIX_REQUIRED, blocker BASELINE_REGRESSION_DELTA/FAIL/TASK_REGRESSION — never INCOMPLETE/PENDING_REQUIRED_VERIFICATION; a missing/unreadable pre-baseline is REQUIRED_VERIFICATION=INCOMPLETE, never PASS.
- §19.1-§19.3 (plan.md:288-430): the v1.1 user-fact record is a versioned re-authoring under owner authorization; §16.4's matching rules themselves are unchanged (no normalization, no relaxation); v1 and the one-shot gate artifact are never rewritten; a passing §16.4 check is the task-level source-correspondence basis (subject to Stage-05 re-verification) and does not claim the product CLI prints Source=CONFIRMED for the real destination.
- §20.1-§20.3 (plan.md:155-264): gate-3 authorized exactly the two corrected inputs (album-card entry, then album-level ⋮), each sent only if every §20.2 precondition holds, each at-most-once, zero retry; attempt-05 sent input #1 only and stopped at S5; non-AFFIRMATIVE is recorded honestly with zero side effects, leaves the route a scoped CORE blocker, and closure is owner-routed only (§19.3's four preconditions re-verified by Stage 05 plus attempt-05's evidence for `ROUTE_NOT_NEEDED`, or a NEW revision) — never automatic. Attempt-04's artifacts remain history and never established the album route.
- Rev21 execution-record naming (supplementary pin, no plan conflict): the Stage-04 durable §7.11 execution record for this wave is `execution-rev21.md` — per the per-wave convention (Rev18 `execution.md` frozen; Rev19 `execution-rev19.md`; the Rev20 route wave planned `execution-rev20.md`, plan.md:239). plan.md:1865's "write execution.md" refers to this §7.11 execution-record requirement itself; overwriting the frozen Rev18 `execution.md` is forbidden by the frozen-evidence/append-only invariants, so the per-wave filename is the only compliant reading.

## BEST_EFFORT_DO_NOT_GATE
- `NFR-VR-2` (helper build record) and the v4 README/docs are SUPPORTING / non-gating (plan.md §21.1, §21.6); a docs/build-record failure must stay local and may not block the wave.
- Deferred live screen-capture layer (`OOS-VR-4`, §21.5): non-gating; it may raise an OS permission prompt (an unbidden UI event) and adds no evidence for C1-C5.
- The group-name field (`旻謙允禎成長日記`): recorded as supplementary non-gating raw evidence only (Phase 0 records the native-1x misread `旻議…` conf 0.50 vs `旻謙…` on s1/s2); the gating fields for this wave are title + count only (pin RV-30-3). Do not elevate the group name to a check.
- `BRIDGE_READINESS` stays SUPPORTING/DIAGNOSTIC and `DOCUMENTATION_RETENTION_HEALTH` stays SUPPORTING/REPOSITORY_HEALTH (waivable only by the project owner within the exact task-evidence scope) per the plan's check table.
- Non-gating review pins (from review/attempt-30 + review/attempt-31; absorbed here as implementer / Stage-05 instructions — do NOT gate, do NOT reopen review, do NOT create new waves, and do NOT edit plan.md for any of them: any plan edit invalidates both approvals and this handoff):
  - RV-30-1 | MINOR | GROUNDING: the anchor figure "15/15" (plan.md header 22, §21.2 at 57, requirement row 1543) is unreconcilable; the measured truth is 14/14 recorded SHA-256 anchors with `handoff_sha_mismatches: []` (16 rows, 14 with a recorded SHA). Do not propagate "15/15"; cite "14/14 recorded anchors, zero mismatches" or the Phase 0 field directly.
  - RV-30-2 | MINOR | SEQUENCING: stale "Stage 05 attempt-06" literals for the route closure (plan.md:257/259/270/278/334/380/389/403/417 — including §19.3's header at 380) are superseded by §21.6's next-unused rule: this wave's Stage 05 = `e2e/attempt-06`; a later authorized route-closure acceptance takes 07.
  - RV-30-3 — group-name coverage (see above).
  - RV-30-4 | MINOR | GROUNDING: the §21.3 claim "measured conf 1.00 at 3x/6x/10x on the post frame" is true (independently re-run by a reviewer) but recorded nowhere → record the 3x/6x/10x sweep as raw evidence in the Stage-04 C1 evidence, or cite only the recorded readings (native conf 1.00; tool scales 3x frame / 10x count region).
  - RV-30-5 | MINOR | TEST: pin "no timestamp, no process-unique path in emitted tool JSON" (already pinned in SEMANTIC_INVARIANTS).
  - RV-30-6 | MINOR | GROUNDING: `phase0/baseline.json` `helper_rebuild.byte_identical_to_recorded: true` is ambiguous — it is computed as `sha256(/tmp/vision_ocr) == f54628e8…` (the recorded volatile binary still matches its own recorded SHA), while the rebuilt binary `ad140ef7…` is NOT byte-identical (swiftc non-determinism; equivalence is established via recorded stdout bytes). Clarify in the v4 README / Stage-04 notes (append-only; never rewrite the committed baseline).
  - RV-31-1 | MINOR | TEST: 5-failure counter semantics → pinned above (`failures_total` = sum across attempts, persisted, no intra-attempt retry, stop evaluated before a new attempt).
  - RV-31-2 | MINOR | TEST: require the v4-vs-v3 structural diff audit recorded in the v4 self-test evidence; v4 expectations = pinned v3 literals + explicit new reader-layer expectations.
  - RV-31-3 | MINOR | DATA: same baseline-field ambiguity as RV-30-6 → one-line clarification/addendum only; never rewrite the committed baseline.
  - RV-31-4 | MINOR | GROUNDING: Phase 0 reproduced raw readings only; the tool-level C1/C3/C5 checks first run at Stage 04/05 — label honestly and never cite Phase 0 as tool-level evidence.
  - RV-31-5 | MINOR | GROUNDING: the `BASELINE_REGRESSION_DELTA` comparison key is the durable frame copies under `evidence/20260917-vision-reader/frames/` (by SHA-256), never the volatile `/tmp` paths.
  - RV-31-6 | MINOR | TEST: C2 scope = helper stdout + per-tool JSON only; the attempt summary is excluded; the tool-JSON determinism half is first measured at Stage 04.
  - RV-31-7 | MINOR | SEMANTIC_CONTRACT: set-but-invalid `$VISION_OCR_BIN` = no rebuild = empty words/refusal; unset may build once (pinned above).
  - RV-31-8 | MINOR | TEST: reader outcome record (`ok|binary_missing|build_failed|nonzero_exit|unparsable|timeout` + exit code/stderr) into the tool JSON (pinned above).
  - RV-31-9 | MINOR | TEST: v4 self-test expectations pinned to the v3 literals (v3 summary SHA `17840e91…` + the v3 run_selftest source) with the new reader-layer expectations stated explicitly (16 cases; target `PASS`, `cases_failed: 0`).

## DEFERRED_NOT_THIS_TASK
- The route attempt-05 closure decision and any route continuation (owner-reserved; if chosen later it requires a NEW revision and a NEW one-shot gate).
- Production Save-All/download; bridge repair/reinstall; TCC changes; legacy migration/normalization; historical cleanup; distributed exactly-once protocol; new persistence schema; unrelated bridge/controller refactor (plan.md §21.9).
- The live screen-capture layer (`OOS-VR-4`) — deferred for this wave with a recorded rationale.
- OCR is no longer deferred in general: the Vision reader IS this wave's CORE. tesseract-replacement work beyond the official chain (e.g., the v2 side tools) stays out of scope.

## REPO_ANCHOR
- Project root: /Users/hsiaojohnny/Documents/ChatGPT/Line_backup
- Branch: master
- Anchor HEAD: e97b2b5 (Rev21 dual-review commit — attempt-30 Halley + attempt-31 Boyle both PLAN_APPROVED; planner Rev21 = 4b01522; Phase 0 baseline = adf1829; H3.0 = 1343b55; attempt-05 input #1 = e53f67b; Vision crosscheck = 4d6c307)
- Relevant dirty state: none at compile time (`git status --porcelain` clean before this regeneration); this regeneration adds/modifies only `handoff.md` and `handoff-history/handoff-plan-r20-20260917-204314.md` and is pending the orchestrator's commit.
- Drift since plan/review: none — plan.md SHA-256 re-verified at 466bda4ad79897cf5f6395beafc0a70c57d99ed4dc15f4b78328fb5c69b68190 (1,877 lines / 260,774 bytes), byte-identical to review/attempt-30/plan_snapshot.md and review/attempt-31/plan_snapshot.md; Stage 04 stops on any handoff↔plan mismatch.

## CURRENT_STATE_DELTA
- Rev21 approved: review/attempt-30 (`92d5be16…`, PLAN_APPROVED, Halley) and review/attempt-31 (`c80a14da…`, PLAN_APPROVED, Boyle), both bound to `466bda4a…`; zero BLOCKER/MAJOR; 15 non-gating MINOR pins (RV-30-1…6 + RV-31-1…9) absorbed above; no plan edit is permitted for them.
- Phase 0 baseline committed in `adf1829`: `evidence/20260917-vision-reader/phase0/baseline.json` (`894006f3…`) + `frames/` (`manifest.json` `7acf8fe8…`; four byte-identical preservation copies: pre `3d926e7d…`, post `4cb8a6b4…`, s1 `aea53a0d…`, s2 `7b9d0a19…`). Helper rebuild equivalence = stdout bytes vs recorded `raw_stdout_sha256` (post `5c86dda8…`, pre `07e3e156…`, s1 `561bb131…`, s2 `abf9d75e…`); the rebuilt binary is NOT byte-identical (swiftc). Phase 0's C1/C3/C5 are raw readings only — the tool-level checks first run at Stage 04/05 (RV-31-4).
- Frozen chain (must stay byte-identical): v3 tools `locate_album_card.py` `500fcadb…`, `verify_album_open.py` `80504262…`, `locate_album_ellipsis.py` `60e3120a…`; v2 side tools `detect_menu_popup.py` `6ae9c250…` + its selftest summary `d8ffc129…`, `locate_card_ellipsis.py` `8c8b6fc7…`; v3 self-test summary `17840e91…`; helper source `tools/vision/vision_ocr.swift` `4fc9fa2b…` (never edited); attempt-05 `run-ledger.json` `17b17203…`; `album-open-verify.json` `ffa5d963…` (`count_digits_read: "75"`, verdict `TARGET_MISMATCH`); `vision-ocr-crosscheck.json` `d3ebbaed…` (SUPPLEMENTARY_NON_AUTHORITATIVE, never promoted).
- The attempt-05 ledger's `handoff_sha256` = the superseded Rev20 handoff `ab88e496…`; that is a historical binding only — Rev21 binds this handoff, and a Stage-05 freshness check of the ledger must not treat `ab88e496…` as current.
- Route state: attempt-05 stopped at S5 (`STOPPED_AT_S5_TARGET_MISMATCH`, `owner_decision_required: true`); exactly one input was sent (album card, once); the ⋮ was never sent; Rev21 grants nothing over the route.
- `evidence/20260916-route/tools/v4/` and `evidence/20260916-route/agent-e2e/` do not exist yet — Stage 04 creates them as append-only additions (the review flagged this as a real add). `tools/vision/` exists (frozen helper source); `tools/selftest/v3/` holds the frozen 13-case matrix + summary.
- `.agent/tasks/T20260916-0102-01-line-backup-acceptance/e2e/` holds attempts 02-05 (no 01); the next unused number is 06 (this wave's Stage 05, §21.6); a later route-closure acceptance would take 07 (RV-30-2).
- Frozen execution records present and untouchable: `execution.md` (Rev18 wave), `execution-rev19.md`; the Rev21 wave writes `execution-rev21.md` (per-wave naming; plan.md:1865's "write execution.md" = this §7.11 requirement).
- The header status block remains the prior wave's verified snapshot (PRIMARY UNKNOWN / IMPLEMENTATION COMPLETE / CORE BLOCKED / REQUIRED_VERIFICATION PASS / INDEPENDENT_ACCEPTANCE PASS / CLOSURE CORE_ACCEPTANCE_BLOCKED) plus the clearly-labelled `REV21_WAVE_STATUS` line (all NOT_STARTED/PENDING); Stage 05 re-derives the wave tuple from fresh evidence — never assumed.
- Formal read-only compatibility profile unchanged: for the requested 禎 key vs the persisted 楨 key the expected real-command axes are Filesystem=PASS, Registry=FAIL, Source=UNRESOLVED, State=LEGACY_PROVENANCE_LIMITED, Overall=UNKNOWN, exit 4; these are disclosed axis facts, never contradictions.

## MUST_READ_PLAN
Rev21 §21.1-§21.9 (plan.md:29-143; §21.2 at 53, §21.3 at 62, §21.4 at 75, §21.5 at 93, §21.6 at 114, §21.7 at 122); Rev21 header (plan.md:1-24); Critical path items 8-9 (plan.md:1518-1529); requirement/evidence/closure table, incl. the Vision rows (plan.md:1530-1545); Runtime route, ledger authority, GUI gate, truthful E2E (plan.md:1733-1764); Canonical Status Contract v2 check matrix (plan.md:1765-1860; the four Rev21 rows `VISION_READER_TOOLCHAIN_V4` / `VISION_AGENT_E2E_C1_C5` / `VISION_READER_FAILCLOSED_MATRIX` / `V3_FROZEN_EVIDENCE_UNCHANGED` at 1791-1794; baseline rules ~1800); Closure and sequencing (plan.md:1861-1874); Owner view (plan.md:1875-1877). Carry-forward context, read only if a check needs its exact literals: Rev20 §20.1-§20.5 (plan.md:145-287) and Rev19 §19.1-§19.5 (plan.md:288-430) as preserved facts; Goal contract (1409-1444); Authoritative roots and product boundary (1445-1517); Source/registry/legacy contract (1546-1571); product verifier oracle (1572-1583); formal config/legacy profile (1584-1591); axis-correct fixture matrix (1592-1654); transaction product contract (1655-1732).

## SETTLED_DO_NOT_REOPEN
- Rev21 §21.1-§21.9: the Vision wave's scope, the REQ-VR/NFR-VR/OOS-VR classes, the v4-as-copy decision (`DEC-VR-1`), the reader contract, C1-C5, the bounded-loop rule, and the gating/veto audit (the four `VISION_*` rows are CORE / HARD_CLEAN / WAIVER_ALLOWED NO / AUTHORITY NONE).
- §21.6: C4 = `DEMONSTRATION_ONLY` (cannot set `CUA_ROUTE_DECISION`, authorize the ⋮, or change §20.3 routing); the route stays stopped with `owner_decision_required: true`; Stage 05 consumes the next unused e2e number (06).
- Rev20 §20.1-§20.5 (gate-3, attempt-05 artifacts) and Rev19 §19.1-§19.5 (v1.1 record, `ROUTE_NOT_NEEDED` preconditions) stand as preserved history; no supersession layer is added.
- Rev18 §18.1-§18.6 and the Rev17/Rev16 decisions stand as corrected.
- The fresh Stage-02 approvals (attempt-30 + attempt-31) bind only Rev21 at `466bda4a…`; any later plan edit increments PLAN_REVISION, voids this handoff, and requires fresh review.

## REVERIFY_ON_START
- Recompute plan.md SHA-256 + byte length; require exact match to PLAN_SHA256 (1,877 lines / 260,774 bytes), else STOP.
- Re-read both review reports and confirm each ends `FINAL_STATUS: PLAN_APPROVED` for Rev21 at `466bda4a…` with no BLOCKER/MAJOR; confirm both `plan_snapshot.md` files re-hash to `466bda4a…`.
- `git status --porcelain` / `git log --oneline -5`: confirm no plan/product/review edits after `e97b2b5` (this handoff regeneration is the only expected pending change until committed).
- Re-hash the four durable frames at test start (must match `3d926e7d…`, `4cb8a6b4…`, `aea53a0d…`, `7b9d0a19…`); a mismatch is an immediate stop with evidence — never a silent continue.
- Re-hash the frozen inputs before reuse: the three v3 tools + the two v2 side tools + the v3 self-test summary `17840e91…` + the helper source `4fc9fa2b…` + attempt-05 `run-ledger.json` `17b17203…` and route artifacts; all must remain byte-identical.
- Confirm `execution.md` and `execution-rev19.md` are untouched and that `execution-rev21.md` does not exist before Stage 04 writes it.
- Mutable read-only facts: formal DATA_CONFIG / DATA_STATE / DATA_RUN_LOG bytes and SHA-256; DATA_DESTINATION inventory (57 files, 17,924,900 bytes). Screen/window state is irrelevant this wave (zero GUI). A missing/unreadable pre-baseline is REQUIRED_VERIFICATION=INCOMPLETE, never PASS.

## TRIGGERED_POLICIES
- workflow-routing.md (§7 Status Semantics v2, §7.4 baseline, §7.6 blockers, §7.7 Stage 04 routing, §7.8 Stage 05, §7.10 DONE, §7.11 artifacts)
- testing-verification.md
- goal-alignment-design-economy.md
- git-change-hygiene.md
- security-privacy.md
- debugging-recovery.md
- high-risk-change.md

## FIRST_ACTION
Execute W6-V4-TOOLCHAIN, offline, zero GUI (plan.md §21.3-§21.4): create `evidence/20260916-route/tools/v4/` (`vision_reader.py`, `locate_album_card.py`, `verify_album_open.py`, `locate_album_ellipsis.py`, `README.md`) as byte-for-byte v3 logic with only the reader layer replaced; create `tools/selftest/v4/` (`run_selftest.py`, `README.md`); run the 16-case matrix to `result: PASS` / `cases_failed: 0`; record (a) the v4-vs-v3 structural diff audit proving only the reader path/lines differ (RV-31-2), (b) expectations pinned to the frozen v3 literals (v3 summary `17840e91…` + the v3 run_selftest source) plus explicit reader-layer expectations (count-region normalization `57張照片` → MATCH, helper-unavailable fail-closed via the outcome record, 5× determinism probe) (RV-31-9), and (c) the v4 README notes: no timestamp/process-unique path in tool JSON (RV-30-5), the `byte_identical_to_recorded` clarification (append-only; never rewrite the committed baseline) (RV-30-6/RV-31-3), and the helper resolution + outcome-record semantics (RV-31-7/RV-31-8). No v3 byte touched; no GUI input.

## IMPLEMENTATION_WAVES
Order is CORE-first; details remain in the plan.
1. W6-V4-TOOLCHAIN (CORE; plan.md §21.3-§21.4): FIRST_ACTION above; freeze `selftest/v4/selftest-summary.json` as the matrix evidence (16 cases; `PASS`; `cases_failed: 0`); the v4 file set is SHA-pinned into the execution record (the plan pins the v3 frozen set; the new v4 hashes are Stage-04 output).
2. W6-AGENT-E2E (CORE; plan.md §21.5): create `evidence/20260916-route/agent-e2e/` (`README.md`, `runner/run_agent_e2e.py`, `attempt-01/{summary.json, raw/*, SHA256SUMS}`); deterministic orchestrator, no model calls in the loop; C1-C5 on the four durable frames (5 repeats for C2); raw lines + conf + bboxes + SHA-256 recorded; C4 labelled `DEMONSTRATION_ONLY` next to the frozen v3 result (`TARGET_MISMATCH` exit 4, count read `75`); bounded loop with the pinned counter; `summary.json` records attempt id, timestamp, runner argv, python version, helper binary path/size/SHA, each check's raw command/exit/parsed verdict, per-artifact SHA-256, `failures_total`, `stop_reason`, `inputs_sent: 0`, `ui_interaction: none`, and the frame SHAs used; include the 3x/6x/10x count sweep as raw evidence (RV-30-4).
3. W6-DELTA-RECORD (CORE; plan.md §21.4 last bullet, §21.6; workflow-routing §7.11): write `execution-rev21.md` with the six orthogonal statuses (PRIMARY_OUTCOME / IMPLEMENTATION / CORE_ACCEPTANCE / REQUIRED_VERIFICATION / INDEPENDENT_ACCEPTANCE / TASK_CLOSURE) plus the wave tuple, the check matrix/results (incl. the four `VISION_*` rows), scoped blockers (scope/subject/result/class/evidence/next_action/owner/waiver fields), the Stage-04 snapshot fields and the artifact hashes (v4 files, selftest summary, agent-e2e attempt-01, and the UNCHANGED re-check results). Re-verify byte-identity of the v2/v3 artifacts, v3 self-test summary, route evidence and frames; record `baseline_delta=UNCHANGED` keyed on the durable frame copies (RV-31-5). Do not touch `execution.md` / `execution-rev19.md` / any frozen evidence.
- After W1-W3, Stage 05 (fresh) at `e2e/attempt-06` re-runs the same frozen inputs and re-derives the tuple (§21.6); Stage 04 must not pre-claim its outcome. `NFR-VR-2`/docs/the live-capture layer stay non-gating; the route stays stopped and owner-reserved.

## ACCEPTANCE_CONTRACT
Required per-check fields: CHECK_ID | COMMAND/SCENARIO | GOAL_CRITICALITY | EVIDENCE_ROLE | CLOSURE_GATE | BASELINE_RULE | FAILURE_ROUTING | WAIVER_ALLOWED | WAIVER_AUTHORITY (the plan's authoritative table is plan.md:1774-1795; every check additionally carries CHECK_RESULT and WAIVER_STATUS; no agent approves its own waiver). COMMAND/SCENARIO comes from the plan's per-check definitions (C1-C5 in §21.5; the wave's baseline command and the v4 self-test matrix in §21.4).
- CORE for this wave: `VISION_READER_TOOLCHAIN_V4` (OUTCOME; must be present with its self-test matrix green — 16 cases, `cases_failed` 0; a reader-layer regression changing a verdict/exit code is TASK_REGRESSION), `VISION_AGENT_E2E_C1_C5` (OUTCOME; a failed/absent check or a non-append-only attempt invalidates the wave's CORE acceptance; C4 stays DEMONSTRATION_ONLY and can never flip a frozen route verdict), `VISION_READER_FAILCLOSED_MATRIX` (MUST_NOT_BREAK; helper-unavailable/unparsable input must reach a documented refusal path with a non-zero exit; a crash or a guessed value is TASK_REGRESSION), `V3_FROZEN_EVIDENCE_UNCHANGED` (MUST_NOT_BREAK; any byte change to a v2/v3 tool, the v3 self-test summary, a route ledger/artifact or the attempt-05 frames is TASK_REGRESSION and stops the wave) — all HARD_CLEAN, WAIVER_ALLOWED NO, WAIVER_AUTHORITY NONE.
- Prior CORE checks carry forward with freshness (VERIFIER_FALSE_POSITIVE_REPRO, VERIFY_REAL_DESTINATION, VERIFY_NEGATIVE_FIXTURES, TRANSACTION_RESUME_CORE, TRANSACTION_COMMIT_CORE, FORMAL_STATE_READONLY_RECONCILIATION, SOURCE_CORRESPONDENCE, CUA_ROUTE_DECISION, STATUS_CLOSURE_CONTRACT, INDEPENDENT_ACCEPTANCE, BASELINE_REGRESSION_DELTA); this wave's open items are the four `VISION_*` rows + INDEPENDENT_ACCEPTANCE (attempt-06) + STATUS_CLOSURE_CONTRACT/BASELINE_REGRESSION_DELTA re-derived with fresh evidence; CUA_ROUTE_DECISION stays a scoped CORE blocker (owner-reserved) and is untouched by C4.
- Wave acceptance: one agent-e2e attempt with all five checks PASS plus the v4 self-test matrix green satisfies the wave's CORE acceptance; Stage 05 independently re-runs the same inputs and re-derives the tuple.
- Expected orthogonal status fields (re-derived by Stage 05, never assumed): if the wave is green and the route facts are preserved, the wave's result ② tuple is recorded in `execution-rev21.md` and re-derived by attempt-06; result ① keeps PRIMARY UNKNOWN / CORE BLOCKED (route) / CLOSURE CORE_ACCEPTANCE_BLOCKED until the owner decides. Never collapse verification blockers into IMPLEMENTATION_BLOCKED; never downgrade proven implementation because acceptance could not run.
- Baseline rule: the wave's pre-change baseline = the committed Phase 0 `baseline.json` + the four frame SHAs; the post-change run must show `UNCHANGED` for every prior signature (v3 tool SHAs, v3 self-test summary `17840e91…`, route evidence SHAs, frames) and may not silently omit any; the comparison key is the durable frame copies (RV-31-5). `WORSENED` → FAIL/FIX_REQUIRED; missing/unreadable pre-baseline → REQUIRED_VERIFICATION=INCOMPLETE, never PASS.
- Stage-04 durable record: `execution-rev21.md` (six orthogonal statuses, check matrix/results, scoped blockers, Stage-04 snapshot fields, artifact hashes). `execution.md` (Rev18) and `execution-rev19.md` stay untouched. plan.md:1865's "write execution.md" refers to this §7.11 execution-record requirement itself; per-wave naming applies (the Rev20 route wave used `execution-rev20.md` at plan.md:239).
- Stage-05 carry-forward/freshness: before acceptance, attempt-06 snapshots `STAGE_04_REPORTED_IMPLEMENTATION_STATUS`, `STAGE_04_REPORTED_CORE_ACCEPTANCE_STATUS`, `STAGE_04_REPORTED_REQUIRED_VERIFICATION_STATUS`, `STAGE_04_EXECUTION_ARTIFACT_SHA256` (= `execution-rev21.md`) plus TASK_ID, PLAN_REVISION, HANDOFF_SHA256, execution timestamp and evidence path; it freshness-checks the execution record and re-runs the same frozen inputs rather than trusting the Stage-04 summary.
- Failure routing: a wave CORE failure keeps result ② open (canonical contract routing), never rewrites result ①; a route-only gap is a scoped CORE blocker (owner-reserved); required-verification debt is INCOMPLETE, never PASS; `baseline-worsened` is a hard failure (FIX_REQUIRED), never INCOMPLETE/PENDING_REQUIRED_VERIFICATION.
- Degraded-mode semantics: helper unavailable → documented refusal path (empty words, non-zero exit) with the outcome record; a missing helper is never a silent pass; C4 `DEMONSTRATION_ONLY` never flips the frozen attempt-05 verdict.
- E2E: `E2E_REQUIRED=NO`; the wave is INTEGRATION (offline replay); no fixture/offline result may be reported as production/live E2E (plan.md:10-11, 1735).

## STOP_AND_ESCALATE_IF
- Any change to semantic validity/requiredness/enums/gating/error/fallback/precedence/authority/retry/closure meaning → stop, write escalation evidence, replan; never improvise (plan.md §21.9, §16.8).
- Handoff↔plan revision/hash mismatch, or any plan edit after this handoff → STOP (both approvals and this handoff are void).
- Any GUI input, capture, menu item, chooser, keyboard input, AX write, Save All, or production download needed to proceed → STOP: this wave is zero-GUI; the route is owner-reserved and requires a new revision + new gate.
- Any action that would decide the route closure (A/B), set `CUA_ROUTE_DECISION`, or flip the frozen `TARGET_MISMATCH` → STOP (owner decision only).
- A frame SHA mismatch at test start, or missing/unreadable frames → immediate stop with evidence; never a silent continue.
- `$VISION_OCR_BIN` set but invalid must NOT trigger a rebuild; if a case can pass only via the build path, that is a semantic deviation → STOP.
- 5 cumulative failures reached → stop before starting a new attempt, write the stop reason + evidence, notify the owner; no long retry.
- Any v2/v3 tool, v3 self-test summary, route ledger/artifact or frame byte change → TASK_REGRESSION (hard failure), never INCOMPLETE.
- A v4 self-test or C1-C5 result that would require loosening a rule, changing an expectation, or re-tagging a failure to pass → STOP (thresholds are not relaxed ad hoc).
- Any 禎/楨 merge/normalization attempt, any third-party dependency, or any helper-source edit → STOP.
- A corrupt/unreadable frame crashing with a Python traceback instead of `BAD_FRAME(6)`: pre-existing v3 behavior — v4 preserves it (candidate for a future revision, not this wave); do not silently "fix" it.
- A `baseline-worsened` delta → hard failure FIX_REQUIRED; never INCOMPLETE/PENDING_REQUIRED_VERIFICATION.

## HISTORICAL_TASK_DEPENDENCIES
- NONE for generic context. The root `/Users/hsiaojohnny/Documents/ChatGPT/Line_backup/handoff.md` is historical/non-authoritative background only (it predates this TASK_ID) and must not be used as a plan or contract.
- Prior handoffs of this task are archived (keep as history; never execute from any of them):
  - `handoff-history/handoff-plan-r13-20260917-021523.md` (Rev13-bound);
  - `handoff-history/handoff-plan-r18-20260917-154007.md` (Rev18-bound);
  - `handoff-history/handoff-plan-r19-20260917-165617.md` (Rev19-bound);
  - `handoff-history/handoff-plan-r20-20260917-204314.md` (Rev20-bound; superseded by this handoff; archived 2026-09-17 20:43 +0800, byte-identical to the superseded handoff `ab88e496…`).
- Frozen execution records: `execution.md` (Rev18 wave), `execution-rev19.md`; the attempt-05 run-ledger's `handoff_sha256` (`ab88e496…`) is a historical binding to the Rev20 handoff only.

TASK_ID: T20260916-0102-01-line-backup-acceptance
HANDOFF_PATH: .agent/tasks/T20260916-0102-01-line-backup-acceptance/handoff.md
PLAN_REVISION: 21
STATUS: READY_FOR_IMPLEMENTATION
NEXT_STAGE: 04_IMPLEMENT
