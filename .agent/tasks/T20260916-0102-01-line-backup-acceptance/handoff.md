# Handoff — LINE album acceptance: Rev24 corrected route attempt-07 (v5 position rule) + gate-5 one-shot

## TASK
- TASK_ID: T20260916-0102-01-line-backup-acceptance
- STATUS: READY_FOR_IMPLEMENTATION
- PLAN_PATH: .agent/tasks/T20260916-0102-01-line-backup-acceptance/plan.md
- PLAN_REVISION: 24
- PLAN_SHA256: 40eb01980c7e11f96b4b12c2db8f53728852f1ab6ca452dfb5e865fd5435a003
- REVIEW_REQUIRED: YES
- REVIEW_REPORT: review/attempt-36/review_report.md (fresh Stage-02 review, PLAN_APPROVED, SHA-256 f7a9d3ebaae5c56f7a93c1d98edebb127a331f784bf19b60aa2984598519aec5) + review/attempt-37/review_report.md (fresh independent review, PLAN_APPROVED, SHA-256 941580825840a3b14bc1da8cb3abf5cec748179481714d445c80ebc3140f1506); both bound to Rev24 at `40eb0198…`; MINOR findings only (RV-36-1/2, RV-37-1/2 — carried below as non-gating notes); zero BLOCKER/MAJOR
- REVIEWED_PLAN_REVISION: 24
- REVIEWED_PLAN_SHA256: 40eb01980c7e11f96b4b12c2db8f53728852f1ab6ca452dfb5e865fd5435a003
- INDEPENDENT_ACCEPTANCE_REQUIRED: YES
- E2E_REQUIRED: NO (plan.md header rationale: the album subject is a read-only verify-only pass over an existing destination — no production download is authorized; the bounded, owner-gated live GUI route observation is not full user-journey E2E. Acceptance = `e2e/attempt-08` re-deriving both closure facts from fresh evidence; Stage 05 runs zero GUI input of its own.)
- ACCEPTANCE_MODE: per plan header: INTEGRATION for the Rev21 wave (already consumed: `e2e/attempt-06`); Rev22 adds the owner-gated live GUI route observation (route attempt-06) as a second subject (accepted: `e2e/attempt-07`); Rev24 adds the corrected route attempt (route attempt-07) as a third subject; its independent acceptance is `e2e/attempt-08`, which must re-derive the corrected-route and §16.4 closure facts from fresh evidence (§24.5) and must not run any GUI input of its own.
- Fresh Implementer required: YES
- Planner/Reviewer transcript required: NO

## GOAL_ANCHOR
PRIMARY_OUTCOME (owner decision B at transcript line 2427, session 01a0af3c-6693-72b2-abf2-095a3317e9a4, verbatim `B我要用一個新的計畫來取代原本的計畫。…` 882 B `a600ac4c…`, recorded in `evidence/20260918-owner-decisions/owner-decisions-rev24.json` `be1f5173de9219bfba2a547223542dde17c6e6a23f653274122f0d8dcbe2360d`; option presentation line 2405 1,931 B `14905585…`; plan.md §24.1-§24.5): execute owner decision B exactly once — the corrected route attempt-07 under gate-5 with the v5 position rule (S6 bound to the frozen v5 locator; S3/S5 stay on the frozen v4 tools; the frozen tesseract detector reused byte-identically and exempt). Two at-most-once inputs only: (1) an album-card left click opening the target album (raw group 旻謙允禎成長日記; title `2024/05/13～05/17`; count 57), (2) an album-level ⋮ left click inside the opened album after `ALBUM_OPEN_VERIFIED`, at the v5 `click_point`. click_count 1 each; retry 0; observation only. NEVER click any menu item (Save All named explicitly); no chooser, no keyboard, no AX writes, no destination/formal-state writes, no re-download, zero images in conversation. Historical coordinates (304,50)/(305,50) never live input; S6 never fallback to v4.
SUCCESS_EVIDENCE: attempt-07 completes under the committed pre-freeze bundle with exactly its two authorized inputs and a machine-observable `AFFIRMATIVE` at the album level (a new AX menu element or the frozen detector's `MENU_DETECTED`) → `CUA_ROUTE_DECISION=PASS` for the corrected route; AND the §16.4 exact-equality check over `source-identity-user-fact.confirmed.v1.1.json` (`a8c10551…`) re-derives `CONFIRMED` → `SOURCE_CORRESPONDENCE=CONFIRMED`, closing BLK-01. Stage 05 `e2e/attempt-08` (fresh, zero GUI) independently re-derives BOTH facts from fresh evidence and writes `result.md`; only if both hold is the §24.5/§20.3 closure tuple expected: PRIMARY ACHIEVED / IMPLEMENTATION COMPLETE / CORE PASS / REQUIRED_VERIFICATION PASS / INDEPENDENT_ACCEPTANCE PASS / TASK_CLOSURE DONE, `BASELINE_REGRESSION_DELTA=UNCHANGED`.
MUST_NOT_BREAK (NFR-V24-1..4; §24.3; §24.6): every v1/v2/v3/v4 tool byte, the v3/v4 self-tests, the attempt-01..06 artifacts, `e2e/attempt-02..07`, gates 1-4, every ledger, the archived handoffs, the `result.md` history, the 57-file destination and all formal config/state/registry/run-logs stay byte-identical; `禎` U+798E and `楨` U+6968 are never merged or normalized; at-most-once inputs (click 1/input, retry 0, menu_item 0, keyboard 0, conversation_images 0); fail-closed stops; the frozen tesseract detector is reused byte-identically (never re-implemented or replaced); no menu-item activation, no chooser, no production Save-All/download, no destination or formal-state write; attempt-05's frozen `TARGET_MISMATCH` and attempt-06's frozen `NO_ELLIPSIS_FOUND` verdicts are never overwritten, re-scored or re-interpreted (v5 applies only to attempt-07's own fresh frames); the disclosed axis facts (Registry=FAIL, State=`LEGACY_PROVENANCE_LIMITED`, product-level Source=`UNRESOLVED`) are reported as axis facts and are never promoted.
ROUTE (owner decision; unchanged): attempt-05 remains stopped (`STOPPED_AT_S5_TARGET_MISMATCH`); attempt-06 remains stopped (`CLOSED_AFTER_INPUT_1`, S6 `NO_ELLIPSIS_FOUND`, ⋮ never sent, zero side effects; FINAL ledger `906c1433…`); gate-4 is spent and never extended; this handoff grants exactly §24.3's two inputs under gate-5 and nothing more. A non-AFFIRMATIVE outcome is recorded honestly with zero side effects and leaves the route a scoped CORE blocker; `ROUTE_NOT_NEEDED` is never taken (the owner chose B, OOS-V24-3); route closure belongs to the owner alone and no agent may take it. A further attempt needs a NEW revision (OOS-V24-5).

## CRITICAL_PATH
The Rev21 wave is delivered and was independently accepted (commit `287e2f5`; `e2e/attempt-06` report `14b44c94…`) — preserved, not repeated. The Rev22 wave is delivered and was independently accepted (`e2e/attempt-07` report `6416a5f4…`, six rows 5 PASS + 1 SUPPORTING FAIL; route attempt-06 S6 `NO_ELLIPSIS_FOUND`, ⋮ never sent, zero side effects) — preserved, not repeated. This wave's live critical path (max two GUI inputs; everything else read-only):
1. Pre-freeze bundle (CORE prerequisite; §24.3 preconditions 2-3): `evidence/20260916-route/attempt-07/` — W7-equivalent (Stage 04) creates then COMMITS it BEFORE any input (never overwrite committed bytes; finalize append-only if incomplete): `gate-5-authorization.json` (17-key mirror of gate-4; cites `evidence/20260918-owner-decisions/owner-decisions-rev24.json` `be1f5173…`, Rev24 `40eb0198…`, reviews 36/37, lines 2427/2405 verbatim+hashes, the two-input scope, `prior_gate e32ccd88…` spent), `route-runbook.md` (S1-S11 with §24.3's S6→v5 substitution; budgets; capture bounds; historical-coordinate ban; S6-no-fallback), `run-ledger.json` (`schema_version 1`; `parent_ledger_sha256` = attempt-06 FINAL `906c14330605122e9b600359892d4400171d62564707d50af26ef8d8bb32fa2c`; budgets `album_card 1 / navigation 1 / ellipsis 1 / retry 0 / menu_item 0 / keyboard 0 / app_acquisition 0 / conversation_images 0`; `click_count_per_input 1`), `tools/v5/` (corrected `locate_album_ellipsis.py` + byte-identical copies of v4 `vision_reader.py`/`locate_album_card.py`/`verify_album_open.py` + `README.md` with geometry/rejected-union/depth-60 rationale/replay commands), `selftest/v5/selftest-summary.json` (≥20 cases, `cases_failed 0`, with the corrected positive + four v5-specific negatives + 5× determinism + v5 tool SHAs), plus the durable offline-replay pair (v5 `ELIGIBLE` + v4 `NO_ELLIPSIS_FOUND` on `4cb8a6b4…`).
2. Preflight (S1-S3; zero input): binding read on a fresh frame + the v4 `locate_album_card` (`bb52aff1…`) — `ELIGIBLE` required; else record `NO_TARGET_ON_FRAME`, notify the owner, zero inputs, stop (resume-eligible). The S2 screen-scope probe is SUPPORTING/non-gating (`screen_scope=UNAVAILABLE` never blocks; `PERMISSION_PROMPT_OBSERVED` → SAFE_ABORT, no input).
3. Input #1 → S5 (CORE gate): exactly one album-card metadata left click at the v4 `click_point`; then the v4 `verify_album_open` (`ffa82aed…`) on the pre/post frames — continue ONLY on `ALBUM_OPEN_VERIFIED` (exit 0); `TARGET_MISMATCH` / `NO_EFFECT` / `INCONCLUSIVE` stop the run before input #2.
4. S6 (CORE gate): the v5 `locate_album_ellipsis` on the same post frame (`--expect-start 2024/05/13 --expect-end 2024/05/17 --title-bbox <S5 bbox> --expect-group-title 「旻謙允禎成長日記」 --header-depth 60 --out …/attempt-07/album-ellipsis-locate.json`) — continue ONLY on `ELIGIBLE` (exactly one candidate inside the header band above the title strip); `NO_ELLIPSIS_FOUND` / `AMBIGUOUS_ELLIPSIS` / `GROUP_LEVEL_ONLY` / `TARGET_TITLE_NOT_FOUND` / `BAD_FRAME` / reader failure → no input #2. Historical coordinates never live input; S6 never fallback to v4.
5. Input #2 → S7-S10 (CORE): exactly one album-level ⋮ left click at the v5 S6 point; then immediate read-only captures (app frame + AX state; up to 5 `screencapture -x` within about 4 s) and the frozen `detect_menu_popup.py` (`6ae9c250…`) on every frozen pre/post pair (screen pre-menu vs each post capture; app pre vs app post) + AX re-read. `AFFIRMATIVE` only for a new AX menu element or `MENU_DETECTED`; OCR-only/human-report-only/incomplete → `UNKNOWN`, never AFFIRMATIVE.
6. S11 (CORE): write `route-result.json` / final `run-ledger.json` / `manifest.json` / the durable execution record `execution-rev24.md` (per §20.2 S11 also `album-card-locate.json`, `album-open-verify.json`, `album-ellipsis-locate.json`, `screen-probe.json`, `menu-analysis.json`); commit; notify the owner (the menu may be left open — the owner closes it; no keyboard input is ever sent). A stop before any route outcome writes no `route-result.json`/`manifest.json` — the final ledger records events, final counts and stop reason (carried note below). Attempt-05/06 verdicts never re-scored.
7. Stage 05 (fresh, zero GUI) at `e2e/attempt-08` (§24.5): re-derives BOTH closure facts from fresh evidence — the §16.4 check over v1.1 and the attempt-07 route result — snapshots the Stage-04 facts from `execution-rev24.md`, and writes `result.md`. Stage 04 must not pre-claim its outcome.

## SEMANTIC_INVARIANTS
- Exactly one semantic delta carries into this wave: the album-level ⋮ position rule (v5). Census/dot/triple/text-blocking logic and all detection constants stay byte-identical to v4. Title strip `band = {x0: tx1+2, x1: width-1, y0: max(0, ty0-6), y1: min(height-1, ty1+10)}` unchanged. NEW header `header = {x0: tx1+2, x1: width-1, y1: max(0, ty0-7), y0: max(0, ty0-7-header_depth)}`, `--header-depth` default 60. Eligible iff middle dot is (a) not inside_text, (b) not in group-title band, (c) inside header. `ELIGIBLE` requires exactly one candidate. Title-strip triples → `in_title_strip`, NOT eligible. Above header → `above_header_band`; below strip → `below_album_title_band`. Exit codes 0/2/3/4/5/6 unchanged; group-only → `GROUP_LEVEL_ONLY`. Depth-60 rationale: observed offset 33.5 px above strip top; 60 is a bounded window (~27 headroom); deeper risks `AMBIGUOUS_ELLIPSIS`; farther is fail-closed. Rejected union (strip ∪ header) documented with the real-frame reason (strip triples are glyph runs). S3/S5/S6 JSONs must carry the v4 `reader` block; the frozen tesseract-based `detect_menu_popup.py` (§20.2 S10; `6ae9c250…`) is reused byte-identically and explicitly exempt; the v3 screen-probe JSON is exempt (RV-35-3 note below).
- Reader failure semantics: `binary_missing` / `build_failed` / `nonzero_exit` / `unparsable` / `timeout` reaches the tool's frozen refusal path (non-zero exit, no guessed value) and stops the run before the next input. Empty stdout = helper outcome `ok` + zero words = recorded UNREADABLE, never a successful read; it refuses only where the rule requires a readable value (e.g. a title region); an unreadable count alone follows frozen v3 parity and does not refuse (verbatim note below).
- S5 verdicts (§24.3/§20.2 S5): `ALBUM_OPEN_VERIFIED` = target date title in the post frame AND frame-diff fraction ≥5% at the frozen `--delta 12`; a readable non-target/mismatching count → `TARGET_MISMATCH`; no surface change → `NO_EFFECT`; no readable title → `INCONCLUSIVE`. S6 `ELIGIBLE` = exactly one candidate inside the header band above the verified title, to the right of it (x∈[tx1+2,width-1]); strip/group/above/below handled per the v5 rule. S10 `AFFIRMATIVE` is machine-observable only; the observed menu is checked against the documented item order (Select items / Rename album / Save All / Delete album / Share album) as a recorded check only — never used to compute or click a row.
- At-most-once and resume semantics (NFR-V24-2; §24.3 resume rule): input #1 and input #2 are each sent at most once, each only if every precondition holds at that moment; retry 0; a stop after input #1 never re-authorizes input #2; a zero-input stop (preflight surface failure, capability unavailable) is resume-eligible with the same unspent authorization once the owner restores the surface.
- §24.5 closure semantics: the closure tuple is reached only when attempt-07 returns machine-observable `AFFIRMATIVE` AND the §16.4 check over v1.1 passes (both re-derived by Stage 05 from fresh evidence); any non-AFFIRMATIVE outcome is recorded honestly with zero side effects, leaves the route a scoped CORE blocker, notifies the owner immediately, and takes neither closure branch automatically (`ROUTE_NOT_NEEDED` never taken, OOS-V24-3). A preflight surface failure spends no input, is not a route result and changes no status. No supporting item may block the wave; no supporting pass may mask a CORE failure. A further attempt needs a NEW revision (OOS-V24-5).
- Frozen bytes (re-hash before reuse; any byte change = TASK_REGRESSION): v4 tools + helper source `vision_ocr.swift` `4fc9fa2b…` + v4 self-test summary `5ad2be10…` (16 cases, `cases_failed 0`); v3 tools `500fcadb…`/`80504262…`/`60e3120a…` + v3 self-test `17840e91…`; v2 locator `8c8b6fc7…`; detector `6ae9c250…` + self-test script `db091703…` (4,018 B) + self-test summary `d8ffc129…` (2,946 B, distinct file per §24.8); gates 1-4 (gate-4 `e32ccd88…` spent); attempt-05 final ledger `17b17203…`; attempt-06 FINAL ledger `906c1433…` (parent `17b17203…`); `album-open-verify.json` attempt-05 `ffa5d963…` and attempt-06 `ALBUM_OPEN_VERIFIED`; `album-ellipsis-locate.json` attempt-06 `NO_ELLIPSIS_FOUND` (`above_album_title_band` middle 304.5,49.5); crosscheck `d3ebbaed…` (SUPPLEMENTARY_NON_AUTHORITATIVE, never promoted); v1.1 `a8c10551…`; v1 `2cd7eccd…`; human-gate answer `03ffff57…`; the 57-file destination and all formal config/state/run-logs; `e2e/attempt-06` report `14b44c94…`; `e2e/attempt-07` report `6416a5f4…`; the archived handoffs (`handoff-history/`). Attempt-05/06 frozen verdicts and artifacts are never re-scored or re-interpreted; the v5 rule applies only to attempt-07's own fresh frames; frames stay in `/tmp` (bytes/size/SHA-256 recorded only). The v5 directory is additive-only (NFR-V24-1).
- Orthogonal Status Contract v2 (plan.md:1981-2076): six orthogonal subjects; no field rewrites another; `Done` means overall closure only; verification blockers never collapse into IMPLEMENTATION_BLOCKED; `BASELINE_REGRESSION_DELTA=WORSENED` → REQUIRED_VERIFICATION=FAIL, closure FIX_REQUIRED (never INCOMPLETE/PENDING_REQUIRED_VERIFICATION); a missing/unreadable pre-baseline → REQUIRED_VERIFICATION=INCOMPLETE, never PASS.
- Execution-record naming (§24.3 S11 addition): this wave's durable record is `execution-rev24.md`; `execution.md`, `execution-rev19.md`, `execution-rev21.md`, `execution-rev22.md` stay untouched.

## BEST_EFFORT_DO_NOT_GATE
- `NFR-V24-4` (SUPPORTING/non-gating): the S2 screen-scope probe (`screen_scope=UNAVAILABLE` never blocks), the v5 README and the build record; a failed probe is recorded, never a CORE failure (V24_SCREEN_SCOPE_PROBE).
- `ANOM-01` stays recorded and non-gating; `ANOM-02` likewise — neither is repaired in this wave.
- Freeze set is Rev22-scoped: `evidence/20260917-freeze-set/rev22-freeze-set.json` (`fea6eb7a…`) records `plan.md` at Rev22's `426fe6a1…` by design; do not refresh mid-wave; compare the plan row against the freeze-era revision (RV-35 residual).
- `baseline.json`'s 14/14 anchors are a baseline-time snapshot; the plan.md and handoff rows have legitimately advanced; `BASELINE_REGRESSION_DELTA` keys on the frozen signature classes, so no false delta arises.
- RV-35 residual notes (non-gating, no plan edit): the `owner-decisions-rev22.json` 1025 excerpt is a marked join of three byte-exact fragments; the `.agent/.../e2e/` series starts at `attempt-02`; `evidence/20260917-owner-briefing/owner-explanation-rev22-audit.md` (if present) is unrelated and must not be bound.

## CARRY_NOTES_NON_GATING (verbatim; apply them, but do not gate the wave on them)
- empty-stdout note (refined by RV-35-1): "empty stdout = helper outcome `ok` + zero words = recorded UNREADABLE, never a successful read; it refuses only where the rule requires a readable value (e.g., a title region); an unreadable count alone follows frozen v3 parity and does not refuse."
- budget taxonomy (RV-35-2): "budgets are album_card 1 / navigation 1 / ellipsis 1 / retry 0 / menu_item 0 / keyboard 0 / app_acquisition 0 / conversation_images 0; input #1 (the album-card click) is the run's single navigation input and is counted under BOTH navigation_inputs and album_card_inputs (attempt-05 precedent); this adds no liberty — no extra navigation input is authorized."
- reader-block scope (RV-35-3): "only the three v4-tool JSONs (locate_album_card, verify_album_open, locate_album_ellipsis) must carry the v4 reader block; the frozen tesseract-based detector JSON (menu-analysis.json) and the v3 screen-probe JSON are exempt. For attempt-07 the three JSONs are S3/S5/S6 (S6 via the v5 directory's v4-reader copies)."
- stop-artifact policy (RV-32-4): "a run that stops before any route outcome writes no route-result.json/manifest.json; the final ledger records events, final counts and stop reason (attempt-05 precedent)."
- series note (extended per §23.1 RV-33-4 + §24): "evidence/20260916-route/attempt-NN is the route-run series; .agent/tasks/<TASK_ID>/e2e/attempt-NN is the acceptance series — route attempt-06 ≠ e2e/attempt-06 (consumed by the Rev21 wave, report 14b44c94…); route attempt-07 ≠ e2e/attempt-07 (consumed by the Rev22 wave, report 6416a5f4…); this wave's route run is evidence/20260916-route/attempt-07/ and its Stage 05 is e2e/attempt-08."
- v5 handoff notes (§24.1–§24.5, must carry): "header band formula header={x0:tx1+2,x1:width-1,y1:max(0,ty0-7),y0:max(0,ty0-7-header_depth)} with --header-depth 60; title-strip triples (in_title_strip) NOT eligible; above_header_band / below_album_title_band NOT eligible; exactly-one-eligible for ELIGIBLE; rejected union (strip ∪ header) with the glyph-run reason; historical coordinates (304,50)/(305,50) banned as live input; S6 never fallback to v4; S3/S5 stay on frozen v4 bytes."
- RV-34-1 (non-gating, note only; do NOT edit plan.md): "§22.1's parenthetical about transcript line 1034 is imprecise — L1034 is the owner's plain-language request answered at L1048; it did not produce the L1025 option presentation."
- RV-36-1 (non-gating, no plan edit): "§24.3 prototype sentence omits the --expect-group-title flag state; Stage 04 runbook states it explicitly (null on the real frame)."
- RV-36-2 (non-gating, no plan edit): "§24.4 bullet splits the attempt-07 gate path across §24.3; Stage 04 freezes evidence/20260916-route/attempt-07/gate-5-authorization.json."
- RV-37-1 (non-gating, robustness note): "header x0: tx1+2 inherited from v4; a narrow future window fails closed, never mis-clicks; do not touch without a real-frame basis."
- RV-37-2 (non-gating, wording): "V24_V5_SELFTEST 'four negatives' means the four §24.3 negatives plus the 5× determinism replay (five items); Stage 04 produces all five."

## DEFERRED_NOT_THIS_TASK
- Any menu-item activation (Save All named explicitly), any chooser, any production Save-All route/download; any destination write or re-download (OOS-V24-1/OOS-V24-2).
- `ROUTE_NOT_NEEDED` recording (OOS-V24-3; the owner chose B and that closure stays owner-reserved).
- Any GUI input beyond §24.3's two inputs (OOS-V24-4); any v3/tesseract reader substitution for S3/S5/S6; bridge repair/reinstall; TCC changes; legacy migration/normalization; historical cleanup.
- A future route/production wave: any new attempt or plan change requires a NEW revision, a new gate and a fresh review (never inside Rev24; OOS-V24-5).

## REPO_ANCHOR
- Project root: /Users/hsiaojohnny/Documents/ChatGPT/Line_backup
- Branch: master
- Anchor HEAD: 61ef7b5 (Rev24 dual-review intake — attempt-36 + attempt-37 both PLAN_APPROVED). Chain: plan Rev24 = 92a167e; owner-decisions Rev24 + GOAL/H4.0 = 5d6e2b1; reviews 36/37 intake = 61ef7b5; plan Rev23 = 192e6f0; owner-decisions Rev22 + reviews 34/35 intake = c301e5e; Rev21 wave = 287e2f5; Rev21 acceptance (`e2e/attempt-06`) = 92202a4; Rev22 acceptance (`e2e/attempt-07`) Stage-05 = per `6416a5f4…`; Phase 0 baseline = adf1829; H3.0 = 1343b55; attempt-05 input #1 = e53f67b; Vision crosscheck = 4d6c307.
- Relevant dirty state: none at compile time (`git status --porcelain` clean before this regeneration except the new archive file); this regeneration adds only `handoff-history/handoff-plan-r23-20260918-105516.md` (byte-identical copy of the superseded Rev23 handoff `19c9c6d3…`) and rewrites `handoff.md`; it is pending the orchestrator's commit.
- Drift since plan/review: none — plan.md SHA-256 re-verified at `40eb0198…` (2,330 lines / 313,461 bytes), byte-identical to the reviewed revision; both review reports re-verified (attempt-36 `f7a9d3eb…`; attempt-37 `94158082…`). Stage 04 stops on any handoff↔plan mismatch.

## CURRENT_STATE_DELTA
- Rev24 approved: review/attempt-36 (`f7a9d3eb…`, PLAN_APPROVED) and review/attempt-37 (`94158082…`, PLAN_APPROVED), both bound to Rev24 at `40eb0198…`; MINOR findings only (RV-36-1/2; RV-37-1/2) — carried above as non-gating notes; no plan edit is permitted for them (an edit voids both approvals and this handoff).
- Owner decisions committed at `5d6e2b1`/`92a167e`: `evidence/20260918-owner-decisions/owner-decisions-rev24.json` (`be1f5173…` 9,872 B; decision B line 2427 882 B `a600ac4c…`; option presentation line 2405 1,931 B `14905585…` with 470 B B-excerpt `5674299e…`; drafts 2412/2420 aborted; questions 2291 243 B `3fc89cbc…` / 2365 60 B `2e729d03…`).
- THE v4 TOOL SET EXISTS AND IS FROZEN: `evidence/20260916-route/tools/v4/` — `vision_reader.py` `22a4e9ef…`, `locate_album_card.py` `bb52aff1…`, `verify_album_open.py` `ffa82aed…`, `locate_album_ellipsis.py` `b77e3d51…`, `README.md` `ed3ebf0a…`; the v4 self-test `evidence/20260916-route/tools/selftest/v4/selftest-summary.json` `5ad2be10…` (16 cases, `cases_failed 0`). The Vision helper source `evidence/20260916-route/tools/vision/vision_ocr.swift` `4fc9fa2b…` stays frozen (never edited). Stage 04 verifies, it does not rebuild the helper.
- Frozen chain (must stay byte-identical): v3 tools + v3 self-test `17840e91…`; detector `6ae9c250…` + self-test script `db091703…` (4,018 B) + self-test summary `d8ffc129…` (2,946 B, distinct); attempt-05 `run-ledger.json` `17b17203…`; attempt-06 FINAL `run-ledger.json` `906c1433…` (parent `17b17203…`, state `CLOSED_AFTER_INPUT_1`); `album-ellipsis-locate.json` attempt-06 `NO_ELLIPSIS_FOUND`; gates 1-4 (gate-4 `e32ccd88…` spent); v1.1 `a8c10551…`; v1 `2cd7eccd…`; human-gate answer `03ffff57…`; the 57-file destination and all formal config/state/run-logs; `e2e/attempt-06` report `14b44c94…`; `e2e/attempt-07` report `6416a5f4…`; the archived handoffs (`handoff-history/`, now including `handoff-plan-r23-20260918-105516.md` `19c9c6d3…`).
- Route state: attempt-05 stopped at S5 (`STOPPED_AT_S5_TARGET_MISMATCH`); attempt-06 stopped at S6 (`NO_ELLIPSIS_FOUND`, ⋮ never sent, zero side effects); each spent budget stays spent — gate-5 is a NEW, independent one-shot authorization (no reset, no extension; §24.4).
- `evidence/20260916-route/attempt-07/` does not exist yet (Stage 04 creates + freezes it before any input); `evidence/20260916-route/tools/v5/` does not exist yet (Stage 04 builds it additive-only); `.agent/tasks/<TASK_ID>/e2e/` holds attempts 02-07; the next unused number is 08 (this wave's Stage 05); route attempt-07 ≠ `e2e/attempt-07` (series note above).
- Frozen execution records present and untouchable: `execution.md` (Rev18 wave), `execution-rev19.md`, `execution-rev21.md`, `execution-rev22.md`; this wave writes `execution-rev24.md` (does not exist yet).
- The header status block remains the result ① tuple (PRIMARY UNKNOWN / IMPLEMENTATION COMPLETE / CORE BLOCKED / REQUIRED_VERIFICATION PASS / INDEPENDENT_ACCEPTANCE PASS / CLOSURE CORE_ACCEPTANCE_BLOCKED) plus `REV22_WAVE_STATUS` (final Stage-05 state per `6416a5f4…`) and `REV24_WAVE_STATUS` (not started; nothing pre-claimed) lines; Stage 05 re-derives the wave tuple from fresh evidence — never assumed.
- The attempt-06 ledger's `plan_binding` = Rev23 `4337e2b5…` with approvals 34/35 (historical binding only — Stage 05 freshness must not treat it as current); attempt-07's ledger will bind Rev24 `40eb0198…` with approvals 36/37.
- Formal read-only compatibility profile unchanged: for the requested 禎 key vs the persisted 楨 key the expected real-command axes are Filesystem=PASS, Registry=FAIL, Source=UNRESOLVED, State=LEGACY_PROVENANCE_LIMITED, Overall=UNKNOWN, exit 4; disclosed axis facts, never contradictions.

## MUST_READ_PLAN
Rev24 §24.1–§24.9 + header (plan.md:1-~250; §24.1 authority/scope/Goal-delta; §24.2 B-reading; §24.3 attempt-07 + v5 rule + S1–S11 substitutions + resume; §24.4 gate-5 17-key; §24.5 gating/veto/closure; §24.6 nine verification rows; §24.7 owner view; §24.8 literals; §24.9 unchanged); Rev23 §23.1–§23.2; Rev22 §22.1–§22.9; Rev20 §20.1–§20.3 as the step base (S1–S11 apply unchanged with §24.3's substitutions; §20.3 closure routing); §16.4 matching contract; §19.1 v1.1 authoring rule; §19.3 `ROUTE_NOT_NEEDED` (NOT taken this wave); §21.6 e2e series rule; requirement table; Runtime route, ledger authority, GUI gate, truthful E2E; Canonical Status Contract v2; Closure and sequencing; Owner view.

## SETTLED_DO_NOT_REOPEN
- Rev24 §24.1–§24.9: the corrected v5 rule, gate-5, attempt-07 envelope, §24.6 rows and closure; the dual approval binds only Rev24 at `40eb0198…`; any plan edit — including folding RV-36-1/2/RV-37-1/2 in — creates a new revision and needs fresh review before handoff.
- Rev23 §23.1–§23.2: the review-response fixes and nothing else; preserved as history.
- Rev22 §22.1–§22.9: the owner's two decisions and their reading, the §22.3 two-input envelope, the budgets, the gate-4 pattern (spent), the v4 bindings, the v1.1 adoption and the §22.6 rows.
- Rev21 §21.x: the Vision wave stands closed and accepted (`e2e/attempt-06` report `14b44c94…`; frozen set 21 anchors UNCHANGED).
- Rev20 §20.1–§20.3 (gate-3, attempt-05 artifacts) and Rev19 §19.1/§19.3 (v1.1 record, `ROUTE_NOT_NEEDED` preconditions) stand as preserved history with the Rev24 `e2e/attempt-08` rebinding; Rev18 §18.1-§18.6 and the Rev17/Rev16 decisions stand as corrected.
- The route closure decision (A/B) is owner-reserved; demonstration semantics never flip a frozen route verdict; no supersession layer is added.

## REVERIFY_ON_START
- Recompute plan.md SHA-256 + byte length; require exact match to PLAN_SHA256 `40eb0198…` (2,330 lines / 313,461 bytes), else STOP.
- Re-read both review reports; confirm each ends with its single gate line `PLAN_APPROVED` bound to Rev24 at `40eb0198…` (attempt-36 `f7a9d3eb…`; attempt-37 `94158082…`) with no BLOCKER/MAJOR.
- `git status --porcelain` / `git log --oneline -6`: confirm no plan/product/review edits after `61ef7b5` (this handoff regeneration + its archive are the only expected pending changes until committed).
- Re-hash before use: the v4 tool set (4 SHAs) + helper source `4fc9fa2b…` + v4 self-test `5ad2be10…`; the v3 tool set + v3 self-test `17840e91…`; v2 locator `8c8b6fc7…`; detector `6ae9c250…` + self-test script `db091703…` + self-test summary `d8ffc129…`; attempt-05 `run-ledger.json` `17b17203…`, attempt-06 FINAL `906c1433…`, `album-ellipsis-locate.json` attempt-06 `NO_ELLIPSIS_FOUND`, crosscheck `d3ebbaed…`; gates 1-4; v1.1 `a8c10551…`, v1 `2cd7eccd…`, human-gate answer `03ffff57…` — all must remain byte-identical.
- Verify the attempt-07 pre-freeze bundle is ABSENT before Stage 04 builds it (expected: `tools/v5/`, `attempt-07/`, `e2e/attempt-08/` all absent); Stage 04 builds them additive-only, reproduces both §24.6 replay rows as durable evidence, and COMMITS gate-5 + runbook + ledger skeleton (parent `906c1433…`) BEFORE any input; if the bundle exists prematurely, STOP (ordering violation). Confirm `execution-rev24.md` does not exist before Stage 04 writes; `execution.md` / `execution-rev19.md` / `execution-rev21.md` / `execution-rev22.md` stay untouched.
- Mutable read-only facts: formal DATA_CONFIG / DATA_STATE / DATA_RUN_LOG bytes and SHA-256; DATA_DESTINATION inventory (57 files, 17,924,900 bytes) — captured read-only, never mutated; the owner's live surface (album-list state, target card visible) is a live precondition captured read-only at run time. A missing/unreadable pre-baseline is REQUIRED_VERIFICATION=INCOMPLETE, never PASS.

## TRIGGERED_POLICIES
- workflow-routing.md (§7 Status Semantics v2, §7.4 baseline, §7.6 blockers, §7.7 Stage 04 routing, §7.8 Stage 05, §7.10 DONE, §7.11 artifacts)
- testing-verification.md
- goal-alignment-design-economy.md
- git-change-hygiene.md
- security-privacy.md
- debugging-recovery.md
- high-risk-change.md

## FIRST_ACTION
Execute Stage-04 build, zero input (§24.3 preconditions 2-3): build `evidence/20260916-route/tools/v5/` (byte-identical copies of the three v4 tools + corrected `locate_album_ellipsis.py` + `README.md` with geometry/rejected-union/depth-60 rationale/replay commands), build `evidence/20260916-route/tools/selftest/v5/` (≥20 cases with the corrected positive + four v5-specific negatives + 5× determinism; `selftest-summary.json` with `cases_failed 0` + v5 tool SHAs), reproduce BOTH §24.6 replay rows as durable evidence (v5 `ELIGIBLE` with `[[304.5,44.0],[304.5,49.5],[304.5,55.0]]`/`[304,50]` + v4 `NO_ELLIPSIS_FOUND` on `4cb8a6b4…` with the S5 title bbox), then freeze + COMMIT `evidence/20260916-route/attempt-07/gate-5-authorization.json` (17 keys; parent gate `e32ccd88…` spent; authority `be1f5173…` + Rev24 `40eb0198…` + reviews 36/37; lines 2427/2405), `route-runbook.md` (S1–S11 with §24.3 substitutions; historical-coordinate ban; S6-no-fallback; capture bounds), `run-ledger.json` (parent `906c1433…`; budgets 1/1/1 + zeros; `click_count_per_input 1`; `retry_budget 0`) BEFORE any input. Verify (not re-derive) the frozen v4/detector/ledger/frame SHAs; on any mismatch STOP. Contents required either way; never overwrite a committed artifact.

## IMPLEMENTATION_WAVES
- W7 (Stage 04 fresh): zero-GUI build per FIRST_ACTION (v5 + selftest/v5 + durable replay + pre-input freeze + commit). Must reproduce the exact dots/point; any other verdict or point = TASK_REGRESSION. S6 never v4; S3/S5 never non-v4; historical coordinates never live.
- W8 (route attempt-07, the wave's only GUI): S1 preflight (binding read; `NO_TARGET_ON_FRAME` → zero-input stop, resume-eligible) → input #1 (card ×1) → S5 (`ALBUM_OPEN_VERIFIED` only) → S6 v5 (`ELIGIBLE` only, sole coordinate source) → S7 pre capture → input #2 (⋮ ×1 at v5 point) → S9–S10 observe + frozen detector transcribe → S11 ledger + `execution-rev24.md` + commit + notify. Any non-ELIGIBLE S6, any reader failure, any surface/permission failure → no ⋮ input, record, notify, stop.
- W9 (Stage 05 fresh, zero GUI): `e2e/attempt-08/` re-derives BOTH closure facts from fresh evidence (corrected-route AFFIRMATIVE + §16.4 CONFIRMED over v1.1), snapshots Stage-04 facts from `execution-rev24.md`, writes `result.md` per §24.5. No GUI input, no product-code change.

## ACCEPTANCE_CONTRACT
- Stage 05 `e2e/attempt-08/` (append-only; zero GUI; no product-code change) re-derives from fresh evidence: (1) the corrected-route outcome (machine-observable `AFFIRMATIVE` iff a new AX menu element or detector `MENU_DETECTED`; OCR-only/human-only/incomplete → `UNKNOWN`, never AFFIRMATIVE), (2) the §16.4 exact-equality check over the adopted v1.1 record (`a8c10551…`) → `CONFIRMED` (closes BLK-01 at task level). Both hold → closure tuple PRIMARY ACHIEVED / IMPLEMENTATION COMPLETE / CORE PASS / REQUIRED_VERIFICATION PASS / INDEPENDENT_ACCEPTANCE PASS / TASK_CLOSURE DONE, `BASELINE_REGRESSION_DELTA=UNCHANGED` (axis facts Registry=FAIL, State=`LEGACY_PROVENANCE_LIMITED`, product Source=`UNRESOLVED` reported, never promoted). Any non-AFFIRMATIVE → honest record, scoped CORE blocker retained, owner notified immediately, no `ROUTE_NOT_NEEDED`, no auto-branch, NEW revision for any retry. Preflight surface failure → resume-eligible, not a route result. The nine §24.6 rows are the gating checklist (eight CORE HARD_CLEAN + one SUPPORTING probe); any CORE miss = TASK_REGRESSION, no waiver.

## STOP_AND_ESCALATE_IF
- Any `REVERIFY_ON_START` mismatch (plan/review SHAs, frozen bytes, ledger parents, frame SHAs, destination/formal-state drift) → stop, write escalation evidence, replan; never a bounded executor fix.
- Any semantic drift from Rev24 (fail-closed relaxed, closure changed, gate效力 changed, S6→v4 fallback, historical coordinate as live input, menu/chooser/keyboard/AX/download/state-write needed) → stop, write escalation evidence, replan; never a bounded executor fix (§24.9; Rev16 §16.8).
- If the v5/cua capability is unavailable → the run stops zero-input (no v3/tesseract substitution for S3/S5/S6 verdicts), notify the owner; resume-eligible with the same unspent authorization.
- If the surface is not the album-list state (target card visible; no menu/dialog/chooser/photo-viewer/permission overlay) → stop zero-input, record `NO_TARGET_ON_FRAME` honestly, notify the owner immediately (no long loops); resume-eligible. After the S2 probe, `PERMISSION_PROMPT_OBSERVED` → SAFE_ABORT, no input.
- A stop after input #1 never re-authorizes input #2 (at-most-once; retry 0); a zero-input stop does not consume the authorization.
- 5 cumulative failures reached → stop before starting a new attempt, write the stop reason + evidence, notify the owner; no long retry.
- Any menu-item activation (Save All named explicitly), chooser, keyboard input, AX write, production download, or destination/formal-state write needed to proceed → STOP (OOS-V24-1/2/4; no production authorization exists).
- Any byte change to a v1/v2/v3/v4 tool, the v3/v4 self-tests, an attempt-01..06 artifact, `e2e/attempt-02..07`, gates 1-4, any ledger, an archived handoff, the `result.md` history, the destination or formal state → TASK_REGRESSION (hard failure), never INCOMPLETE.
- A reader outcome `binary_missing` / `build_failed` / `nonzero_exit` / `unparsable` / `timeout` → the frozen refusal path (non-zero exit, no guessed value); stop before the next input; never a silent continue.
- An `AFFIRMATIVE` claimed from OCR-only / human-report-only / incomplete observation → STOP (V24_AFFIRMATIVE_MACHINE_OBSERVABLE_ONLY).
- Any 禎/楨 merge/normalization attempt, any attempt to take `ROUTE_NOT_NEEDED` or to decide route closure, or any re-scoring/overwrite/re-interpretation of attempt-05/06 frozen verdicts → STOP.
- A `baseline-worsened` delta → hard failure FIX_REQUIRED; never INCOMPLETE/PENDING_REQUIRED_VERIFICATION.

## OWNER_VIEW（白話，給非技術主持人；非 gating）
- 這一版（Rev24，雙複審通過）＝您選的 **B**：把上一輪「找不到 ⋮ 就停」的同一條路，用修正版位置規則再走一次。只改一件事：找 ⋮ 的格子從「標題列裡面」改成「標題列上方、往上 60 像素」；其他（開相簿一次、點 ⋮ 一次、只觀察不點任何選單、每次恰點一下、不重試、找不到就停）完全沒變。
- 先離線證明：用「上次那張已保存的真實畫面」證明新規則找得到（dots [[304.5,44.0],[304.5,49.5],[304.5,55.0]]、點 [304,50]）、舊規則還是找不到；證明完成才准凍結一次性授權（gate-5），凍結後才准碰真機。
- 兩個都成立才會整體結案（相簿層級選單被機器證實 + v1.1 檢核通過）；任一不成立就照實記錄、立刻回報，不會自行放行，也不會用 A 方案（記成「不需要」）。
- 執行時需要您把 LINE 停在「相簿列表」、目標相簿卡片（`2024/05/13～05/17`、57 張）可見；跑時不動滑鼠鍵盤。若不在這個畫面，這次不會送出任何輸入，只會記錄後請您復原畫面再續（同一份授權可續用）。
- 最大風險：若這次仍看不到相簿層級的 ⋮ 選單，路線維持卡點、立即回報；獨立驗收（`e2e/attempt-08`）全程不碰滑鼠鍵盤，只重新核對證據。

## HISTORICAL_TASK_DEPENDENCIES
- NONE for generic context. The root `/Users/hsiaojohnny/Documents/ChatGPT/Line_backup/handoff.md` is historical/non-authoritative background only (it predates this TASK_ID) and must not be used as a plan or contract.
- Prior handoffs of this task are archived (keep as history; never execute from any of them):
  - `handoff-history/handoff-plan-r13-20260917-021523.md` (Rev13-bound);
  - `handoff-history/handoff-plan-r18-20260917-154007.md` (Rev18-bound);
  - `handoff-history/handoff-plan-r19-20260917-165617.md` (Rev19-bound);
  - `handoff-history/handoff-plan-r20-20260917-204314.md` (Rev20-bound);
  - `handoff-history/handoff-plan-r21-20260917-230851.md` (Rev21-bound);
  - `handoff-history/handoff-plan-r23-20260918-105516.md` (Rev23-bound; superseded by this handoff; archived 2026-09-18 10:55 +0800, byte-identical to the superseded handoff `19c9c6d3…`).
- Frozen execution records: `execution.md` (Rev18 wave), `execution-rev19.md`, `execution-rev21.md`, `execution-rev22.md`; this wave adds `execution-rev24.md`. The attempt-05 run-ledger's `handoff_sha256` (`ab88e496…`) is a historical binding to the Rev20 handoff only; the attempt-06 ledger's `plan_binding` (`4337e2b5…` + approvals 34/35) is a historical binding to Rev23 only.


TASK_ID: T20260916-0102-01-line-backup-acceptance
HANDOFF_PATH: .agent/tasks/T20260916-0102-01-line-backup-acceptance/handoff.md
PLAN_REVISION: 24
STATUS: READY_FOR_IMPLEMENTATION
NEXT_STAGE: 04_IMPLEMENT
HANDOFF_SHA256: fee9858146ae2752dda9bdd557820a69fcc0022130d2526fc89c90ab8bdee88d | HANDOFF_BYTES: 39073 (SHA-256 and byte count of all file bytes above this footer line; verify: `sed '$d' handoff.md | shasum -a 256` → this SHA-256, `sed '$d' handoff.md | wc -c` → this byte count)
