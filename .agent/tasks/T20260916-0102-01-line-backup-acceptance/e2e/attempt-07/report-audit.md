# Independent audit of the Stage-05 attempt-07 acceptance report

- Auditing role: independent Stage-05 report auditor (read-only; no GUI; no git commit; all Python runs used `-B`; no repo file written except this one).
- Auditor working dir: `/Users/hsiaojohnny/Documents/ChatGPT/Line_backup`
- Audit timestamp (local +0800): `2026-09-18` (morning, Asia/Taipei); repo `HEAD dfbe669b2f0c0e8f111949e03f0882c560c544c8`, branch `master`.
- Audited claim set: the audit brief's items (A)–(G) against `e2e/attempt-07/e2e_report.md`, `result.md`, and the `attempt-07/evidence/**` files they cite.
- Method: every audited file re-hashed with `shasum -a 256` + `wc -c`; cited JSON/JSONL artifacts parsed read-only with `/opt/homebrew/bin/python3 -B`; the §16.4 matcher re-run by importing the frozen module `src/line_backup_acceptance/common.py`; ledger/tool facts recomputed from `evidence/20260916-route/attempt-06/**`; `git cat-file` used for freeze-era and pre-overwrite blob provenance.
- Verdict vocabulary below: MATCH / MISMATCH / UNVERIFIABLE, one line per audited claim.

## 0. Audited object identities (independently recomputed)

| Object | Reported bytes / SHA-256 | Recomputed | Verdict |
|---|---|---|---|
| `e2e/attempt-07/e2e_report.md` | 35,998 B / `03094c12f8eaf860f8adef342836ca436a424978b018e49e5b3d502744283a91` | 35,998 B / `03094c12…a91` | MATCH |
| `result.md` (current, Stage-05 closing) | 11,217 B / `daf5ba11e41583a5c69a733b52e2ce7010c9041096f4f75f1e0ab6199c73485e` | 11,217 B / `daf5ba11…85e` | MATCH |
| attempt-07 tree (39 files incl. `evidence/**` + `independent-recheck/**`) | — | all 39 re-hashed; every hash cited in the report/result tables verified | MATCH |

The report's own `STAGE_04_EXECUTION_ARTIFACT_SHA256` line and `result.md`'s durable table also name `execution-rev22.md` 30,711 B / `c9b590802c7b27f34796c8623005ad106343bcf0c43b23b799cdb94fcfade131`; independently recomputed: 30,711 B / `c9b59080…131` — MATCH.

## (A) The six §22.6 rows — check result vs evidence

Claim audited: the TEST_MATRIX rows' PASS/FAIL results (and their stated reasons) match the evidence; row 4 and row 6 honesty conditions hold.

1. `V22_SOURCE_RECORD_V1_1_CONFIRMED` — report: “**PASS** — re-derived `True` over v1.1 and `False` over v1; v1/v1.1/gate-answer bytes unchanged (`2cd7eccd…`/`a8c10551…`/`03ffff57…`); … exactly one leaf differs”. Independent: §16.4 matcher re-run → v1.1 `True`, v1 `False`; 27 leaves each; single differing leaf `/answer/part_2/confirmed_album`; file hashes and gate-answer entry re-hash OK. — MATCH.
2. `V22_ROUTE_ATTEMPT_06_SINGLE_ONESHOT` — report: “exactly 1 album-card click (= also the single navigation input; `click_count_per_input 1`), 0 ⋮ clicks (ellipsis UNSPENT 0/1), 0 retries, 0 menu-item/chooser/keyboard/app-acquisition inputs, no input after the S6 gate failure”. Independent ledger re-read: `final_counts` = album_card 1 / navigation 1 / ellipsis 0 / app_acquisition 0 / menu_item 0 / keyboard 0 / retry 0; event 6 `click_count 1`, `retry_count 0`; run `STOPPED_AT_S6_NO_ELLIPSIS_FOUND`, `state CLOSED_AFTER_INPUT_1`. — MATCH.
3. `V22_S5_BOUND_TO_V4_READER` — report: “**PASS** — all three verdicts from the frozen v4 tools; all three JSONs carry the reader block (`helper.source_sha256 4fc9fa2b…`, binary `c7087d98…`, per-call ok/exit-0/stdout-sha); no tesseract-based S3/S5/S6 verdict; detector never invoked”. Independent: `album-card-locate.json` / `album-open-verify.json` / `album-ellipsis-locate.json` all carry `reader.helper.source_sha256 4fc9fa2b…` and `binary_sha256 c7087d98…` (binary present on disk, 67,200 B, hash matches); replays exit 0/0/3; detector `6ae9c250…` hash OK and uninvoked (S7–S10 not executed; no menu artifacts). — MATCH (result). One documentary MISMATCH is raised in the “verbatim” header claim — see §(A-note).
4. `V22_AFFIRMATIVE_MACHINE_OBSERVABLE_ONLY` — report: “**PASS (constraint honored — adjudicated against the row's own failure classification; see note)** — this wave produced **no AFFIRMATIVE claim at all** … This PASS is **not** positive route evidence and must not be read as the album-level ⋮ being proven; `CORE_ACCEPTANCE` remains BLOCKED on `CUA_ROUTE_DECISION`.” Independent: ledger verdict set is only `ELIGIBLE` / `ALBUM_OPEN_VERIFIED` / `NO_ELLIPSIS_FOUND`; no AFFIRMATIVE anywhere in `20-route-ledger-verify.json` or the tool JSONs; the adjudication note explicitly records that `NOT_RUN` was considered and rejected, and that the row “is deliberately **not** used as closure evidence”. The PASS rationale does state the honesty constraint in the report body (row text + adjudication note) and in `result.md` item 4. — MATCH (honesty requirement satisfied).
5. `V22_PRIOR_EVIDENCE_IMMUTABLE` — report: “38-anchor re-hash 0 mismatches; freeze-set 69/71 prior-evidence rows byte-identical (the 2 differing rows are the wave's own plan/handoff authority artifacts …); destination 57 files / 17,924,900 B; baseline byte-identical”. Independent: 38 anchors in `56-frozen-anchors-rehash.json` re-walked and spot-verified on disk (v4 ×4, v3 ×3, detector, gates 1–3, both ledgers, v1/v1.1, archived handoffs, `execution-rev22.md`, `result.md`-pre-overwrite): all OK; freeze-set rows 69 identical + 2 = plan.md/handoff.md (see §(F)); destination walk: 57 files / 17,924,900 B / 0 zero-byte; baseline `ab6747f2…` byte-identical pre/post. — MATCH.
6. `V22_SCREEN_SCOPE_PROBE` — report: “**FAIL (non-gating)** — … reproduces `TARGET_COUNT_MISMATCH` (exit 5) at both scales with byte-identical outputs … `screen_scope=UNAVAILABLE` recorded; never a CORE failure (NFR-V22-4)”. Independent: `70-s2-probe-rederivation.json` = exit 5 both scales, stdout `a315f7a0…`/`ce1b0ced…` byte-identical to recorded, counts `5` and `27` of `57`, `screen_scope=UNAVAILABLE`, `gating: NON_GATING`. Stage-04 (`execution-rev22.md` row 6) recorded the same `FAIL` (non-gating) disclosure; the report's non-gating FAIL therefore does not contradict Stage 04. — MATCH.

(A-note) The matrix header says “policy columns verbatim; results re-derived by this attempt”. Five of six rows' policy columns are byte-for-byte the §22.6 text (plan.md lines 208–213; also handoff lines 126–131). Row 3's failure-class cell is a compressed rewrite: plan/handoff say “the frozen tesseract-based detect_menu_popup.py (§20.2 S10, reused byte-identically per §22.3) is explicitly exempt from the v4-reader requirement”, the report says “the frozen tesseract-based detect_menu_popup.py is explicitly exempt” (parenthetical dropped, citation dropped). Operative semantics are preserved, and the byte-identical reuse is separately stated in report section (c); but the word “verbatim” for that cell is not byte-true. This is the audit's one MISMATCH (documentary, non-material) — see FINAL VERDICT.

## (B) Closure tuple derivation vs plan §22.5 / §20.3 / §19.3 and the canonical status contract

Claim audited: the six-column tuple and `BASELINE_REGRESSION_DELTA=UNCHANGED` are correctly derived, are not pre-claimed, do not smuggle in `ROUTE_NOT_NEEDED`, and do not assert the route is “proven non-existent”.

- Report: “Required closure tuple per §22.5/§20.3 (both facts): `ACHIEVED | COMPLETE | PASS | PASS | PASS | DONE` + `BASELINE_REGRESSION_DELTA=UNCHANGED`. **Not reached** because closure fact 2 is non-AFFIRMATIVE (S6 `NO_ELLIPSIS_FOUND`, exit 3; input #2 unspent; owner-reserved). Independently derived Stage-05 values (canonical vocabulary, orthogonal subjects): `PRIMARY_OUTCOME_STATUS: UNKNOWN … TASK_CLOSURE_STATUS: CORE_ACCEPTANCE_BLOCKED`”. Sources: plan §22.5 (L192–200), §20.3 (L459–475), §19.3 (L596–613), canonical routing table (L2030 row “Complete implementation, CORE blocked | UNKNOWN | COMPLETE | BLOCKED | NOT_RUN | PENDING | CORE_ACCEPTANCE_BLOCKED”).
- Independent observation: every component traces. (i) Non-AFFIRMATIVE branch of §20.3 says the route stays a scoped CORE blocker and the owner is notified — matches `CORE_ACCEPTANCE_STATUS=BLOCKED`, `PRIMARY_OUTCOME_STATUS=UNKNOWN`, `TASK_CLOSURE_STATUS=CORE_ACCEPTANCE_BLOCKED`. (ii) `IMPLEMENTATION_STATUS=COMPLETE` (all wave deliverables exist) and `REQUIRED_VERIFICATION_STATUS=PASS` (baseline UNCHANGED, anchors unchanged, both facts re-derived) are consistent with the canonical row whose pre-Stage-05 fields are the defaults `NOT_RUN | PENDING`; the report explains the two Stage-05-set fields (independent acceptance executed in this attempt; required verification passed). (iii) The DONE tuple remains explicitly “not reached”, so nothing is pre-claimed. (iv) `ROUTE_NOT_NEEDED` is nowhere adopted: report L42/L57/L119 and result.md L27/L40/L50 all say not taken/owner-reserved; ledger `next_action` = “OWNER DECISION REQUIRED (do not act autonomously) … ROUTE_NOT_NEEDED must not be taken automatically”. (v) No sentence asserts the route/⋮ is proven non-existent; the residual-risk wording is “album-level ⋮ machine verification for the corrected route is not established” with the ⋮ identity question left to the owner/Plan. — MATCH.

## (C) Independent reproduction of the two closure facts

Claim audited: both closure facts were genuinely re-derived in attempt-07 and are reproducible by the auditor.

Fact 1 (§16.4 over v1.1) — report: “`user_fact_v1_matches` on the adopted v1.1 record returns `True` and on v1 returns `False`, both files byte-unchanged”. Independent reproduction (fresh process, frozen module loaded from `src/line_backup_acceptance/common.py`, 34,351 B, `df9fe0bc6333a8afec6f0bc87b336cf905e3683a209cbc8155190d4e5a0c60c0`):
- `user_fact_v1_matches(v1.1, "jp.naver.line.mac", "line:jp.naver.line.mac:旻謙允禎成長日記", {start 2024-05-13, end 2024-05-17, expected_images 57})` → `True`; same call on v1 → `False`. `_rehash_evidence_entries(record["evidence"])` → `True` for both (entry `human-gate-answer-20260917.json` 1,438 B `03ffff57…`).
- Leaf diff: 27/27; single difference `/answer/part_2/confirmed_album` = `2024/05/13～05/17` (v1.1) vs `2024/05/13～2024/05/17` (v1); both use U+FF5E, no ASCII 0x7E; `raw_requested_group` contains U+798E not U+6968, `raw_persisted_group` the reverse. — MATCH.

Fact 2 (corrected route attempt-06 non-AFFIRMATIVE) — report: “exactly one input in the whole run … S5 `ALBUM_OPEN_VERIFIED` … S6 `NO_ELLIPSIS_FOUND` (exit 3) … no `route-result.json` / `manifest.json` … zero side effects”. Independent reproduction from `evidence/20260916-route/attempt-06/run-ledger.json` (28,390 B, `906c14330605122e9b600359892d4400171d62564707d50af26ef8d8bb32fa2c`) and the three tool JSONs:
- Inputs: `final_counts.album_card_input 1`, `navigation_input 1` (= the same single click), `ellipsis_input 0` (budget 1, UNSPENT), `retry 0`, `menu_item_input 0`, `keyboard_input 0`, `app_acquisition_input 0`, observation 3/5, captures 1/5; event 6 `click_count 1`/`retry_count 0`; all 9 events have `menu_item_click`/`save_all_click`/`chooser_interaction`/`backup_state_write` = false. `run_status STOPPED_AT_S6_NO_ELLIPSIS_FOUND`, `state CLOSED_AFTER_INPUT_1`, `owner_decision_required true`, `route_result_sha256 null`.
- Tool replay stdout files in attempt-07 are byte-identical to the frozen tool JSONs (`cmp` true): `b822012d…` (S3, exit 0), `a1e5fa49…` (S5, exit 0), `433e8693…` (S6, exit 3); all three carry the v4 reader block.
- Stop-artifact policy: `route-result.json`, `manifest.json`, `menu-analysis.json` all ABSENT on disk. — MATCH.

## (D) STAGE_04_SNAPSHOT verbatimness

Claim audited: the snapshot block corresponds word-for-word to `execution-rev22.md` and does not rewrite the Stage-04 reported values.
- Report L26–33 vs `execution-rev22.md` L119–124: `diff` on the six `STAGE_04_REPORTED_*` lines → SNAPSHOT-LINES-IDENTICAL. `execution-rev22.md` re-hashed: 30,711 B / `c9b59080…`, identical at audit time.
- The report additionally carries `STAGE_04_EXECUTION_ARTIFACT_SHA256` (with the note that the Stage-04 file's internal §7 self-reference is not embedded by design) and `EVIDENCE_PATH`; both are additions, not rewrites. `STAGE_04_REPORTED_INDEPENDENT_ACCEPTANCE_STATUS: PENDING` is preserved un-promoted (the Stage-05 value `PASS` is set only in the new closure block). — MATCH.

## (E) Language scan: ⋮ identity claims and E2E naming

Claim audited: no sentence claims an un-clicked ⋮ identity/function; INTEGRATION is never called live E2E.
- Report L10: “**This attempt is never reported as live/production E2E.**”; result.md L7: “never reported as live/production E2E”. No occurrence of “live E2E” as a claim about this attempt.
- ⋮ phrasing checked line by line: the report consistently says “machine verification not established”, “must not be read as the album-level ⋮ being proven”, “the only non-text ⋮ glyph candidate sits above the album-title band the frozen rule allows, so the question is whether the rule's position assumption matches the real LINE surface — an owner/Plan question”. No identity/function claim from an un-clicked glyph. — MATCH.

## (F) Disclosure completeness

Claim audited: the freeze-set 69/71+2 disclosure, the `__pycache__` anomaly, ledger timestamp normalization, and post-frame byte-identity are all recorded honestly and are not used to soften the CORE blocker.
- Freeze set: `55-freeze-set-reverify.json` (23:45:49) recomputed 2 mismatch rows = `plan.md` `426fe6a1…`→`4337e2b5…` and `handoff.md` `df595831…`→`19c9c6d3…`; `57-freeze-era-plan-recovery.json` verifies both at freeze commit `a68f89091f381415fb4b017558fe4924112e126c` via `git cat-file` (independently reproduced: `426fe6a1…` / `df595831…`; archived r21 handoff on disk still 37,807 B `df595831…`). The report documents this as ANOM-07-03/Row 5 (“the wave's own authority artifacts, advanced by the approved revision/Stage-03 regeneration — disclosed design fact, no prior-evidence byte change”) and keeps the CORE blocker stated independently. — MATCH. (Auditor note: a fresh full re-walk today yields 68/71, because `result.md` was itself overwritten at 23:57:13, after the 23:45:49 measurement; this is the authorized Stage-05 overwrite, not a contradiction of the disclosed measurement window — see DOC-3.)
- `__pycache__`: report ANOM-06-01 + `60-tmp-frame-forensics.json.pycache_scan_frozen_tool_dirs = {found: [], count: 0}`; `anomaly-transient-pycache.json` present (1,129 B, `fdfd8a96…`). Independent scan of the frozen tool dirs: none present. (Repo-wide, two `__pycache__` dirs exist under `tests/…`, unrelated to the frozen tool dirs the claim scopes.) — MATCH.
- Ledger normalization: report ANOM-07-04 cites `/tmp/route6_ledger_pre_normalize.json` `43761035…` (28,390 B) and “two pre-commit placeholder fields”. Independent leaf diff of the pre-normalize copy vs the committed ledger: exactly 2 differing leaves — `/finalized_at_local` and `/events/8/timestamp_local` (event seq 9), both `23:2x+0800` → `23:25+0800`. — MATCH.
- Post-frame byte-identity: report ANOM-07-05 quotes the two journals and marks the conclusion `[INFERRED — high confidence]` with a residual limit. Independent: both files `4cb8a6b4…`/66,686 B, different inodes (124696180 / 124594685) and mtimes; journals re-hash to the recorded values (attempt-06 journal 4,975,687 B `4d4101f5…`; attempt-05r journal 81,966,783 B `8683ea20…`) and contain the recorded write events (`saved post frame bytes=66686`; `shotBytes 66686 / postSha 4cb8a6b4…`) with differing pre-frames `3dd1a3bf…` vs `3d926e7d…`. — MATCH.

## (G) Old `result.md` provenance and new `result.md` authority

Claim audited: the old `result.md` (`7c0f3ab6…` / git blob) is correctly memorialized; the new one writes only what Stage 05 may write.
- `git cat-file -p 6cb3089e7930b55118079dbcc777de6a2b47584a` → 6,873 B / `7c0f3ab62eade530da3bdd134640c3450f4ad28182de32579fcb55effea837cc`, matching report L9, `00-start-state.json`, and the `56` anchor “result.md (pre-overwrite provenance) 7c0f3ab6… ok=true”. New file's blob `0175f3da…` recorded in `96-post-close-state.json` with pre-overwrite blob/hash; independently reproduced.
- New `result.md` content inspected end-to-end: statuses, closure facts, matrix, blocker, durable identities, no-touch statement, NEXT_ACTION. It makes no claim about itself (its own hash is not self-embedded); it writes only Stage-05-status content and explicitly defers routing to the owner. `git status` shows only ` M result.md`, the untracked attempt-07 tree, and concurrent-writer files. — MATCH.

## Documentary observations (non-blocking; recorded for completeness)

- DOC-1: `51-baseline-compare.json` carries `destination_inventory.files = null` and `verdict.files_57 = false` (the comparator did not read the destination count from the baseline JSON), while its `pre_expected.files = 57` and `bytes_17924900 = true` hold. The report does not rely on that field for the 57-file claim — it cites `56-frozen-anchors-rehash.json`'s read-only walk (independently reproduced: 57 files / 17,924,900 B / 0 zero-byte). Non-material evidence-internal quirk.
- DOC-2: Report RUN_METADATA says “report written 23:53”; the report file's mtime is `23:55:17`, and two supplement evidence files were added afterwards (`57-freeze-era-plan-recovery.json` 23:58:38, `96-post-close-state.json` 23:59:04); the report's evidence index does not list `57`/`96` (it does not need to for any cited claim; `result.md` cites `57`). Timing description is approximate, not misleading.
- DOC-3: A fresh full freeze-set re-walk today gives 68/71 (third differing row = `result.md` `7c0f3ab6…`→`daf5ba11…`). This is the authorized 23:57:13 overwrite occurring after the 23:45:49 measurement; it neither refutes the report's 69/71 reading nor indicates an unauthorized change.
- DOC-4: The `__pycache__` claim is scoped to the frozen tool dirs (correct as written); two `__pycache__` directories exist elsewhere under `tests/` but are outside that scope and unrelated.
- DOC-5: The pre-normalize ledger and the final ledger have equal byte length (28,390) and differ in exactly two leaves; the normalization did not alter event counts, flags, or any other recorded fact.

## Key commands used (reproducible summary)

- `shasum -a 256 <file>`, `wc -c <file>` over the report, `result.md`, all 39 attempt-07 files, and every cited frozen artifact.
- `/opt/homebrew/bin/python3 -B` + `importlib` on `src/line_backup_acceptance/common.py` → `user_fact_v1_matches` on v1.1/v1; `_rehash_evidence_entries(record["evidence"])`; leaf diff.
- JSON parse of `run-ledger.json` (`final_counts`, events, `route_outcome`, `plan_binding`, `frozen_inputs`) and the three tool JSONs; `cmp` of frozen JSONs vs attempt-07 replay stdout; existence checks for `route-result.json`/`manifest.json`/`menu-analysis.json`.
- Leaf diff `/tmp/route6_ledger_pre_normalize.json` vs committed ledger; `shasum`/`ls -li`/`stat` on both post frames and both session journals.
- `git cat-file -p a68f890…:<plan|handoff>` and `git cat-file -p 6cb3089e…` for freeze-era/pre-overwrite provenance; `diff` of report L27–32 vs `execution-rev22.md` L119–124.

## FINAL VERDICT

**REPORT_AUDIT_ISSUES** — 1 MISMATCH (documentary, non-material), 0 UNVERIFIABLE.

MISMATCH list:
1. `e2e_report.md` TEST_MATRIX header “(policy columns verbatim; results re-derived by this attempt)”: row `V22_S5_BOUND_TO_V4_READER`'s failure-class cell is a compressed paraphrase of the §22.6/handoff text — the parenthetical “(§20.2 S10, reused byte-identically per §22.3)” and the words “from the v4-reader requirement” are omitted (the operational meaning and the exemption are preserved; the report states the byte-identical reuse in section (c)). The other five rows are byte-identical to plan §22.6. Classification: documentary only; it changes no check result, tuple field, gate, or closure meaning.

UNVERIFIABLE list: none. Every audited claim was independently recomputed and matched, except the one documentary verbatim claim above.

Substantive conclusions that stand as MATCH: (A) all six row adjudications match evidence, including the row-4 honesty constraint (“no AFFIRMATIVE claim; not positive route evidence”) and the stage-04-consistent non-gating FAIL of the screen probe; (B) the tuple `UNKNOWN | COMPLETE | BLOCKED | PASS | PASS | CORE_ACCEPTANCE_BLOCKED` + `UNCHANGED` is correctly derived, pre-claims nothing, does not adopt `ROUTE_NOT_NEEDED`, and does not assert the route is proven non-existent; (C) both closure facts reproduce (v1.1 `True` / v1 `False`; exactly one input; ⋮ 0/1; retry 0; menu 0; keyboard 0; stop=NO_ELLIPSIS_FOUND; no route-result.json); (D) the snapshot is line-identical and unrewritten; (E) no ⋮ identity claim and no live-E2E naming; (F) all four disclosures are present and honestly scoped; (G) old `result.md` provenance and new `result.md` authority are correct.

Weakest inference in the report (auditor's pick): the `result.md` NEXT_ACTION Option A sentence “The §19.3/§20.3(a) preconditions are re-verified in this attempt (source correspondence `CONFIRMED` over v1.1; no side effect needed — the 57 files are final for this wave; attempt-06's artifacts honestly record the observed outcome with zero side effects; residual uncertainty recorded).” Preconditions (1) and (3) have dedicated evidence (`10-`/`20-`), but precondition (2) is supported only indirectly (destination walk + baseline byte-identity) and precondition (4) reads as policy restatement rather than an artifact-level re-derivation of “why further repetition adds no evidence”; also, `NO_ELLIPSIS_FOUND` is not in the §19.3 verdict list to which those preconditions attach (it routes through §20.3(a) with an explicit owner decision). The word “re-verified” is therefore slightly stronger than the underlying artifacts. This is auxiliary owner-framing, not a closure fact, and does not affect the CORE blocker conclusion. (Runner-up: PASS-by-constraint for `V22_AFFIRMATIVE_MACHINE_OBSERVABLE_ONLY` instead of `NOT_RUN` — a disclosed adjudication choice, defensible.)

